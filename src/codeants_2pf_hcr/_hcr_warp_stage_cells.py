"""Captured notebook stage sources for package-owned HCR warp wrappers."""

CELL_43_SOURCE = '# [43]\ntry:\n    REQUIRE_FISH_STATE = require_fish_state\nexcept NameError:\n    REQUIRE_FISH_STATE = None\nif callable(REQUIRE_FISH_STATE) and not REQUIRE_FISH_STATE():\n    raise SystemExit\n# Prepare confocal masks for external FIJI BigWarp: export rn->rbest labels (optional), then load FIJI 2P outputs.\nimport json\nimport os\nimport re\nfrom concurrent.futures import ThreadPoolExecutor, as_completed\nimport numpy as np, tifffile as tiff\nimport pandas as pd\nfrom pathlib import Path\nfrom scipy import ndimage as ndi\n\ntry:\n    FISH_DIR = Path(FISH_DIR)\nexcept NameError:\n    raise RuntimeError("FISH_DIR missing; run path-initialization cells first.")\ntry:\n    FISH_ID_LOCAL = FISH_ID\nexcept NameError:\n    FISH_ID_LOCAL = None\ntry:\n    REG_DIR = Path(REG_DIR)\nexcept NameError:\n    REG_DIR = FISH_DIR / \'02_reg\'\ntry:\n    PREPROC_DIR = Path(PREPROC_DIR)\nexcept NameError:\n    PREPROC_DIR = REG_DIR / \'00_preprocessing\'\ntry:\n    RBEST_TM_DIR = Path(RBEST_TM_DIR)\nexcept NameError:\n    RBEST_TM_DIR = REG_DIR / \'01_rbest-2p\' / \'transMatrices\'\ntry:\n    RN_TO_RBEST_TM_DIR = Path(RN_TO_RBEST_TM_DIR)\nexcept NameError:\n    RN_TO_RBEST_TM_DIR = REG_DIR / \'02_rn-rbest\' / \'transMatrices\'\ntry:\n    ANAT_INT_PATH = Path(ANAT_INT_PATH)\nexcept NameError:\n    ANAT_INT_PATH = PREPROC_DIR / \'2p_anatomy\' / f"{FISH_ID_LOCAL}_anatomy_2P_GCaMP.nrrd"\ntry:\n    HCR_CP_MASK_DIR = Path(HCR_CP_MASK_DIR)\nexcept NameError:\n    HCR_CP_MASK_DIR = FISH_DIR / \'03_analysis\' / \'confocal\' / \'raw\' / \'cp_masks\'\ntry:\n    HCR_ALIGNED_DIR = Path(HCR_ALIGNED_DIR)\nexcept NameError:\n    HCR_ALIGNED_DIR = FISH_DIR / \'03_analysis\' / \'confocal\' / \'aligned\'\ntry:\n    RN_TO_RBEST_LABELS_DIR = Path(RN_TO_RBEST_LABELS_DIR)\nexcept NameError:\n    RN_TO_RBEST_LABELS_DIR = FISH_DIR / \'03_analysis\' / \'confocal\' / \'rn_to_rbest\' / \'labels\'\n\nHCR_ALIGNED_DIR.mkdir(parents=True, exist_ok=True)\nRN_TO_RBEST_LABELS_DIR.mkdir(parents=True, exist_ok=True)\n\ntry:\n    _DATA_MODE_LOCAL = str(DATA_MODE).strip().lower() == \'local\'\nexcept NameError:\n    _DATA_MODE_LOCAL = False\ntry:\n    _DATA_ROOT_LOCAL = Path(DATA_ROOT) if DATA_ROOT else None\nexcept NameError:\n    try:\n        _DATA_ROOT_LOCAL = Path(LOCAL_ROOT) if LOCAL_ROOT else None\n    except NameError:\n        _DATA_ROOT_LOCAL = None\ntry:\n    _NAS_ROOT_LOCAL = Path(NAS_ROOT) if NAS_ROOT else None\nexcept NameError:\n    _NAS_ROOT_LOCAL = None\ntry:\n    _MATCHING_DEFAULT = Path(MATCHING_METADATA_CSV_DEFAULT) if MATCHING_METADATA_CSV_DEFAULT else None\nexcept NameError:\n    if _DATA_MODE_LOCAL and _DATA_ROOT_LOCAL is not None:\n        _MATCHING_DEFAULT = _DATA_ROOT_LOCAL / \'matchingMetadata.csv\'\n    elif _NAS_ROOT_LOCAL is not None:\n        _MATCHING_DEFAULT = _NAS_ROOT_LOCAL / \'Danin\' / \'matchingMetadata.csv\'\n    else:\n        _MATCHING_DEFAULT = None\ntry:\n    _MATCHING_OVERRIDE = Path(MATCHING_METADATA_CSV_OVERRIDE) if MATCHING_METADATA_CSV_OVERRIDE not in (None, \'\', False) else None\nexcept NameError:\n    _MATCHING_OVERRIDE = None\nif _MATCHING_OVERRIDE is None:\n    MATCHING_METADATA_CSV = Path(_MATCHING_DEFAULT) if _MATCHING_DEFAULT else None\nelse:\n    MATCHING_METADATA_CSV = Path(_MATCHING_OVERRIDE)\n\n# Native-HCR filtering policy for mask warp:\n# - optionally split labels into 3D connected components (disabled by default)\n# - hard drop obvious dust by an absolute size floor\n# - optionally apply a second relative low-end filter on the survivors\n# - keep a q95 large-object flag for downstream QA\ntry:\n    HCR_WARP_FILTER_LOW_Q = float(HCR_WARP_FILTER_LOW_Q)\nexcept (NameError, TypeError, ValueError):\n    try:\n        HCR_WARP_FILTER_LOW_Q = float(FILTER_LOW_QUANTILE)\n    except (NameError, TypeError, ValueError):\n        HCR_WARP_FILTER_LOW_Q = 0.05\ntry:\n    HCR_WARP_FILTER_HIGH_Q = float(HCR_WARP_FILTER_HIGH_Q)\nexcept (NameError, TypeError, ValueError):\n    try:\n        HCR_WARP_FILTER_HIGH_Q = float(FILTER_HIGH_QUANTILE)\n    except (NameError, TypeError, ValueError):\n        HCR_WARP_FILTER_HIGH_Q = 0.95\nHCR_WARP_FILTER_LOW_Q = min(max(HCR_WARP_FILTER_LOW_Q, 0.0), 1.0)\nHCR_WARP_FILTER_HIGH_Q = min(max(HCR_WARP_FILTER_HIGH_Q, 0.0), 1.0)\nif HCR_WARP_FILTER_HIGH_Q < HCR_WARP_FILTER_LOW_Q:\n    HCR_WARP_FILTER_HIGH_Q = HCR_WARP_FILTER_LOW_Q\ntry:\n    HCR_WARP_DROP_LOW_Q = bool(HCR_WARP_DROP_LOW_Q)\nexcept NameError:\n    HCR_WARP_DROP_LOW_Q = False\ntry:\n    HCR_WARP_FLAG_HIGH_Q = bool(HCR_WARP_FLAG_HIGH_Q)\nexcept NameError:\n    HCR_WARP_FLAG_HIGH_Q = True\ntry:\n    HCR_WARP_LOW_CONF_COL = str(LOW_CONF_SEG_COL)\nexcept NameError:\n    HCR_WARP_LOW_CONF_COL = "is_low_confidence_segmentation"\ntry:\n    HCR_WARP_SPLIT_CONNECTED_COMPONENTS = bool(HCR_WARP_SPLIT_CONNECTED_COMPONENTS)\nexcept NameError:\n    HCR_WARP_SPLIT_CONNECTED_COMPONENTS = False\ntry:\n    HCR_WARP_COMPONENT_CONNECTIVITY = int(HCR_WARP_COMPONENT_CONNECTIVITY)\nexcept (NameError, TypeError, ValueError):\n    HCR_WARP_COMPONENT_CONNECTIVITY = 3\nHCR_WARP_COMPONENT_CONNECTIVITY = min(max(HCR_WARP_COMPONENT_CONNECTIVITY, 1), 3)\nHCR_WARP_MIN_COMPONENT_VOXELS = 200 # Just tried a few values and eyeballed results; 200 seems to remove small specks without breaking real signal. Adjust as needed.\ntry:\n    _HCR_WARP_MIN_COMPONENT_VOLUME_UM3_RAW = HCR_WARP_MIN_COMPONENT_VOLUME_UM3\nexcept NameError:\n    _HCR_WARP_MIN_COMPONENT_VOLUME_UM3_RAW = None\nif _HCR_WARP_MIN_COMPONENT_VOLUME_UM3_RAW in (None, "", False):\n    HCR_WARP_MIN_COMPONENT_VOLUME_UM3 = None\nelse:\n    try:\n        HCR_WARP_MIN_COMPONENT_VOLUME_UM3 = max(0.0, float(_HCR_WARP_MIN_COMPONENT_VOLUME_UM3_RAW))\n    except Exception:\n        HCR_WARP_MIN_COMPONENT_VOLUME_UM3 = None\ntry:\n    HCR_WARP_LOW_FILTER_METRIC = str(HCR_WARP_LOW_FILTER_METRIC).strip() or "n_voxels"\nexcept NameError:\n    HCR_WARP_LOW_FILTER_METRIC = "n_voxels"\nif HCR_WARP_LOW_FILTER_METRIC not in {"volume_um3", "equivalent_diameter_um", "n_voxels"}:\n    HCR_WARP_LOW_FILTER_METRIC = "n_voxels"\ntry:\n    HCR_WARP_HIGH_FILTER_METRIC = str(HCR_WARP_HIGH_FILTER_METRIC).strip() or "equivalent_diameter_um"\nexcept NameError:\n    HCR_WARP_HIGH_FILTER_METRIC = "equivalent_diameter_um"\nif HCR_WARP_HIGH_FILTER_METRIC not in {"volume_um3", "equivalent_diameter_um", "n_voxels"}:\n    HCR_WARP_HIGH_FILTER_METRIC = "equivalent_diameter_um"\nHCR_WARP_FILTER_POLICY_VERSION = "label_voxel_floor_v3"\n\nRECOMPUTE_WARP = False # Whether to recompute the HCR->2P warp transform (if False, will attempt to reuse any existing warp in HCR_RN_TO_RBEST_DIR)\nRECOMPUTE_FILTER_STATS = False # Whether to recompute the per-segment stats used for filtering (if False, will attempt to reuse any existing stats in HCR_RN_TO_RBEST_DIR)\ntry:\n    ALLOW_LEGACY_WARP_CACHE = bool(ALLOW_LEGACY_WARP_CACHE)\nexcept NameError:\n    ALLOW_LEGACY_WARP_CACHE = False\nHCR_WARP_THREADS = max(1, min(4, os.cpu_count() or 1))\ntry:\n    EXPORT_BEST_ROUND_LABELS_TO_RBEST = bool(EXPORT_BEST_ROUND_LABELS_TO_RBEST)\nexcept NameError:\n    EXPORT_BEST_ROUND_LABELS_TO_RBEST = False\ntry:\n    VOX_HCR_LOCAL = VOX_HCR if isinstance(VOX_HCR, dict) else {}\nexcept NameError:\n    VOX_HCR_LOCAL = {}\ntry:\n    VOX_ANAT_LOCAL = VOX_ANAT if isinstance(VOX_ANAT, dict) else {}\nexcept NameError:\n    VOX_ANAT_LOCAL = {}\ntry:\n    BEST_FIX_IMG_INT_LOCAL = best_fix_img_int if best_fix_img_int is not None else None\nexcept NameError:\n    BEST_FIX_IMG_INT_LOCAL = None\n\n\ndef _as_bool_local(value, default=False):\n    if value is None:\n        return bool(default)\n    try:\n        if pd.isna(value):\n            return bool(default)\n    except Exception:\n        pass\n    if isinstance(value, bool):\n        return value\n    if isinstance(value, (int, float)):\n        return bool(value)\n    s = str(value).strip().lower()\n    if s in {"1", "true", "t", "yes", "y", "on"}:\n        return True\n    if s in {"0", "false", "f", "no", "n", "off", "", "nan", "none", "null"}:\n        return False\n    return bool(default)\n\n\ndef _preproc_search_dirs():\n    dirs = []\n    for name in (\'rbest\', \'rn\'):\n        p = PREPROC_DIR / name\n        if p.exists() and p.is_dir():\n            dirs.append(p)\n    if PREPROC_DIR.exists():\n        for p in sorted(PREPROC_DIR.iterdir()):\n            if p.is_dir() and p not in dirs:\n                dirs.append(p)\n    return dirs\n\n\ntry:\n    _parse_round_from_name\nexcept NameError:\n    def _parse_round_from_name(path: Path):\n        m = re.search(r"round(\\d+)", Path(path).name.lower())\n        return int(m.group(1)) if m else None\n\n\n_meta_row_local = None\nif MATCHING_METADATA_CSV is not None and MATCHING_METADATA_CSV.exists():\n    try:\n        _mdf = pd.read_csv(MATCHING_METADATA_CSV)\n        _hit = _mdf.loc[_mdf[\'fish_id\'].astype(str) == str(FISH_ID)]\n        if not _hit.empty:\n            _meta_row_local = _hit.iloc[0]\n    except Exception as _e:\n        print(f"[HCR] WARNING: failed reading metadata CSV {MATCHING_METADATA_CSV}: {_e}")\n\ntry:\n    best_round_idx = int(best_round_idx)\nexcept (NameError, TypeError, ValueError):\n    try:\n        _bro = BEST_ROUND_OVERRIDE\n    except NameError:\n        _bro = None\n    if _bro is not None:\n        best_round_idx = int(str(_bro).lower().lstrip(\'r\'))\n    elif _meta_row_local is not None and \'best_round\' in _meta_row_local.index:\n        best_round_idx = int(str(_meta_row_local[\'best_round\']).lower().lstrip(\'r\'))\n    else:\n        best_round_idx = 1\n        print(\'[HCR] WARNING: best_round not found in globals/metadata; defaulting to r1\')\nbest_round_idx = int(best_round_idx)\n\n_bw_meta = False\nif _meta_row_local is not None and \'bigwarp\' in _meta_row_local.index:\n    _bw_meta = _as_bool_local(_meta_row_local.get(\'bigwarp\', False), default=False)\ntry:\n    RUN_EXTERNAL_FIJI_BIGWARP = bool(USE_EXTERNAL_FIJI_BIGWARP)\nexcept NameError:\n    RUN_EXTERNAL_FIJI_BIGWARP = bool(_bw_meta)\nBIGWARP_FROM_METADATA = bool(_bw_meta)\nUSE_EXTERNAL_FIJI_BIGWARP = RUN_EXTERNAL_FIJI_BIGWARP\n\ntry:\n    _find_intensity_for_round\nexcept NameError:\n    def _find_intensity_for_round(round_idx: int):\n        round_idx = int(round_idx)\n        exact_hits = []\n        round_hits = []\n        for sub in _preproc_search_dirs():\n            candidates = sorted(sub.glob(\'*\')) if sub.exists() else []\n            for p in candidates:\n                name = p.name.lower()\n                if p.suffix.lower() != \'.nrrd\':\n                    continue\n                if f"round{round_idx}" not in name:\n                    continue\n                round_hits.append(p)\n                if \'channel1\' in name and \'gcamp\' in name:\n                    exact_hits.append(p)\n        if exact_hits:\n            return exact_hits[0]\n        return round_hits[0] if round_hits else None\n\ntry:\n    _find_hcr_intensity_for_mask\nexcept NameError:\n    def _find_hcr_intensity_for_mask(mask_path: Path, round_idx: int):\n        stem = Path(mask_path).stem\n        base = stem[:-9] if stem.endswith(\'_cp_masks\') else stem\n        ch_match = re.search(r"channel(\\d+)", stem.lower())\n        ch = int(ch_match.group(1)) if ch_match else None\n        gene_match = re.search(rf"round{int(round_idx)}_channel{ch}_(.+?)(?:_cp_masks)?$", stem.lower()) if ch is not None else None\n        gene_token = gene_match.group(1) if gene_match else None\n        token_hits = []\n        round_hits = []\n        for sub in _preproc_search_dirs():\n            if not sub.exists():\n                continue\n            exact = sub / f"{base}.nrrd"\n            if exact.exists():\n                return exact\n            pattern = f"{FISH_ID}_round{int(round_idx)}_channel{ch}_*.nrrd" if ch is not None else f"*round{int(round_idx)}*.nrrd"\n            candidates = [p for p in sorted(sub.glob(pattern)) if \'gcamp\' not in p.name.lower()]\n            for p in candidates:\n                round_hits.append(p)\n                if gene_token and gene_token in p.stem.lower():\n                    token_hits.append(p)\n        if token_hits:\n            return token_hits[0]\n        return round_hits[0] if round_hits else None\n\ntry:\n    _find_transform\nexcept NameError:\n    def _find_transform(trans_dir: Path, round_idx: int, target_tag: str, kind: str):\n        if kind not in {\'warp\', \'affine\'}:\n            raise ValueError(f"Unknown transform kind: {kind}")\n\n        suffix = \'1Warp.nii.gz\' if kind == \'warp\' else \'0GenericAffine.mat\'\n        round_idx = int(round_idx)\n        target_tag = str(target_tag or \'\').lower().strip()\n\n        search_roots = []\n        if trans_dir is not None and Path(trans_dir).exists():\n            search_roots.append(Path(trans_dir))\n        for cand in sorted(REG_DIR.glob(\'*/transMatrices\')):\n            cand = Path(cand)\n            if cand.exists() and cand not in search_roots:\n                search_roots.append(cand)\n\n        all_files = []\n        for root in search_roots:\n            all_files.extend([p for p in sorted(root.glob(f"*{suffix}")) if \'inverse\' not in p.name.lower()])\n        if not all_files:\n            return None\n\n        def _has_source_round(name: str) -> bool:\n            if re.search(rf"(^|[_-])round{round_idx}(?=([_.-]|$))", name):\n                return True\n            if re.search(rf"(^|[_-])r{round_idx}(?=([_.-]|$))", name):\n                return True\n            if int(round_idx) == int(best_round_idx) and re.search(r"(^|[_-])rbest(?=([_.-]|$))", name):\n                return True\n            return False\n\n        def _target_aliases(tag: str):\n            if tag == \'to_2p\':\n                return [\'to_2p\', \'in_2p\', \'to2p\', \'to_ref\', \'in_ref\']\n            m = re.match(r"to_r(\\d+)$", tag)\n            if m:\n                trg = int(m.group(1))\n                aliases = [f"to_r{trg}", f"in_r{trg}"]\n                if int(trg) == int(best_round_idx):\n                    aliases += [\'to_rbest\', \'in_rbest\']\n                return aliases\n            return [tag] if tag else []\n\n        source_hits = [p for p in all_files if _has_source_round(p.name.lower())]\n        aliases = _target_aliases(target_tag)\n        if aliases:\n            both_hits = [p for p in source_hits if any(a in p.name.lower() for a in aliases)]\n            if both_hits:\n                return both_hits[0]\n\n        if source_hits:\n            return source_hits[0]\n        if len(all_files) == 1:\n            return all_files[0]\n        return None\n\ntry:\n    PREPARE_RN_TO_RBEST_LABELS = bool(PREPARE_RN_TO_RBEST_LABELS)\nexcept NameError:\n    PREPARE_RN_TO_RBEST_LABELS = bool(RUN_EXTERNAL_FIJI_BIGWARP)\nPREPARE_RN_TO_RBEST_LABELS = bool(PREPARE_RN_TO_RBEST_LABELS and RUN_EXTERNAL_FIJI_BIGWARP)\ntry:\n    BIGWARP_QC_VALIDATE_OUTPUTS = bool(BIGWARP_QC_VALIDATE_OUTPUTS)\nexcept NameError:\n    BIGWARP_QC_VALIDATE_OUTPUTS = bool(RUN_EXTERNAL_FIJI_BIGWARP)\nBIGWARP_QC_VALIDATE_OUTPUTS = bool(BIGWARP_QC_VALIDATE_OUTPUTS and RUN_EXTERNAL_FIJI_BIGWARP)\ntry:\n    BIGWARP_QC_CHECK_ANAT_Z = bool(BIGWARP_QC_CHECK_ANAT_Z)\nexcept NameError:\n    BIGWARP_QC_CHECK_ANAT_Z = bool(RUN_EXTERNAL_FIJI_BIGWARP)\nBIGWARP_QC_CHECK_ANAT_Z = bool(BIGWARP_QC_CHECK_ANAT_Z and RUN_EXTERNAL_FIJI_BIGWARP)\nprint(f"[HCR] [43] bootstrap: best_round=r{best_round_idx}, bigwarp={RUN_EXTERNAL_FIJI_BIGWARP}, prep_labels={PREPARE_RN_TO_RBEST_LABELS}")\nif RUN_EXTERNAL_FIJI_BIGWARP:\n    print(f"[HCR] [43] external-bigwarp QC: validate_outputs={BIGWARP_QC_VALIDATE_OUTPUTS}, check_anat_z={BIGWARP_QC_CHECK_ANAT_Z}")\n\ntry:\n    import ants  # type: ignore\n    HAVE_ANTSPY = True\nexcept Exception as e:\n    HAVE_ANTSPY = False\n    print("ANTsPy not available; rn->rbest prep disabled.", e)\n\nif not ANAT_INT_PATH.exists():\n    raise FileNotFoundError(f"Fixed 2P anatomy intensity not found: {ANAT_INT_PATH}")\n\nif PREPARE_RN_TO_RBEST_LABELS and not HAVE_ANTSPY:\n    raise RuntimeError("ANTsPy is required to prepare rn->rbest labels for FIJI.")\n\nprint(\n    f"[HCR] cache mode: warp={\'recompute\' if RECOMPUTE_WARP else \'reuse-if-valid\'}; "\n    f"workflow={\'external_fiji_bigwarp\' if RUN_EXTERNAL_FIJI_BIGWARP else \'direct_ants_to_2p\'}; "\n    f"prepare_rn_to_rbest_labels={PREPARE_RN_TO_RBEST_LABELS}"\n)\nprint(\n    f"[HCR] filter policy: split_components={HCR_WARP_SPLIT_CONNECTED_COMPONENTS}; "\n    f"connectivity={HCR_WARP_COMPONENT_CONNECTIVITY}; "\n    f"min_component_voxels={HCR_WARP_MIN_COMPONENT_VOXELS}; "\n    f"min_component_volume_um3={HCR_WARP_MIN_COMPONENT_VOLUME_UM3}; "\n    f"low_metric={HCR_WARP_LOW_FILTER_METRIC}; high_metric={HCR_WARP_HIGH_FILTER_METRIC}; "\n    f"q_low={HCR_WARP_FILTER_LOW_Q:.2f}; q_high={HCR_WARP_FILTER_HIGH_Q:.2f}"\n)\nif RUN_EXTERNAL_FIJI_BIGWARP and RECOMPUTE_WARP:\n    print(\n        "[HCR] NOTE: rn->rbest prep labels will be regenerated. "\n        "Overwrite the external FIJI *_in_2p_labels*.tif outputs before trusting downstream matches."\n    )\n\n\ndef _ants_clone_geometry(dst_img, like_img):\n    dst_img.set_spacing(like_img.spacing)\n    dst_img.set_origin(like_img.origin)\n    dst_img.set_direction(like_img.direction)\n    return dst_img\n\n\ndef _hcr_vox_xy_um():\n    dx = float(VOX_HCR_LOCAL.get(\'X\', 1.0)) if VOX_HCR_LOCAL else 1.0\n    dy = float(VOX_HCR_LOCAL.get(\'Y\', 1.0)) if VOX_HCR_LOCAL else 1.0\n    return dx, dy\n\n\ndef _hcr_vox_zyx_um():\n    dz = float(VOX_HCR_LOCAL.get(\'Z\', 1.0)) if VOX_HCR_LOCAL else 1.0\n    dy = float(VOX_HCR_LOCAL.get(\'Y\', 1.0)) if VOX_HCR_LOCAL else 1.0\n    dx = float(VOX_HCR_LOCAL.get(\'X\', 1.0)) if VOX_HCR_LOCAL else 1.0\n    return dz, dy, dx\n\n\ndef _filter_policy_signature():\n    payload = {\n        "version": str(HCR_WARP_FILTER_POLICY_VERSION),\n        "drop_low_q": bool(HCR_WARP_DROP_LOW_Q),\n        "flag_high_q": bool(HCR_WARP_FLAG_HIGH_Q),\n        "q_low": float(HCR_WARP_FILTER_LOW_Q),\n        "q_high": float(HCR_WARP_FILTER_HIGH_Q),\n        "split_connected_components": bool(HCR_WARP_SPLIT_CONNECTED_COMPONENTS),\n        "component_connectivity": int(HCR_WARP_COMPONENT_CONNECTIVITY),\n        "min_component_voxels": int(HCR_WARP_MIN_COMPONENT_VOXELS),\n        "min_component_volume_um3": None if HCR_WARP_MIN_COMPONENT_VOLUME_UM3 is None else float(HCR_WARP_MIN_COMPONENT_VOLUME_UM3),\n        "low_metric": str(HCR_WARP_LOW_FILTER_METRIC),\n        "high_metric": str(HCR_WARP_HIGH_FILTER_METRIC),\n        "absolute_metric": "volume_um3" if HCR_WARP_MIN_COMPONENT_VOLUME_UM3 is not None else "n_voxels",\n    }\n    return json.dumps(payload, sort_keys=True)\n\n\ndef _filter_stats_match_policy(fs):\n    if not isinstance(fs, dict) or (not fs):\n        return False\n    return str(fs.get("filter_policy_signature", "")) == _filter_policy_signature()\n\n\ndef _metadata_filter_stats(save_basename: Path):\n    meta = read_warp_metadata(save_basename)\n    if not isinstance(meta, dict):\n        return None\n    fs = meta.get("filter_stats", {})\n    if not _filter_stats_match_policy(fs):\n        return None\n    return dict(fs)\n\n\ndef _split_labels_into_components(lab_zyx):\n    lab_zyx = np.asarray(lab_zyx)\n    if lab_zyx.ndim != 3:\n        raise ValueError(f"Expected 3D labels for connected-component split; got {lab_zyx.shape}")\n\n    structure = ndi.generate_binary_structure(3, HCR_WARP_COMPONENT_CONNECTIVITY)\n    out = np.zeros(lab_zyx.shape, dtype=np.int32)\n    records = []\n    next_label = 1\n    src_labels = np.unique(lab_zyx)\n    src_labels = src_labels[src_labels != 0]\n    for src_label in src_labels.astype(np.int64, copy=False):\n        cc_map, n_cc = ndi.label(lab_zyx == int(src_label), structure=structure)\n        if int(n_cc) <= 0:\n            continue\n        comp_ids, comp_counts = np.unique(cc_map[cc_map != 0], return_counts=True)\n        order = np.argsort(-comp_counts, kind=\'stable\')\n        comp_ids = comp_ids[order]\n        comp_counts = comp_counts[order]\n        for comp_rank, (comp_id, n_vox) in enumerate(zip(comp_ids, comp_counts), start=1):\n            new_label = int(next_label)\n            next_label += 1\n            out[cc_map == int(comp_id)] = new_label\n            records.append({\n                "source_label": int(src_label),\n                "component_rank_within_source": int(comp_rank),\n                "label": int(new_label),\n                "n_voxels": int(n_vox),\n            })\n    comp_df = pd.DataFrame.from_records(records, columns=["source_label", "component_rank_within_source", "label", "n_voxels"])\n    return out, comp_df\n\n\ndef _build_component_table(lab_zyx):\n    lab_zyx = np.asarray(lab_zyx)\n    src_labels = np.unique(lab_zyx)\n    src_labels = src_labels[src_labels != 0]\n    if src_labels.size == 0:\n        empty = pd.DataFrame(columns=["source_label", "component_rank_within_source", "label", "n_voxels"])\n        return np.zeros(lab_zyx.shape, dtype=np.int32), empty\n\n    if HCR_WARP_SPLIT_CONNECTED_COMPONENTS:\n        comp_labels_zyx, comp_df = _split_labels_into_components(lab_zyx)\n    else:\n        counts = np.bincount(lab_zyx.ravel().astype(np.int64))\n        records = []\n        for label in src_labels.astype(np.int64, copy=False):\n            n_vox = int(counts[int(label)]) if int(label) < len(counts) else int(np.sum(lab_zyx == int(label)))\n            records.append({\n                "source_label": int(label),\n                "component_rank_within_source": 1,\n                "label": int(label),\n                "n_voxels": int(n_vox),\n            })\n        comp_labels_zyx = lab_zyx.astype(np.int32, copy=False)\n        comp_df = pd.DataFrame.from_records(records)\n\n    if comp_df.empty:\n        comp_df = pd.DataFrame(columns=["source_label", "component_rank_within_source", "label", "n_voxels"])\n        return comp_labels_zyx.astype(np.int32, copy=False), comp_df\n\n    dz, dy, dx = _hcr_vox_zyx_um()\n    voxel_um3 = float(dz * dy * dx)\n    comp_df = comp_df.copy()\n    comp_df["n_voxels"] = pd.to_numeric(comp_df["n_voxels"], errors="coerce").fillna(0).astype(int)\n    comp_df["volume_um3"] = comp_df["n_voxels"].astype(float) * voxel_um3\n    comp_df["equivalent_diameter_um"] = np.where(\n        comp_df["volume_um3"] > 0,\n        2.0 * np.power((3.0 * comp_df["volume_um3"]) / (4.0 * np.pi), 1.0 / 3.0),\n        np.nan,\n    )\n    return comp_labels_zyx.astype(np.int32, copy=False), comp_df\n\n\ndef warp_cache_candidates(save_basename: Path):\n    return [\n        Path(f"{save_basename}_labels_uint16.tif"),\n        Path(f"{save_basename}_labels_uint16.tiff"),\n        Path(f"{save_basename}_labels.tif"),\n        Path(f"{save_basename}_labels.tiff"),\n        Path(f"{save_basename}_labels_int32.npy"),\n    ]\n\n\ndef load_cached_warp(save_basename: Path):\n    for c in warp_cache_candidates(save_basename):\n        if c.exists():\n            if c.suffix == ".npy":\n                return np.load(c, mmap_mode="r"), c\n            if c.suffix.lower() in {".tif", ".tiff"}:\n                return tiff.imread(str(c)), c\n    return None, None\n\n\ndef warp_metadata_path(save_basename: Path):\n    return Path(f"{save_basename}_warp_meta.json")\n\n\ndef write_warp_metadata(save_basename: Path, metadata: dict):\n    warp_metadata_path(save_basename).write_text(json.dumps(metadata, indent=2))\n\n\ndef read_warp_metadata(save_basename: Path):\n    mp = warp_metadata_path(save_basename)\n    if not mp.exists():\n        return None\n    try:\n        return json.loads(mp.read_text())\n    except Exception:\n        return None\n\n\ndef _metric_label(metric_name):\n    return {\n        "n_voxels": "voxel count",\n        "volume_um3": "volume",\n        "equivalent_diameter_um": "equivalent diameter",\n    }.get(str(metric_name), str(metric_name))\n\n\ndef _metric_unit(metric_name):\n    return {\n        "n_voxels": "voxels",\n        "volume_um3": "um^3",\n        "equivalent_diameter_um": "um",\n    }.get(str(metric_name), "")\n\n\ndef _load_and_filter_label_tiff(tiff_path):\n    lab_zyx_native = tiff.imread(str(tiff_path))\n    if lab_zyx_native.ndim != 3:\n        raise ValueError(f"Expected 3D TIFF (ZYX); got shape {lab_zyx_native.shape}")\n\n    original_labels = np.unique(lab_zyx_native)\n    original_labels = original_labels[original_labels != 0]\n    comp_labels_zyx, comp_df = _build_component_table(lab_zyx_native)\n\n    filter_stats = {\n        "n_labels_before": int(len(comp_df)),\n        "n_labels_after": int(len(comp_df)),\n        "n_dropped_low_q": 0,\n        "n_dropped_noise_prewarp_total": 0,\n        "n_dropped_low_q_quantile_only": 0,\n        "n_dropped_small_components_abs": 0,\n        "n_low_confidence_high_q": 0,\n        "q_low_um": None,\n        "q_high_um": None,\n        "q_low_volume_um3": None,\n        "q_high_volume_um3": None,\n        "q_low_voxels": None,\n        "q_high_voxels": None,\n        "low_conf_col": str(HCR_WARP_LOW_CONF_COL),\n        "low_conf_labels": [],\n        "dropped_labels": [],\n        "dropped_labels_low_q_only": [],\n        "dropped_labels_small_components_abs": [],\n        "filter_metric_name": str(HCR_WARP_LOW_FILTER_METRIC),\n        "low_filter_metric_name": str(HCR_WARP_LOW_FILTER_METRIC),\n        "high_filter_metric_name": str(HCR_WARP_HIGH_FILTER_METRIC),\n        "absolute_filter_metric_name": "volume_um3" if HCR_WARP_MIN_COMPONENT_VOLUME_UM3 is not None else "n_voxels",\n        "min_component_volume_um3": None,\n        "min_component_voxels_fallback": int(HCR_WARP_MIN_COMPONENT_VOXELS),\n        "filter_policy_signature": _filter_policy_signature(),\n        "filter_policy_version": str(HCR_WARP_FILTER_POLICY_VERSION),\n        "n_original_labels_before_split": int(len(original_labels)),\n        "n_components_before_abs_filter": int(len(comp_df)),\n        "n_components_after_abs_filter": int(len(comp_df)),\n    }\n\n    if comp_df.empty:\n        return np.asarray(comp_labels_zyx), filter_stats\n\n    try:\n        work_df = comp_df.copy()\n        entity_word = "components" if HCR_WARP_SPLIT_CONNECTED_COMPONENTS else "labels"\n        abs_metric_name = None\n        abs_threshold_value = None\n        drop_abs_labels = np.array([], dtype=np.int64)\n        if HCR_WARP_MIN_COMPONENT_VOLUME_UM3 is not None and float(HCR_WARP_MIN_COMPONENT_VOLUME_UM3) > 0:\n            abs_metric_name = "volume_um3"\n            abs_threshold_value = float(HCR_WARP_MIN_COMPONENT_VOLUME_UM3)\n            drop_abs_labels = work_df.loc[\n                pd.to_numeric(work_df["volume_um3"], errors="coerce") < float(abs_threshold_value),\n                "label",\n            ].to_numpy(dtype=np.int64)\n        elif HCR_WARP_MIN_COMPONENT_VOXELS > 0:\n            abs_metric_name = "n_voxels"\n            abs_threshold_value = int(HCR_WARP_MIN_COMPONENT_VOXELS)\n            drop_abs_labels = work_df.loc[\n                pd.to_numeric(work_df["n_voxels"], errors="coerce") < int(abs_threshold_value),\n                "label",\n            ].to_numpy(dtype=np.int64)\n        if drop_abs_labels.size:\n            work_df = work_df.loc[~work_df["label"].isin(drop_abs_labels)].copy()\n        filter_stats["n_components_after_abs_filter"] = int(len(work_df))\n        filter_stats["absolute_filter_metric_name"] = abs_metric_name or "none"\n        filter_stats["min_component_volume_um3"] = float(abs_threshold_value) if abs_metric_name == "volume_um3" else None\n\n        low_metric_col = str(HCR_WARP_LOW_FILTER_METRIC)\n        high_metric_col = str(HCR_WARP_HIGH_FILTER_METRIC)\n        drop_lowq_labels = np.array([], dtype=np.int64)\n        low_conf_labels = np.array([], dtype=np.int64)\n        q_low = None\n        q_high = None\n\n        low_vals = pd.to_numeric(work_df.get(low_metric_col, np.nan), errors="coerce")\n        low_vals = low_vals[np.isfinite(low_vals.to_numpy(dtype=float))]\n        low_arr = low_vals.to_numpy(dtype=float)\n        if low_arr.size:\n            q_low = float(np.quantile(low_arr, HCR_WARP_FILTER_LOW_Q))\n            if HCR_WARP_DROP_LOW_Q:\n                drop_lowq_labels = work_df.loc[\n                    pd.to_numeric(work_df.get(low_metric_col, np.nan), errors="coerce") < q_low,\n                    "label",\n                ].to_numpy(dtype=np.int64)\n\n        high_vals = pd.to_numeric(work_df.get(high_metric_col, np.nan), errors="coerce")\n        high_vals = high_vals[np.isfinite(high_vals.to_numpy(dtype=float))]\n        high_arr = high_vals.to_numpy(dtype=float)\n        if high_arr.size:\n            q_high = float(np.quantile(high_arr, HCR_WARP_FILTER_HIGH_Q))\n            if HCR_WARP_FLAG_HIGH_Q:\n                low_conf_labels = work_df.loc[\n                    pd.to_numeric(work_df.get(high_metric_col, np.nan), errors="coerce") > q_high,\n                    "label",\n                ].to_numpy(dtype=np.int64)\n\n        drop_labels = np.unique(np.concatenate([drop_abs_labels, drop_lowq_labels])).astype(np.int64, copy=False) if (drop_abs_labels.size or drop_lowq_labels.size) else np.array([], dtype=np.int64)\n        if low_conf_labels.size and drop_labels.size:\n            low_conf_labels = np.setdiff1d(low_conf_labels, drop_labels, assume_unique=False)\n\n        if drop_labels.size:\n            comp_labels_zyx = np.where(np.isin(comp_labels_zyx, drop_labels), 0, comp_labels_zyx)\n\n        n_original = int(len(original_labels))\n        n_components = int(len(comp_df))\n        if HCR_WARP_SPLIT_CONNECTED_COMPONENTS and n_components != n_original:\n            print(\n                f"[HCR] {Path(tiff_path).name}: split {n_original} native labels into "\n                f"{n_components} connected components before size filtering"\n            )\n        if drop_abs_labels.size:\n            if abs_metric_name == "volume_um3":\n                print(\n                    f"[HCR] {Path(tiff_path).name}: dropped {len(drop_abs_labels)} labels "\n                    f"with volume < {float(abs_threshold_value):.3f} um^3 before relative filtering"\n                )\n            else:\n                print(\n                    f"[HCR] {Path(tiff_path).name}: dropped {len(drop_abs_labels)} labels "\n                    f"with voxel count < {int(abs_threshold_value)} before relative filtering"\n                )\n        if drop_lowq_labels.size:\n            q_low_unit = _metric_unit(low_metric_col)\n            q_low_suffix = f" {q_low_unit}" if q_low_unit else ""\n            print(\n                f"[HCR] {Path(tiff_path).name}: dropped {len(drop_lowq_labels)} {entity_word} "\n                f"with {_metric_label(low_metric_col)} < q05 ({q_low:.3f}{q_low_suffix}) before prep"\n            )\n        if low_conf_labels.size:\n            q_high_unit = _metric_unit(high_metric_col)\n            q_high_suffix = f" {q_high_unit}" if q_high_unit else ""\n            print(\n                f"[HCR] {Path(tiff_path).name}: flagged {len(low_conf_labels)} {entity_word} "\n                f"as low-confidence ({HCR_WARP_LOW_CONF_COL}=True; "\n                f"{_metric_label(high_metric_col)} > q95 {q_high:.3f}{q_high_suffix})"\n            )\n\n        filter_stats.update({\n            "n_labels_after": int(len(comp_df) - len(drop_labels)),\n            "n_dropped_low_q": int(len(drop_labels)),\n            "n_dropped_noise_prewarp_total": int(len(drop_labels)),\n            "n_dropped_low_q_quantile_only": int(len(drop_lowq_labels)),\n            "n_dropped_small_components_abs": int(len(drop_abs_labels)),\n            "n_low_confidence_high_q": int(len(low_conf_labels)),\n            "q_low_um": q_low if low_metric_col == "equivalent_diameter_um" else None,\n            "q_high_um": q_high if high_metric_col == "equivalent_diameter_um" else None,\n            "q_low_volume_um3": q_low if low_metric_col == "volume_um3" else None,\n            "q_high_volume_um3": q_high if high_metric_col == "volume_um3" else None,\n            "q_low_voxels": q_low if low_metric_col == "n_voxels" else None,\n            "q_high_voxels": q_high if high_metric_col == "n_voxels" else None,\n            "low_conf_labels": [int(x) for x in low_conf_labels.tolist()],\n            "dropped_labels": [int(x) for x in drop_labels.tolist()],\n            "dropped_labels_low_q_only": [int(x) for x in drop_lowq_labels.tolist()],\n            "dropped_labels_small_components_abs": [int(x) for x in drop_abs_labels.tolist()],\n        })\n    except Exception as _e:\n        print(f"[HCR] Component-wise filter failed for {Path(tiff_path).name}: {_e}")\n\n    return np.asarray(comp_labels_zyx), filter_stats\n\n\ndef _warp_zyx_with_ants(lab_zyx, mov_int_path, fix_img_int, transformlist, whichtoinvert, interpolator=\'nearestNeighbor\', out_dtype=np.int32):\n    arr_xyz = np.transpose(np.asarray(lab_zyx), (2, 1, 0)).astype(np.float32 if interpolator != \'nearestNeighbor\' else np.int32, copy=False)\n    mov_arr_img = ants.from_numpy(arr_xyz)\n    mov_img_int = ants.image_read(str(mov_int_path))\n    _ants_clone_geometry(mov_arr_img, mov_img_int)\n    if mov_arr_img.shape != mov_img_int.shape:\n        raise RuntimeError(\n            f"Array XYZ shape {mov_arr_img.shape} != moving intensity shape {mov_img_int.shape}."\n        )\n\n    warped_xyz = ants.apply_transforms(\n        fixed=fix_img_int,\n        moving=mov_arr_img,\n        transformlist=[str(t) for t in transformlist],\n        whichtoinvert=list(whichtoinvert),\n        interpolator=interpolator,\n    ).numpy()\n\n    warped_zyx = np.transpose(warped_xyz, (2, 1, 0))\n    if out_dtype is not None:\n        warped_zyx = warped_zyx.astype(out_dtype, copy=False)\n    return warped_zyx\n\n\ndef _save_warped_labels(save_basename: Path, warped_zyx, like_img=None):\n    warped_zyx = np.asarray(warped_zyx)\n    max_label = int(np.max(warped_zyx)) if warped_zyx.size else 0\n    if max_label > 65535:\n        raise RuntimeError(\n            f"Label id overflow for FIJI-compatible uint16 output ({max_label} > 65535) at {save_basename}."\n        )\n\n    tif_path = Path(f"{save_basename}_labels_uint16.tif")\n    arr_u16 = warped_zyx.astype(np.uint16, copy=False)\n\n    spacing_zyx = None\n    try:\n        img_for_spacing = like_img\n        if img_for_spacing is None and BEST_FIX_IMG_INT_LOCAL is not None:\n            img_for_spacing = BEST_FIX_IMG_INT_LOCAL\n        if img_for_spacing is not None:\n            sx, sy, sz = [float(v) for v in img_for_spacing.spacing]\n            if all(np.isfinite(v) and v > 0 for v in (sx, sy, sz)):\n                spacing_zyx = (sz, sy, sx)\n    except Exception:\n        spacing_zyx = None\n\n    kwargs = {\'compression\': \'deflate\'}\n    if spacing_zyx is not None:\n        dz, dy, dx = [float(v) for v in spacing_zyx]\n        kwargs.update(\n            imagej=True,\n            metadata={\'axes\': \'ZYX\', \'spacing\': dz, \'unit\': \'um\'},\n            resolution=(1.0 / dx, 1.0 / dy),\n        )\n    else:\n        kwargs.update(imagej=True, metadata={\'axes\': \'ZYX\'})\n\n    tiff.imwrite(tif_path, arr_u16, **kwargs)\n    return {"tif": tif_path}\n\n\ndef _invert_flags_for_chain(chain):\n    # Match applyTransform.py behavior: do not invert affines for forward chains.\n    return [False for _ in chain]\n\n\ndef _build_rn_to_best_chain(rnd):\n    if rnd == best_round_idx:\n        return []\n    r_to_best_warp = _find_transform(RN_TO_RBEST_TM_DIR, rnd, f"to_r{best_round_idx}", "warp")\n    r_to_best_aff = _find_transform(RN_TO_RBEST_TM_DIR, rnd, f"to_r{best_round_idx}", "affine")\n    chain = [r_to_best_warp, r_to_best_aff]\n    if any(c is None for c in chain):\n        raise FileNotFoundError(\n            f"Missing rn->rbest transforms for round r{rnd}: {chain}"\n        )\n    return chain\n\n\ndef _build_round_to_2p_chain(rnd):\n    best_to_2p_warp = _find_transform(RBEST_TM_DIR, best_round_idx, "to_2p", "warp")\n    best_to_2p_aff = _find_transform(RBEST_TM_DIR, best_round_idx, "to_2p", "affine")\n    chain = [best_to_2p_warp, best_to_2p_aff]\n    if any(c is None for c in chain):\n        raise FileNotFoundError(\n            f"Missing best->2p transforms for best round r{best_round_idx}: {chain}"\n        )\n    if int(rnd) != int(best_round_idx):\n        chain.extend(_build_rn_to_best_chain(int(rnd)))\n    return chain\n\n\ndef _save_rn_to_rbest_labels(mask_path: Path, rnd: int, labels_best_zyx, r_to_best_chain=None, r_to_best_flags=None, filter_stats=None):\n    if not PREPARE_RN_TO_RBEST_LABELS:\n        return None\n    if int(rnd) == int(best_round_idx) and not EXPORT_BEST_ROUND_LABELS_TO_RBEST:\n        return None\n\n    save_base = RN_TO_RBEST_LABELS_DIR / f"{Path(mask_path).stem}_in_rbest"\n    existing = next((p for p in warp_cache_candidates(save_base) if p.exists()), None)\n    if existing is not None and (not _metadata_filter_stats(save_base)):\n        existing = None\n    if existing is not None and (not RECOMPUTE_WARP):\n        print(f"[HCR] reused rn->rbest label prep for {Path(mask_path).name} -> {existing}")\n        return {"cache": existing}\n\n    out_paths = _save_warped_labels(save_base, np.asarray(labels_best_zyx, dtype=np.int32), like_img=best_fix_img_int)\n    meta = {\n        "round": int(rnd),\n        "mask": str(mask_path),\n        "method": "ants",\n        "output_space": "rbest",\n        "transformlist": [str(t) for t in (r_to_best_chain or [])],\n        "whichtoinvert": [bool(x) for x in (r_to_best_flags or [])],\n        "outputs": {k: str(v) for k, v in out_paths.items()},\n        "filter_stats": dict(filter_stats or {}),\n    }\n    write_warp_metadata(save_base, meta)\n    print(f"[HCR] saved rn->rbest labels for {Path(mask_path).name} -> {[str(v) for v in out_paths.values()]}")\n    return out_paths\n\n\ndef _save_aligned_2p_labels(mask_path: Path, rnd: int, labels_2p_zyx, transform_chain=None, transform_flags=None, filter_stats=None):\n    save_base = HCR_ALIGNED_DIR / f"{Path(mask_path).stem}_in_2p"\n    out_paths = _save_warped_labels(save_base, np.asarray(labels_2p_zyx, dtype=np.int32), like_img=anat_fix_img_int)\n    meta = {\n        "round": int(rnd),\n        "mask": str(mask_path),\n        "method": "ants_direct_to_2p",\n        "output_space": "2p",\n        "transformlist": [str(t) for t in (transform_chain or [])],\n        "whichtoinvert": [bool(x) for x in (transform_flags or [])],\n        "outputs": {k: str(v) for k, v in out_paths.items()},\n        "filter_stats": dict(filter_stats or {}),\n    }\n    write_warp_metadata(save_base, meta)\n    print(f"[HCR] saved direct 2p labels for {Path(mask_path).name} -> {[str(v) for v in out_paths.values()]}")\n    return out_paths\n\n\ndef _load_filter_stats_for_mask(mask_path: Path, fallback: dict = None):\n    fallback = dict(fallback or {})\n    stem = Path(mask_path).stem\n    candidates = [\n        RN_TO_RBEST_LABELS_DIR / f"{stem}_in_rbest",\n        HCR_ALIGNED_DIR / f"{stem}_in_2p",\n    ]\n\n    for save_base in candidates:\n        fs = _metadata_filter_stats(save_base)\n        if fs is None:\n            continue\n        out = dict(fallback)\n        out.update(fs)\n        return out\n\n    return fallback\ndef _write_external_sidecar(mask_path: Path, rnd: int, filter_stats: dict, rn_to_rbest_label_paths: dict = None):\n    save_base = HCR_ALIGNED_DIR / f"{Path(mask_path).stem}_in_2p"\n    expected_paths = [str(p) for p in warp_cache_candidates(save_base)]\n    meta = {\n        "round": int(rnd),\n        "mask": str(mask_path),\n        "method": "external_fiji_bigwarp",\n        "output_space": "2p",\n        "expected_outputs": expected_paths,\n        "rn_to_rbest_labels_paths": {k: str(v) for k, v in (rn_to_rbest_label_paths or {}).items()},\n        "filter_stats": dict(filter_stats or {}),\n    }\n    write_warp_metadata(save_base, meta)\n\n\ndef _count_nonzero_unique_labels(arr):\n    vals = np.unique(np.asarray(arr))\n    return int(np.count_nonzero(vals))\n\n\ndef _read_raw_anatomy_step_size_um():\n    meta_dir = FISH_DIR / \'01_raw\' / \'2p\' / \'metadata\'\n    if not meta_dir.exists():\n        return None\n    candidates = sorted(meta_dir.glob(\'*metadata*.csv\'))\n    if not candidates:\n        candidates = sorted(meta_dir.glob(\'*.csv\'))\n    for path in candidates:\n        try:\n            df = pd.read_csv(path, header=None)\n        except Exception:\n            continue\n        if int(df.shape[1]) < 2:\n            continue\n        keys = df.iloc[:, 0].astype(str).str.strip().str.lower()\n        vals = df.iloc[:, 1]\n        hit = vals.loc[keys == \'step_size_um_anatomy\']\n        if hit.empty:\n            continue\n        try:\n            return {\n                \'path\': str(path),\n                \'step_size_um_anatomy\': float(hit.iloc[0]),\n            }\n        except Exception:\n            continue\n    return None\n\n\nwarp_results = []\nmask_paths = sorted(HCR_CP_MASK_DIR.glob("*round*_cp_masks*.tif")) + sorted(HCR_CP_MASK_DIR.glob("*round*_cp_masks*.tiff"))\nif not mask_paths:\n    print(f"[HCR] No confocal masks found in {HCR_CP_MASK_DIR}")\n\nbest_fix_img_int = None\nbest_fix_int_path = _find_intensity_for_round(best_round_idx)\nanat_fix_img_int = None\nif PREPARE_RN_TO_RBEST_LABELS:\n    if best_fix_int_path is None or not Path(best_fix_int_path).exists():\n        raise FileNotFoundError(\n            f"Could not find best-round intensity for rn->rbest prep (r{best_round_idx}): {best_fix_int_path}"\n        )\n    best_fix_img_int = ants.image_read(str(best_fix_int_path))\nif HAVE_ANTSPY and (not RUN_EXTERNAL_FIJI_BIGWARP):\n    anat_fix_img_int = ants.image_read(str(ANAT_INT_PATH))\n\nprint(f"[HCR] External FIJI aligned dir: {HCR_ALIGNED_DIR}")\nprint(f"[HCR] Label prep workers: {max(1, min(HCR_WARP_THREADS, len(mask_paths) if mask_paths else 1))}")\n\n\ndef _process_single_mask(idx, mp: Path):\n    rnd = _parse_round_from_name(mp)\n    if rnd is None:\n        print("[HCR] Skip (no round in name):", mp)\n        return idx, None\n\n    should_prepare_labels = bool(\n        PREPARE_RN_TO_RBEST_LABELS\n        and (int(rnd) != int(best_round_idx) or EXPORT_BEST_ROUND_LABELS_TO_RBEST)\n    )\n\n    rn_to_rbest_save_base = RN_TO_RBEST_LABELS_DIR / f"{mp.stem}_in_rbest"\n    existing_rn_to_rbest = None\n    if should_prepare_labels:\n        existing_rn_to_rbest = next((p for p in warp_cache_candidates(rn_to_rbest_save_base) if p.exists()), None)\n        if existing_rn_to_rbest is not None and (not _metadata_filter_stats(rn_to_rbest_save_base)):\n            existing_rn_to_rbest = None\n\n    filter_stats = _load_filter_stats_for_mask(mp, fallback=None)\n    has_cached_filter_stats = isinstance(filter_stats, dict) and bool(filter_stats)\n\n    need_prepare_compute = bool(should_prepare_labels and (RECOMPUTE_WARP or existing_rn_to_rbest is None))\n    need_filter_compute = bool(RECOMPUTE_FILTER_STATS or (not has_cached_filter_stats))\n\n    labels_zyx = None\n    if need_prepare_compute or need_filter_compute:\n        labels_zyx, filter_stats = _load_and_filter_label_tiff(mp)\n    else:\n        print(f"[HCR] reused cached filter stats for {mp.name}")\n\n    mov_int_path = None\n    rn_to_rbest_label_paths = None\n    if should_prepare_labels:\n        if (not RECOMPUTE_WARP) and existing_rn_to_rbest is not None:\n            rn_to_rbest_label_paths = {"cache": existing_rn_to_rbest}\n        else:\n            if labels_zyx is None:\n                labels_zyx, filter_stats = _load_and_filter_label_tiff(mp)\n\n            mov_int_path = _find_hcr_intensity_for_mask(mp, rnd)\n            if mov_int_path is None or not Path(mov_int_path).exists():\n                mov_int_path = _find_intensity_for_round(rnd)\n                print(f"[HCR] Fallback to round geometry for {mp.name}: {mov_int_path}")\n\n            if mov_int_path is None or not Path(mov_int_path).exists():\n                print(f"[HCR] Missing moving intensity for rn->rbest prep ({mp.name}, round {rnd})")\n            elif int(rnd) == int(best_round_idx):\n                rn_to_rbest_label_paths = _save_rn_to_rbest_labels(\n                    mp,\n                    rnd,\n                    labels_zyx,\n                    r_to_best_chain=[],\n                    r_to_best_flags=[],\n                    filter_stats=filter_stats,\n                )\n            else:\n                r_to_best_chain = _build_rn_to_best_chain(rnd)\n                r_to_best_flags = _invert_flags_for_chain(r_to_best_chain)\n                labels_best_zyx = _warp_zyx_with_ants(\n                    labels_zyx,\n                    mov_int_path,\n                    best_fix_img_int,\n                    transformlist=r_to_best_chain,\n                    whichtoinvert=r_to_best_flags,\n                    interpolator=\'nearestNeighbor\',\n                    out_dtype=np.int32,\n                )\n                rn_to_rbest_label_paths = _save_rn_to_rbest_labels(\n                    mp,\n                    rnd,\n                    labels_best_zyx,\n                    r_to_best_chain=r_to_best_chain,\n                    r_to_best_flags=r_to_best_flags,\n                    filter_stats=filter_stats,\n                )\n\n    save_base = HCR_ALIGNED_DIR / f"{mp.stem}_in_2p"\n    filter_stats = dict(filter_stats or {})\n    cached_warp_meta = _metadata_filter_stats(save_base)\n    cached_warp_meta_valid = cached_warp_meta is not None\n    warped, warped_src = load_cached_warp(save_base)\n\n    if warped is None and ALLOW_LEGACY_WARP_CACHE:\n        legacy_candidates = []\n        for pat in [f"{mp.stem}*in_2p*label*.tif", f"{mp.stem}*in_2p*label*.tiff"]:\n            legacy_candidates.extend(sorted(HCR_ALIGNED_DIR.glob(pat)))\n        if legacy_candidates:\n            warped_src = legacy_candidates[0]\n            warped = tiff.imread(str(warped_src))\n\n    if not RUN_EXTERNAL_FIJI_BIGWARP:\n        need_direct_2p_compute = bool(RECOMPUTE_WARP or warped is None or (not cached_warp_meta_valid))\n        if need_direct_2p_compute:\n            if not HAVE_ANTSPY:\n                raise RuntimeError("ANTsPy is required to regenerate direct HCR->2P label warps when bigwarp=False.")\n            if labels_zyx is None:\n                labels_zyx, filter_stats = _load_and_filter_label_tiff(mp)\n                filter_stats = dict(filter_stats or {})\n\n            mov_int_path = _find_hcr_intensity_for_mask(mp, rnd)\n            if mov_int_path is None or not Path(mov_int_path).exists():\n                mov_int_path = _find_intensity_for_round(rnd)\n                print(f"[HCR] Fallback to round geometry for direct 2p warp {mp.name}: {mov_int_path}")\n            if mov_int_path is None or not Path(mov_int_path).exists():\n                raise FileNotFoundError(f"Missing moving intensity for direct 2p warp ({mp.name}, round {rnd})")\n\n            direct_chain = _build_round_to_2p_chain(int(rnd))\n            direct_flags = _invert_flags_for_chain(direct_chain)\n            warped = _warp_zyx_with_ants(\n                labels_zyx,\n                mov_int_path,\n                anat_fix_img_int,\n                transformlist=direct_chain,\n                whichtoinvert=direct_flags,\n                interpolator=\'nearestNeighbor\',\n                out_dtype=np.int32,\n            )\n            out_paths = _save_aligned_2p_labels(\n                mp,\n                rnd,\n                warped,\n                transform_chain=direct_chain,\n                transform_flags=direct_flags,\n                filter_stats=filter_stats,\n            )\n            warped_src = Path(out_paths.get("tif")) if out_paths.get("tif") else None\n        elif cached_warp_meta_valid:\n            print(f"[HCR] reused cached direct 2p labels for {mp.name}")\n    else:\n        # Write a sidecar in aligned-dir so downstream q95 lookup remains available.\n        _write_external_sidecar(mp, rnd, filter_stats, rn_to_rbest_label_paths=rn_to_rbest_label_paths)\n\n    if warped is None:\n        if RUN_EXTERNAL_FIJI_BIGWARP:\n            print(f"[HCR] Missing FIJI output for {mp.name}; expected something like {save_base}_labels_uint16.tif")\n        else:\n            print(f"[HCR] Missing direct 2p label warp for {mp.name}; expected {save_base}_labels_uint16.tif")\n        return idx, None\n\n    if np.asarray(warped).ndim != 3:\n        print(f"[HCR] Invalid FIJI output dimensions for {mp.name}: {np.asarray(warped).shape}")\n        return idx, None\n\n    filter_stats_loaded = _load_filter_stats_for_mask(mp, fallback=filter_stats)\n    out_paths = {"cache": warped_src} if warped_src is not None else {}\n    external_bigwarp_qc = None\n    if RUN_EXTERNAL_FIJI_BIGWARP and BIGWARP_QC_VALIDATE_OUTPUTS:\n        expected_n = filter_stats_loaded.get("n_labels_after", None)\n        loaded_n = _count_nonzero_unique_labels(warped)\n        loaded_max = int(np.max(warped)) if np.asarray(warped).size else 0\n        expected_n_int = None\n        try:\n            expected_n_int = int(expected_n)\n        except Exception:\n            expected_n_int = None\n        matches_expected = None if expected_n_int is None else bool(loaded_n == expected_n_int)\n        external_bigwarp_qc = {\n            "mask_name": mp.name,\n            "aligned_path": str(warped_src) if warped_src is not None else None,\n            "n_labels_expected_filtered": expected_n_int,\n            "n_labels_loaded_external": int(loaded_n),\n            "max_label_loaded_external": int(loaded_max),\n            "matches_filtered_label_count": matches_expected,\n        }\n        if matches_expected is False:\n            print(\n                f"[HCR] WARNING: external FIJI labels for {mp.name} contain {loaded_n} nonzero labels "\n                f"but filter_stats expects {expected_n_int}; verify BigWarp used the filtered rn->rbest labels"\n            )\n\n    return idx, {\n        "round": rnd,\n        "mask_path": mp,\n        "warped": np.asarray(warped),\n        "warped_paths": out_paths,\n        "save_base": save_base,\n        "filter_stats": filter_stats_loaded,\n        "low_conf_labels_native": filter_stats_loaded.get("low_conf_labels", []),\n        "rn_to_rbest_labels_paths": rn_to_rbest_label_paths,\n        "external_bigwarp_qc": external_bigwarp_qc,\n    }\nif mask_paths:\n    workers = max(1, min(HCR_WARP_THREADS, len(mask_paths)))\n    if workers == 1:\n        for idx, mp in enumerate(mask_paths):\n            _, res = _process_single_mask(idx, mp)\n            if res is not None:\n                warp_results.append(res)\n    else:\n        by_index = {}\n        with ThreadPoolExecutor(max_workers=workers) as ex:\n            fut_map = {ex.submit(_process_single_mask, idx, mp): (idx, mp) for idx, mp in enumerate(mask_paths)}\n            for fut in as_completed(fut_map):\n                idx, mp = fut_map[fut]\n                try:\n                    out_idx, res = fut.result()\n                except Exception as _e:\n                    print(f"[HCR] Worker failed for {mp.name}: {_e}")\n                    continue\n                by_index[out_idx] = res\n        for idx in sorted(by_index):\n            res = by_index[idx]\n            if res is not None:\n                warp_results.append(res)\n\nprint(f"[HCR] Loaded FIJI-aligned masks: {len(warp_results)}/{len(mask_paths)}")\nHCR_BIGWARP_OUTPUT_QC_DF = pd.DataFrame()\nHCR_BIGWARP_VOXEL_QC_DF = pd.DataFrame()\nif RUN_EXTERNAL_FIJI_BIGWARP:\n    qc_rows = [\n        dict(res.get(\'external_bigwarp_qc\', {}))\n        for res in warp_results\n        if isinstance(res.get(\'external_bigwarp_qc\'), dict) and res.get(\'external_bigwarp_qc\')\n    ]\n    HCR_BIGWARP_OUTPUT_QC_DF = pd.DataFrame.from_records(qc_rows)\n    if BIGWARP_QC_VALIDATE_OUTPUTS and not HCR_BIGWARP_OUTPUT_QC_DF.empty:\n        n_bad = int((HCR_BIGWARP_OUTPUT_QC_DF[\'matches_filtered_label_count\'] == False).sum())\n        if n_bad:\n            print(f"[HCR] WARNING: {n_bad}/{len(HCR_BIGWARP_OUTPUT_QC_DF)} external BigWarp outputs disagree with filtered label counts")\n    if BIGWARP_QC_CHECK_ANAT_Z:\n        raw_anat_step = _read_raw_anatomy_step_size_um()\n        cached_anat_z = np.nan\n        try:\n            cached_anat_z = float(VOX_ANAT_LOCAL.get(\'Z\', np.nan)) if VOX_ANAT_LOCAL else np.nan\n        except Exception:\n            cached_anat_z = np.nan\n        if raw_anat_step is not None:\n            raw_anat_z = float(raw_anat_step[\'step_size_um_anatomy\'])\n            z_matches = bool(np.isfinite(cached_anat_z) and np.isclose(cached_anat_z, raw_anat_z, atol=1e-6, rtol=0.0))\n            HCR_BIGWARP_VOXEL_QC_DF = pd.DataFrame.from_records([{\n                \'metadata_path\': raw_anat_step[\'path\'],\n                \'raw_step_size_um_anatomy\': raw_anat_z,\n                \'cached_vox_anat_z\': cached_anat_z,\n                \'matches_cached_vox_anat_z\': z_matches,\n            }])\n            if not z_matches:\n                print(\n                    f"[HCR] WARNING: raw metadata step_size_um_anatomy={raw_anat_z} but VOX_ANAT[\'Z\']={cached_anat_z}; "\n                    "Z-calibrated plots and distance thresholds may be misleading for this fish"\n                )\n'

CELL_43B_SOURCE = '# [43b]\ntry:\n    REQUIRE_FISH_STATE = require_fish_state\nexcept NameError:\n    REQUIRE_FISH_STATE = None\nif callable(REQUIRE_FISH_STATE) and not REQUIRE_FISH_STATE():\n    raise SystemExit\n# Prepare rn->rbest intensity stacks for external FIJI BigWarp input (no notebook-side rbest->2p warp).\nimport json\nimport os\nfrom concurrent.futures import ThreadPoolExecutor, as_completed\nfrom pathlib import Path\nimport numpy as np\nimport tifffile as tiff\n\ntry:\n    import ants  # type: ignore\n    HAVE_ANTSPY = True\nexcept Exception as _e:\n    HAVE_ANTSPY = False\n    _ANTSPY_ERR = _e\n\nRUN_EXTERNAL_FIJI_BIGWARP = bool(USE_EXTERNAL_FIJI_BIGWARP if \'USE_EXTERNAL_FIJI_BIGWARP\' in locals() else BIGWARP_FROM_METADATA)\nUSE_EXTERNAL_FIJI_BIGWARP = RUN_EXTERNAL_FIJI_BIGWARP\n\nif not RUN_EXTERNAL_FIJI_BIGWARP:\n    print(\'[43b] bigwarp metadata/config is False; skipping rn->rbest intensity prep and TIFF export.\')\nelif not HAVE_ANTSPY:\n    print(\'[43b] ANTsPy missing; skip rn->rbest intensity prep.\')\nelse:\n    RECOMPUTE_INTENSITY_WARP = False\n    INTENSITY_WARP_THREADS = max(1, min(4, os.cpu_count() or 1))\n    INTENSITY_INTERPOLATOR = \'welchWindowedSinc\'\n\n    PREFER_CELLPOSE_TIFF_INPUT = True\n    INTENSITY_INCLUDE_CHANNEL1 = True\n    CELLPOSE_CONVERT_DIR = FISH_DIR / \'03_analysis\' / \'confocal\' / \'raw\' / \'converted_nrrd_to_tif\'\n\n    PREPARE_RN_TO_RBEST_INTENSITY = bool(RUN_EXTERNAL_FIJI_BIGWARP)\n    INCLUDE_BEST_ROUND_INTENSITY_FOR_FIJI = True\n\n    RN_TO_RBEST_INTENSITY_FORMAT = \'tif\'\n    if RN_TO_RBEST_INTENSITY_FORMAT not in {\'tif\', \'tiff\', \'nrrd\'}:\n        print(f"[43b] Unsupported RN_TO_RBEST_INTENSITY_FORMAT={RN_TO_RBEST_INTENSITY_FORMAT}; using \'tif\'")\n        RN_TO_RBEST_INTENSITY_FORMAT = \'tif\'\n\n    # Optional explicit list of intensity paths; leave empty to auto-discover.\n    HCR_INTENSITY_PATHS = []\n    MANUAL_INTENSITY_WARP_PATH = None\n    MANUAL_INTENSITY_WARP_ROUND = None\n    INTENSITY_ROUNDS_FILTER = None\n\n    if PREPARE_RN_TO_RBEST_INTENSITY:\n        RN_TO_RBEST_INTENSITY_DIR.mkdir(parents=True, exist_ok=True)\n\n    intensity_geom_ref = {}\n    intensity_paths = [Path(p) for p in HCR_INTENSITY_PATHS] if HCR_INTENSITY_PATHS else []\n    if not intensity_paths:\n        base_nrrd_paths = sorted(\n            list((PREPROC_DIR / \'rbest\').glob(\'*round*_channel*.nrrd\')) +\n            list((PREPROC_DIR / \'rn\').glob(\'*round*_channel*.nrrd\'))\n        )\n        if not INTENSITY_INCLUDE_CHANNEL1:\n            base_nrrd_paths = [p for p in base_nrrd_paths if \'channel1\' not in p.name.lower()]\n\n        if PREFER_CELLPOSE_TIFF_INPUT and CELLPOSE_CONVERT_DIR.exists():\n            remapped = []\n            n_tiff = 0\n            for nrrd_path in base_nrrd_paths:\n                tif = CELLPOSE_CONVERT_DIR / f"{nrrd_path.stem}_uint8.tif"\n                tiff_path = CELLPOSE_CONVERT_DIR / f"{nrrd_path.stem}_uint8.tiff"\n                if tif.exists():\n                    chosen = tif\n                elif tiff_path.exists():\n                    chosen = tiff_path\n                else:\n                    chosen = nrrd_path\n\n                remapped.append(chosen)\n                if chosen != nrrd_path:\n                    intensity_geom_ref[str(chosen)] = Path(nrrd_path)\n                    n_tiff += 1\n            intensity_paths = remapped\n            print(f"[43b] using converted TIFF inputs for {n_tiff}/{len(base_nrrd_paths)} stacks")\n        else:\n            if PREFER_CELLPOSE_TIFF_INPUT and not CELLPOSE_CONVERT_DIR.exists():\n                print(f"[43b] converted TIFF dir not found ({CELLPOSE_CONVERT_DIR}); using NRRD inputs")\n            intensity_paths = base_nrrd_paths\n\n    manual_int_path = Path(MANUAL_INTENSITY_WARP_PATH) if MANUAL_INTENSITY_WARP_PATH else None\n    manual_int_round = MANUAL_INTENSITY_WARP_ROUND\n    if manual_int_path is not None:\n        intensity_paths.append(manual_int_path)\n\n    dedup = []\n    seen = set()\n    for p in intensity_paths:\n        ps = str(Path(p))\n        if ps in seen:\n            continue\n        seen.add(ps)\n        dedup.append(Path(p))\n    intensity_paths = dedup\n\n    if INTENSITY_ROUNDS_FILTER:\n        allowed = {int(r) for r in INTENSITY_ROUNDS_FILTER}\n        _filtered = []\n        for p in intensity_paths:\n            rnd = _parse_round_from_name(p)\n            if manual_int_path is not None and p == manual_int_path and manual_int_round is not None:\n                try:\n                    rnd = int(str(manual_int_round).lstrip(\'r\'))\n                except Exception:\n                    rnd = None\n            if rnd in allowed:\n                _filtered.append(p)\n        intensity_paths = _filtered\n\n    print(f"[43b] intensity stacks discovered: {len(intensity_paths)}")\n    print(f"[43b] prepare_rn_to_rbest_intensity={PREPARE_RN_TO_RBEST_INTENSITY} recompute={RECOMPUTE_INTENSITY_WARP}")\n    print(f"[43b] include_best_round={INCLUDE_BEST_ROUND_INTENSITY_FOR_FIJI} format={RN_TO_RBEST_INTENSITY_FORMAT}")\n\n    def _output_stem(ip_path: Path):\n        ip_path = Path(ip_path)\n        return ip_path.stem[:-6] if ip_path.stem.endswith(\'_uint8\') else ip_path.stem\n\n    def _rn_to_rbest_intensity_output_paths(ip_path: Path):\n        out_stem = _output_stem(ip_path)\n        out_ext = \'.nrrd\' if RN_TO_RBEST_INTENSITY_FORMAT == \'nrrd\' else \'.tif\'\n        out_path = RN_TO_RBEST_INTENSITY_DIR / f"{out_stem}_in_rbest{out_ext}"\n        meta_path = RN_TO_RBEST_INTENSITY_DIR / f"{out_stem}_in_rbest_warp_meta.json"\n        return out_path, meta_path\n\n    def _load_moving_image(ip_path: Path, rnd: int):\n        ip_path = Path(ip_path)\n        suffix = ip_path.suffix.lower()\n        if suffix in {\'.tif\', \'.tiff\'}:\n            arr = np.asarray(imread_any(ip_path))\n            if arr.ndim != 3:\n                raise RuntimeError(f"[43b] Expected 3D TIFF for moving image: {ip_path} (shape={arr.shape})")\n\n            geom_ref = intensity_geom_ref.get(str(ip_path), None)\n            if geom_ref is None:\n                stem0 = ip_path.stem[:-6] if ip_path.stem.endswith(\'_uint8\') else ip_path.stem\n                guess = PREPROC_DIR / (\'rbest\' if int(rnd) == int(best_round_idx) else \'rn\') / f"{stem0}.nrrd"\n                if guess.exists():\n                    geom_ref = guess\n\n            # Default notebook convention: TIFF stacks are stored as ZYX.\n            mov_xyz = np.transpose(arr, (2, 1, 0)).astype(np.float32, copy=False)\n\n            if geom_ref is not None and Path(geom_ref).exists():\n                ref_img = ants.image_read(str(geom_ref))\n                ref_shape_xyz = tuple(int(v) for v in ref_img.shape)\n                if tuple(int(v) for v in mov_xyz.shape) != ref_shape_xyz:\n                    # Defensive fallback if a TIFF was saved in XYZ instead of ZYX.\n                    if tuple(int(v) for v in arr.shape) == ref_shape_xyz:\n                        print(f"[43b] WARNING: TIFF appears XYZ (not ZYX) for {ip_path.name}; using direct shape match")\n                        mov_xyz = arr.astype(np.float32, copy=False)\n                    else:\n                        raise RuntimeError(\n                            f"[43b] TIFF shape mismatch for {ip_path.name}: "\n                            f"arr={tuple(arr.shape)} transposeZYX->XYZ={tuple(mov_xyz.shape)} refXYZ={ref_shape_xyz}"\n                        )\n\n                mov_img = ants.from_numpy(mov_xyz)\n                _ants_clone_geometry(mov_img, ref_img)\n                return mov_img\n\n            mov_img = ants.from_numpy(mov_xyz)\n            print(f"[43b] WARNING: no geometry reference for TIFF input {ip_path.name}; using assumed ZYX->XYZ")\n            return mov_img\n\n        return ants.image_read(str(ip_path))\n\n    def _build_rn_to_best_chain(rnd):\n        if int(rnd) == int(best_round_idx):\n            return []\n        r_to_best_warp = _find_transform(RN_TO_RBEST_TM_DIR, int(rnd), f\'to_r{best_round_idx}\', \'warp\')\n        r_to_best_aff = _find_transform(RN_TO_RBEST_TM_DIR, int(rnd), f\'to_r{best_round_idx}\', \'affine\')\n        chain = [r_to_best_warp, r_to_best_aff]\n        if any(c is None for c in chain):\n            raise FileNotFoundError(\n                f\'rn->rbest transforms are missing for round r{rnd}: {chain}\'\n            )\n        return chain\n\n    best_fix_path = _find_intensity_for_round(int(best_round_idx))\n    if best_fix_path is None or not Path(best_fix_path).exists():\n        raise FileNotFoundError(f"[43b] Could not locate best-round fixed intensity for r{best_round_idx}: {best_fix_path}")\n    best_fix_img_int = ants.image_read(str(best_fix_path))\n\n    def _ants_spacing_zyx(img):\n        try:\n            sx, sy, sz = [float(v) for v in img.spacing]\n            if all(np.isfinite(v) and v > 0 for v in (sx, sy, sz)):\n                return (sz, sy, sx)\n        except Exception:\n            pass\n        return None\n\n    def _write_zyx_tiff(path: Path, arr_zyx, spacing_zyx=None):\n        path = Path(path)\n        arr_zyx = np.asarray(arr_zyx)\n        kwargs = {\'compression\': \'deflate\'}\n        if spacing_zyx is not None:\n            dz, dy, dx = [float(v) for v in spacing_zyx]\n            if all(np.isfinite(v) and v > 0 for v in (dz, dy, dx)):\n                kwargs.update(\n                    imagej=True,\n                    metadata={\'axes\': \'ZYX\', \'spacing\': dz, \'unit\': \'um\'},\n                    resolution=(1.0 / dx, 1.0 / dy),\n                )\n            else:\n                kwargs.update(imagej=True, metadata={\'axes\': \'ZYX\'})\n        else:\n            kwargs.update(imagej=True, metadata={\'axes\': \'ZYX\'})\n        tiff.imwrite(path, arr_zyx, **kwargs)\n\n    def _save_rn_to_rbest_intensity(ip_path: Path, rnd: int, best_space_img, rn_to_best_chain=None, rn_to_best_flags=None):\n        out_path, meta_path = _rn_to_rbest_intensity_output_paths(ip_path)\n\n        if RN_TO_RBEST_INTENSITY_FORMAT == \'nrrd\':\n            ants.image_write(best_space_img, str(out_path))\n        else:\n            arr_zyx = np.transpose(best_space_img.numpy(), (2, 1, 0)).astype(np.float32, copy=False)\n            _write_zyx_tiff(\n                out_path,\n                arr_zyx,\n                spacing_zyx=_ants_spacing_zyx(best_space_img),\n            )\n\n        meta = {\n            \'round\': int(rnd),\n            \'moving_intensity\': str(ip_path),\n            \'output_space\': \'rbest\',\n            \'method\': \'ants\',\n            \'transformlist\': [str(t) for t in (rn_to_best_chain or [])],\n            \'whichtoinvert\': [bool(x) for x in (rn_to_best_flags or [])],\n            \'interpolator\': INTENSITY_INTERPOLATOR,\n            \'output_format\': RN_TO_RBEST_INTENSITY_FORMAT,\n            \'output\': str(out_path),\n        }\n        meta_path.write_text(json.dumps(meta, indent=2))\n        print(f"[43b] saved rn->rbest intensity {ip_path.name} -> {out_path}")\n        return out_path\n\n    def _process_single_intensity_path(ip_path: Path):\n        if not PREPARE_RN_TO_RBEST_INTENSITY:\n            return None\n        if not ip_path.exists():\n            print(f"[43b] Intensity path missing, skip: {ip_path}")\n            return None\n\n        rnd = None\n        if manual_int_path is not None and ip_path == manual_int_path and manual_int_round is not None:\n            try:\n                rnd = int(str(manual_int_round).lstrip(\'r\'))\n            except Exception:\n                rnd = None\n        if rnd is None:\n            rnd = _parse_round_from_name(ip_path)\n        if rnd is None:\n            print(f"[43b] Could not parse round from intensity path: {ip_path}")\n            return None\n\n        if int(rnd) == int(best_round_idx) and not INCLUDE_BEST_ROUND_INTENSITY_FOR_FIJI:\n            return None\n\n        out_path, _ = _rn_to_rbest_intensity_output_paths(ip_path)\n        if out_path.exists() and (not RECOMPUTE_INTENSITY_WARP):\n            print(f"[43b] reused rn->rbest intensity prep {ip_path.name} -> {out_path}")\n            return out_path\n\n        mov_img_int = _load_moving_image(ip_path, int(rnd))\n        if int(rnd) == int(best_round_idx):\n            best_space_img = mov_img_int\n            rn_to_best_chain = []\n            rn_to_best_flags = []\n        else:\n            rn_to_best_chain = _build_rn_to_best_chain(int(rnd))\n            rn_to_best_flags = _invert_flags_for_chain(rn_to_best_chain)\n            best_space_img = ants.apply_transforms(\n                fixed=best_fix_img_int,\n                moving=mov_img_int,\n                transformlist=[str(t) for t in rn_to_best_chain],\n                whichtoinvert=list(rn_to_best_flags),\n                interpolator=INTENSITY_INTERPOLATOR,\n            )\n\n        return _save_rn_to_rbest_intensity(\n            ip_path,\n            int(rnd),\n            best_space_img,\n            rn_to_best_chain=rn_to_best_chain,\n            rn_to_best_flags=rn_to_best_flags,\n        )\n\n    if intensity_paths and PREPARE_RN_TO_RBEST_INTENSITY:\n        workers = max(1, min(INTENSITY_WARP_THREADS, len(intensity_paths)))\n        print(f"[43b] rn->rbest intensity workers: {workers}")\n        if workers == 1:\n            for ip_path in intensity_paths:\n                _process_single_intensity_path(ip_path)\n        else:\n            with ThreadPoolExecutor(max_workers=workers) as ex:\n                fut_map = {ex.submit(_process_single_intensity_path, ip_path): ip_path for ip_path in intensity_paths}\n                for fut in as_completed(fut_map):\n                    ip_path = fut_map[fut]\n                    try:\n                        fut.result()\n                    except Exception as _e:\n                        print(f"[43b] Intensity worker failed for {ip_path.name}: {_e}")\n    elif PREPARE_RN_TO_RBEST_INTENSITY:\n        print(\'[43b] No intensity stacks discovered.\')\n    else:\n        print(\'[43b] rn->rbest intensity prep disabled by PREPARE_RN_TO_RBEST_INTENSITY flag.\')\n'
