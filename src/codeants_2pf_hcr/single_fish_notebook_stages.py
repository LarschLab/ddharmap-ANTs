"""Package-owned migration shims for heavy single-fish notebook cells.

These wrappers preserve legacy cell behavior while keeping notebook cells
orchestration-thin during notebook-to-package migration. New reusable logic
should still move to explicit owner APIs instead of adding more embedded
cell bodies here.
"""

from __future__ import annotations

from collections.abc import MutableMapping
from typing import Any

from ._single_fish_migrated_cell_sources import MIGRATED_CELL_SOURCE_BY_TAG

_CELL_SOURCE_BY_TAG: dict[str, str] = {
    '22c': '# [22c]\ntry:\n    REQUIRE_FISH_STATE = require_fish_state\nexcept NameError:\n    REQUIRE_FISH_STATE = None\nif callable(REQUIRE_FISH_STATE) and not REQUIRE_FISH_STATE():\n    raise SystemExit\n# Interactive midline tuning (DY + angle), standalone (replaces [22a]/[22b])\nimport json\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nfrom pathlib import Path\nfrom skimage import transform\nfrom skimage.filters import gaussian, threshold_otsu\nfrom skimage.measure import label, regionprops\n\ntry:\n    import ipywidgets as widgets\n    from IPython.display import display, clear_output\n    _HAS_WIDGETS = True\nexcept Exception:\n    _HAS_WIDGETS = False\n    print(\'[midline-tune] ipywidgets not available; falling back to static preview.\')\n\nif not plane_refs:\n    print(\'[midline-tune] plane_refs missing; run [16] first.\')\nelse:\n    _midline_out_root = Path(OUT_REG if OUT_REG is not None else OUTDIR)\n    MIDLINE_JSON_PATH = _midline_out_root / \'midline_params_func_ref.json\'\n    MIDLINE_REUSE_IF_EXISTS = True\n    MIDLINE_FORCE_REESTIMATE = False\n    MIDLINE_SAVE = True\n\n    MIDLINE_REF_PLANE_IDX = len(plane_refs) // 2\n    MIDLINE_DX = 0.0\n    MIDLINE_DY = 0.0\n    MIDLINE_DTHETA_DEG = 0.0\n    MIDLINE_AX = 0.0\n    MIDLINE_AY = 0.0\n    MIDLINE_UNCERTAIN_BAND_PX = 0.0\n    MIDLINE_POS_SIDE_LABEL = \'left\'\n    MIDLINE_NEG_SIDE_LABEL = \'right\'\n\n    MIDLINE_PLANE_OFFSETS = {}\n\n    # If False (default), this cell loads saved fish-specific manual params from JSON.\n    # Set True only when you intentionally want to start from current explicit values.\n    MIDLINE_USE_MANUAL_OVERRIDES = False\n    MIDLINE_USE_REF_PLANE_OVERRIDE = False\n\n    MIDLINE_TUNE_ZOOM = 1.5\n    MIDLINE_TUNE_NCOLS = 2\n\n    def _norm01_local(a):\n        a = np.asarray(a, dtype=np.float32)\n        finite = np.isfinite(a)\n        if not finite.any():\n            return np.zeros_like(a, dtype=np.float32)\n        lo = float(np.nanpercentile(a[finite], 1.0))\n        hi = float(np.nanpercentile(a[finite], 99.0))\n        if hi <= lo:\n            lo = float(np.nanmin(a[finite]))\n            hi = float(np.nanmax(a[finite]))\n            if hi <= lo:\n                return np.zeros_like(a, dtype=np.float32)\n        out = (a - lo) / (hi - lo)\n        out[~finite] = 0.0\n        return np.clip(out, 0.0, 1.0).astype(np.float32)\n\n    def _apply_tform_preview_local(img, tform, output_shape):\n        try:\n            return apply_transform_2d(img, tform, output_shape=output_shape, order=1)\n        except Exception:\n            pass\n        return transform.warp(\n            np.asarray(img, dtype=np.float32),\n            tform.inverse,\n            output_shape=output_shape,\n            order=1,\n            mode=\'constant\',\n            cval=0.0,\n            preserve_range=True,\n        ).astype(np.float32)\n\n    def _pick_func_ref_for_midline(pr, anat_shape=None):\n        img = None\n        src_label = \'raw\'\n        if pr.get(\'ref_warped\') is not None:\n            img = pr.get(\'ref_warped\')\n            src_label = \'warped\'\n        elif pr.get(\'tform\') is not None:\n            mov = pr.get(\'ref_match\', pr.get(\'ref2d_raw\', pr.get(\'ref2d\')))\n            if mov is not None and anat_shape is not None:\n                img = _apply_tform_preview_local(mov, pr[\'tform\'], output_shape=anat_shape)\n                src_label = \'tform-preview\'\n        elif pr.get(\'ref_warped_raw\') is not None:\n            img = pr.get(\'ref_warped_raw\')\n            src_label = \'warped-raw\'\n        else:\n            img = pr.get(\'ref2d\', pr.get(\'ref2d_raw\'))\n            src_label = \'raw\'\n\n        if img is None:\n            return None, None\n\n        arr = np.asarray(img, dtype=np.float32)\n        if anat_shape is not None and arr.shape != tuple(anat_shape):\n            try:\n                arr = transform.resize(\n                    arr,\n                    anat_shape,\n                    order=1,\n                    mode=\'reflect\',\n                    preserve_range=True,\n                    anti_aliasing=True,\n                ).astype(np.float32)\n            except Exception:\n                pass\n        return _norm01_local(arr), src_label\n\n    def _midline_mask_from_ref(img01):\n        if img01 is None:\n            raise RuntimeError(\'functional reference image missing\')\n        sm = gaussian(np.asarray(img01, dtype=np.float32), sigma=2.0, preserve_range=True)\n        vals = sm[np.isfinite(sm)]\n        if vals.size < 100:\n            raise RuntimeError(\'not enough finite pixels in functional reference image\')\n        try:\n            thr = float(threshold_otsu(vals))\n        except Exception:\n            thr = float(np.percentile(vals, 70.0))\n        mask = sm > thr\n        if int(mask.sum()) < max(50, int(0.01 * mask.size)):\n            thr = float(np.percentile(vals, 85.0))\n            mask = sm > thr\n        if int(mask.sum()) == 0:\n            raise RuntimeError(\'could not build functional mask for midline estimation\')\n        lab = label(mask.astype(np.uint8), connectivity=2)\n        if int(lab.max()) > 0:\n            regs = regionprops(lab)\n            best = max(regs, key=lambda r: r.area)\n            mask = lab == int(best.label)\n        return mask.astype(bool)\n\n    def _estimate_midline_from_mask(mask):\n        ys, xs = np.nonzero(mask)\n        if xs.size < 20:\n            raise RuntimeError(\'mask too small for PCA midline estimation\')\n        x0 = float(xs.mean())\n        y0 = float(ys.mean())\n        coords = np.column_stack([xs - x0, ys - y0]).astype(np.float64)\n        cov = np.cov(coords, rowvar=False)\n        eigvals, eigvecs = np.linalg.eigh(cov)\n        v = eigvecs[:, int(np.argmax(eigvals))]\n        theta_deg = float(np.degrees(np.arctan2(v[1], v[0])))\n        theta_deg = ((theta_deg + 90.0) % 180.0) - 90.0\n        return x0, y0, theta_deg\n\n    def _wrap_angle(theta_deg):\n        return ((float(theta_deg) + 90.0) % 180.0) - 90.0\n\n    def _line_segment(x0, y0, theta_deg, shape):\n        h, w = int(shape[0]), int(shape[1])\n        L = float(max(h, w) * 2.0)\n        th = np.deg2rad(float(theta_deg))\n        dx = np.cos(th) * L\n        dy = np.sin(th) * L\n        return (x0 - dx, y0 - dy), (x0 + dx, y0 + dy)\n\n    def _plane_offset_dict(plane_idx):\n        raw = MIDLINE_PLANE_OFFSETS.get(plane_idx, MIDLINE_PLANE_OFFSETS.get(str(plane_idx), {}))\n        if not isinstance(raw, dict):\n            return {\'dx\': 0.0, \'dy\': 0.0, \'dtheta_deg\': 0.0}\n        return {\n            \'dx\': float(raw.get(\'dx\', 0.0)),\n            \'dy\': float(raw.get(\'dy\', 0.0)),\n            \'dtheta_deg\': float(raw.get(\'dtheta_deg\', raw.get(\'dtheta\', 0.0))),\n        }\n\n    def _midline_signed_distance_px(x, y, params):\n        x = np.asarray(x, dtype=np.float64)\n        y = np.asarray(y, dtype=np.float64)\n        th = np.deg2rad(float(params[\'theta_deg\']))\n        nx = -np.sin(th)\n        ny = np.cos(th)\n        return (x - float(params[\'x0\'])) * nx + (y - float(params[\'y0\'])) * ny\n\n    def _save_bundle(bundle, out_path):\n        out_path = Path(out_path)\n        out_path.parent.mkdir(parents=True, exist_ok=True)\n        out_path.write_text(json.dumps(bundle, indent=2))\n\n    loaded_bundle = None\n    if MIDLINE_JSON_PATH.exists() and MIDLINE_REUSE_IF_EXISTS and (not MIDLINE_FORCE_REESTIMATE):\n        try:\n            loaded_bundle = json.loads(MIDLINE_JSON_PATH.read_text())\n            print(f"[midline-tune] loaded existing params: {MIDLINE_JSON_PATH}")\n        except Exception as e:\n            print(f"[midline-tune] failed to load existing params ({MIDLINE_JSON_PATH}): {e}")\n            loaded_bundle = None\n\n    if loaded_bundle is not None and (not MIDLINE_USE_MANUAL_OVERRIDES):\n        try:\n            _man = loaded_bundle.get(\'manual\', {}) if isinstance(loaded_bundle, dict) else {}\n            MIDLINE_DX = float(_man.get(\'dx\', MIDLINE_DX))\n            MIDLINE_DY = float(_man.get(\'dy\', MIDLINE_DY))\n            MIDLINE_DTHETA_DEG = float(_man.get(\'dtheta_deg\', MIDLINE_DTHETA_DEG))\n            MIDLINE_AX = float(_man.get(\'ax\', MIDLINE_AX))\n            MIDLINE_AY = float(_man.get(\'ay\', MIDLINE_AY))\n            MIDLINE_UNCERTAIN_BAND_PX = float(_man.get(\'uncertain_band_px\', MIDLINE_UNCERTAIN_BAND_PX))\n            _po = _man.get(\'plane_offsets\', MIDLINE_PLANE_OFFSETS)\n            if isinstance(_po, dict):\n                MIDLINE_PLANE_OFFSETS = _po\n            _sides = loaded_bundle.get(\'side_labels\', {}) if isinstance(loaded_bundle, dict) else {}\n            MIDLINE_POS_SIDE_LABEL = str(_sides.get(\'positive\', MIDLINE_POS_SIDE_LABEL)).strip().lower()\n            if MIDLINE_POS_SIDE_LABEL not in {\'left\', \'right\'}:\n                MIDLINE_POS_SIDE_LABEL = \'left\'\n            MIDLINE_NEG_SIDE_LABEL = \'left\' if MIDLINE_POS_SIDE_LABEL == \'right\' else \'right\'\n            print(\'[midline-tune] using saved manual corrections from JSON.\')\n        except Exception as e:\n            print(f"[midline-tune] could not apply saved manual corrections: {e}")\n\n    if loaded_bundle is not None and (not MIDLINE_USE_REF_PLANE_OVERRIDE):\n        try:\n            MIDLINE_REF_PLANE_IDX = int(loaded_bundle.get(\'ref_plane_idx\', MIDLINE_REF_PLANE_IDX))\n        except Exception:\n            pass\n\n    MIDLINE_REF_PLANE_IDX = max(0, min(int(MIDLINE_REF_PLANE_IDX), len(plane_refs) - 1))\n    ref_pr = plane_refs[int(MIDLINE_REF_PLANE_IDX)]\n    ref_best_z = float(ref_pr.get(\'best_z\', MIDLINE_REF_PLANE_IDX))\n\n    anat_shape = None\n    try:\n        anat_shape = tuple(anat.shape[1:]) if anat is not None else None\n    except Exception:\n        anat_shape = None\n\n    ref_img, ref_src = _pick_func_ref_for_midline(ref_pr, anat_shape=anat_shape)\n    if ref_img is None:\n        raise RuntimeError(\'[midline-tune] could not load functional reference image for selected ref plane\')\n\n    base_x0 = np.nan\n    base_y0 = np.nan\n    base_theta = np.nan\n    base_mode = \'auto\'\n\n    if loaded_bundle is not None and (not MIDLINE_FORCE_REESTIMATE):\n        try:\n            _b = loaded_bundle.get(\'base\', {}) if isinstance(loaded_bundle, dict) else {}\n            base_x0 = float(_b.get(\'x0\', np.nan))\n            base_y0 = float(_b.get(\'y0\', np.nan))\n            base_theta = float(_b.get(\'theta_deg\', np.nan))\n            if np.isfinite(base_x0) and np.isfinite(base_y0) and np.isfinite(base_theta):\n                base_mode = \'loaded\'\n        except Exception:\n            pass\n\n    ref_mask = None\n    if not (np.isfinite(base_x0) and np.isfinite(base_y0) and np.isfinite(base_theta)):\n        ref_mask = _midline_mask_from_ref(ref_img)\n        base_x0, base_y0, base_theta = _estimate_midline_from_mask(ref_mask)\n        base_mode = \'auto\'\n        print(\'[midline-tune] auto-estimated base line from functional reference mask.\')\n\n    base_theta = _wrap_angle(base_theta)\n\n    payload = []\n    for p_idx, pr in enumerate(plane_refs):\n        img, src_label = _pick_func_ref_for_midline(pr, anat_shape=anat_shape)\n        payload.append({\n            \'plane_idx\': int(p_idx),\n            \'best_z\': float(pr.get(\'best_z\', p_idx)),\n            \'img\': img,\n            \'src\': src_label,\n        })\n\n    def _params_for_plane(plane_idx, dy_manual, dtheta_manual):\n        pr = plane_refs[int(plane_idx)]\n        best_z_i = float(pr.get(\'best_z\', plane_idx))\n        dz = best_z_i - float(ref_best_z)\n\n        x0 = float(base_x0) + float(MIDLINE_DX) + float(MIDLINE_AX) * dz\n        y0 = float(base_y0) + float(dy_manual) + float(MIDLINE_AY) * dz\n        th = _wrap_angle(float(base_theta) + float(dtheta_manual))\n\n        off = _plane_offset_dict(int(plane_idx))\n        x0 += float(off[\'dx\'])\n        y0 += float(off[\'dy\'])\n        th = _wrap_angle(th + float(off[\'dtheta_deg\']))\n\n        return {\n            \'plane_idx\': int(plane_idx),\n            \'best_z\': float(best_z_i),\n            \'x0\': float(x0),\n            \'y0\': float(y0),\n            \'theta_deg\': float(th),\n        }\n\n    def _build_bundle(dy_manual, dtheta_manual):\n        per_plane = {}\n        for item in payload:\n            p = _params_for_plane(int(item[\'plane_idx\']), dy_manual, dtheta_manual)\n            per_plane[int(item[\'plane_idx\'])] = p\n\n        bundle = {\n            \'fish_id\': str(FISH_ID),\n            \'method\': \'functional_ref_middle_plane\',\n            \'base_mode\': str(base_mode),\n            \'ref_plane_idx\': int(MIDLINE_REF_PLANE_IDX),\n            \'ref_plane_label\': str(ref_pr.get(\'label\', f\'plane{MIDLINE_REF_PLANE_IDX}\')),\n            \'ref_best_z\': float(ref_best_z),\n            \'base\': {\n                \'x0\': float(base_x0),\n                \'y0\': float(base_y0),\n                \'theta_deg\': float(base_theta),\n                \'source_label\': str(ref_src) if ref_src is not None else None,\n                \'mask_pixels\': int(ref_mask.sum()) if ref_mask is not None else None,\n            },\n            \'manual\': {\n                \'dx\': float(MIDLINE_DX),\n                \'dy\': float(dy_manual),\n                \'dtheta_deg\': float(dtheta_manual),\n                \'ax\': float(MIDLINE_AX),\n                \'ay\': float(MIDLINE_AY),\n                \'plane_offsets\': MIDLINE_PLANE_OFFSETS,\n                \'uncertain_band_px\': float(MIDLINE_UNCERTAIN_BAND_PX),\n            },\n            \'side_labels\': {\n                \'positive\': MIDLINE_POS_SIDE_LABEL,\n                \'negative\': MIDLINE_NEG_SIDE_LABEL,\n            },\n            \'per_plane\': {str(k): v for k, v in per_plane.items()},\n        }\n        return bundle, per_plane\n\n    def _apply_zoom(ax, shape, zoom, center_xy):\n        if not np.isfinite(zoom) or float(zoom) <= 1.0:\n            return\n        h, w = int(shape[0]), int(shape[1])\n        cx, cy = float(center_xy[0]), float(center_xy[1])\n        half_w = max(5.0, float(w) / (2.0 * float(zoom)))\n        half_h = max(5.0, float(h) / (2.0 * float(zoom)))\n        x0 = max(0.0, cx - half_w)\n        x1 = min(float(w), cx + half_w)\n        y0 = max(0.0, cy - half_h)\n        y1 = min(float(h), cy + half_h)\n        ax.set_xlim(x0, x1)\n        ax.set_ylim(y1, y0)\n\n    def _render_preview(dy_manual, dtheta_manual, zoom, ncols):\n        valid = [it for it in payload if it[\'img\'] is not None]\n        if not valid:\n            print(\'[midline-tune] no valid functional reference images to render.\')\n            return\n        n = len(valid)\n        ncols = max(1, min(int(ncols), n))\n        nrows = int(np.ceil(n / float(ncols)))\n        fig, axes = plt.subplots(nrows, ncols, figsize=(5.5 * ncols, 5.5 * nrows))\n        axes = np.atleast_1d(axes).ravel()\n\n        for ax, item in zip(axes, valid):\n            p = _params_for_plane(int(item[\'plane_idx\']), dy_manual, dtheta_manual)\n            img = item[\'img\']\n            (x1, y1), (x2, y2) = _line_segment(p[\'x0\'], p[\'y0\'], p[\'theta_deg\'], img.shape)\n\n            ax.imshow(img, cmap=\'gray\')\n            ax.plot([x1, x2], [y1, y2], color=\'yellow\', linewidth=1.8)\n            ax.scatter([p[\'x0\']], [p[\'y0\']], s=10, c=\'yellow\')\n            _apply_zoom(ax, img.shape, zoom, center_xy=(p[\'x0\'], p[\'y0\']))\n            ax.set_title(f"p{p[\'plane_idx\']} z={p[\'best_z\']:g}")\n            ax.axis(\'off\')\n\n        for ax in axes[n:]:\n            ax.axis(\'off\')\n\n        fig.suptitle(\n            f"Midline tuning | DY={dy_manual:.1f}px | dTheta={dtheta_manual:.2f}deg | zoom={zoom:.2f}x"\n        )\n        fig.tight_layout(rect=[0, 0, 1, 0.95])\n        plt.show()\n\n    _bundle_init, _per_plane_init = _build_bundle(MIDLINE_DY, MIDLINE_DTHETA_DEG)\n    midline_params = _bundle_init\n    midline_by_plane = _per_plane_init\n    midline_json_path = str(MIDLINE_JSON_PATH)\n    MIDLINE_POS_SIDE_LABEL = MIDLINE_POS_SIDE_LABEL\n    MIDLINE_NEG_SIDE_LABEL = MIDLINE_NEG_SIDE_LABEL\n    MIDLINE_UNCERTAIN_BAND_PX = float(MIDLINE_UNCERTAIN_BAND_PX)\n    MIDLINE_FISH_ID = str(FISH_ID)\n\n    def _midline_params_for_plane(plane_idx):\n        _mbp = midline_by_plane\n        return _mbp.get(int(plane_idx), _mbp.get(str(plane_idx), _mbp.get(int(MIDLINE_REF_PLANE_IDX), None)))\n\n    def _midline_signed_distance_for_plane(x, y, plane_idx):\n        _p = _midline_params_for_plane(int(plane_idx))\n        if _p is None:\n            return np.full(np.asarray(x).shape, np.nan, dtype=float)\n        return _midline_signed_distance_px(x, y, _p)\n\n    def _annotate_midline_side(df, x_col=\'x\', y_col=\'y\', plane_col=\'plane\', uncertain_band_px=None):\n        if df is None or getattr(df, \'empty\', True):\n            return df\n        out = df.copy()\n        band = float(MIDLINE_UNCERTAIN_BAND_PX if uncertain_band_px is None else uncertain_band_px)\n        if plane_col in out.columns:\n            d = np.full(len(out), np.nan, dtype=float)\n            for p_idx, idx in out.groupby(plane_col).groups.items():\n                p = _midline_params_for_plane(int(p_idx))\n                if p is None:\n                    continue\n                xv = pd.to_numeric(out.loc[idx, x_col], errors=\'coerce\').to_numpy(dtype=float)\n                yv = pd.to_numeric(out.loc[idx, y_col], errors=\'coerce\').to_numpy(dtype=float)\n                d[idx] = _midline_signed_distance_px(xv, yv, p)\n        else:\n            p = _midline_params_for_plane(int(MIDLINE_REF_PLANE_IDX))\n            xv = pd.to_numeric(out[x_col], errors=\'coerce\').to_numpy(dtype=float)\n            yv = pd.to_numeric(out[y_col], errors=\'coerce\').to_numpy(dtype=float)\n            d = _midline_signed_distance_px(xv, yv, p) if p is not None else np.full(len(out), np.nan, dtype=float)\n\n        out[\'midline_signed_dist_px\'] = d\n        side = np.full(len(out), \'unknown\', dtype=object)\n        finite = np.isfinite(d)\n        side[(finite) & (d > band)] = MIDLINE_POS_SIDE_LABEL\n        side[(finite) & (d < -band)] = MIDLINE_NEG_SIDE_LABEL\n        side[(finite) & (np.abs(d) <= band)] = \'midline\'\n        out[\'midline_side\'] = side\n        out[\'midline_uncertain\'] = np.abs(d) <= band\n        return out\n\n    print(\n        f"[midline-tune] fish={FISH_ID} | ref_plane={MIDLINE_REF_PLANE_IDX} | "\n        f"base_mode={base_mode} | start DY={MIDLINE_DY:.2f}, dTheta={MIDLINE_DTHETA_DEG:.2f}"\n    )\n\n    if _HAS_WIDGETS:\n        dy_slider = widgets.FloatSlider(\n            value=float(MIDLINE_DY), min=-250.0, max=250.0, step=0.5,\n            description=\'DY\', continuous_update=False,\n        )\n        dtheta_slider = widgets.FloatSlider(\n            value=float(MIDLINE_DTHETA_DEG), min=-30.0, max=30.0, step=0.1,\n            description=\'Angle\', continuous_update=False,\n        )\n        zoom_slider = widgets.FloatSlider(\n            value=float(MIDLINE_TUNE_ZOOM), min=1.0, max=8.0, step=0.1,\n            description=\'Zoom\', continuous_update=False,\n        )\n        ncols_slider = widgets.IntSlider(\n            value=max(1, min(int(MIDLINE_TUNE_NCOLS), len(plane_refs))),\n            min=1, max=min(6, len(plane_refs)), step=1,\n            description=\'N cols\', continuous_update=False,\n        )\n\n        btn_commit = widgets.Button(description=\'Commit + Save\', button_style=\'success\')\n        btn_reset = widgets.Button(description=\'Reset Sliders\', button_style=\'warning\')\n        out_status = widgets.Output()\n\n        def _on_commit(_):\n            global midline_params, midline_by_plane, MIDLINE_DX, MIDLINE_DY, MIDLINE_DTHETA_DEG, MIDLINE_AX, MIDLINE_AY, MIDLINE_PLANE_OFFSETS, MIDLINE_UNCERTAIN_BAND_PX, MIDLINE_USE_MANUAL_OVERRIDES, MIDLINE_FISH_ID\n            dy = float(dy_slider.value)\n            dth = float(dtheta_slider.value)\n            bundle, per_plane = _build_bundle(dy, dth)\n\n            midline_params = bundle\n            midline_by_plane = per_plane\n            MIDLINE_DX = float(MIDLINE_DX)\n            MIDLINE_DY = float(dy)\n            MIDLINE_DTHETA_DEG = float(dth)\n            MIDLINE_AX = float(MIDLINE_AX)\n            MIDLINE_AY = float(MIDLINE_AY)\n            MIDLINE_PLANE_OFFSETS = MIDLINE_PLANE_OFFSETS\n            MIDLINE_UNCERTAIN_BAND_PX = float(MIDLINE_UNCERTAIN_BAND_PX)\n            MIDLINE_USE_MANUAL_OVERRIDES = True\n            MIDLINE_FISH_ID = str(FISH_ID)\n\n            msg = \'[midline-tune] committed in memory.\'\n            if MIDLINE_SAVE:\n                try:\n                    _save_bundle(bundle, MIDLINE_JSON_PATH)\n                    msg = f"[midline-tune] committed + saved {MIDLINE_JSON_PATH}"\n                except Exception as e:\n                    msg = f"[midline-tune] committed in memory; save failed: {e}"\n\n            with out_status:\n                clear_output(wait=True)\n                print(msg)\n                print(\'[midline-tune] rerun this cell or run downstream cells directly.\')\n\n        def _on_reset(_):\n            dy_slider.value = float(MIDLINE_DY)\n            dtheta_slider.value = float(MIDLINE_DTHETA_DEG)\n\n        btn_commit.on_click(_on_commit)\n        btn_reset.on_click(_on_reset)\n\n        ui = widgets.VBox([\n            widgets.HTML(\n                f"<b>Interactive Midline Tuning</b><br>"\n                f"Adjust DY + angle, inspect all planes, then click <i>Commit + Save</i>. "\n                f"(DX={MIDLINE_DX:.2f}, AX={MIDLINE_AX:.3f}, AY={MIDLINE_AY:.3f})"\n            ),\n            widgets.HBox([dy_slider, dtheta_slider, zoom_slider, ncols_slider]),\n            widgets.HBox([btn_commit, btn_reset]),\n            out_status,\n        ])\n\n        out = widgets.interactive_output(\n            _render_preview,\n            {\n                \'dy_manual\': dy_slider,\n                \'dtheta_manual\': dtheta_slider,\n                \'zoom\': zoom_slider,\n                \'ncols\': ncols_slider,\n            },\n        )\n\n        display(ui, out)\n    else:\n        _render_preview(MIDLINE_DY, MIDLINE_DTHETA_DEG, MIDLINE_TUNE_ZOOM, MIDLINE_TUNE_NCOLS)\n        print(\'[midline-tune] for sliders install ipywidgets, then rerun this cell.\')\n',
    '50': '# [50]\ntry:\n    REQUIRE_FISH_STATE = require_fish_state\nexcept NameError:\n    REQUIRE_FISH_STATE = None\nif callable(REQUIRE_FISH_STATE) and not REQUIRE_FISH_STATE():\n    raise SystemExit\n\n# Save a response-aware HCR-centric identified-cell activity mapping.\n# Outputs:\n#   - hcr_activity_status.csv   : one row per accepted (gene, anatomy label)\n#   - conf_to_func_pairs_raw.csv: candidate functional ROIs for each accepted HCR/anatomy label\n#   - conf_to_func_pairs.csv    : response-positive trace mapping when a local responsive ROI exists\nimport numpy as np\nimport pandas as pd\nfrom pathlib import Path\nfrom codeants_2pf_hcr import build_hcr_activity_tables, gene_from_mask\n\nOUT_STATUS = OUT_REG / \'hcr_activity_status.csv\'\nOUT_PATH_RAW = OUT_REG / \'conf_to_func_pairs_raw.csv\'\nOUT_PATH_DEDUP = OUT_REG / \'conf_to_func_pairs.csv\'\nOUT_CANDIDATES = OUT_REG / \'hcr_func_candidates.csv\'\nMASTER_ROI_CSV = OUT_REG / \'functional_roi_activity_identity.csv\'\n\nRECOMPUTE_CONF_FUNC_PAIRS = True\nACTIVE_CLASS = \'Active neurons\'\nINACTIVE_CLASS = \'Low-quality traces\'\nRESPONSE_SUMMARY_RESPONSIVE = \'Responsive neurons\'\nRESPONSE_SUMMARY_LOW = \'Low activity\'\nRESPONSE_SUMMARY_UNAVAILABLE = \'Response unavailable\'\nRESPONSE_CLASS_UNAVAILABLE = \'response unavailable\'\nHCR_ACTIVITY_MATCH_POLICY = \'hcr_anat_first_local_geometry_response_v5_identified_priority\'\nSELECTION_RULE = \'hcr_matched_anat_label_then_local_geometry_prefer_responsive_then_low\'\nHCR_OUT_OF_PLANE = \'out-of-plane anatomy label\'\nHCR_IN_PLANE_RESPONSIVE = \'in-plane responsive ROI\'\nHCR_IN_PLANE_LOW = \'in-plane low-activity ROI\'\nHCR_IN_PLANE_UNAVAILABLE = \'in-plane response unavailable\'\nHCR_IN_PLANE_NO_FUNC = \'in-plane no functional ROI candidate\'\nMIN_OVERLAP_FUNC_ANAT = 1\nREQUIRE_OVERLAP_FUNC_ANAT = True\nMAX_DIST_FUNC_ANAT = float(np.inf)\n\nBUILD_HCR_ACTIVITY_TABLES = build_hcr_activity_tables\ntry:\n    PLANE_REFS_LOCAL = plane_refs if isinstance(plane_refs, list) else []\nexcept NameError:\n    PLANE_REFS_LOCAL = []\ntry:\n    HCR_MATCH_RESULTS_LOCAL = hcr_match_results if isinstance(hcr_match_results, list) else []\nexcept NameError:\n    HCR_MATCH_RESULTS_LOCAL = []\ntry:\n    SUITE2P_BY_REF_IDX = suite2p_by_ref_idx if isinstance(suite2p_by_ref_idx, dict) else {}\nexcept NameError:\n    SUITE2P_BY_REF_IDX = {}\ntry:\n    ANAT_LABELS_PATH_LOCAL = Path(ANAT_LABELS_PATH) if ANAT_LABELS_PATH is not None else None\nexcept NameError:\n    ANAT_LABELS_PATH_LOCAL = None\ntry:\n    VOX_ANAT_LOCAL = VOX_ANAT if isinstance(VOX_ANAT, dict) else {}\nexcept NameError:\n    VOX_ANAT_LOCAL = {}\ntry:\n    FISH_ID_LOCAL = FISH_ID\nexcept NameError:\n    FISH_ID_LOCAL = None\nGENE_FROM_MASK_FUNC = gene_from_mask\n\n\ndef _bool_from_any(value):\n    if value is None:\n        return False\n    try:\n        if pd.isna(value):\n            return False\n    except Exception:\n        pass\n    if isinstance(value, (bool, np.bool_)):\n        return bool(value)\n    if isinstance(value, (int, np.integer)):\n        return bool(value)\n    if isinstance(value, (float, np.floating)):\n        if not np.isfinite(value):\n            return False\n        return bool(int(value))\n    return str(value).strip().lower() in {\'1\', \'true\', \'t\', \'yes\', \'y\'}\n\n\ndef _maybe_bool_or_na(value):\n    if value is None:\n        return pd.NA\n    try:\n        if pd.isna(value):\n            return pd.NA\n    except Exception:\n        pass\n    return bool(_bool_from_any(value))\n\n\ndef _to_int_or_na(value):\n    try:\n        if pd.isna(value):\n            return pd.NA\n    except Exception:\n        pass\n    try:\n        return int(value)\n    except Exception:\n        return pd.NA\n\n\ndef _to_float_or_nan(value):\n    try:\n        out = float(value)\n    except Exception:\n        return np.nan\n    return out if np.isfinite(out) else np.nan\n\n\ndef _build_hcr_uid(df):\n    if df is None or df.empty:\n        return pd.Series(dtype=str)\n    gene = df.get(\'gene\', pd.Series(\'\', index=df.index)).astype(str)\n    conf_mask = df.get(\'conf_mask\', pd.Series(\'\', index=df.index)).astype(str)\n    anat = pd.to_numeric(df.get(\'anat_label\', pd.Series(np.nan, index=df.index)), errors=\'coerce\').fillna(-1).astype(int).astype(str)\n    primary = pd.to_numeric(df.get(\'primary_conf_label\', pd.Series(np.nan, index=df.index)), errors=\'coerce\').fillna(-1).astype(int).astype(str)\n    return gene + \'||\' + conf_mask + \'||\' + anat + \'||\' + primary\n\n\ndef _load_response_lookup(master_csv, fish_id):\n    if not Path(master_csv).exists():\n        raise RuntimeError(\n            f\'missing response-aware ROI table: {master_csv}. \'\n            \'Run [50i] and [50ia] before rebuilding the HCR-centric exports.\'\n        )\n    master_df = pd.read_csv(master_csv)\n    if \'fish_id\' in master_df.columns:\n        master_df = master_df[master_df[\'fish_id\'].astype(str) == str(fish_id)].copy()\n    if master_df.empty:\n        raise RuntimeError(\n            f\'response-aware ROI table has no rows for fish_id={fish_id}. \'\n            \'Run [50i] and [50ia] for the current fish first.\'\n        )\n\n    required_cols = {\'plane_idx\', \'func_label\', \'response_is_active\', \'response_class\', \'response_summary_class\'}\n    missing_cols = sorted(required_cols - set(master_df.columns))\n    if missing_cols:\n        raise RuntimeError(\n            f\'response-aware ROI table is missing columns {missing_cols}; \'\n            \'rerun [50ia] before rebuilding HCR-centric exports.\'\n        )\n\n    if \'suite2p_is_cell\' not in master_df.columns:\n        master_df[\'suite2p_is_cell\'] = master_df.get(\'is_active\', pd.Series(False, index=master_df.index))\n    if \'suite2p_activity_class\' not in master_df.columns:\n        master_df[\'suite2p_activity_class\'] = master_df.get(\'activity_class\', pd.Series(pd.NA, index=master_df.index, dtype=\'object\'))\n\n    lookup = master_df[\n        [\'plane_idx\', \'func_label\', \'response_is_active\', \'response_class\', \'response_summary_class\', \'suite2p_is_cell\', \'suite2p_activity_class\']\n    ].copy()\n    lookup[\'plane_idx_key\'] = pd.to_numeric(lookup[\'plane_idx\'], errors=\'coerce\').astype(\'Int64\')\n    lookup[\'func_label_key\'] = pd.to_numeric(lookup[\'func_label\'], errors=\'coerce\').astype(\'Int64\')\n    lookup = lookup.drop(columns=[\'plane_idx\', \'func_label\'])\n    lookup = lookup.drop_duplicates(subset=[\'plane_idx_key\', \'func_label_key\'], keep=\'last\').reset_index(drop=True)\n    lookup[\'response_is_active\'] = lookup[\'response_is_active\'].map(_bool_from_any).astype(bool)\n    lookup[\'response_class\'] = lookup[\'response_class\'].fillna(RESPONSE_CLASS_UNAVAILABLE).astype(str)\n    lookup[\'response_summary_class\'] = lookup[\'response_summary_class\'].fillna(RESPONSE_SUMMARY_UNAVAILABLE).astype(str)\n    lookup[\'suite2p_is_cell\'] = lookup[\'suite2p_is_cell\'].map(_maybe_bool_or_na)\n    lookup[\'suite2p_activity_class\'] = lookup[\'suite2p_activity_class\'].where(lookup[\'suite2p_activity_class\'].notna(), pd.NA)\n    return lookup\n\n\ndef _attach_response_columns(df, response_lookup):\n    if df is None:\n        return pd.DataFrame()\n    out = df.copy()\n    if out.empty:\n        if \'plane_idx\' not in out.columns:\n            out[\'plane_idx\'] = pd.Series(dtype=\'Int64\')\n        if \'func_label\' not in out.columns:\n            out[\'func_label\'] = pd.Series(dtype=\'Int64\')\n        out[\'plane_idx_key\'] = pd.to_numeric(out[\'plane_idx\'], errors=\'coerce\').astype(\'Int64\')\n        out[\'func_label_key\'] = pd.to_numeric(out[\'func_label\'], errors=\'coerce\').astype(\'Int64\')\n        out[\'response_is_active\'] = pd.Series(dtype=bool)\n        out[\'response_class\'] = pd.Series(dtype=object)\n        out[\'response_summary_class\'] = pd.Series(dtype=object)\n        out[\'suite2p_is_cell\'] = pd.Series(dtype=object)\n        out[\'suite2p_activity_class\'] = pd.Series(dtype=object)\n        out[\'legacy_is_active\'] = pd.Series(dtype=object)\n        out[\'legacy_activity_class\'] = pd.Series(dtype=object)\n        return out\n\n    legacy_is_active = out.get(\'is_active\', pd.Series(pd.NA, index=out.index, dtype=\'object\')).copy()\n    legacy_activity_class = out.get(\'activity_class\', pd.Series(pd.NA, index=out.index, dtype=\'object\')).copy()\n    prior_response_is_active = out.get(\'response_is_active\', pd.Series(pd.NA, index=out.index, dtype=\'object\')).copy()\n    prior_response_class = out.get(\'response_class\', pd.Series(pd.NA, index=out.index, dtype=\'object\')).copy()\n    prior_response_summary_class = out.get(\'response_summary_class\', pd.Series(pd.NA, index=out.index, dtype=\'object\')).copy()\n    prior_suite2p_is_cell = out.get(\'suite2p_is_cell\', pd.Series(pd.NA, index=out.index, dtype=\'object\')).copy()\n    prior_suite2p_activity_class = out.get(\'suite2p_activity_class\', pd.Series(pd.NA, index=out.index, dtype=\'object\')).copy()\n    out[\'plane_idx_key\'] = pd.to_numeric(out.get(\'plane_idx\', pd.Series(np.nan, index=out.index)), errors=\'coerce\').astype(\'Int64\')\n    out[\'func_label_key\'] = pd.to_numeric(out.get(\'func_label\', pd.Series(np.nan, index=out.index)), errors=\'coerce\').astype(\'Int64\')\n    response_lookup_local = response_lookup.rename(columns={\n        \'response_is_active\': \'_lookup_response_is_active\',\n        \'response_class\': \'_lookup_response_class\',\n        \'response_summary_class\': \'_lookup_response_summary_class\',\n        \'suite2p_is_cell\': \'_lookup_suite2p_is_cell\',\n        \'suite2p_activity_class\': \'_lookup_suite2p_activity_class\',\n    })\n    out = out.merge(response_lookup_local, on=[\'plane_idx_key\', \'func_label_key\'], how=\'left\')\n\n    lookup_response_is_active = out.get(\'_lookup_response_is_active\', pd.Series(pd.NA, index=out.index, dtype=\'object\'))\n    lookup_response_class = out.get(\'_lookup_response_class\', pd.Series(pd.NA, index=out.index, dtype=\'object\'))\n    lookup_response_summary_class = out.get(\'_lookup_response_summary_class\', pd.Series(pd.NA, index=out.index, dtype=\'object\'))\n    lookup_suite2p_is_cell = out.get(\'_lookup_suite2p_is_cell\', pd.Series(pd.NA, index=out.index, dtype=\'object\'))\n    lookup_suite2p_activity_class = out.get(\'_lookup_suite2p_activity_class\', pd.Series(pd.NA, index=out.index, dtype=\'object\'))\n\n    out[\'response_is_active\'] = lookup_response_is_active.where(lookup_response_is_active.notna(), prior_response_is_active)\n    out[\'response_class\'] = lookup_response_class.where(lookup_response_class.notna(), prior_response_class)\n    out[\'response_summary_class\'] = lookup_response_summary_class.where(lookup_response_summary_class.notna(), prior_response_summary_class)\n    out[\'suite2p_is_cell\'] = lookup_suite2p_is_cell.where(lookup_suite2p_is_cell.notna(), prior_suite2p_is_cell)\n    out[\'suite2p_activity_class\'] = lookup_suite2p_activity_class.where(lookup_suite2p_activity_class.notna(), prior_suite2p_activity_class)\n    out[\'suite2p_is_cell\'] = out[\'suite2p_is_cell\'].where(out[\'suite2p_is_cell\'].notna(), legacy_is_active)\n    out[\'suite2p_activity_class\'] = out[\'suite2p_activity_class\'].where(out[\'suite2p_activity_class\'].notna(), legacy_activity_class)\n\n    out[\'response_is_active\'] = out[\'response_is_active\'].map(_bool_from_any).astype(bool)\n    out[\'response_class\'] = out[\'response_class\'].fillna(RESPONSE_CLASS_UNAVAILABLE).astype(str)\n    out[\'response_summary_class\'] = out[\'response_summary_class\'].fillna(RESPONSE_SUMMARY_UNAVAILABLE).astype(str)\n    out[\'suite2p_is_cell\'] = out[\'suite2p_is_cell\'].map(_maybe_bool_or_na)\n    out[\'suite2p_activity_class\'] = out[\'suite2p_activity_class\'].where(out[\'suite2p_activity_class\'].notna(), pd.NA)\n    out = out.drop(columns=[\n        c for c in [\n            \'_lookup_response_is_active\', \'_lookup_response_class\', \'_lookup_response_summary_class\',\n            \'_lookup_suite2p_is_cell\', \'_lookup_suite2p_activity_class\'\n        ] if c in out.columns\n    ])\n    out[\'legacy_is_active\'] = legacy_is_active.map(_maybe_bool_or_na)\n    out[\'legacy_activity_class\'] = legacy_activity_class.where(legacy_activity_class.notna(), pd.NA)\n    out[\'is_active\'] = out[\'response_is_active\']\n    out[\'activity_class\'] = out[\'response_summary_class\']\n    return out\n\n\ndef _candidate_sort_df_local(df):\n    if df is None or df.empty:\n        return df.copy()\n    out = df.copy()\n    out[\'_rank_sort\'] = pd.to_numeric(out.get(\'candidate_rank_for_anat\', pd.Series(np.nan, index=out.index)), errors=\'coerce\').astype(float).fillna(np.inf)\n    out[\'_dist_sort\'] = pd.to_numeric(out.get(\'dist_func_anat_um\', pd.Series(np.nan, index=out.index)), errors=\'coerce\').astype(float).fillna(np.inf)\n    out[\'_overlap_sort\'] = pd.to_numeric(out.get(\'overlap_px_func_anat\', pd.Series(np.nan, index=out.index)), errors=\'coerce\').astype(float).fillna(0.0)\n    out[\'_plane_sort\'] = pd.to_numeric(out.get(\'plane_idx\', pd.Series(np.nan, index=out.index)), errors=\'coerce\').astype(float).fillna(np.inf)\n    out[\'_func_sort\'] = pd.to_numeric(out.get(\'func_label\', pd.Series(np.nan, index=out.index)), errors=\'coerce\').astype(float).fillna(np.inf)\n    out = out.sort_values(\n        [\'_rank_sort\', \'_dist_sort\', \'_overlap_sort\', \'_plane_sort\', \'_func_sort\'],\n        ascending=[True, True, False, True, True],\n        na_position=\'last\',\n    )\n    return out.drop(columns=[\'_rank_sort\', \'_dist_sort\', \'_overlap_sort\', \'_plane_sort\', \'_func_sort\'])\n\n\ndef _candidate_key(row):\n    if row is None:\n        return None\n    plane = _to_int_or_na(row.get(\'plane_idx\', pd.NA))\n    func_label = _to_int_or_na(row.get(\'func_label\', pd.NA))\n    if pd.isna(plane) or pd.isna(func_label):\n        return None\n    return int(plane), int(func_label)\n\n\ndef _empty_payload():\n    return {\n        \'plane\': pd.NA,\n        \'plane_label\': None,\n        \'func_label\': pd.NA,\n        \'roi_idx\': pd.NA,\n        \'overlap_px\': np.nan,\n        \'dist_um\': np.nan,\n        \'func_source\': None,\n        \'response_is_active\': False,\n        \'response_class\': RESPONSE_CLASS_UNAVAILABLE,\n        \'response_summary_class\': RESPONSE_SUMMARY_UNAVAILABLE,\n        \'suite2p_is_cell\': pd.NA,\n        \'suite2p_activity_class\': pd.NA,\n    }\n\n\ndef _candidate_payload(row):\n    if row is None:\n        return _empty_payload()\n    payload = {\n        \'plane\': _to_int_or_na(row.get(\'plane_idx\', pd.NA)),\n        \'plane_label\': None if pd.isna(row.get(\'plane\', pd.NA)) else str(row.get(\'plane\')),\n        \'func_label\': _to_int_or_na(row.get(\'func_label\', pd.NA)),\n        \'roi_idx\': _to_int_or_na(row.get(\'roi_idx\', pd.NA)),\n        \'overlap_px\': _to_float_or_nan(row.get(\'overlap_px_func_anat\', np.nan)),\n        \'dist_um\': _to_float_or_nan(row.get(\'dist_func_anat_um\', np.nan)),\n        \'func_source\': row.get(\'func_source\', None),\n        \'response_is_active\': bool(_bool_from_any(row.get(\'response_is_active\', False))),\n        \'response_class\': str(row.get(\'response_class\', RESPONSE_CLASS_UNAVAILABLE)) if row.get(\'response_class\', None) is not None else RESPONSE_CLASS_UNAVAILABLE,\n        \'response_summary_class\': str(row.get(\'response_summary_class\', RESPONSE_SUMMARY_UNAVAILABLE)) if row.get(\'response_summary_class\', None) is not None else RESPONSE_SUMMARY_UNAVAILABLE,\n        \'suite2p_is_cell\': _maybe_bool_or_na(row.get(\'suite2p_is_cell\', pd.NA)),\n        \'suite2p_activity_class\': row.get(\'suite2p_activity_class\', pd.NA),\n    }\n    if payload[\'plane_label\'] in {\'nan\', \'None\'}:\n        payload[\'plane_label\'] = None\n    if payload[\'response_class\'] in {\'nan\', \'None\'}:\n        payload[\'response_class\'] = RESPONSE_CLASS_UNAVAILABLE\n    if payload[\'response_summary_class\'] in {\'nan\', \'None\'}:\n        payload[\'response_summary_class\'] = RESPONSE_SUMMARY_UNAVAILABLE\n    return payload\n\n\nUSE_CACHED_CONF_FUNC_PAIRS = False\nif (not RECOMPUTE_CONF_FUNC_PAIRS) and OUT_STATUS.exists() and OUT_PATH_RAW.exists() and OUT_PATH_DEDUP.exists():\n    try:\n        status_df_cached = pd.read_csv(OUT_STATUS)\n        raw_df_cached = pd.read_csv(OUT_PATH_RAW)\n        analysis_df_cached = pd.read_csv(OUT_PATH_DEDUP)\n\n        if \'fish_id\' in status_df_cached.columns:\n            status_df_cached = status_df_cached[status_df_cached[\'fish_id\'].astype(str) == str(FISH_ID_LOCAL)].copy()\n        if \'fish_id\' in raw_df_cached.columns:\n            raw_df_cached = raw_df_cached[raw_df_cached[\'fish_id\'].astype(str) == str(FISH_ID_LOCAL)].copy()\n        if \'fish_id\' in analysis_df_cached.columns:\n            analysis_df_cached = analysis_df_cached[analysis_df_cached[\'fish_id\'].astype(str) == str(FISH_ID_LOCAL)].copy()\n\n        cache_versions = set(\n            analysis_df_cached.get(\'match_policy_version\', pd.Series(dtype=str))\n            .dropna()\n            .astype(str)\n            .unique()\n            .tolist()\n        )\n        required_cached_cols = {\'response_is_active\', \'response_class\', \'response_summary_class\'}\n        missing_status_cols = sorted(required_cached_cols - set(status_df_cached.columns))\n        missing_analysis_cols = sorted(required_cached_cols - set(analysis_df_cached.columns))\n        if (\'match_policy_version\' not in analysis_df_cached.columns) or (HCR_ACTIVITY_MATCH_POLICY not in cache_versions):\n            raise RuntimeError(\n                f\'stale HCR activity mapping cache: expected {HCR_ACTIVITY_MATCH_POLICY}, \'\n                f\'found {sorted(cache_versions) if cache_versions else "missing"}\'\n            )\n        if missing_status_cols or missing_analysis_cols:\n            raise RuntimeError(\n                \'stale HCR activity mapping cache: \'\n                f\'status missing {missing_status_cols or []}, analysis missing {missing_analysis_cols or []}\'\n            )\n\n        HCR_ACTIVITY_STATUS_CSV = OUT_STATUS\n        HCR_ACTIVITY_STATUS_DF = status_df_cached\n        HCR_FUNC_CANDIDATES_CSV = OUT_CANDIDATES\n        CONF_FUNC_CSV_RAW = OUT_PATH_RAW\n        CONF_FUNC_CSV = OUT_PATH_DEDUP\n        CONF_FUNC_CSV_ANALYSIS = OUT_PATH_DEDUP\n        CONF_FUNC_STATUS_CSV = OUT_STATUS\n        CONF_FUNC_STATUS_DF = status_df_cached\n        CONF_FUNC_RAW_DF = raw_df_cached\n        CONF_FUNC_ANALYSIS_DF = analysis_df_cached\n\n        print(f"[50] Reusing cached HCR activity status: {OUT_STATUS} (rows={len(status_df_cached)})")\n        print(f"[50] Reusing cached raw candidate mapping: {OUT_PATH_RAW} (rows={len(raw_df_cached)})")\n        print(f"[50] Reusing cached responsive trace mapping: {OUT_PATH_DEDUP} (rows={len(analysis_df_cached)})")\n        USE_CACHED_CONF_FUNC_PAIRS = True\n        try:\n            display(status_df_cached.head())\n        except Exception:\n            print(status_df_cached.head().to_string(index=False))\n    except Exception as cache_err:\n        print(f"[50] Failed to load cached HCR activity mapping ({cache_err}); recomputing.")\n\nif (not USE_CACHED_CONF_FUNC_PAIRS) and ((not PLANE_REFS_LOCAL) or (not HCR_MATCH_RESULTS_LOCAL)):\n    print(\'[50] Need plane_refs and hcr_match_results; run [15]-[44] first.\')\nelif (not USE_CACHED_CONF_FUNC_PAIRS) and (not SUITE2P_BY_REF_IDX):\n    print(\'[50] Need suite2p_by_ref_idx; run [23a] first.\')\nelif (not USE_CACHED_CONF_FUNC_PAIRS) and (ANAT_LABELS_PATH_LOCAL is None or (not ANAT_LABELS_PATH_LOCAL.exists())):\n    print(\'[50] Need ANAT_LABELS_PATH; run the anatomy setup cells first.\')\nelif (not USE_CACHED_CONF_FUNC_PAIRS) and (BUILD_HCR_ACTIVITY_TABLES is None):\n    print(\'[50] HCR activity helper missing; run the utility cells first.\')\nelif (not USE_CACHED_CONF_FUNC_PAIRS) and (not MASTER_ROI_CSV.exists()):\n    print(f\'[50] Missing response-aware ROI table: {MASTER_ROI_CSV}. Run [50i] and [50ia] first, then rerun [50].\')\nelif not USE_CACHED_CONF_FUNC_PAIRS:\n    DX = float(VOX_ANAT_LOCAL.get(\'X\', 1.0)) if VOX_ANAT_LOCAL else 1.0\n    DY = float(VOX_ANAT_LOCAL.get(\'Y\', 1.0)) if VOX_ANAT_LOCAL else 1.0\n    twoP_labels_all = _ensure_uint_labels(imread_any(ANAT_LABELS_PATH_LOCAL))\n\n    status_df_legacy, raw_df_legacy, analysis_df_legacy, candidate_df, plane_meta_df = BUILD_HCR_ACTIVITY_TABLES(\n        SUITE2P_BY_REF_IDX,\n        PLANE_REFS_LOCAL,\n        twoP_labels_all,\n        HCR_MATCH_RESULTS_LOCAL,\n        dx_um=DX,\n        dy_um=DY,\n        fish_id=FISH_ID_LOCAL,\n        active_class=ACTIVE_CLASS,\n        inactive_class=INACTIVE_CLASS,\n        require_overlap=REQUIRE_OVERLAP_FUNC_ANAT,\n        min_overlap=MIN_OVERLAP_FUNC_ANAT,\n        max_dist_um=MAX_DIST_FUNC_ANAT,\n        out_of_plane=HCR_OUT_OF_PLANE,\n        in_plane_active=\'in-plane active ROI\',\n        in_plane_inactive=\'in-plane low-quality-trace ROI\',\n        in_plane_no_func=HCR_IN_PLANE_NO_FUNC,\n        match_policy_version=HCR_ACTIVITY_MATCH_POLICY,\n        selection_rule=SELECTION_RULE,\n        gene_from_mask_func=GENE_FROM_MASK_FUNC,\n        ensure_uint_labels_func=_ensure_uint_labels,\n        apply_func_orientation_func=_apply_func_orientation,\n        tform_for_plane_func=_tform_for_plane,\n        resample_labels_nn_func=resample_labels_nn,\n    )\n\n    if status_df_legacy.empty:\n        print(\'[50] No accepted HCR/anatomy labels were available for identified-cell activity mapping.\')\n    else:\n        response_lookup = _load_response_lookup(MASTER_ROI_CSV, FISH_ID_LOCAL)\n        candidate_df = _attach_response_columns(candidate_df, response_lookup)\n        raw_df = _attach_response_columns(raw_df_legacy, response_lookup)\n        status_df_work = status_df_legacy.copy()\n        status_df_work[\'hcr_status_uid\'] = _build_hcr_uid(status_df_work)\n        raw_df[\'hcr_status_uid\'] = _build_hcr_uid(raw_df)\n\n        status_rows = []\n        raw_rows = []\n        analysis_rows = []\n\n        for _, status_old in status_df_work.iterrows():\n            uid = str(status_old.get(\'hcr_status_uid\', \'\'))\n            group = raw_df[raw_df[\'hcr_status_uid\'].astype(str) == uid].copy()\n            group = _candidate_sort_df_local(group)\n            cand = group[group[\'plane_idx_key\'].notna() & group[\'func_label_key\'].notna()].copy()\n            cand = _candidate_sort_df_local(cand)\n\n            responsive_cand = cand[cand[\'response_is_active\'].astype(bool)].copy()\n            low_cand = cand[cand[\'response_summary_class\'].astype(str) == RESPONSE_SUMMARY_LOW].copy()\n            unavailable_cand = cand[cand[\'response_summary_class\'].astype(str) == RESPONSE_SUMMARY_UNAVAILABLE].copy()\n            nonresponsive_cand = cand[cand[\'response_is_active\'].astype(bool) == False].copy()\n\n            geom_row = cand.iloc[0].to_dict() if not cand.empty else None\n            response_row = responsive_cand.iloc[0].to_dict() if not responsive_cand.empty else None\n            low_row = low_cand.iloc[0].to_dict() if not low_cand.empty else None\n            unavailable_row = unavailable_cand.iloc[0].to_dict() if not unavailable_cand.empty else None\n            nonresponsive_row = nonresponsive_cand.iloc[0].to_dict() if not nonresponsive_cand.empty else None\n            selected_row = response_row if response_row is not None else (low_row if low_row is not None else (unavailable_row if unavailable_row is not None else geom_row))\n\n            geom_key = _candidate_key(geom_row)\n            response_key = _candidate_key(response_row)\n            low_key = _candidate_key(low_row)\n            unavailable_key = _candidate_key(unavailable_row)\n            nonresponsive_key = _candidate_key(nonresponsive_row)\n            selected_key = _candidate_key(selected_row)\n\n            represented = bool(_bool_from_any(status_old.get(\'represented_on_func_plane\', False)))\n            if not represented:\n                try:\n                    represented = int(status_old.get(\'represented_plane_count\', 0) or 0) > 0\n                except Exception:\n                    represented = False\n\n            if not represented:\n                functional_status = HCR_OUT_OF_PLANE\n            elif cand.empty:\n                functional_status = HCR_IN_PLANE_NO_FUNC\n            elif response_row is not None:\n                functional_status = HCR_IN_PLANE_RESPONSIVE\n            elif low_row is not None:\n                functional_status = HCR_IN_PLANE_LOW\n            else:\n                functional_status = HCR_IN_PLANE_UNAVAILABLE\n\n            selected_payload = _candidate_payload(selected_row)\n            geom_payload = _candidate_payload(geom_row)\n            response_payload = _candidate_payload(response_row)\n            low_payload = _candidate_payload(low_row)\n            unavailable_payload = _candidate_payload(unavailable_row)\n            nonresponsive_payload = _candidate_payload(nonresponsive_row)\n\n            status_new = status_old.to_dict()\n            status_new[\'functional_status_legacy\'] = str(status_old.get(\'functional_status\', pd.NA))\n            status_new[\'match_policy_version\'] = HCR_ACTIVITY_MATCH_POLICY\n            status_new[\'selection_rule\'] = SELECTION_RULE\n            status_new[\'functional_status\'] = functional_status\n            status_new[\'response_is_active\'] = bool(selected_payload[\'response_is_active\'])\n            status_new[\'response_class\'] = selected_payload[\'response_class\']\n            status_new[\'response_summary_class\'] = selected_payload[\'response_summary_class\']\n            status_new[\'is_active\'] = bool(selected_payload[\'response_is_active\'])\n            status_new[\'activity_class\'] = selected_payload[\'response_summary_class\']\n            status_new[\'selected_plane\'] = selected_payload[\'plane\']\n            status_new[\'selected_plane_label\'] = selected_payload[\'plane_label\']\n            status_new[\'selected_func_label\'] = selected_payload[\'func_label\']\n            status_new[\'selected_roi_idx\'] = selected_payload[\'roi_idx\']\n            status_new[\'selected_is_active\'] = bool(selected_payload[\'response_is_active\'])\n            status_new[\'selected_activity_class\'] = selected_payload[\'response_summary_class\']\n            status_new[\'selected_overlap_px\'] = selected_payload[\'overlap_px\']\n            status_new[\'selected_dist_um\'] = selected_payload[\'dist_um\']\n            status_new[\'selected_func_source\'] = selected_payload[\'func_source\']\n            status_new[\'selected_response_is_active\'] = bool(selected_payload[\'response_is_active\'])\n            status_new[\'selected_response_class\'] = selected_payload[\'response_class\']\n            status_new[\'selected_response_summary_class\'] = selected_payload[\'response_summary_class\']\n            status_new[\'selected_suite2p_is_cell\'] = selected_payload[\'suite2p_is_cell\']\n            status_new[\'selected_suite2p_activity_class\'] = selected_payload[\'suite2p_activity_class\']\n\n            status_new[\'selected_geometry_plane\'] = geom_payload[\'plane\']\n            status_new[\'selected_geometry_plane_label\'] = geom_payload[\'plane_label\']\n            status_new[\'selected_geometry_func_label\'] = geom_payload[\'func_label\']\n            status_new[\'selected_geometry_roi_idx\'] = geom_payload[\'roi_idx\']\n            status_new[\'selected_geometry_overlap_px\'] = geom_payload[\'overlap_px\']\n            status_new[\'selected_geometry_dist_um\'] = geom_payload[\'dist_um\']\n            status_new[\'selected_geometry_func_source\'] = geom_payload[\'func_source\']\n            status_new[\'selected_geometry_response_is_active\'] = bool(geom_payload[\'response_is_active\'])\n            status_new[\'selected_geometry_response_class\'] = geom_payload[\'response_class\']\n            status_new[\'selected_geometry_response_summary_class\'] = geom_payload[\'response_summary_class\']\n            status_new[\'selected_geometry_suite2p_is_cell\'] = geom_payload[\'suite2p_is_cell\']\n            status_new[\'selected_geometry_suite2p_activity_class\'] = geom_payload[\'suite2p_activity_class\']\n\n            status_new[\'selected_response_plane\'] = response_payload[\'plane\']\n            status_new[\'selected_response_plane_label\'] = response_payload[\'plane_label\']\n            status_new[\'selected_response_func_label\'] = response_payload[\'func_label\']\n            status_new[\'selected_response_roi_idx\'] = response_payload[\'roi_idx\']\n            status_new[\'selected_response_overlap_px\'] = response_payload[\'overlap_px\']\n            status_new[\'selected_response_dist_um\'] = response_payload[\'dist_um\']\n            status_new[\'selected_response_func_source\'] = response_payload[\'func_source\']\n\n            status_new[\'selected_low_plane\'] = low_payload[\'plane\']\n            status_new[\'selected_low_plane_label\'] = low_payload[\'plane_label\']\n            status_new[\'selected_low_func_label\'] = low_payload[\'func_label\']\n            status_new[\'selected_low_roi_idx\'] = low_payload[\'roi_idx\']\n            status_new[\'selected_low_overlap_px\'] = low_payload[\'overlap_px\']\n            status_new[\'selected_low_dist_um\'] = low_payload[\'dist_um\']\n            status_new[\'selected_low_func_source\'] = low_payload[\'func_source\']\n\n            status_new[\'selected_nonresponsive_plane\'] = nonresponsive_payload[\'plane\']\n            status_new[\'selected_nonresponsive_plane_label\'] = nonresponsive_payload[\'plane_label\']\n            status_new[\'selected_nonresponsive_func_label\'] = nonresponsive_payload[\'func_label\']\n            status_new[\'selected_nonresponsive_roi_idx\'] = nonresponsive_payload[\'roi_idx\']\n            status_new[\'selected_nonresponsive_overlap_px\'] = nonresponsive_payload[\'overlap_px\']\n            status_new[\'selected_nonresponsive_dist_um\'] = nonresponsive_payload[\'dist_um\']\n            status_new[\'selected_nonresponsive_func_source\'] = nonresponsive_payload[\'func_source\']\n\n            status_new[\'selected_unavailable_plane\'] = unavailable_payload[\'plane\']\n            status_new[\'selected_unavailable_plane_label\'] = unavailable_payload[\'plane_label\']\n            status_new[\'selected_unavailable_func_label\'] = unavailable_payload[\'func_label\']\n            status_new[\'selected_unavailable_roi_idx\'] = unavailable_payload[\'roi_idx\']\n            status_new[\'selected_unavailable_overlap_px\'] = unavailable_payload[\'overlap_px\']\n            status_new[\'selected_unavailable_dist_um\'] = unavailable_payload[\'dist_um\']\n            status_new[\'selected_unavailable_func_source\'] = unavailable_payload[\'func_source\']\n\n            status_new[\'n_func_candidates_total\'] = int(len(cand))\n            status_new[\'n_responsive_candidates\'] = int(len(responsive_cand))\n            status_new[\'n_low_candidates\'] = int(len(low_cand))\n            status_new[\'n_response_unavailable_candidates\'] = int(len(unavailable_cand))\n            status_new[\'has_any_responsive_candidate\'] = bool(response_row is not None)\n            status_new[\'has_any_low_candidate\'] = bool(low_row is not None)\n            status_new[\'has_any_response_unavailable_candidate\'] = bool(unavailable_row is not None)\n            status_new[\'has_responsive_alternative\'] = bool(response_key is not None and geom_key is not None and response_key != geom_key)\n            status_new[\'has_low_alternative\'] = bool(low_key is not None and geom_key is not None and low_key != geom_key)\n            status_new[\'has_unavailable_alternative\'] = bool(unavailable_key is not None and geom_key is not None and unavailable_key != geom_key)\n            status_new[\'selected_for_trace_export\'] = bool(response_row is not None)\n\n            # Backward-compatible aliases, now defined in response terms.\n            status_new[\'n_active_candidates\'] = int(len(responsive_cand))\n            status_new[\'n_inactive_candidates\'] = int(len(nonresponsive_cand))\n            status_new[\'has_any_active_candidate\'] = bool(response_row is not None)\n            status_new[\'has_any_inactive_candidate\'] = bool(nonresponsive_row is not None)\n            status_new[\'has_active_alternative\'] = bool(response_key is not None and geom_key is not None and response_key != geom_key)\n            status_new[\'has_inactive_alternative\'] = bool(nonresponsive_key is not None and geom_key is not None and nonresponsive_key != geom_key)\n            status_new[\'selected_plane_active\'] = response_payload[\'plane\']\n            status_new[\'selected_plane_label_active\'] = response_payload[\'plane_label\']\n            status_new[\'selected_func_label_active\'] = response_payload[\'func_label\']\n            status_new[\'selected_roi_idx_active\'] = response_payload[\'roi_idx\']\n            status_new[\'selected_overlap_px_active\'] = response_payload[\'overlap_px\']\n            status_new[\'selected_dist_um_active\'] = response_payload[\'dist_um\']\n            status_new[\'selected_func_source_active\'] = response_payload[\'func_source\']\n            status_new[\'selected_plane_inactive\'] = nonresponsive_payload[\'plane\']\n            status_new[\'selected_plane_label_inactive\'] = nonresponsive_payload[\'plane_label\']\n            status_new[\'selected_func_label_inactive\'] = nonresponsive_payload[\'func_label\']\n            status_new[\'selected_roi_idx_inactive\'] = nonresponsive_payload[\'roi_idx\']\n            status_new[\'selected_overlap_px_inactive\'] = nonresponsive_payload[\'overlap_px\']\n            status_new[\'selected_dist_um_inactive\'] = nonresponsive_payload[\'dist_um\']\n            status_new[\'selected_func_source_inactive\'] = nonresponsive_payload[\'func_source\']\n            status_rows.append(status_new)\n\n            if group.empty:\n                group = pd.DataFrame([status_old.to_dict()])\n            for _, raw_row in group.iterrows():\n                raw_new = raw_row.to_dict()\n                rr_key = _candidate_key(raw_row)\n                rr_is_candidate = rr_key is not None\n                rr_response_summary = str(raw_row.get(\'response_summary_class\', RESPONSE_SUMMARY_UNAVAILABLE)) if raw_row.get(\'response_summary_class\', None) is not None else RESPONSE_SUMMARY_UNAVAILABLE\n                if not rr_is_candidate:\n                    rr_bucket = \'no functional ROI candidate\'\n                elif bool(_bool_from_any(raw_row.get(\'response_is_active\', False))):\n                    rr_bucket = \'responsive\'\n                elif rr_response_summary == RESPONSE_SUMMARY_LOW:\n                    rr_bucket = \'low activity\'\n                else:\n                    rr_bucket = \'response unavailable\'\n\n                raw_new[\'match_policy_version\'] = HCR_ACTIVITY_MATCH_POLICY\n                raw_new[\'selection_rule\'] = SELECTION_RULE\n                raw_new[\'functional_status\'] = functional_status\n                raw_new[\'functional_status_legacy\'] = str(status_old.get(\'functional_status\', pd.NA))\n                raw_new[\'response_is_active\'] = bool(_bool_from_any(raw_row.get(\'response_is_active\', False))) if rr_is_candidate else False\n                raw_new[\'response_class\'] = str(raw_row.get(\'response_class\', RESPONSE_CLASS_UNAVAILABLE)) if rr_is_candidate else RESPONSE_CLASS_UNAVAILABLE\n                raw_new[\'response_summary_class\'] = rr_response_summary if rr_is_candidate else RESPONSE_SUMMARY_UNAVAILABLE\n                raw_new[\'is_active\'] = raw_new[\'response_is_active\']\n                raw_new[\'activity_class\'] = raw_new[\'response_summary_class\']\n                raw_new[\'candidate_response_bucket\'] = rr_bucket\n                raw_new[\'selected_response_is_active\'] = bool(selected_payload[\'response_is_active\'])\n                raw_new[\'selected_response_class\'] = selected_payload[\'response_class\']\n                raw_new[\'selected_response_summary_class\'] = selected_payload[\'response_summary_class\']\n                raw_new[\'is_selected_best_any\'] = bool(rr_is_candidate and geom_key is not None and rr_key == geom_key)\n                raw_new[\'is_selected_best_geometry\'] = bool(rr_is_candidate and geom_key is not None and rr_key == geom_key)\n                raw_new[\'is_selected_best_response\'] = bool(rr_is_candidate and response_key is not None and rr_key == response_key)\n                raw_new[\'is_selected_best_low\'] = bool(rr_is_candidate and low_key is not None and rr_key == low_key)\n                raw_new[\'is_selected_best_unavailable\'] = bool(rr_is_candidate and unavailable_key is not None and rr_key == unavailable_key)\n                raw_new[\'is_selected_canonical\'] = bool(rr_is_candidate and selected_key is not None and rr_key == selected_key)\n                raw_new[\'is_selected_best_active\'] = bool(rr_is_candidate and response_key is not None and rr_key == response_key)\n                raw_new[\'is_selected_best_inactive\'] = bool(rr_is_candidate and nonresponsive_key is not None and rr_key == nonresponsive_key)\n                raw_new[\'is_selected_for_analysis\'] = bool(rr_is_candidate and response_key is not None and rr_key == response_key)\n                raw_rows.append(raw_new)\n\n            if response_row is not None:\n                analysis_rows.append({\n                    \'fish_id\': status_old.get(\'fish_id\', FISH_ID_LOCAL),\n                    \'gene\': str(status_old.get(\'gene\', \'unknown\')),\n                    \'conf_mask\': status_old.get(\'conf_mask\', None),\n                    \'conf_label\': _to_int_or_na(status_old.get(\'primary_conf_label\', status_old.get(\'conf_label\', pd.NA))),\n                    \'conf_labels\': status_old.get(\'conf_labels\', None),\n                    \'conf_label_count\': _to_int_or_na(status_old.get(\'conf_label_count\', pd.NA)),\n                    \'anat_label\': _to_int_or_na(status_old.get(\'anat_label\', pd.NA)),\n                    \'func_label\': response_payload[\'func_label\'],\n                    \'plane\': response_payload[\'plane\'],\n                    \'plane_label\': response_payload[\'plane_label\'],\n                    \'roi_idx\': response_payload[\'roi_idx\'],\n                    \'is_active\': True,\n                    \'activity_class\': RESPONSE_SUMMARY_RESPONSIVE,\n                    \'response_is_active\': True,\n                    \'response_class\': response_payload[\'response_class\'],\n                    \'response_summary_class\': response_payload[\'response_summary_class\'],\n                    \'suite2p_is_cell\': response_payload[\'suite2p_is_cell\'],\n                    \'suite2p_activity_class\': response_payload[\'suite2p_activity_class\'],\n                    \'dist_conf_anat_um\': _to_float_or_nan(status_old.get(\'dist_conf_anat_um\', np.nan)),\n                    \'dist_func_anat_um\': response_payload[\'dist_um\'],\n                    \'overlap_px_func_anat\': response_payload[\'overlap_px\'],\n                    \'represented_on_func_plane\': represented,\n                    \'functional_status\': functional_status,\n                    \'functional_status_legacy\': str(status_old.get(\'functional_status\', pd.NA)),\n                    \'n_func_candidates_total\': int(len(cand)),\n                    \'n_responsive_candidates\': int(len(responsive_cand)),\n                    \'n_low_candidates\': int(len(low_cand)),\n                    \'n_response_unavailable_candidates\': int(len(unavailable_cand)),\n                    \'has_any_responsive_candidate\': bool(response_row is not None),\n                    \'has_any_low_candidate\': bool(low_row is not None),\n                    \'has_any_response_unavailable_candidate\': bool(unavailable_row is not None),\n                    \'has_responsive_alternative\': bool(response_key is not None and geom_key is not None and response_key != geom_key),\n                    \'has_low_alternative\': bool(low_key is not None and geom_key is not None and low_key != geom_key),\n                    \'selected_geometry_plane\': geom_payload[\'plane\'],\n                    \'selected_geometry_func_label\': geom_payload[\'func_label\'],\n                    \'selection_rule\': SELECTION_RULE,\n                    \'selection_rank_gene_anat\': 1,\n                    \'is_selected_for_analysis\': True,\n                    \'match_policy_version\': HCR_ACTIVITY_MATCH_POLICY,\n                })\n        status_df = pd.DataFrame(status_rows)\n        raw_df = pd.DataFrame(raw_rows)\n        analysis_df = pd.DataFrame(analysis_rows)\n        if analysis_df.empty:\n            analysis_df = pd.DataFrame(columns=[\n                \'fish_id\', \'gene\', \'conf_mask\', \'conf_label\', \'conf_labels\', \'conf_label_count\',\n                \'anat_label\', \'func_label\', \'plane\', \'plane_label\', \'roi_idx\', \'is_active\',\n                \'activity_class\', \'response_is_active\', \'response_class\', \'response_summary_class\',\n                \'suite2p_is_cell\', \'suite2p_activity_class\', \'dist_conf_anat_um\',\n                \'dist_func_anat_um\', \'overlap_px_func_anat\', \'represented_on_func_plane\',\n                \'functional_status\', \'functional_status_legacy\', \'n_func_candidates_total\',\n                \'n_responsive_candidates\', \'n_low_candidates\',\n                \'n_response_unavailable_candidates\', \'has_any_responsive_candidate\',\n                \'has_any_low_candidate\', \'has_any_response_unavailable_candidate\',\n                \'has_responsive_alternative\', \'has_low_alternative\',\n                \'selected_geometry_plane\', \'selected_geometry_func_label\', \'selection_rule\',\n                \'selection_rank_gene_anat\', \'is_selected_for_analysis\', \'match_policy_version\',\n                \'roi_reuse_count_within_gene\', \'roi_reused_within_gene\',\n            ])\n\n        if not candidate_df.empty:\n            if \'gene\' not in candidate_df.columns:\n                candidate_df[\'gene\'] = pd.NA\n            candidate_df[\'candidate_response_bucket\'] = np.select(\n                [\n                    candidate_df[\'response_is_active\'].astype(bool),\n                    candidate_df[\'response_summary_class\'].astype(str) == RESPONSE_SUMMARY_LOW,\n                ],\n                [\'responsive\', \'low activity\'],\n                default=\'response unavailable\',\n            )\n\n        if not status_df.empty:\n            int_cols_status = [\n                \'anat_label\', \'conf_label\', \'primary_conf_label\', \'conf_label_count\',\n                \'represented_plane_count\', \'n_func_candidates_total\', \'n_responsive_candidates\',\n                \'n_low_candidates\', \'n_response_unavailable_candidates\',\n                \'n_active_candidates\', \'n_inactive_candidates\',\n                \'selected_plane\', \'selected_func_label\', \'selected_roi_idx\',\n                \'selected_geometry_plane\', \'selected_geometry_func_label\', \'selected_geometry_roi_idx\',\n                \'selected_response_plane\', \'selected_response_func_label\', \'selected_response_roi_idx\',\n                \'selected_low_plane\', \'selected_low_func_label\', \'selected_low_roi_idx\',\n                \'selected_nonresponsive_plane\', \'selected_nonresponsive_func_label\', \'selected_nonresponsive_roi_idx\',\n                \'selected_unavailable_plane\', \'selected_unavailable_func_label\', \'selected_unavailable_roi_idx\',\n                \'selected_plane_active\', \'selected_func_label_active\', \'selected_roi_idx_active\',\n                \'selected_plane_inactive\', \'selected_func_label_inactive\', \'selected_roi_idx_inactive\',\n            ]\n            for col in int_cols_status:\n                if col in status_df.columns:\n                    status_df[col] = pd.to_numeric(status_df[col], errors=\'coerce\').astype(\'Int64\')\n\n            if status_df[\'selected_plane\'].notna().any() and status_df[\'selected_func_label\'].notna().any():\n                reuse_df = status_df[\n                    status_df[\'selected_plane\'].notna()\n                    & status_df[\'selected_func_label\'].notna()\n                ].copy()\n                reuse_counts = (\n                    reuse_df.groupby([\'gene\', \'selected_plane\', \'selected_func_label\'])\n                    .size()\n                    .rename(\'selected_roi_reuse_count\')\n                    .reset_index()\n                )\n                status_df = status_df.merge(\n                    reuse_counts,\n                    on=[\'gene\', \'selected_plane\', \'selected_func_label\'],\n                    how=\'left\',\n                )\n                status_df[\'selected_roi_reuse_count\'] = pd.to_numeric(\n                    status_df.get(\'selected_roi_reuse_count\', pd.Series(0, index=status_df.index)),\n                    errors=\'coerce\'\n                ).fillna(0).astype(int)\n                status_df[\'selected_roi_reused_within_gene\'] = status_df[\'selected_roi_reuse_count\'] > 1\n            else:\n                status_df[\'selected_roi_reuse_count\'] = 0\n                status_df[\'selected_roi_reused_within_gene\'] = False\n\n        if not raw_df.empty:\n            for col in [\'plane_idx\', \'best_z\', \'func_label\', \'roi_idx\', \'anat_label\', \'candidate_rank_for_anat\']:\n                if col in raw_df.columns:\n                    raw_df[col] = pd.to_numeric(raw_df[col], errors=\'coerce\').astype(\'Int64\')\n\n        if not analysis_df.empty:\n            for col in [\'conf_label\', \'conf_label_count\', \'anat_label\', \'func_label\', \'plane\', \'roi_idx\', \'selection_rank_gene_anat\']:\n                if col in analysis_df.columns:\n                    analysis_df[col] = pd.to_numeric(analysis_df[col], errors=\'coerce\').astype(\'Int64\')\n            reuse_counts = (\n                analysis_df.groupby([\'gene\', \'plane\', \'func_label\'])\n                .size()\n                .rename(\'roi_reuse_count_within_gene\')\n                .reset_index()\n            )\n            analysis_df = analysis_df.merge(reuse_counts, on=[\'gene\', \'plane\', \'func_label\'], how=\'left\')\n            analysis_df[\'roi_reuse_count_within_gene\'] = pd.to_numeric(\n                analysis_df.get(\'roi_reuse_count_within_gene\', pd.Series(0, index=analysis_df.index)),\n                errors=\'coerce\'\n            ).fillna(0).astype(int)\n            analysis_df[\'roi_reused_within_gene\'] = analysis_df[\'roi_reuse_count_within_gene\'] > 1\n\n        status_df = status_df.sort_values([\'gene\', \'anat_label\']).reset_index(drop=True)\n        raw_df = raw_df.sort_values(\n            [\'gene\', \'anat_label\', \'is_selected_best_geometry\', \'is_selected_for_analysis\', \'candidate_rank_for_anat\', \'plane_idx\', \'func_label\'],\n            ascending=[True, True, False, False, True, True, True],\n            na_position=\'last\',\n        ).reset_index(drop=True)\n        analysis_df = analysis_df.sort_values([\'gene\', \'anat_label\', \'plane\', \'func_label\']).reset_index(drop=True)\n        candidate_df = candidate_df.sort_values([\'anat_label\', \'plane_idx\', \'func_label\']).reset_index(drop=True) if not candidate_df.empty else candidate_df\n\n        status_df.to_csv(OUT_STATUS, index=False)\n        raw_df.to_csv(OUT_PATH_RAW, index=False)\n        analysis_df.to_csv(OUT_PATH_DEDUP, index=False)\n        candidate_df.to_csv(OUT_CANDIDATES, index=False)\n\n        HCR_ACTIVITY_STATUS_CSV = OUT_STATUS\n        HCR_ACTIVITY_STATUS_DF = status_df\n        HCR_FUNC_CANDIDATES_CSV = OUT_CANDIDATES\n        CONF_FUNC_CSV_RAW = OUT_PATH_RAW\n        CONF_FUNC_CSV = OUT_PATH_DEDUP\n        CONF_FUNC_CSV_ANALYSIS = OUT_PATH_DEDUP\n        CONF_FUNC_STATUS_CSV = OUT_STATUS\n        CONF_FUNC_STATUS_DF = status_df\n        CONF_FUNC_RAW_DF = raw_df\n        CONF_FUNC_ANALYSIS_DF = analysis_df\n\n        print(f\'[50] Saved HCR activity status to {OUT_STATUS} (rows={len(status_df)})\')\n        print(f\'[50] Saved raw HCR candidate mapping to {OUT_PATH_RAW} (rows={len(raw_df)})\')\n        print(f\'[50] Saved responsive trace mapping to {OUT_PATH_DEDUP} (rows={len(analysis_df)})\')\n        if not candidate_df.empty:\n            print(f\'[50] Saved functional candidate table to {OUT_CANDIDATES} (rows={len(candidate_df)})\')\n        print(\n            f\'[50] HCR-centric identified-cell activity mapping ({HCR_ACTIVITY_MATCH_POLICY}); \'\n            \'local ROI candidates compete geometrically within each accepted HCR/anatomy label, response-aware ROI calls come from [50ia], and traces are exported when a local responsive ROI exists.\'\n        )\n\n        summary = (\n            status_df.groupby([\'gene\', \'functional_status\'], as_index=False)\n            .size()\n            .rename(columns={\'size\': \'n_labels\'})\n            .sort_values([\'gene\', \'functional_status\'])\n            .reset_index(drop=True)\n        )\n        try:\n            display(summary)\n            display(status_df.head())\n        except Exception:\n            print(summary.to_string(index=False))\n            print(status_df.head().to_string(index=False))\n',
    '50e': '# [50e]\ntry:\n    REQUIRE_FISH_STATE = require_fish_state\nexcept NameError:\n    REQUIRE_FISH_STATE = None\nif callable(REQUIRE_FISH_STATE) and not REQUIRE_FISH_STATE():\n    raise SystemExit\n\n# HCR-centric identified-cell response overview.\n# Inner ring: whether the HCR-matched anatomy label is represented on functional planes.\n# Outer ring: local best ROI response state using the response-aware master ROI table from [50ia].\nimport math\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nfrom pathlib import Path\nfrom matplotlib.colors import to_rgb\nfrom matplotlib.patches import Patch\nfrom codeants_2pf_hcr.plots.annotations import place_labels_no_overlap\n\nRESPONSIVE_STATUS = \'in-plane responsive ROI\'\nLOW_STATUS = \'in-plane low-activity ROI\'\nUNAVAILABLE_STATUS = \'in-plane response unavailable\'\nNO_FUNC_STATUS = \'in-plane no functional ROI candidate\'\nOUT_OF_PLANE_STATUS = \'out-of-plane anatomy label\'\nINNER_WITHIN = \'within functional planes\'\nINNER_OUTSIDE = \'outside functional planes\'\nINNER_UNMATCHED = \'unmatched\'\nRESPONSE_SUMMARY_RESPONSIVE = \'Responsive neurons\'\nRESPONSE_SUMMARY_LOW = \'Low activity\'\nRESPONSE_SUMMARY_UNAVAILABLE = \'Response unavailable\'\n\nSTATUS_LABELS = {\n    RESPONSIVE_STATUS: \'responsive ROI\',\n    LOW_STATUS: \'low-activity ROI\',\n    UNAVAILABLE_STATUS: \'response unavailable\',\n    NO_FUNC_STATUS: \'no functional match\',\n    OUT_OF_PLANE_STATUS: \'out of plane\',\n    INNER_UNMATCHED: \'unmatched\',\n}\nINNER_LABELS = {\n    INNER_WITHIN: \'within plane\',\n    INNER_OUTSIDE: \'outside plane\',\n    INNER_UNMATCHED: \'unmatched\',\n}\n\nSTATUS_CSV = OUT_REG / \'hcr_activity_status.csv\'\nMASTER_CSV = OUT_REG / \'functional_roi_activity_identity.csv\'\nSUMMARY_CSV = OUT_REG / \'hcr_activity_status_summary.csv\'\nFIG_PATH = OUT_QA / \'hcr_activity_status_overview.png\'\nSAVE_HCR_ACTIVITY_OVERVIEW = True\nHCR_ACTIVITY_OVERVIEW_DPI = 320\nPANEL_SIZE = (6, 6)\nDONUT_WIDTH_OUTER = 0.12\nDONUT_WIDTH_INNER = 0.3\nOUTER_RADIUS = 1.08\nRING_GAP = 0.03\nMAX_COLS = 4\n\nif not STATUS_CSV.exists():\n    print(f\'[50e] Missing HCR activity status table: {STATUS_CSV}. Run [50] first.\')\n    raise SystemExit\nif not MASTER_CSV.exists():\n    print(f\'[50e] Missing response-aware ROI table: {MASTER_CSV}. Run [50ia] first so low activity uses the notebook-wide null-tested definition.\')\n    raise SystemExit\n\n\ndef _bool_from_any_local(value):\n    if value is None:\n        return False\n    try:\n        if pd.isna(value):\n            return False\n    except Exception:\n        pass\n    if isinstance(value, (bool, np.bool_)):\n        return bool(value)\n    if isinstance(value, (int, np.integer)):\n        return bool(value)\n    if isinstance(value, (float, np.floating)):\n        if not np.isfinite(value):\n            return False\n        return bool(int(value))\n    return str(value).strip().lower() in {\'1\', \'true\', \'t\', \'yes\', \'y\'}\n\ndef _gene_from_mask_path_local(mask_path):\n    try:\n        gene_func = gene_from_mask if callable(gene_from_mask) else None\n    except NameError:\n        gene_func = None\n    m = str(mask_path) if mask_path is not None else \'\'\n    if gene_func is not None:\n        try:\n            return str(gene_func(m))\n        except Exception:\n            pass\n    return Path(m).name or \'unknown\'\n\ndef _compute_hq_mask_counts_by_gene_local():\n    try:\n        summary_tbl = hcr_match_summary_table if isinstance(hcr_match_summary_table, pd.DataFrame) else None\n    except NameError:\n        summary_tbl = None\n    if summary_tbl is not None and (not summary_tbl.empty):\n        req = {\'gene\', \'conf_labels_after_prewarp_filter\', \'n_low_conf_q95_prewarp\'}\n        if req.issubset(summary_tbl.columns):\n            work = summary_tbl.copy()\n            work[\'gene\'] = work[\'gene\'].astype(str)\n            work[\'conf_labels_after_prewarp_filter\'] = pd.to_numeric(work[\'conf_labels_after_prewarp_filter\'], errors=\'coerce\').fillna(0).astype(int)\n            work[\'n_low_conf_q95_prewarp\'] = pd.to_numeric(work[\'n_low_conf_q95_prewarp\'], errors=\'coerce\').fillna(0).astype(int)\n            work[\'n_hq_masks\'] = (work[\'conf_labels_after_prewarp_filter\'] - work[\'n_low_conf_q95_prewarp\']).clip(lower=0)\n            return {str(k): int(v) for k, v in work.groupby(\'gene\', dropna=False)[\'n_hq_masks\'].sum().to_dict().items()}\n    try:\n        results = hcr_match_results if isinstance(hcr_match_results, list) else []\n    except NameError:\n        results = []\n    counts = {}\n    for res in results:\n        if not isinstance(res, dict):\n            continue\n        warped = res.get(\'warped\', None)\n        if warped is None:\n            continue\n        arr = np.asarray(warped)\n        labels = np.unique(arr)\n        labels = labels[labels > 0]\n        fs = res.get(\'filter_stats\', {}) if isinstance(res.get(\'filter_stats\', {}), dict) else {}\n        low_conf = fs.get(\'low_conf_labels\', [])\n        low_conf_set = set()\n        for x in low_conf:\n            try:\n                low_conf_set.add(int(x))\n            except Exception:\n                pass\n        n_hq = int(sum(1 for x in labels if int(x) not in low_conf_set))\n        gene = _gene_from_mask_path_local(res.get(\'mask_path\', None))\n        counts[str(gene)] = int(counts.get(str(gene), 0)) + int(n_hq)\n    return counts\n\nstatus_df = pd.read_csv(STATUS_CSV)\nmaster_df = pd.read_csv(MASTER_CSV)\n\nif \'fish_id\' in status_df.columns:\n    status_df = status_df[status_df[\'fish_id\'].astype(str) == str(FISH_ID)].copy()\nif \'fish_id\' in master_df.columns:\n    master_df = master_df[master_df[\'fish_id\'].astype(str) == str(FISH_ID)].copy()\n\nrequired_master_cols = {\'plane_idx\', \'func_label\', \'response_class\', \'response_summary_class\', \'response_is_active\'}\nmissing_master_cols = sorted(required_master_cols - set(master_df.columns))\nif missing_master_cols:\n    print(f"[50e] Master ROI table missing response columns {missing_master_cols}; rerun [50ia].")\n    raise SystemExit\n\nif status_df.empty:\n    print(\'[50e] HCR activity status table is empty; nothing to plot.\')\nelif master_df.empty:\n    print(\'[50e] Response-aware ROI table is empty; rerun [50ia].\')\nelse:\n    status_df[\'gene\'] = status_df[\'gene\'].astype(str)\n    status_df[\'functional_status_legacy\'] = status_df[\'functional_status\'].astype(str)\n    status_df[\'selected_plane_idx_key\'] = pd.to_numeric(status_df.get(\'selected_plane\', pd.Series(index=status_df.index, dtype=float)), errors=\'coerce\').astype(\'Int64\')\n    status_df[\'selected_func_label_key\'] = pd.to_numeric(status_df.get(\'selected_func_label\', pd.Series(index=status_df.index, dtype=float)), errors=\'coerce\').astype(\'Int64\')\n\n    prior_response_class = status_df.get(\'response_class\', pd.Series(pd.NA, index=status_df.index, dtype=\'object\')).copy()\n    prior_response_summary_class = status_df.get(\'response_summary_class\', pd.Series(pd.NA, index=status_df.index, dtype=\'object\')).copy()\n    prior_response_is_active = status_df.get(\'response_is_active\', pd.Series(pd.NA, index=status_df.index, dtype=\'object\')).copy()\n\n    response_lookup = master_df[[\'plane_idx\', \'func_label\', \'response_class\', \'response_summary_class\', \'response_is_active\']].copy()\n    response_lookup[\'plane_idx_key\'] = pd.to_numeric(response_lookup[\'plane_idx\'], errors=\'coerce\').astype(\'Int64\')\n    response_lookup[\'func_label_key\'] = pd.to_numeric(response_lookup[\'func_label\'], errors=\'coerce\').astype(\'Int64\')\n    response_lookup = (\n        response_lookup\n        .drop(columns=[\'plane_idx\', \'func_label\'])\n        .drop_duplicates(subset=[\'plane_idx_key\', \'func_label_key\'], keep=\'last\')\n        .reset_index(drop=True)\n    )\n    response_lookup = response_lookup.rename(columns={\n        \'response_class\': \'_lookup_response_class\',\n        \'response_summary_class\': \'_lookup_response_summary_class\',\n        \'response_is_active\': \'_lookup_response_is_active\',\n    })\n\n    status_df = status_df.merge(\n        response_lookup,\n        left_on=[\'selected_plane_idx_key\', \'selected_func_label_key\'],\n        right_on=[\'plane_idx_key\', \'func_label_key\'],\n        how=\'left\',\n    )\n    lookup_response_class = status_df.get(\'_lookup_response_class\', pd.Series(pd.NA, index=status_df.index, dtype=\'object\'))\n    lookup_response_summary_class = status_df.get(\'_lookup_response_summary_class\', pd.Series(pd.NA, index=status_df.index, dtype=\'object\'))\n    lookup_response_is_active = status_df.get(\'_lookup_response_is_active\', pd.Series(pd.NA, index=status_df.index, dtype=\'object\'))\n    status_df[\'response_class\'] = lookup_response_class.where(lookup_response_class.notna(), prior_response_class)\n    status_df[\'response_summary_class\'] = lookup_response_summary_class.where(lookup_response_summary_class.notna(), prior_response_summary_class)\n    status_df[\'response_is_active\'] = lookup_response_is_active.where(lookup_response_is_active.notna(), prior_response_is_active)\n    status_df[\'response_class\'] = status_df[\'response_class\'].fillna(\'response unavailable\').astype(str)\n    status_df[\'response_summary_class\'] = status_df[\'response_summary_class\'].fillna(RESPONSE_SUMMARY_UNAVAILABLE).astype(str)\n    status_df[\'response_is_active\'] = status_df[\'response_is_active\'].map(_bool_from_any_local).astype(bool)\n    status_df = status_df.drop(columns=[\n        c for c in [\'plane_idx_key\', \'func_label_key\', \'_lookup_response_class\', \'_lookup_response_summary_class\', \'_lookup_response_is_active\']\n        if c in status_df.columns\n    ])\n\n    status_df[\'outer_status\'] = np.select(\n        [\n            status_df[\'functional_status_legacy\'].eq(OUT_OF_PLANE_STATUS),\n            status_df[\'functional_status_legacy\'].eq(NO_FUNC_STATUS),\n            status_df[\'response_summary_class\'].eq(RESPONSE_SUMMARY_RESPONSIVE),\n            status_df[\'response_summary_class\'].eq(RESPONSE_SUMMARY_LOW),\n        ],\n        [\n            OUT_OF_PLANE_STATUS,\n            NO_FUNC_STATUS,\n            RESPONSIVE_STATUS,\n            LOW_STATUS,\n        ],\n        default=UNAVAILABLE_STATUS,\n    )\n    status_df[\'inner_status\'] = np.where(\n        status_df[\'outer_status\'].eq(OUT_OF_PLANE_STATUS),\n        INNER_OUTSIDE,\n        INNER_WITHIN,\n    )\n    hq_counts_by_gene = _compute_hq_mask_counts_by_gene_local()\n    status_df[\'gene\'] = status_df[\'gene\'].astype(str)\n    within_counts = status_df[~status_df[\'functional_status_legacy\'].eq(OUT_OF_PLANE_STATUS)].groupby(\'gene\').size().to_dict()\n    outside_counts = status_df[status_df[\'functional_status_legacy\'].eq(OUT_OF_PLANE_STATUS)].groupby(\'gene\').size().to_dict()\n    all_genes_for_inner = sorted(set([str(g) for g in status_df[\'gene\'].dropna().astype(str).tolist()]) | set([str(g) for g in hq_counts_by_gene.keys()]))\n    inner_rows = []\n    for g in all_genes_for_inner:\n        n_within = int(within_counts.get(str(g), 0))\n        n_outside = int(outside_counts.get(str(g), 0))\n        n_hq = int(hq_counts_by_gene.get(str(g), n_within + n_outside))\n        n_unmatched = max(0, int(n_hq) - int(n_within) - int(n_outside))\n        if n_within > 0:\n            inner_rows.append({\'gene\': str(g), \'inner_status\': INNER_WITHIN, \'n_labels\': int(n_within)})\n        if n_outside > 0:\n            inner_rows.append({\'gene\': str(g), \'inner_status\': INNER_OUTSIDE, \'n_labels\': int(n_outside)})\n        if n_unmatched > 0:\n            inner_rows.append({\'gene\': str(g), \'inner_status\': INNER_UNMATCHED, \'n_labels\': int(n_unmatched)})\n    inner_counts_by_gene_df = pd.DataFrame(inner_rows)\n\n    status_order = [RESPONSIVE_STATUS, LOW_STATUS, UNAVAILABLE_STATUS, NO_FUNC_STATUS, OUT_OF_PLANE_STATUS, INNER_UNMATCHED]\n    inner_order = [INNER_WITHIN, INNER_OUTSIDE, INNER_UNMATCHED]\n    outer_colors = {\n        RESPONSIVE_STATUS: \'#1b9e77\',\n        LOW_STATUS: \'#6a6a6a\',\n        UNAVAILABLE_STATUS: \'#fdb462\',\n        NO_FUNC_STATUS: \'#d73027\',\n        OUT_OF_PLANE_STATUS: \'#80b1d3\',\n        INNER_UNMATCHED: \'#bdbdbd\',\n    }\n    inner_colors = {\n        INNER_WITHIN: \'#4daf4a\',\n        INNER_OUTSIDE: \'#377eb8\',\n        INNER_UNMATCHED: \'#bdbdbd\',\n    }\n\n    def _wedge_midpoint(wedge, radius):\n        theta = np.deg2rad((float(wedge.theta1) + float(wedge.theta2)) / 2.0)\n        return theta, float(radius) * np.cos(theta), float(radius) * np.sin(theta)\n\n    def _tangent_rotation(theta_rad):\n        theta_deg = ((float(np.rad2deg(theta_rad)) + 180.0) % 360.0) - 180.0\n        rot = theta_deg - 90.0\n        if rot < -90.0:\n            rot += 180.0\n        elif rot > 90.0:\n            rot -= 180.0\n        return rot\n\n    def _contrast_text_color(color):\n        try:\n            r, g, b = to_rgb(color)\n        except Exception:\n            return \'black\'\n        luminance = 0.2126 * r + 0.7152 * g + 0.0722 * b\n        return \'white\' if luminance < 0.52 else \'black\'\n\n    summary_df = (\n        status_df.groupby([\'gene\', \'inner_status\', \'outer_status\'], as_index=False)\n        .size()\n        .rename(columns={\'size\': \'n_labels\'})\n        .sort_values([\'gene\', \'inner_status\', \'outer_status\'])\n        .reset_index(drop=True)\n    )\n    if not inner_counts_by_gene_df.empty:\n        unmatched_summary = inner_counts_by_gene_df[inner_counts_by_gene_df[\'inner_status\'].eq(INNER_UNMATCHED)].copy()\n        if not unmatched_summary.empty:\n            unmatched_summary[\'outer_status\'] = INNER_UNMATCHED\n            summary_df = pd.concat([\n                summary_df,\n                unmatched_summary[[\'gene\', \'inner_status\', \'outer_status\', \'n_labels\']]\n            ], ignore_index=True)\n            summary_df = summary_df.sort_values([\'gene\', \'inner_status\', \'outer_status\']).reset_index(drop=True)\n    SUMMARY_CSV.parent.mkdir(parents=True, exist_ok=True)\n    summary_df.to_csv(SUMMARY_CSV, index=False)\n\n    overall_df = status_df.copy()\n    overall_df[\'gene\'] = \'All genes\'\n    plot_df = pd.concat([overall_df, status_df], ignore_index=True)\n    gene_order = [\'All genes\'] + sorted(status_df[\'gene\'].dropna().astype(str).unique().tolist())\n\n    n_panels = len(gene_order)\n    ncols = min(MAX_COLS, max(1, math.ceil(math.sqrt(n_panels))))\n    nrows = math.ceil(n_panels / ncols)\n    fig, axes = plt.subplots(\n        nrows,\n        ncols,\n        figsize=(PANEL_SIZE[0] * ncols, PANEL_SIZE[1] * nrows),\n        subplot_kw={\'aspect\': \'equal\'},\n    )\n    axes = np.atleast_1d(axes).ravel()\n\n    inner_outer_radius = OUTER_RADIUS - DONUT_WIDTH_OUTER - RING_GAP\n    inner_mid_radius = inner_outer_radius - (DONUT_WIDTH_INNER / 2.0)\n    outer_mid_radius = OUTER_RADIUS - (DONUT_WIDTH_OUTER / 2.0)\n    centre_radius = inner_outer_radius - DONUT_WIDTH_INNER\n\n    for ax_idx, gene in enumerate(gene_order):\n        ax = axes[ax_idx]\n        sub = plot_df[plot_df[\'gene\'] == gene].copy()\n        if not inner_counts_by_gene_df.empty:\n            if gene == \'All genes\':\n                inner_counts = (\n                    inner_counts_by_gene_df.groupby(\'inner_status\', as_index=False)[\'n_labels\']\n                    .sum()\n                )\n            else:\n                inner_counts = inner_counts_by_gene_df[inner_counts_by_gene_df[\'gene\'].astype(str) == str(gene)][[\'inner_status\', \'n_labels\']].copy()\n        else:\n            inner_counts = (\n                sub.groupby(\'inner_status\', as_index=False)\n                .size()\n                .rename(columns={\'size\': \'n_labels\'})\n            )\n        inner_counts[\'inner_order\'] = inner_counts[\'inner_status\'].map({k: i for i, k in enumerate(inner_order)}).fillna(10**6)\n        inner_counts = inner_counts.sort_values(\'inner_order\').reset_index(drop=True)\n\n        outer_counts = (\n            sub.groupby(\'outer_status\', as_index=False)\n            .size()\n            .rename(columns={\'size\': \'n_labels\'})\n        )\n        unmatched_outer_n = 0\n        if not inner_counts.empty:\n            unmatched_outer_n = int(pd.to_numeric(\n                inner_counts.loc[inner_counts[\'inner_status\'].astype(str) == INNER_UNMATCHED, \'n_labels\'],\n                errors=\'coerce\'\n            ).fillna(0).sum())\n        if unmatched_outer_n > 0:\n            outer_counts = pd.concat([\n                outer_counts,\n                pd.DataFrame([{\'outer_status\': INNER_UNMATCHED, \'n_labels\': int(unmatched_outer_n)}])\n            ], ignore_index=True)\n            outer_counts = (\n                outer_counts.groupby(\'outer_status\', as_index=False)[\'n_labels\']\n                .sum()\n            )\n        outer_counts[\'status_order\'] = outer_counts[\'outer_status\'].map({k: i for i, k in enumerate(status_order)}).fillna(10**6)\n        outer_counts = outer_counts.sort_values(\'status_order\').reset_index(drop=True)\n\n        inner_sizes = inner_counts[\'n_labels\'].astype(float).tolist()\n        inner_labels = inner_counts[\'inner_status\'].astype(str).tolist()\n        inner_cols = [inner_colors.get(lbl, \'#cccccc\') for lbl in inner_labels]\n\n        outer_sizes = outer_counts[\'n_labels\'].astype(float).tolist()\n        outer_labels = outer_counts[\'outer_status\'].astype(str).tolist()\n        outer_cols = [outer_colors.get(lbl, \'#cccccc\') for lbl in outer_labels]\n\n        outer_wedges, _ = ax.pie(\n            outer_sizes,\n            radius=OUTER_RADIUS,\n            labels=None,\n            colors=outer_cols,\n            startangle=90,\n            counterclock=False,\n            wedgeprops=dict(width=DONUT_WIDTH_OUTER, edgecolor=\'white\', linewidth=1.0),\n        )\n        inner_wedges, _ = ax.pie(\n            inner_sizes,\n            radius=inner_outer_radius,\n            labels=None,\n            colors=inner_cols,\n            startangle=90,\n            counterclock=False,\n            wedgeprops=dict(width=DONUT_WIDTH_INNER, edgecolor=\'white\', linewidth=1.0),\n        )\n\n        for wedge, label, val, color in zip(inner_wedges, inner_labels, inner_sizes, inner_cols):\n            if float(val) <= 0:\n                continue\n            theta, x, y = _wedge_midpoint(wedge, inner_mid_radius)\n            ax.text(\n                x,\n                y,\n                f"{INNER_LABELS.get(label, label)}\\n{int(round(val))}",\n                ha=\'center\',\n                va=\'center\',\n                rotation=_tangent_rotation(theta),\n                rotation_mode=\'anchor\',\n                fontsize=10,\n                color=_contrast_text_color(color),\n            )\n\n        for wedge, label, val, color in zip(outer_wedges, outer_labels, outer_sizes, outer_cols):\n            if float(val) <= 0:\n                continue\n            theta, x, y = _wedge_midpoint(wedge, outer_mid_radius)\n            ax.text(\n                x,\n                y,\n                f"{int(round(val))}",\n                ha=\'center\',\n                va=\'center\',\n                rotation=_tangent_rotation(theta),\n                rotation_mode=\'anchor\',\n                fontsize=9,\n                color=_contrast_text_color(color),\n            )\n\n        centre_circle = plt.Circle((0, 0), centre_radius, fc=\'white\', ec=\'white\')\n        ax.add_artist(centre_circle)\n        total_n = int(round(float(np.sum(inner_sizes)))) if len(inner_sizes) else int(len(sub))\n        ax.text(0, 0, f\'n = {total_n}\', ha=\'center\', va=\'center\', fontsize=11, fontweight=\'bold\')\n        ax.set_title(gene, fontsize=11, pad=12)\n\n    for ax in axes[n_panels:]:\n        ax.axis(\'off\')\n\n    legend_handles = [\n        Patch(facecolor=outer_colors[RESPONSIVE_STATUS], edgecolor=\'none\', label=STATUS_LABELS[RESPONSIVE_STATUS]),\n        Patch(facecolor=outer_colors[LOW_STATUS], edgecolor=\'none\', label=STATUS_LABELS[LOW_STATUS]),\n        Patch(facecolor=outer_colors[UNAVAILABLE_STATUS], edgecolor=\'none\', label=STATUS_LABELS[UNAVAILABLE_STATUS]),\n        Patch(facecolor=outer_colors[NO_FUNC_STATUS], edgecolor=\'none\', label=STATUS_LABELS[NO_FUNC_STATUS]),\n        Patch(facecolor=outer_colors[OUT_OF_PLANE_STATUS], edgecolor=\'none\', label=STATUS_LABELS[OUT_OF_PLANE_STATUS]),\n    ]\n    fig.legend(\n        handles=legend_handles,\n        loc=\'lower center\',\n        bbox_to_anchor=(0.5, 0.01),\n        ncol=5,\n        frameon=False,\n        title=\'Outer ring\',\n    )\n    fig.suptitle(\'Gene-linked anatomy labels are not equally represented on the functional planes\', fontsize=14, y=0.98)\n    fig.subplots_adjust(left=0.04, right=0.98, top=0.90, bottom=0.16, wspace=0.16, hspace=0.24)\n\n    try:\n        display(summary_df)\n    except Exception:\n        print(summary_df.to_string(index=False))\n\n    if SAVE_HCR_ACTIVITY_OVERVIEW:\n        FIG_PATH.parent.mkdir(parents=True, exist_ok=True)\n        fig.savefig(FIG_PATH, dpi=HCR_ACTIVITY_OVERVIEW_DPI, bbox_inches=\'tight\')\n        print(f\'[50e] saved -> {FIG_PATH}\')\n\n    plt.show()\n',
    '50f': "# [50f]\ntry:\n    REQUIRE_FISH_STATE = require_fish_state\nexcept NameError:\n    REQUIRE_FISH_STATE = None\nif callable(REQUIRE_FISH_STATE) and not REQUIRE_FISH_STATE():\n    raise SystemExit\n\n# Export selected rejected HCR masks in warped 2P space for FIJI overlay.\n# Current scope: single gene sanity check for sst1.1.\nimport numpy as np\nimport pandas as pd\nimport tifffile as tiff\nfrom pathlib import Path\nfrom codeants_2pf_hcr import build_hcr_mask_fate_df\n\ntry:\n    HCR_MASK_FATE_DF_LOCAL = HCR_MASK_FATE_DF\nexcept NameError:\n    HCR_MASK_FATE_DF_LOCAL = None\ntry:\n    HCR_MATCH_RESULTS_LOCAL = hcr_match_results\nexcept NameError:\n    HCR_MATCH_RESULTS_LOCAL = []\ntry:\n    WARP_RESULTS_LOCAL = warp_results\nexcept NameError:\n    WARP_RESULTS_LOCAL = []\ntry:\n    VOX_ANAT_LOCAL = VOX_ANAT\nexcept NameError:\n    VOX_ANAT_LOCAL = None\n\nHCR_EXPORT_GENE = 'sst1.1'\nHCR_QA_DIR = HCR_ALIGNED_DIR.parent / 'qa'\nHCR_QA_DIR.mkdir(parents=True, exist_ok=True)\n\nREJECT_REASON_FAR = 'too far / no overlap anatomy'\nREJECT_REASON_IOU = '1-to-1 anatomy relation, IoU below threshold'\n\nif HCR_MASK_FATE_DF_LOCAL is None or getattr(HCR_MASK_FATE_DF_LOCAL, 'empty', True):\n    if not HCR_MATCH_RESULTS_LOCAL:\n        print('[50f] Need hcr_match_results from [44]; run [44] first.')\n    else:\n        HCR_MASK_FATE_DF = build_hcr_mask_fate_df(HCR_MATCH_RESULTS_LOCAL)\n        HCR_MASK_FATE_DF_LOCAL = HCR_MASK_FATE_DF\nif HCR_MASK_FATE_DF_LOCAL is None or getattr(HCR_MASK_FATE_DF_LOCAL, 'empty', True):\n    print('[50f] No HCR mask fate rows available from [44].')\nelif not WARP_RESULTS_LOCAL:\n    print('[50f] Need warp_results from [43]; run [43] first.')\nelse:\n    fate_df = HCR_MASK_FATE_DF_LOCAL.copy()\n    fate_df = fate_df[fate_df['gene'].astype(str) == HCR_EXPORT_GENE].copy()\n    if fate_df.empty:\n        print(f'[50f] No masks found for gene {HCR_EXPORT_GENE}.')\n    else:\n        warp_by_name = {Path(str(w.get('mask_path', ''))).name: np.asarray(w.get('warped')) for w in WARP_RESULTS_LOCAL}\n        base_shape = None\n        for arr in warp_by_name.values():\n            if getattr(arr, 'ndim', 0) == 3:\n                base_shape = tuple(arr.shape)\n                break\n        if base_shape is None:\n            print('[50f] Could not determine warped stack shape from warp_results.')\n        else:\n            def _empty_stack():\n                return np.zeros(base_shape, dtype=np.uint32)\n\n            far_stack = _empty_stack()\n            iou_stack = _empty_stack()\n\n            def _write_selected_labels(out_arr, reason_label):\n                sub = fate_df[fate_df['anat_unmatched_reason'].astype(str) == str(reason_label)].copy()\n                if sub.empty:\n                    return 0, 0\n                kept_labels = 0\n                kept_voxels = 0\n                for mask_name, sdf in sub.groupby('conf_mask_name', sort=True):\n                    warped = warp_by_name.get(str(mask_name), None)\n                    if warped is None:\n                        print(f'[50f] Missing warped labels for {mask_name}; skipping.')\n                        continue\n                    labels = np.unique(pd.to_numeric(sdf['conf_label'], errors='coerce').dropna().astype(int).to_numpy())\n                    if labels.size == 0:\n                        continue\n                    sel = np.isin(warped, labels)\n                    if not np.any(sel):\n                        continue\n                    out_arr[sel] = warped[sel].astype(out_arr.dtype, copy=False)\n                    kept_labels += int(labels.size)\n                    kept_voxels += int(sel.sum())\n                return kept_labels, kept_voxels\n\n            n_far_labels, n_far_vox = _write_selected_labels(far_stack, REJECT_REASON_FAR)\n            n_iou_labels, n_iou_vox = _write_selected_labels(iou_stack, REJECT_REASON_IOU)\n\n            def _min_unsigned_dtype(max_val: int):\n                if max_val <= np.iinfo(np.uint16).max:\n                    return np.uint16\n                if max_val <= np.iinfo(np.uint32).max:\n                    return np.uint32\n                return np.uint64\n\n            def _save_stack(arr, out_path):\n                max_val = int(arr.max()) if arr.size else 0\n                out_dtype = _min_unsigned_dtype(max_val)\n                arr_out = arr.astype(out_dtype, copy=False)\n                dz = float(VOX_ANAT_LOCAL.get('Z', 1.0)) if VOX_ANAT_LOCAL else 1.0\n                dy = float(VOX_ANAT_LOCAL.get('Y', 1.0)) if VOX_ANAT_LOCAL else 1.0\n                dx = float(VOX_ANAT_LOCAL.get('X', 1.0)) if VOX_ANAT_LOCAL else 1.0\n                kwargs = dict(compression='deflate')\n                if all(np.isfinite(v) and v > 0 for v in (dz, dy, dx)):\n                    kwargs.update(\n                        imagej=True,\n                        metadata={'axes': 'ZYX', 'spacing': dz, 'unit': 'um'},\n                        resolution=(1.0 / dx, 1.0 / dy),\n                    )\n                else:\n                    kwargs.update(imagej=True, metadata={'axes': 'ZYX'})\n                tiff.imwrite(out_path, arr_out, **kwargs)\n\n            out_far = HCR_QA_DIR / f'{HCR_EXPORT_GENE}_too_far_or_no_overlap_in_2p_labels.tif'\n            out_iou = HCR_QA_DIR / f'{HCR_EXPORT_GENE}_iou_below_threshold_in_2p_labels.tif'\n            _save_stack(far_stack, out_far)\n            _save_stack(iou_stack, out_iou)\n\n            HCR_SST11_TOO_FAR_TIF_PATH = str(out_far)\n            HCR_SST11_IOU_BELOW_THRESHOLD_TIF_PATH = str(out_iou)\n\n            print(f'[50f] Saved {HCR_EXPORT_GENE} too-far/no-overlap stack to {out_far} (labels={n_far_labels}, voxels={n_far_vox})')\n            print(f'[50f] Saved {HCR_EXPORT_GENE} IoU-below-threshold stack to {out_iou} (labels={n_iou_labels}, voxels={n_iou_vox})')\n",
    '50g': "# [50g]\ntry:\n    REQUIRE_FISH_STATE = require_fish_state\nexcept NameError:\n    REQUIRE_FISH_STATE = None\nif callable(REQUIRE_FISH_STATE) and not REQUIRE_FISH_STATE():\n    raise SystemExit\n\n# Centroid-offset sanity plot for rejected HCR->anatomy masks.\n# Mirrors the simple XY/Z violin style of [53a] bottom-left, but restricted to rejected sst1.1 masks.\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nfrom pathlib import Path\nfrom codeants_2pf_hcr import build_hcr_mask_fate_df\n\ntry:\n    HCR_MASK_FATE_DF_LOCAL = HCR_MASK_FATE_DF\nexcept NameError:\n    HCR_MASK_FATE_DF_LOCAL = None\ntry:\n    HCR_MATCH_RESULTS_LOCAL = hcr_match_results\nexcept NameError:\n    HCR_MATCH_RESULTS_LOCAL = []\ntry:\n    VOX_ANAT_LOCAL = VOX_ANAT\nexcept NameError:\n    VOX_ANAT_LOCAL = None\n\nHCR_OFFSET_PLOT_GENE = 'sst1.1'\nHCR_QA_DIR = HCR_ALIGNED_DIR.parent / 'qa'\nHCR_QA_DIR.mkdir(parents=True, exist_ok=True)\n\nREJECT_REASON_FAR = 'too far / no overlap anatomy'\nREJECT_REASON_IOU = '1-to-1 anatomy relation, IoU below threshold'\nPLOT_REASONS = [REJECT_REASON_FAR, REJECT_REASON_IOU]\ntry:\n    MAX_DISTANCE_LOCAL = float(MAX_DISTANCE_UM)\nexcept NameError:\n    MAX_DISTANCE_LOCAL = np.nan\ntry:\n    IOU_MIN_LOCAL = float(IOU_MIN)\nexcept NameError:\n    IOU_MIN_LOCAL = 0.05\ntry:\n    USE_FRAC_FILTERS_LOCAL = bool(USE_FRAC_FILTERS)\nexcept NameError:\n    USE_FRAC_FILTERS_LOCAL = False\ntry:\n    MIN_OVERLAP_FRAC_CONF_LOCAL = float(MIN_OVERLAP_FRAC_CONF)\nexcept NameError:\n    MIN_OVERLAP_FRAC_CONF_LOCAL = 0.0\ntry:\n    MIN_OVERLAP_FRAC_TWOP_LOCAL = float(MIN_OVERLAP_FRAC_TWOP)\nexcept NameError:\n    MIN_OVERLAP_FRAC_TWOP_LOCAL = 0.0\n\nif HCR_MASK_FATE_DF_LOCAL is None or getattr(HCR_MASK_FATE_DF_LOCAL, 'empty', True):\n    if not HCR_MATCH_RESULTS_LOCAL:\n        print('[50g] Need hcr_match_results from [44]; run [44] first.')\n    else:\n        HCR_MASK_FATE_DF = build_hcr_mask_fate_df(HCR_MATCH_RESULTS_LOCAL)\n        HCR_MASK_FATE_DF_LOCAL = HCR_MASK_FATE_DF\nif HCR_MASK_FATE_DF_LOCAL is None or getattr(HCR_MASK_FATE_DF_LOCAL, 'empty', True):\n    print('[50g] No HCR mask fate rows available from [44].')\nelif not HCR_MATCH_RESULTS_LOCAL:\n    print('[50g] Need hcr_match_results from [44]; run [44] first.')\nelse:\n    DX = float(VOX_ANAT_LOCAL.get('X', 1.0)) if VOX_ANAT_LOCAL else 1.0\n    DY = float(VOX_ANAT_LOCAL.get('Y', 1.0)) if VOX_ANAT_LOCAL else 1.0\n    DZ = float(VOX_ANAT_LOCAL.get('Z', 1.0)) if VOX_ANAT_LOCAL else 1.0\n\n    fate_df = HCR_MASK_FATE_DF_LOCAL.copy()\n    fate_df = fate_df[(fate_df['gene'].astype(str) == HCR_OFFSET_PLOT_GENE) & (fate_df['anat_unmatched_reason'].astype(str).isin(PLOT_REASONS))].copy()\n    if fate_df.empty:\n        print(f'[50g] No rejected masks found for gene {HCR_OFFSET_PLOT_GENE} in the requested categories.')\n    else:\n        def _to_bool_series_local(s):\n            if s is None:\n                return pd.Series([], dtype=bool)\n            if pd.api.types.is_bool_dtype(s):\n                return s.fillna(False).astype(bool)\n            if pd.api.types.is_numeric_dtype(s):\n                return s.fillna(0).astype(float) != 0\n            return s.astype(str).str.strip().str.lower().isin({'1', 'true', 't', 'yes', 'y'})\n\n        res_by_mask = {Path(str(res.get('mask_path', ''))).name: res for res in HCR_MATCH_RESULTS_LOCAL}\n        rows = []\n        nearest_only_count = 0\n\n        for rr in fate_df.itertuples(index=False):\n            mask_name = str(getattr(rr, 'conf_mask_name'))\n            conf_label = int(getattr(rr, 'conf_label'))\n            reason = str(getattr(rr, 'anat_unmatched_reason'))\n            res = res_by_mask.get(mask_name, None)\n            if res is None:\n                continue\n\n            df_conf = res.get('df_conf', None)\n            df_2p = res.get('df_2p', None)\n            matches = res.get('matches', None)\n            if df_conf is None or df_2p is None or getattr(df_conf, 'empty', True) or getattr(df_2p, 'empty', True):\n                continue\n\n            df_conf = df_conf.copy()\n            df_2p = df_2p.copy()\n            df_conf['label'] = pd.to_numeric(df_conf['label'], errors='coerce')\n            df_2p['label'] = pd.to_numeric(df_2p['label'], errors='coerce')\n            df_conf = df_conf[np.isfinite(df_conf['label'])].copy()\n            df_2p = df_2p[np.isfinite(df_2p['label'])].copy()\n            df_conf['label'] = df_conf['label'].astype(int)\n            df_2p['label'] = df_2p['label'].astype(int)\n            if conf_label not in set(df_conf['label'].tolist()):\n                continue\n\n            conf_row = df_conf[df_conf['label'] == conf_label].iloc[0]\n            cx = float(conf_row['x'])\n            cy = float(conf_row['y'])\n            cz = float(conf_row['z'])\n\n            cand_choice = None\n            candidate_mode = 'nearest_anatomy_centroid'\n            iou_val = np.nan\n            quality_val = ''\n            pair_type_val = ''\n            overlap_vox = np.nan\n            distance_um_matches_row = np.nan\n\n            if matches is not None and not getattr(matches, 'empty', True):\n                cand = matches.copy()\n                cand['conf_label'] = pd.to_numeric(cand['conf_label'], errors='coerce')\n                cand = cand[np.isfinite(cand['conf_label'])].copy()\n                cand['conf_label'] = cand['conf_label'].astype(int)\n                cand = cand[cand['conf_label'] == conf_label].copy()\n                if not cand.empty:\n                    if 'within_gate' in cand.columns:\n                        cand['within_gate'] = _to_bool_series_local(cand['within_gate']).to_numpy(dtype=bool)\n                    else:\n                        cand['within_gate'] = False\n                    if 'distance_um' in cand.columns:\n                        cand['distance_um'] = pd.to_numeric(cand['distance_um'], errors='coerce')\n                    if 'iou' in cand.columns:\n                        cand['iou'] = pd.to_numeric(cand['iou'], errors='coerce')\n                    if 'overlap_voxels' in cand.columns:\n                        cand['overlap_voxels'] = pd.to_numeric(cand['overlap_voxels'], errors='coerce')\n                    cand = cand.sort_values(['distance_um'], ascending=[True], na_position='last')\n\n                    if reason == REJECT_REASON_IOU:\n                        sub = cand[cand['within_gate']].copy()\n                        if 'pair_type' in sub.columns:\n                            sub = sub[sub['pair_type'].astype(str) == '1-1'].copy()\n                        if 'quality' in sub.columns:\n                            sub_bad = sub[sub['quality'].astype(str) != 'good'].copy()\n                            if not sub_bad.empty:\n                                sub = sub_bad\n                        if not sub.empty:\n                            cand_choice = sub.iloc[0]\n                            candidate_mode = 'within_gate_1to1_candidate'\n                    else:\n                        cand_nonnull = cand[cand['twoP_label'].notna()].copy() if 'twoP_label' in cand.columns else cand.iloc[0:0].copy()\n                        if not cand_nonnull.empty:\n                            cand_choice = cand_nonnull.iloc[0]\n                            candidate_mode = 'nearest_overlap_candidate'\n\n            anat_label = None\n            if cand_choice is not None and pd.notna(cand_choice.get('twoP_label', np.nan)):\n                anat_label = int(cand_choice['twoP_label'])\n                arow = df_2p[df_2p['label'] == anat_label]\n                if not arow.empty:\n                    arow = arow.iloc[0]\n                    ax = float(arow['x'])\n                    ay = float(arow['y'])\n                    az = float(arow['z'])\n                    if 'iou' in cand_choice.index and pd.notna(cand_choice['iou']):\n                        iou_val = float(cand_choice['iou'])\n                    if 'quality' in cand_choice.index and pd.notna(cand_choice['quality']):\n                        quality_val = str(cand_choice['quality'])\n                    if 'pair_type' in cand_choice.index and pd.notna(cand_choice['pair_type']):\n                        pair_type_val = str(cand_choice['pair_type'])\n                    if 'overlap_voxels' in cand_choice.index and pd.notna(cand_choice['overlap_voxels']):\n                        overlap_vox = float(cand_choice['overlap_voxels'])\n                    if 'distance_um' in cand_choice.index and pd.notna(cand_choice['distance_um']):\n                        distance_um_matches_row = float(cand_choice['distance_um'])\n                else:\n                    cand_choice = None\n\n            if cand_choice is None:\n                anat_xyz = df_2p[['x', 'y', 'z']].apply(pd.to_numeric, errors='coerce').to_numpy(dtype=float)\n                anat_labels = df_2p['label'].to_numpy(dtype=int)\n                if anat_xyz.size == 0:\n                    continue\n                diff = anat_xyz - np.array([[cx, cy, cz]], dtype=float)\n                d_um = np.sqrt((diff[:, 0] * DX) ** 2 + (diff[:, 1] * DY) ** 2 + (diff[:, 2] * DZ) ** 2)\n                if d_um.size == 0 or not np.isfinite(d_um).any():\n                    continue\n                j = int(np.nanargmin(d_um))\n                ax, ay, az = anat_xyz[j].tolist()\n                anat_label = int(anat_labels[j])\n                distance_um_matches_row = float(d_um[j])\n                nearest_only_count += 1\n\n            dx_um = (cx - ax) * DX\n            dy_um = (cy - ay) * DY\n            dz_um = (cz - az) * DZ\n            xy_um = float(np.sqrt(dx_um * dx_um + dy_um * dy_um))\n            dist_um = float(np.sqrt(dx_um * dx_um + dy_um * dy_um + dz_um * dz_um))\n            rows.append({\n                'gene': HCR_OFFSET_PLOT_GENE,\n                'conf_mask_name': mask_name,\n                'conf_label': conf_label,\n                'anat_label_ref': int(anat_label),\n                'reason': reason,\n                'candidate_mode': candidate_mode,\n                'dx_um': float(dx_um),\n                'dy_um': float(dy_um),\n                'dz_um': float(dz_um),\n                'abs_dz_um': float(abs(dz_um)),\n                'xy_um': xy_um,\n                'distance_um': dist_um,\n                'distance_um_matches_row': distance_um_matches_row,\n                'iou': float(iou_val) if np.isfinite(iou_val) else np.nan,\n                'quality': quality_val,\n                'pair_type': pair_type_val,\n                'overlap_voxels': overlap_vox,\n            })\n\n        plot_df = pd.DataFrame(rows)\n        HCR_REJECTED_OFFSET_DF = plot_df.copy()\n\n        if plot_df.empty:\n            print(f'[50g] No centroid-offset rows could be assembled for gene {HCR_OFFSET_PLOT_GENE}.')\n        else:\n            axis_specs = [('XY', 'xy_um', '#4c78a8'), ('Z', 'abs_dz_um', '#f58518')]\n            reason_short = {\n                REJECT_REASON_FAR: 'too far / no overlap',\n                REJECT_REASON_IOU: 'IoU below threshold',\n            }\n            plot_values = []\n            plot_positions = []\n            plot_colors = []\n            plot_ticklabels = []\n            block = len(axis_specs) + 1\n            for ri, reason in enumerate(PLOT_REASONS):\n                sub_reason = plot_df[plot_df['reason'].astype(str) == str(reason)].copy()\n                for ai, (axis_label, col, color) in enumerate(axis_specs, start=1):\n                    vals = pd.to_numeric(sub_reason.get(col, np.nan), errors='coerce').dropna().to_numpy(dtype=float)\n                    if vals.size == 0:\n                        continue\n                    pos = ri * block + ai\n                    plot_values.append(vals)\n                    plot_positions.append(float(pos))\n                    plot_colors.append(color)\n                    plot_ticklabels.append(f'{axis_label}\\n{reason_short.get(reason, reason)}')\n\n            fig, ax = plt.subplots(figsize=(11, 7))\n            if not plot_values:\n                ax.text(0.5, 0.5, 'No rejected centroid-offset data available', ha='center', va='center', transform=ax.transAxes)\n                ax.set_title(f'Rejected {HCR_OFFSET_PLOT_GENE} masks sit far from plausible anatomy matches')\n            else:\n                vp = ax.violinplot(plot_values, positions=plot_positions, showmeans=False, showmedians=False, showextrema=False, widths=0.8)\n                for body, color in zip(vp['bodies'], plot_colors):\n                    body.set_facecolor(color)\n                    body.set_edgecolor('black')\n                    body.set_alpha(0.75)\n                medians = [float(np.median(v)) for v in plot_values]\n                ax.scatter(plot_positions, medians, color='black', s=22, zorder=3)\n\n                for vals, pos in zip(plot_values, plot_positions):\n                    arr = np.asarray(vals, dtype=float)\n                    arr = arr[np.isfinite(arr)]\n                    if arr.size == 0:\n                        continue\n                    y_ann = float(np.nanpercentile(arr, 97.0)) if arr.size > 2 else float(np.nanmax(arr))\n                    if not np.isfinite(y_ann):\n                        y_ann = float(np.nanmax(arr))\n                    ax.text(\n                        float(pos) + 0.12,\n                        y_ann,\n                        f'n={int(arr.size)}',\n                        ha='left',\n                        va='bottom',\n                        fontsize=8,\n                        bbox=dict(boxstyle='round,pad=0.15', facecolor='white', edgecolor='none', alpha=0.75),\n                    )\n\n                if np.isfinite(MAX_DISTANCE_LOCAL):\n                    ax.axhline(MAX_DISTANCE_LOCAL, color='black', linestyle='--', linewidth=1.4, alpha=0.9)\n                    ax.text(\n                        0.99,\n                        MAX_DISTANCE_LOCAL,\n                        f' MAX_DISTANCE_UM={MAX_DISTANCE_LOCAL:.2f}',\n                        transform=ax.get_yaxis_transform(),\n                        ha='right',\n                        va='bottom',\n                        fontsize=8,\n                        color='black',\n                        bbox=dict(boxstyle='round,pad=0.15', facecolor='white', edgecolor='none', alpha=0.8),\n                    )\n\n                ax.set_xticks(plot_positions)\n                ax.set_xticklabels(plot_ticklabels)\n                ax.set_ylabel('Absolute centroid offset (\xb5m)')\n                ax.set_title(f'Rejected {HCR_OFFSET_PLOT_GENE} masks sit far from plausible anatomy matches')\n                ax.grid(axis='y', alpha=0.25)\n\n                note_lines = [\n                    'Dashed line: MAX_DISTANCE_UM (3D gate shown as per-axis reference).',\n                    f'IoU threshold for the second subset: IOU_MIN={IOU_MIN_LOCAL:.2f}.',\n                ]\n                if USE_FRAC_FILTERS_LOCAL:\n                    note_lines.append(\n                        f'Frac filters also enabled: conf>={MIN_OVERLAP_FRAC_CONF_LOCAL:.2f}, twoP>={MIN_OVERLAP_FRAC_TWOP_LOCAL:.2f}.'\n                    )\n                else:\n                    note_lines.append('Frac filters disabled in current notebook config.')\n                note_lines.append(f'Nearest-anatomy fallback used for {nearest_only_count} no-overlap labels.')\n                ax.text(\n                    0.02,\n                    0.98,\n                    '\\n'.join(note_lines),\n                    transform=ax.transAxes,\n                    ha='left',\n                    va='top',\n                    fontsize=8,\n                    bbox=dict(boxstyle='round,pad=0.25', facecolor='white', edgecolor='#666666', alpha=0.9),\n                )\n\n            out_path = HCR_QA_DIR / f'{HCR_OFFSET_PLOT_GENE}_rejected_centroid_offsets_by_reason.png'\n            fig.tight_layout()\n            fig.savefig(out_path, dpi=220, bbox_inches='tight')\n            HCR_REJECTED_OFFSET_FIG_PATH = str(out_path)\n            print(f'[50g] Saved rejected-offset plot to {out_path}')\n            plt.show()\n",
    '53': "# [53]\ntry:\n    REQUIRE_FISH_STATE = require_fish_state\nexcept NameError:\n    REQUIRE_FISH_STATE = None\nif callable(REQUIRE_FISH_STATE) and not REQUIRE_FISH_STATE():\n    raise SystemExit\n# --- Report tables (figures moved to [53a]) ---\n\nimport numpy as np\nimport pandas as pd\nfrom pathlib import Path\nimport os\nimport re\nfrom skimage.measure import regionprops_table\n\npalette = {\n    'Anatomy': '#6b6b6b',\n    'Functional': '#2e9f4d',\n    'HCR': '#d000a7',\n}\nRC = RUN_CONFIG if isinstance(RUN_CONFIG, dict) else {}\nOFFSET_PCTL = min(100.0, max(0.0, float(RC.get('OFFSET_PCTL', 95.0))))\n\ndef _display_df(df):\n    try:\n        display(df)\n    except Exception:\n        print(df.to_string(index=False))\n\nDEFAULT_GENE_ORDER = ['sst1.1', 'sst1.2', 'npy', 'tac3b', 'pth2', 'cfos', 'cort']\nDEFAULT_GENE_COLORS = {\n    'sst1.1': '#d62728',\n    'sst1.2': '#d61ad2',\n    'npy': '#1f9d55',\n    'tac3b': '#ffd400',\n    'pth2': '#00bcd4',\n    'cfos': '#ff7f0e',\n    'cort': '#8c564b',\n}\nGENE_ORDER = list(RC.get('GENE_ORDER', DEFAULT_GENE_ORDER))\nGENE_COLORS = dict(DEFAULT_GENE_COLORS)\ntry:\n    _gc = RC.get('GENE_COLORS', {})\n    if isinstance(_gc, dict):\n        GENE_COLORS.update(_gc)\nexcept Exception:\n    pass\nfor _g in DEFAULT_GENE_ORDER:\n    GENE_COLORS.setdefault(_g, DEFAULT_GENE_COLORS[_g])\nGENE_ORDER = GENE_ORDER\nGENE_COLORS = GENE_COLORS\npalette = palette\n\n# --- Noise-filtered diameters table prep (q05 hard-drop + q95 low-confidence flag) ---\nFILTER_MODALITIES = ['Anatomy', 'Functional', 'HCR']\nFILTER_LOW_QUANTILE = 0.05\nFILTER_HIGH_QUANTILE = 0.95\nLOW_CONF_COL = 'is_low_confidence_segmentation'\n\ndef _apply_modality_xy_filter(\n    df_in,\n    modalities=FILTER_MODALITIES,\n    low_quantile=FILTER_LOW_QUANTILE,\n    high_quantile=FILTER_HIGH_QUANTILE,\n    low_conf_col=LOW_CONF_COL,\n):\n    if df_in is None or getattr(df_in, 'empty', True):\n        return df_in, pd.DataFrame()\n    required_cols = {'dataset', 'x_um', 'y_um'}\n    if not required_cols.issubset(df_in.columns):\n        return df_in.copy(), pd.DataFrame()\n\n    parts = []\n    summary_rows = []\n    datasets_present = [ds for ds in modalities if ds in df_in['dataset'].unique()]\n    datasets_present += [ds for ds in df_in['dataset'].unique() if ds not in modalities]\n    q_low_pct = int(round(float(low_quantile) * 100.0))\n    q_high_pct = int(round(float(high_quantile) * 100.0))\n\n    for ds in datasets_present:\n        sub = df_in[df_in['dataset'] == ds].copy()\n        n_before = int(len(sub))\n        xy_vals = sub[['x_um', 'y_um']].mean(axis=1).to_numpy(dtype=float)\n        finite = np.isfinite(xy_vals)\n        finite_vals = xy_vals[finite]\n\n        med = float(np.median(finite_vals)) if finite_vals.size else np.nan\n        sd = float(np.std(finite_vals)) if finite_vals.size else np.nan\n        q_low = float(np.quantile(finite_vals, low_quantile)) if finite_vals.size else np.nan\n        q_high = float(np.quantile(finite_vals, high_quantile)) if finite_vals.size else np.nan\n        cutoff_low = q_low\n        cutoff_high = q_high\n\n        keep = np.ones(n_before, dtype=bool)\n        low_conf_mask = np.zeros(n_before, dtype=bool)\n        is_filtered = (ds in modalities) and finite_vals.size > 0\n        if is_filtered:\n            finite_idx = np.where(finite)[0]\n            keep[finite_idx] = finite_vals >= cutoff_low\n            low_conf_mask[finite_idx] = finite_vals > cutoff_high\n\n        sub['xy_um'] = xy_vals\n        sub[low_conf_col] = low_conf_mask\n\n        n_after = int(keep.sum())\n        n_dropped = int(n_before - n_after)\n        pct_dropped = float((100.0 * n_dropped / n_before) if n_before > 0 else 0.0)\n        n_low_conf_before = int(low_conf_mask.sum())\n        n_low_conf_after = int(low_conf_mask[keep].sum())\n        pct_low_conf_after = float((100.0 * n_low_conf_after / n_after) if n_after > 0 else 0.0)\n\n        summary_rows.append({\n            'dataset': ds,\n            'is_filtered': bool(ds in modalities),\n            'n_before': n_before,\n            'n_dropped': n_dropped,\n            'n_after': n_after,\n            'pct_dropped': pct_dropped,\n            'xy_median_um': med,\n            'xy_sd_um': sd,\n            'xy_q05_um': q_low if q_low_pct == 5 else np.nan,\n            'xy_q95_um': q_high if q_high_pct == 95 else np.nan,\n            'xy_cutoff_um': cutoff_low,\n            'xy_flag_um': cutoff_high,\n            'n_low_confidence_q95_before_filter': n_low_conf_before,\n            'n_low_confidence_q95_after_filter': n_low_conf_after,\n            'pct_low_confidence_after_filter': pct_low_conf_after,\n        })\n        parts.append(sub.iloc[keep].copy())\n\n    df_out = pd.concat(parts, ignore_index=True) if parts else df_in.iloc[0:0].copy()\n    summary_df = pd.DataFrame(summary_rows)\n    return df_out, summary_df\n\ndf_all_src = df_all if 'df_all' in locals() else None\nif df_all_src is None or getattr(df_all_src, 'empty', True):\n    _diam_paths = []\n    if OUT_QA is not None:\n        _diam_paths.append(OUT_QA / 'diameters_df_all.pkl')\n    if OUTDIR is not None:\n        _diam_paths.append(OUTDIR / 'diameters_df_all.pkl')\n    for _p in _diam_paths:\n        if os.path.exists(_p):\n            try:\n                df_all_src = pd.read_pickle(_p)\n                break\n            except Exception:\n                df_all_src = None\n\nif df_all_src is None or getattr(df_all_src, 'empty', True):\n    print('No diameter dataframe available; run the diameters cell first.')\nelse:\n    df_all_filt, df_all_filter_summary = _apply_modality_xy_filter(df_all_src)\n    df_all_filt = df_all_filt\n    df_all_filter_summary = df_all_filter_summary\n    LOW_CONF_SEG_COL = LOW_CONF_COL\n\n    if not df_all_filter_summary.empty:\n        _order = FILTER_MODALITIES + [ds for ds in df_all_filter_summary['dataset'].tolist() if ds not in FILTER_MODALITIES]\n        df_all_filter_summary['dataset'] = pd.Categorical(df_all_filter_summary['dataset'], categories=_order, ordered=True)\n        df_all_filter_summary = df_all_filter_summary.sort_values('dataset').reset_index(drop=True)\n        df_all_filter_summary = df_all_filter_summary\n        print('[53] Table: Diameter filtering summary per dataset (q05 drop, q95 low-confidence flag)')\n        _display_df(df_all_filter_summary)\n\n    cats = ['Anatomy', 'Functional', 'HCR']\n    present = [c for c in cats if c in df_all_filt['dataset'].unique()]\n\n    summary_rows = []\n    for ds in present:\n        for axis, col in [('X', 'x_um'), ('Y', 'y_um'), ('Z', 'z_um')]:\n            vals = df_all_filt.loc[df_all_filt['dataset'] == ds, col].to_numpy(dtype=float)\n            summary_rows.append({\n                'dataset': ds,\n                'axis': axis,\n                'n': int(vals.size),\n                'median_um': float(np.median(vals)) if vals.size else np.nan,\n                'sd_um': float(np.std(vals)) if vals.size else np.nan,\n            })\n    if summary_rows:\n        diam_summary = pd.DataFrame(summary_rows)\n        diam_summary = diam_summary\n        print('[53] Table: Diameter summary by dataset and axis (after filtering)')\n        _display_df(diam_summary)\n\n# --- XY and Z centroid offsets (table only) using [36] data ---\ndef _gather_xy_z_offsets():\n    if not plane_refs:\n        return None, None, None, None, 'No plane_refs available.'\n    if '_prepare_plane_data' not in locals():\n        return None, None, None, None, 'Centroid QA helpers not initialized (run [34]).'\n    DZ = float(VOX_ANAT.get('Z', 1.0)) if VOX_ANAT else 1.0\n\n    def _get_anat_volume():\n        if anat_labels_all is not None:\n            return anat_labels_all\n        try:\n            return _ensure_uint_labels(imread_any(ANAT_LABELS_PATH))\n        except Exception:\n            return None\n\n    anat_vol = _get_anat_volume()\n    if anat_vol is None:\n        return None, None, None, None, 'Anatomy labels unavailable for Z offsets.'\n\n    try:\n        props3d = regionprops_table(anat_vol, properties=('label', 'centroid', 'bbox'))\n        adf3d = pd.DataFrame(props3d)\n        adf3d = adf3d[adf3d['label'] != 0]\n        adf3d = adf3d.rename(columns={\n            'centroid-0': 'cz', 'centroid-1': 'cy', 'centroid-2': 'cx',\n            'bbox-0': 'zmin', 'bbox-1': 'ymin', 'bbox-2': 'xmin',\n            'bbox-3': 'zmax', 'bbox-4': 'ymax', 'bbox-5': 'xmax'\n        })\n        z_cent = adf3d.set_index('label')['cz'].to_dict()\n        vx = float(VOX_ANAT.get('X', 1.0) if VOX_ANAT else 1.0)\n        vy = float(VOX_ANAT.get('Y', 1.0) if VOX_ANAT else 1.0)\n        vz = float(VOX_ANAT.get('Z', 1.0) if VOX_ANAT else 1.0)\n        x_um = (adf3d['xmax'] - adf3d['xmin']) * vx\n        y_um = (adf3d['ymax'] - adf3d['ymin']) * vy\n        z_um = (adf3d['zmax'] - adf3d['zmin']) * vz\n        diam_xy_um = (x_um + y_um) / 2.0\n        mean_rad_xy_um = float((diam_xy_um / 2.0).mean()) if not adf3d.empty else 0.0\n    except Exception:\n        z_cent = {}\n        mean_rad_xy_um = 0.0\n    if not z_cent:\n        return None, None, None, None, 'Could not compute anatomy centroids for Z offsets.'\n\n    xy_all, z_all = [], []\n    for p_idx in range(len(plane_refs)):\n        pdata = _prepare_plane_data(p_idx)\n        if pdata is None or pdata.get('d') is None or not pdata['d'].size:\n            continue\n        xy_all.append(np.asarray(pdata['d'], float))\n        df_links = pdata.get('df', None)\n        if df_links is None or 'anat_label' not in df_links.columns:\n            continue\n        bz = pdata.get('best_z', plane_refs[p_idx].get('best_z', p_idx))\n        bz_um = float(bz) * DZ\n        z_offs = []\n        for al in df_links['anat_label'].to_numpy(int):\n            zc = z_cent.get(int(al))\n            if zc is None:\n                continue\n            z_offs.append(abs(float(zc) * DZ - bz_um))\n        if z_offs:\n            z_all.append(np.asarray(z_offs, float))\n    if not xy_all:\n        return None, None, None, None, 'No centroid matches available.'\n    xy_vals = np.concatenate(xy_all)\n    z_vals = np.concatenate(z_all) if z_all else None\n    return xy_vals, z_vals, mean_rad_xy_um, None, None\n\nxy_vals, z_vals, mean_rad_xy_um, _, err = _gather_xy_z_offsets()\nxy_vals = xy_vals\nz_vals = z_vals\nmean_rad_xy_um = mean_rad_xy_um\nif err:\n    print(err)\nelse:\n    rows = []\n    if xy_vals is not None and np.size(xy_vals):\n        rows.append({\n            'axis': 'XY',\n            'n': int(np.size(xy_vals)),\n            'median_offset_um': float(np.median(xy_vals)),\n            'sd_offset_um': float(np.std(xy_vals)),\n            'percentile': float(OFFSET_PCTL),\n            'percentile_offset_um': float(np.percentile(xy_vals, OFFSET_PCTL)),\n            'anat_mean_radius_um': float(mean_rad_xy_um) if mean_rad_xy_um is not None else np.nan,\n        })\n    if z_vals is not None and np.size(z_vals):\n        rows.append({\n            'axis': 'Z',\n            'n': int(np.size(z_vals)),\n            'median_offset_um': float(np.median(z_vals)),\n            'sd_offset_um': float(np.std(z_vals)),\n            'percentile': float(OFFSET_PCTL),\n            'percentile_offset_um': float(np.percentile(z_vals, OFFSET_PCTL)),\n            'anat_mean_radius_um': np.nan,\n        })\n    if rows:\n        offset_summary_df = pd.DataFrame(rows)\n        offset_summary_df = offset_summary_df\n        print('[53] Table: Functional-to-anatomy centroid offset summary (XY and Z)')\n        _display_df(offset_summary_df)\n\n# --- HCR -> 2P summary tables (final analysis pairs from [50]) ---\nif not hcr_match_results:\n    print('Need hcr_match_results; skipping [53] HCR summary tables.')\nelse:\n    DZ = float(VOX_ANAT.get('Z', 1.0)) if VOX_ANAT else 1.0\n    DY = float(VOX_ANAT.get('Y', 1.0)) if VOX_ANAT else 1.0\n    DX = float(VOX_ANAT.get('X', 1.0)) if VOX_ANAT else 1.0\n    ALIGN_WITH_56_PLANES = bool(RC.get('ALIGN_WITH_56_PLANES', True))\n\n    def _infer_gene_from_mask(path_str):\n        if callable(gene_from_mask):\n            try:\n                return str(gene_from_mask(path_str))\n            except Exception:\n                pass\n        name = Path(str(path_str)).name\n        m = re.search(r'channel\\d+_(.+?)_cp_masks', name)\n        gene = m.group(1) if m else name\n        return gene.replace('sst1_', 'sst1.')\n\n    def _as_bool_series(s):\n        if pd.api.types.is_bool_dtype(s):\n            return s.fillna(False).astype(bool)\n        if pd.api.types.is_numeric_dtype(s):\n            return s.fillna(0).astype(float) != 0\n        return s.astype(str).str.strip().str.lower().isin({'1', 'true', 't', 'yes', 'y'})\n\n    def _load_final_analysis_pairs_53():\n        _conf_override = RC.get('CONF_FUNC_CSV_ANALYSIS', None)\n        _conf_base = _conf_override if _conf_override else (CONF_FUNC_CSV if 'CONF_FUNC_CSV' in locals() else str(OUT_REG / 'conf_to_func_pairs.csv'))\n        conf_path = Path(str(_conf_base))\n        if not conf_path.exists():\n            print(f'[53] Missing analysis mapping: {conf_path}. Run [50].')\n            return pd.DataFrame()\n\n        df = pd.read_csv(conf_path)\n        if 'fish_id' in df.columns:\n            df = df[df['fish_id'].astype(str) == str(FISH_ID)].copy()\n        if df.empty:\n            return df\n\n        req_cols = ['gene', 'conf_mask', 'conf_label', 'anat_label', 'func_label', 'plane']\n        missing = [c for c in req_cols if c not in df.columns]\n        if missing:\n            raise RuntimeError(f'[53] analysis mapping missing required columns: {missing}')\n\n        if 'is_selected_for_analysis' in df.columns:\n            sel = _as_bool_series(df['is_selected_for_analysis'])\n            if sel.any():\n                df = df[sel].copy()\n\n        df['gene'] = df['gene'].astype(str)\n        for c in ('conf_label', 'anat_label', 'func_label', 'plane'):\n            df[c] = pd.to_numeric(df[c], errors='coerce')\n        df = df.dropna(subset=req_cols).copy()\n        if df.empty:\n            return df\n        for c in ('conf_label', 'anat_label', 'func_label', 'plane'):\n            df[c] = df[c].astype(int)\n\n        if ALIGN_WITH_56_PLANES and suite2p_by_ref_idx:\n            s2p_planes = set(int(k) for k in suite2p_by_ref_idx.keys())\n            df = df[df['plane'].isin(s2p_planes)].copy()\n        if df.empty:\n            return df\n\n        df['_sort_dist_func'] = pd.to_numeric(df.get('dist_func_anat_um', pd.Series(np.nan, index=df.index)), errors='coerce').fillna(np.inf)\n        df['_sort_overlap'] = pd.to_numeric(df.get('overlap_px_func_anat', df.get('overlap_px', pd.Series(np.nan, index=df.index))), errors='coerce').fillna(0)\n        df['_sort_dist_conf'] = pd.to_numeric(df.get('dist_conf_anat_um', pd.Series(np.nan, index=df.index)), errors='coerce').fillna(np.inf)\n        df = df.sort_values(\n            ['gene', 'anat_label', '_sort_dist_func', '_sort_overlap', '_sort_dist_conf', 'plane', 'func_label'],\n            ascending=[True, True, True, False, True, True, True],\n        )\n        df = df.drop_duplicates(subset=['gene', 'anat_label'], keep='first')\n        df = df.drop_duplicates(subset=['plane', 'func_label', 'gene'], keep='first')\n        df = df.drop(columns=['_sort_dist_func', '_sort_overlap', '_sort_dist_conf'])\n        return df.reset_index(drop=True)\n\n    selected_pairs = _load_final_analysis_pairs_53()\n    selected_pairs = selected_pairs\n    if selected_pairs.empty:\n        print('[53] No final analysis pairs available for HCR summary tables.')\n    else:\n        res_by_mask = {Path(str(res.get('mask_path', ''))).name: res for res in hcr_match_results}\n\n        hcr_diam_cache = {}\n        for _mname, _res in res_by_mask.items():\n            _warped = _res.get('warped', None)\n            if _warped is None:\n                continue\n            try:\n                _props = regionprops_table(_ensure_uint_labels(_warped), properties=('label', 'bbox'))\n                _ddf = pd.DataFrame(_props)\n                if _ddf.empty:\n                    continue\n                _ddf = _ddf[_ddf['label'] != 0].copy()\n                if _ddf.empty:\n                    continue\n                _ddf = _ddf.rename(columns={\n                    'bbox-0': 'zmin', 'bbox-1': 'ymin', 'bbox-2': 'xmin',\n                    'bbox-3': 'zmax', 'bbox-4': 'ymax', 'bbox-5': 'xmax',\n                })\n                _ddf['x_um'] = (_ddf['xmax'] - _ddf['xmin']) * DX\n                _ddf['y_um'] = (_ddf['ymax'] - _ddf['ymin']) * DY\n                _ddf['z_um'] = (_ddf['zmax'] - _ddf['zmin']) * DZ\n                _ddf['xy_um'] = (_ddf['x_um'] + _ddf['y_um']) / 2.0\n                hcr_diam_cache[_mname] = _ddf.set_index('label')[['xy_um', 'z_um']].to_dict('index')\n            except Exception:\n                continue\n\n        pair_rows = []\n        for _, r in selected_pairs.iterrows():\n            mask_name = Path(str(r['conf_mask'])).name\n            res = res_by_mask.get(mask_name)\n            if res is None:\n                continue\n            df_conf = res.get('df_conf')\n            df_2p = res.get('df_2p')\n            if df_conf is None or df_2p is None or df_conf.empty or df_2p.empty:\n                continue\n            conf_map = df_conf.set_index('label')[['x', 'y', 'z']].to_dict('index')\n            anat_map = df_2p.set_index('label')[['x', 'y', 'z']].to_dict('index')\n            cl = int(r['conf_label'])\n            al = int(r['anat_label'])\n            c = conf_map.get(cl)\n            a = anat_map.get(al)\n            if c is None or a is None:\n                continue\n            dx_um = (float(c['x']) - float(a['x'])) * DX\n            dy_um = (float(c['y']) - float(a['y'])) * DY\n            dz_um = (float(c['z']) - float(a['z'])) * DZ\n            _diam = hcr_diam_cache.get(mask_name, {}).get(cl, {})\n            pair_rows.append({\n                'gene': str(r['gene']),\n                'plane': int(r['plane']),\n                'mask': mask_name,\n                'conf_label': cl,\n                'anat_label': al,\n                'dx_um': float(dx_um),\n                'dy_um': float(dy_um),\n                'dz_um': float(dz_um),\n                'xy_um': float(np.sqrt(dx_um * dx_um + dy_um * dy_um)),\n                '|dz_um|': float(abs(dz_um)),\n                'distance_um': float(np.sqrt(dx_um * dx_um + dy_um * dy_um + dz_um * dz_um)),\n                'hcr_diam_xy_um': float(_diam.get('xy_um', np.nan)),\n                'hcr_diam_z_um': float(_diam.get('z_um', np.nan)),\n            })\n\n        if not pair_rows:\n            print('[53] Final analysis pairs could not be reconciled to hcr_match_results centroids.')\n        else:\n            df_final = pd.DataFrame(pair_rows)\n            df_final = df_final\n            _genes_present = sorted(set(df_final['gene'].astype(str)))\n            gene_order_plot = [g for g in GENE_ORDER if g in _genes_present] + [g for g in _genes_present if g not in GENE_ORDER]\n\n            dist_summary = (\n                df_final.groupby('gene')\n                .agg(n=('distance_um', 'size'), median_um=('distance_um', 'median'), sd_um=('distance_um', 'std'))\n                .reindex(gene_order_plot)\n            )\n            print('[53] Table: HCR-to-anatomy 3D distance summary by gene')\n            _display_df(dist_summary.dropna(how='all').reset_index().rename(columns={'index': 'gene'}))\n\n            rows = []\n            for axis_label, col in [('XY', 'xy_um'), ('Z', '|dz_um|')]:\n                for g in gene_order_plot:\n                    vals = df_final.loc[df_final['gene'] == g, col].to_numpy(float)\n                    if vals.size:\n                        rows.append({\n                            'gene': g,\n                            'axis': axis_label,\n                            'n': int(vals.size),\n                            'median_um': float(np.median(vals)),\n                            'sd_um': float(np.std(vals)),\n                            'percentile': float(OFFSET_PCTL),\n                            'percentile_um': float(np.percentile(vals, OFFSET_PCTL)),\n                        })\n            if rows:\n                off_df = pd.DataFrame(rows)\n                hcr_offset_by_gene_df = off_df\n                print('[53] Table: HCR offset summary by gene and axis (XY/Z)')\n                _display_df(off_df)\n\n            diam_gene_rows = []\n            for g in gene_order_plot:\n                _sub = df_final[df_final['gene'] == g]\n                if _sub.empty:\n                    continue\n                _xy = pd.to_numeric(_sub.get('hcr_diam_xy_um', np.nan), errors='coerce').dropna().to_numpy(float)\n                _z = pd.to_numeric(_sub.get('hcr_diam_z_um', np.nan), errors='coerce').dropna().to_numpy(float)\n                if _xy.size == 0 and _z.size == 0:\n                    continue\n                diam_gene_rows.append({\n                    'gene': g,\n                    'n_cells': int(_sub['anat_label'].nunique()) if 'anat_label' in _sub.columns else int(len(_sub)),\n                    'n_pairs': int(len(_sub)),\n                    'xy_diameter_median_um': float(np.median(_xy)) if _xy.size else np.nan,\n                    'xy_diameter_sd_um': float(np.std(_xy)) if _xy.size else np.nan,\n                    'z_diameter_median_um': float(np.median(_z)) if _z.size else np.nan,\n                    'z_diameter_sd_um': float(np.std(_z)) if _z.size else np.nan,\n                })\n            if diam_gene_rows:\n                diam_gene_df = pd.DataFrame(diam_gene_rows)\n                hcr_diam_by_gene_df = diam_gene_df\n                print('[53] Table: HCR matched-label diameter summary by gene (XY/Z)')\n                _display_df(diam_gene_df)\n\n            if {'plane', 'gene', 'anat_label'}.issubset(selected_pairs.columns):\n                plane_df = (\n                    selected_pairs.groupby('plane')\n                    .agg(\n                        anat_labels=('anat_label', 'nunique'),\n                        genes=('gene', lambda x: ', '.join(sorted(set(map(str, x))))),\n                    )\n                    .reset_index()\n                    .sort_values('plane')\n                )\n                plane_gene_summary_df = plane_df\n                print('[53] Table: Genes represented per functional plane (final analysis pairs)')\n                _display_df(plane_df)\n",
    '53a': '# [53a]\ntry:\n    REQUIRE_FISH_STATE = require_fish_state\nexcept NameError:\n    REQUIRE_FISH_STATE = None\nif callable(REQUIRE_FISH_STATE) and not REQUIRE_FISH_STATE():\n    raise SystemExit\n# Composite summary figure from [53]\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nfrom pathlib import Path\n\n# reset cached render buffers for downstream composites\nFIG_53A_LAST = None\nFIG_53A_RGBA = None\nFIG_53A_FISH_ID = None\n\nif not plane_refs:\n    print(\'Need plane_refs; run [53] first.\')\nelse:\n    RC = RUN_CONFIG if isinstance(RUN_CONFIG, dict) else {}\n    palette_local = palette if \'palette\' in locals() else {\n        \'Anatomy\': \'#6b6b6b\',\n        \'Functional\': \'#2e9f4d\',\n        \'HCR\': \'#d000a7\',\n    }\n    offset_pctl = float(RC.get(\'OFFSET_PCTL\', OFFSET_PCTL if \'OFFSET_PCTL\' in locals() else 95.0))\n    offset_pctl = min(100.0, max(0.0, offset_pctl))\n\n    DEFAULT_GENE_ORDER = [\'sst1.1\', \'sst1.2\', \'npy\', \'tac3b\', \'pth2\', \'cfos\', \'cort\']\n    DEFAULT_GENE_COLORS = {\n        \'sst1.1\': \'#d62728\',\n        \'sst1.2\': \'#d61ad2\',\n        \'npy\': \'#1f9d55\',\n        \'tac3b\': \'#ffd400\',\n        \'pth2\': \'#00bcd4\',\n        \'cfos\': \'#ff7f0e\',\n    \'cort\': \'#8c564b\',\n    }\n    GENE_ORDER = list(RC.get(\'GENE_ORDER\', DEFAULT_GENE_ORDER))\n    GENE_COLORS = dict(DEFAULT_GENE_COLORS)\n    try:\n        _gc = RC.get(\'GENE_COLORS\', {})\n        if isinstance(_gc, dict):\n            GENE_COLORS.update(_gc)\n    except Exception:\n        pass\n\n    fig, axes = plt.subplots(2, 2, figsize=(18, 12))\n    ax_ncc, ax_diam = axes[0]\n    ax_off, ax_hcr = axes[1]\n\n    # Median anatomy diameters/radii for reference lines in bottom panels\n    anat_med_diam_xy_um = np.nan\n    anat_med_diam_z_um = np.nan\n    anat_med_rad_xy_um = np.nan\n    anat_med_rad_z_um = np.nan\n    df_diam_ref = df_all_filt if \'df_all_filt\' in locals() else (df_all if \'df_all\' in locals() else None)\n    if df_diam_ref is not None and not getattr(df_diam_ref, \'empty\', True):\n        req_ref = {\'dataset\', \'x_um\', \'y_um\', \'z_um\'}\n        if req_ref.issubset(df_diam_ref.columns):\n            _anat = df_diam_ref[df_diam_ref[\'dataset\'].astype(str) == \'Anatomy\'].copy()\n            if not _anat.empty:\n                _x = pd.to_numeric(_anat[\'x_um\'], errors=\'coerce\').to_numpy(dtype=float)\n                _y = pd.to_numeric(_anat[\'y_um\'], errors=\'coerce\').to_numpy(dtype=float)\n                _z = pd.to_numeric(_anat[\'z_um\'], errors=\'coerce\').to_numpy(dtype=float)\n                _xy = (_x + _y) / 2.0\n                _xy = _xy[np.isfinite(_xy)]\n                _z = _z[np.isfinite(_z)]\n                if _xy.size:\n                    anat_med_diam_xy_um = float(np.median(_xy))\n                    anat_med_rad_xy_um = float(anat_med_diam_xy_um / 2.0)\n                if _z.size:\n                    anat_med_diam_z_um = float(np.median(_z))\n                    anat_med_rad_z_um = float(anat_med_diam_z_um / 2.0)\n    ANAT_MED_DIAM_XY_UM = anat_med_diam_xy_um\n    ANAT_MED_DIAM_Z_UM = anat_med_diam_z_um\n    ANAT_MED_RADIUS_XY_UM = anat_med_rad_xy_um\n    ANAT_MED_RADIUS_Z_UM = anat_med_rad_z_um\n\n    # --- Top-left: Best-Z NCC scores per plane ---\n    n_ncc = 0\n    for p_idx, pr in enumerate(plane_refs):\n        scores = pr.get(\'ncc_scores\', None)\n        if scores is None:\n            continue\n        lbl = pr.get(\'label\', f\'plane{p_idx}\')\n        line, = ax_ncc.plot(np.asarray(scores, dtype=float), linewidth=1.6, label=lbl)\n        bz = pr.get(\'best_z\', None)\n        if bz is not None:\n            ax_ncc.axvline(float(bz), color=line.get_color(), linestyle=\'--\', alpha=0.7, linewidth=1.0)\n        n_ncc += 1\n    if n_ncc == 0:\n        ax_ncc.text(0.5, 0.5, \'No NCC score curves available\', ha=\'center\', va=\'center\', transform=ax_ncc.transAxes)\n    else:\n        ax_ncc.legend(fontsize=8, ncol=1, title=\'Planes\', frameon=False)\n    ax_ncc.set_title(\'Functional planes align to a clear anatomy depth\')\n    ax_ncc.set_xlabel(\'Z\')\n    ax_ncc.set_ylabel(\'NCC score\')\n    ax_ncc.grid(alpha=0.25)\n\n    # --- Top-right: Mask diameters by axis (XY and Z) ---\n    df_diam = df_diam_ref\n    if df_diam is None or getattr(df_diam, \'empty\', True):\n        ax_diam.text(0.5, 0.5, \'No diameter dataframe available\', ha=\'center\', va=\'center\', transform=ax_diam.transAxes)\n        ax_diam.set_title(\'Mask sizes agree across anatomy, function, and HCR\')\n    else:\n        df_diam = df_diam.copy()\n        required = {\'dataset\', \'x_um\', \'y_um\', \'z_um\'}\n        if not required.issubset(df_diam.columns):\n            ax_diam.text(0.5, 0.5, \'Diameter dataframe missing required columns\', ha=\'center\', va=\'center\', transform=ax_diam.transAxes)\n            ax_diam.set_title(\'Mask sizes agree across anatomy, function, and HCR\')\n        else:\n            ds_order = [\'Anatomy\', \'Functional\', \'HCR\']\n            present_ds = [d for d in ds_order if d in set(df_diam[\'dataset\'].astype(str))]\n            if not present_ds:\n                ax_diam.text(0.5, 0.5, \'No recognized datasets for diameter plot\', ha=\'center\', va=\'center\', transform=ax_diam.transAxes)\n                ax_diam.set_title(\'Mask sizes agree across anatomy, function, and HCR\')\n            else:\n                # Per-dataset sample sizes (overall + per axis).\n                ds_sample_sizes = {}\n                for ds in present_ds:\n                    sub_all = df_diam[df_diam[\'dataset\'] == ds]\n                    _x_all = pd.to_numeric(sub_all[\'x_um\'], errors=\'coerce\').to_numpy(dtype=float)\n                    _y_all = pd.to_numeric(sub_all[\'y_um\'], errors=\'coerce\').to_numpy(dtype=float)\n                    _z_all = pd.to_numeric(sub_all[\'z_um\'], errors=\'coerce\').to_numpy(dtype=float)\n                    _xy_all = (_x_all + _y_all) / 2.0\n                    _ok_xy = np.isfinite(_xy_all)\n                    _ok_z = np.isfinite(_z_all)\n                    ds_sample_sizes[str(ds)] = {\n                        \'n_total\': int(np.sum(_ok_xy | _ok_z)),\n                        \'n_xy\': int(np.sum(_ok_xy)),\n                        \'n_z\': int(np.sum(_ok_z)),\n                    }\n\n                axis_specs = [(\'XY\', None), (\'Z\', \'z_um\')]\n                values, positions, colors, meta = [], [], [], []\n                block = len(present_ds) + 1\n\n                for ai, (axis_label, col) in enumerate(axis_specs):\n                    for di, ds in enumerate(present_ds):\n                        sub = df_diam[df_diam[\'dataset\'] == ds]\n                        if axis_label == \'XY\':\n                            _x = pd.to_numeric(sub[\'x_um\'], errors=\'coerce\').to_numpy(dtype=float)\n                            _y = pd.to_numeric(sub[\'y_um\'], errors=\'coerce\').to_numpy(dtype=float)\n                            vals = (_x + _y) / 2.0\n                            vals = vals[np.isfinite(vals)]\n                        else:\n                            vals = pd.to_numeric(sub[col], errors=\'coerce\').dropna().to_numpy(dtype=float)\n                        if vals.size == 0:\n                            continue\n                        pos = ai * block + di + 1\n                        values.append(vals)\n                        positions.append(float(pos))\n                        colors.append(palette_local.get(ds, \'#999999\'))\n                        meta.append({\'axis\': axis_label, \'dataset\': str(ds), \'pos\': float(pos), \'n\': int(vals.size)})\n\n                if not values:\n                    ax_diam.text(0.5, 0.5, \'No finite diameter values\', ha=\'center\', va=\'center\', transform=ax_diam.transAxes)\n                    ax_diam.set_title(\'Mask sizes agree across anatomy, function, and HCR\')\n                else:\n                    vp = ax_diam.violinplot(values, positions=positions, showmeans=False, showmedians=False, showextrema=False, widths=0.8)\n                    for body, c in zip(vp[\'bodies\'], colors):\n                        body.set_facecolor(c)\n                        body.set_edgecolor(\'black\')\n                        body.set_alpha(0.8)\n                    medians = [float(np.median(v)) for v in values]\n                    ax_diam.scatter(positions, medians, color=\'black\', s=18, zorder=3)\n\n                    # Label each violin with its local sample size.\n                    for _vals, _pos in zip(values, positions):\n                        _arr = np.asarray(_vals, dtype=float)\n                        _arr = _arr[np.isfinite(_arr)]\n                        if _arr.size == 0:\n                            continue\n                        _y_ann = float(np.nanpercentile(_arr, 97.0)) if _arr.size > 2 else float(np.nanmax(_arr))\n                        if not np.isfinite(_y_ann):\n                            _y_ann = float(np.nanmax(_arr))\n                        _x_ann = float(_pos) + 0.14\n                        ax_diam.text(\n                            _x_ann, _y_ann, f\'n={int(_arr.size)}\',\n                            ha=\'left\', va=\'bottom\', fontsize=8, zorder=4,\n                            bbox=dict(boxstyle=\'round,pad=0.15\', facecolor=\'white\', edgecolor=\'none\', alpha=0.75),\n                        )\n\n                    centers = [ai * block + (len(present_ds) + 1) / 2.0 for ai in range(len(axis_specs))]\n                    ax_diam.set_xticks(centers)\n                    ax_diam.set_xticklabels([a for a, _ in axis_specs])\n\n                    # q95 XY segmentation-threshold lines (dataset-specific, only over XY violins)\n                    _seg_q95_map = {}\n                    _filt_sum = df_all_filter_summary if \'df_all_filter_summary\' in locals() else None\n                    if _filt_sum is not None and not getattr(_filt_sum, \'empty\', True) and \'dataset\' in _filt_sum.columns:\n                        for ds in present_ds:\n                            _row = _filt_sum[_filt_sum[\'dataset\'].astype(str) == str(ds)]\n                            if _row.empty:\n                                continue\n                            _q95 = pd.to_numeric(_row.get(\'xy_q95_um\', np.nan), errors=\'coerce\').dropna().to_numpy(float)\n                            if _q95.size:\n                                _seg_q95_map[str(ds)] = float(_q95[0])\n\n                    for m in meta:\n                        if m.get(\'axis\') != \'XY\':\n                            continue\n                        _thr = _seg_q95_map.get(str(m.get(\'dataset\')), np.nan)\n                        if np.isfinite(_thr):\n                            p = float(m.get(\'pos\'))\n                            ax_diam.hlines(\n                                _thr,\n                                p - 0.32,\n                                p + 0.32,\n                                color=palette_local.get(str(m.get(\'dataset\')), \'#666666\'),\n                                linestyle=\'--\',\n                                linewidth=1.3,\n                                alpha=0.95,\n                            )\n\n                    # Dataset sample size summary (requested): one line per dataset.\n                    _n_lines = [\'Sample size by dataset:\']\n                    for _ds in present_ds:\n                        _n = ds_sample_sizes.get(str(_ds), {})\n                        _n_lines.append(f"{_ds}: n={int(_n.get(\'n_total\', 0))} (XY {int(_n.get(\'n_xy\', 0))}, Z {int(_n.get(\'n_z\', 0))})")\n                    ax_diam.text(\n                        0.02, 0.98, \'\\n\'.join(_n_lines),\n                        transform=ax_diam.transAxes,\n                        ha=\'left\', va=\'top\', fontsize=8,\n                        bbox=dict(boxstyle=\'round,pad=0.25\', facecolor=\'white\', edgecolor=\'#666666\', alpha=0.85),\n                    )\n\n                    if _seg_q95_map:\n                        _line_txt = [\'Dashed segments over XY: q95 segmentation threshold\']\n                        for _ds in present_ds:\n                            if str(_ds) in _seg_q95_map:\n                                _line_txt.append(f"{_ds}={_seg_q95_map[str(_ds)]:.2f} \xb5m")\n                        ax_diam.text(\n                            0.98, 0.98, \'\\n\'.join(_line_txt),\n                            transform=ax_diam.transAxes,\n                            ha=\'right\', va=\'top\', fontsize=8,\n                            bbox=dict(boxstyle=\'round,pad=0.25\', facecolor=\'white\', edgecolor=\'#666666\', alpha=0.85),\n                        )\n\n                    ax_diam.grid(axis=\'y\', alpha=0.25)\n                    ax_diam.set_ylabel(\'Diameter (\xb5m)\')\n                    ax_diam.set_title(\'Mask sizes agree across anatomy, function, and HCR\')\n\n    # Shared range accumulator for bottom-row Y axis\n    bottom_y_values = []\n    # --- Bottom-left: Functional -> Anatomy centroid offsets (all planes) ---\n    def _as_bool_series_53a(series):\n        if pd.api.types.is_bool_dtype(series):\n            return series.fillna(False).astype(bool)\n        if pd.api.types.is_numeric_dtype(series):\n            return series.fillna(0).astype(float) != 0\n        return series.astype(str).str.strip().str.lower().isin({\'1\', \'true\', \'t\', \'yes\', \'y\'})\n\n    def _load_func_anat_offsets_from_roi_table_53a():\n        detail_df = None\n        if \'FUNC_ACTIVITY_IDENTITY_DF\' in locals() and isinstance(FUNC_ACTIVITY_IDENTITY_DF, pd.DataFrame) and not FUNC_ACTIVITY_IDENTITY_DF.empty:\n            detail_df = FUNC_ACTIVITY_IDENTITY_DF.copy()\n        else:\n            roi_csv = Path(FUNC_ACTIVITY_IDENTITY_CSV) if \'FUNC_ACTIVITY_IDENTITY_CSV\' in locals() else (OUT_REG / \'functional_roi_activity_identity.csv\')\n            if roi_csv.exists():\n                try:\n                    detail_df = pd.read_csv(roi_csv)\n                except Exception:\n                    detail_df = None\n        if detail_df is None or getattr(detail_df, \'empty\', True):\n            return np.array([]), np.array([])\n\n        mask = pd.Series(True, index=detail_df.index)\n        if \'plane_match_outcome\' in detail_df.columns:\n            mask &= detail_df[\'plane_match_outcome\'].astype(str).eq(\'anatomy match\')\n        if \'has_unique_anat_match\' in detail_df.columns:\n            mask &= _as_bool_series_53a(detail_df[\'has_unique_anat_match\'])\n\n        xy_vals = np.array([])\n        if \'selected_dist_um\' in detail_df.columns:\n            xy_vals = pd.to_numeric(detail_df.loc[mask, \'selected_dist_um\'], errors=\'coerce\').dropna().to_numpy(dtype=float)\n            xy_vals = xy_vals[np.isfinite(xy_vals)]\n\n        z_vals = np.array([])\n        if {\'selected_anat_label\', \'best_z\'}.issubset(detail_df.columns):\n            try:\n                anat_path = Path(ANAT_LABELS_PATH) if \'ANAT_LABELS_PATH\' in locals() and ANAT_LABELS_PATH is not None else None\n            except Exception:\n                anat_path = None\n            if anat_path is not None and anat_path.exists() and \'_ensure_uint_labels\' in locals() and \'imread_any\' in locals():\n                try:\n                    from skimage.measure import regionprops_table as _regionprops_table\n                    anat_vol = _ensure_uint_labels(imread_any(anat_path))\n                    props3d = _regionprops_table(anat_vol, properties=(\'label\', \'centroid\'))\n                    adf3d = pd.DataFrame(props3d)\n                    if not adf3d.empty:\n                        z_cent = dict(zip(adf3d[\'label\'].astype(int), pd.to_numeric(adf3d[\'centroid-0\'], errors=\'coerce\')))\n                        dz_um = float(VOX_ANAT.get(\'Z\', 1.0)) if isinstance(VOX_ANAT, dict) else 1.0\n                        matched = detail_df.loc[mask, [\'selected_anat_label\', \'best_z\']].copy()\n                        matched[\'selected_anat_label\'] = pd.to_numeric(matched[\'selected_anat_label\'], errors=\'coerce\')\n                        matched[\'best_z\'] = pd.to_numeric(matched[\'best_z\'], errors=\'coerce\')\n                        matched = matched.dropna(subset=[\'selected_anat_label\', \'best_z\'])\n                        z_list = []\n                        for _, rr in matched.iterrows():\n                            al = int(rr[\'selected_anat_label\'])\n                            zc = z_cent.get(al)\n                            if zc is None or not np.isfinite(zc):\n                                continue\n                            z_list.append(abs(float(zc) - float(rr[\'best_z\'])) * dz_um)\n                        if z_list:\n                            z_vals = np.asarray(z_list, dtype=float)\n                            z_vals = z_vals[np.isfinite(z_vals)]\n                except Exception:\n                    z_vals = np.array([])\n\n        return xy_vals, z_vals\n\n    xy_vals_comp, z_vals_comp = _load_func_anat_offsets_from_roi_table_53a()\n    if xy_vals_comp.size == 0 and z_vals_comp.size == 0 and (\'_gather_xy_z_offsets\' in locals()):\n        try:\n            xy_vals_comp, z_vals_comp, _, _, _ = _gather_xy_z_offsets()\n        except Exception:\n            xy_vals_comp, z_vals_comp = np.array([]), np.array([])\n    xy_vals_comp = np.asarray(xy_vals_comp, dtype=float) if xy_vals_comp is not None else np.array([])\n    z_vals_comp = np.asarray(z_vals_comp, dtype=float) if z_vals_comp is not None else np.array([])\n\n    if xy_vals_comp.size == 0 and z_vals_comp.size == 0:\n        ax_off.text(0.5, 0.5, \'No functional->anatomy offsets available\', ha=\'center\', va=\'center\', transform=ax_off.transAxes)\n        ax_off.set_title(\'Functional ROIs stay close to their anatomy matches\')\n    else:\n        vals = []\n        labels = []\n        cset = []\n        anat_refs = []\n        if xy_vals_comp.size:\n            vals.append(np.abs(xy_vals_comp))\n            labels.append(\'XY\')\n            cset.append(\'#4c78a8\')\n            anat_refs.append(float(anat_med_rad_xy_um))\n        if z_vals_comp.size:\n            vals.append(np.abs(z_vals_comp))\n            labels.append(\'Z\')\n            cset.append(\'#f58518\')\n            anat_refs.append(float(anat_med_rad_z_um))\n\n        vp = ax_off.violinplot(vals, positions=np.arange(1, len(vals) + 1), showmeans=False, showmedians=False, showextrema=False)\n        bottom_y_values.extend([np.asarray(v, dtype=float) for v in vals if np.size(v)])\n        for body, c in zip(vp[\'bodies\'], cset):\n            body.set_facecolor(c)\n            body.set_edgecolor(\'black\')\n            body.set_alpha(0.75)\n        medians = [float(np.median(v)) for v in vals]\n        ax_off.scatter(np.arange(1, len(vals) + 1), medians, color=\'black\', s=24, zorder=3)\n\n        low_conf_counts_func = {}\n        for i, (v, ref_rad, axis_label) in enumerate(zip(vals, anat_refs, labels), start=1):\n            _v = np.asarray(v, dtype=float)\n            _v = _v[np.isfinite(_v)]\n            n_total = int(_v.size)\n            if np.isfinite(ref_rad) and n_total > 0:\n                n_low = int(np.sum(_v > ref_rad))\n                low_conf_counts_func[axis_label] = {\n                    \'n_low\': n_low,\n                    \'n_total\': n_total,\n                    \'threshold_um\': float(ref_rad),\n                }\n                ax_off.hlines(ref_rad, i - 0.28, i + 0.28, colors=\'#c1121f\', linestyles=\'--\', linewidth=1.4)\n                ax_off.text(i + 0.32, ref_rad, f\'Anat R50={ref_rad:.2f}\', fontsize=8, va=\'center\', color=\'#c1121f\')\n            else:\n                low_conf_counts_func[axis_label] = {\n                    \'n_low\': np.nan,\n                    \'n_total\': n_total,\n                    \'threshold_um\': float(ref_rad) if np.isfinite(ref_rad) else np.nan,\n                }\n\n        func_anat_low_conf_counts_globalR50 = low_conf_counts_func\n\n        n_xy = int(xy_vals_comp.size)\n        n_z = int(z_vals_comp.size)\n        n_matches = n_xy if n_xy > 0 else n_z\n        _lc_parts = []\n        for _axis in labels:\n            _c = low_conf_counts_func.get(_axis, {})\n            _n_low = _c.get(\'n_low\', np.nan)\n            _n_tot = _c.get(\'n_total\', 0)\n            if np.isfinite(_n_low):\n                _lc_parts.append(f"{_axis} {_n_low:.0f}/{_n_tot}")\n        _lc_txt = \'; \'.join(_lc_parts) if _lc_parts else \'n/a\'\n        ax_off.set_title(\'Functional ROIs stay close to their anatomy matches\')\n        ax_off.set_xticks(np.arange(1, len(vals) + 1))\n        ax_off.set_xticklabels(labels)\n        ax_off.set_ylabel(\'Offset (\xb5m)\')\n        ax_off.grid(axis=\'y\', alpha=0.25)\n        if xy_vals_comp.size:\n            print(f"[53a] Bottom-left (Functional\u2192Anatomy) median XY offset: {float(np.median(np.abs(xy_vals_comp))):.3f} \xb5m (n={int(xy_vals_comp.size)})")\n\n    # --- Bottom-right: In-plane HCR -> Anatomy centroid offsets (status-driven, label-level) ---\n    df_final_comp = pd.DataFrame()\n    status_path_53a = OUT_REG / \'hcr_activity_status.csv\'\n    if status_path_53a.exists():\n        try:\n            status_df_53a = pd.read_csv(status_path_53a)\n        except Exception:\n            status_df_53a = pd.DataFrame()\n        if not status_df_53a.empty:\n            if \'fish_id\' in status_df_53a.columns:\n                status_df_53a = status_df_53a[status_df_53a[\'fish_id\'].astype(str) == str(FISH_ID)].copy()\n            if \'gene\' not in status_df_53a.columns:\n                status_df_53a[\'gene\'] = \'unknown\'\n            status_df_53a[\'gene\'] = status_df_53a[\'gene\'].astype(str).str.strip()\n            status_df_53a[\'anat_label\'] = pd.to_numeric(status_df_53a.get(\'anat_label\', np.nan), errors=\'coerce\').astype(\'Int64\')\n            rep = _as_bool_series_53a(status_df_53a.get(\'represented_on_func_plane\', pd.Series(False, index=status_df_53a.index)))\n            fs = status_df_53a.get(\'functional_status\', pd.Series(\'\', index=status_df_53a.index)).astype(str).str.strip()\n            allowed_status = {\n                \'in-plane responsive ROI\',\n                \'in-plane low-activity ROI\',\n                \'in-plane response unavailable\',\n                \'in-plane no functional ROI candidate\',\n            }\n            status_df_53a = status_df_53a[rep & fs.isin(allowed_status)].copy()\n            status_df_53a = status_df_53a.dropna(subset=[\'gene\', \'anat_label\']).copy()\n            if not status_df_53a.empty:\n                status_df_53a[\'dist_conf_sort\'] = pd.to_numeric(status_df_53a.get(\'dist_conf_anat_um\', status_df_53a.get(\'dist_conf\', np.nan)), errors=\'coerce\')\n                status_df_53a[\'dist_func_sort\'] = pd.to_numeric(status_df_53a.get(\'selected_dist_um\', np.nan), errors=\'coerce\')\n                status_df_53a[\'overlap_sort\'] = pd.to_numeric(status_df_53a.get(\'selected_overlap_px\', status_df_53a.get(\'overlap_px_func_anat\', np.nan)), errors=\'coerce\')\n                status_df_53a[\'plane_sort\'] = pd.to_numeric(status_df_53a.get(\'selected_plane\', np.nan), errors=\'coerce\')\n                status_df_53a[\'func_sort\'] = pd.to_numeric(status_df_53a.get(\'selected_func_label\', np.nan), errors=\'coerce\')\n                status_df_53a = status_df_53a.sort_values(\n                    [\'gene\', \'anat_label\', \'dist_conf_sort\', \'dist_func_sort\', \'overlap_sort\', \'plane_sort\', \'func_sort\'],\n                    ascending=[True, True, True, True, False, True, True],\n                    na_position=\'last\',\n                )\n                status_df_53a = status_df_53a.drop_duplicates(subset=[\'gene\', \'anat_label\'], keep=\'first\').reset_index(drop=True)\n\n                anat_path_53a = Path(ANAT_LABELS_PATH) if \'ANAT_LABELS_PATH\' in locals() and ANAT_LABELS_PATH is not None else None\n                if anat_path_53a is not None and anat_path_53a.exists() and \'_ensure_uint_labels\' in locals() and \'imread_any\' in locals():\n                    try:\n                        anat_vol_53a = _ensure_uint_labels(imread_any(anat_path_53a))\n                        anat_cent_53a = compute_centroids(anat_vol_53a)\n                        anat_cent_53a[\'label\'] = pd.to_numeric(anat_cent_53a[\'label\'], errors=\'coerce\').astype(\'Int64\')\n                        anat_cent_53a = anat_cent_53a.dropna(subset=[\'label\'])\n                        anat_lookup_53a = {\n                            int(rr.label): (float(rr.x), float(rr.y), float(rr.z))\n                            for rr in anat_cent_53a.itertuples(index=False)\n                        }\n                        DX = float(VOX_ANAT.get(\'X\', 1.0)) if isinstance(VOX_ANAT, dict) else 1.0\n                        DY = float(VOX_ANAT.get(\'Y\', 1.0)) if isinstance(VOX_ANAT, dict) else 1.0\n                        DZ = float(VOX_ANAT.get(\'Z\', 1.0)) if isinstance(VOX_ANAT, dict) else 1.0\n                        rows_53a = []\n                        for rr in status_df_53a.itertuples(index=False):\n                            al = int(rr.anat_label)\n                            anat_xyz = anat_lookup_53a.get(al)\n                            if anat_xyz is None:\n                                continue\n\n                            xy_from_table = np.nan\n                            for col in (\'dist_conf_anat_um\', \'dist_conf\', \'dist_um\', \'distance_um\'):\n                                rv = getattr(rr, col, np.nan)\n                                vv = pd.to_numeric(pd.Series([rv]), errors=\'coerce\').iloc[0]\n                                if pd.notna(vv) and np.isfinite(float(vv)):\n                                    xy_from_table = float(abs(vv))\n                                    break\n\n                            abs_dz_from_table = np.nan\n                            for col in (\'abs_dz_um\', \'dz_um\'):\n                                rv = getattr(rr, col, np.nan)\n                                vv = pd.to_numeric(pd.Series([rv]), errors=\'coerce\').iloc[0]\n                                if pd.notna(vv) and np.isfinite(float(vv)):\n                                    abs_dz_from_table = float(abs(vv))\n                                    break\n\n                            dx_um = np.nan\n                            dy_um = np.nan\n                            dz_um = np.nan\n                            xy_from_cent = np.nan\n                            abs_dz_from_cent = np.nan\n                            dist_from_cent = np.nan\n                            conf_mask_val = getattr(rr, \'conf_mask\', None)\n                            conf_mask_path = None\n                            if conf_mask_val is not None and not (isinstance(conf_mask_val, float) and np.isnan(conf_mask_val)):\n                                cand = Path(str(conf_mask_val))\n                                if cand.exists():\n                                    conf_mask_path = cand\n                                else:\n                                    joined = OUTDIR / cand\n                                    if joined.exists():\n                                        conf_mask_path = joined\n                            conf_label_val = getattr(rr, \'conf_label\', getattr(rr, \'primary_conf_label\', np.nan))\n                            conf_label_num = pd.to_numeric(pd.Series([conf_label_val]), errors=\'coerce\').iloc[0]\n                            if conf_mask_path is not None and not pd.isna(conf_label_num):\n                                try:\n                                    conf_labels_53a = _ensure_uint_labels(imread_any(conf_mask_path))\n                                    if conf_labels_53a.shape == anat_vol_53a.shape:\n                                        conf_cent_53a = compute_centroids(conf_labels_53a)\n                                        if not conf_cent_53a.empty:\n                                            conf_cent_53a[\'label\'] = pd.to_numeric(conf_cent_53a[\'label\'], errors=\'coerce\').astype(\'Int64\')\n                                            conf_hit = conf_cent_53a[conf_cent_53a[\'label\'] == int(conf_label_num)]\n                                            if not conf_hit.empty:\n                                                conf_xyz = (\n                                                    float(conf_hit.iloc[0][\'x\']),\n                                                    float(conf_hit.iloc[0][\'y\']),\n                                                    float(conf_hit.iloc[0][\'z\']),\n                                                )\n                                                dx_um = (conf_xyz[0] - anat_xyz[0]) * DX\n                                                dy_um = (conf_xyz[1] - anat_xyz[1]) * DY\n                                                dz_um = (conf_xyz[2] - anat_xyz[2]) * DZ\n                                                xy_from_cent = float(np.hypot(dx_um, dy_um))\n                                                abs_dz_from_cent = float(abs(dz_um))\n                                                dist_from_cent = float(np.sqrt(dx_um * dx_um + dy_um * dy_um + dz_um * dz_um))\n                                except Exception:\n                                    pass\n\n                            xy_um = float(xy_from_table) if np.isfinite(xy_from_table) else float(xy_from_cent)\n                            abs_dz_um = float(abs_dz_from_table) if np.isfinite(abs_dz_from_table) else float(abs_dz_from_cent)\n                            distance_um = float(\n                                np.sqrt(xy_um * xy_um + abs_dz_um * abs_dz_um)\n                                if np.isfinite(xy_um) and np.isfinite(abs_dz_um)\n                                else (xy_um if np.isfinite(xy_um) else dist_from_cent)\n                            )\n                            if not np.isfinite(xy_um) and not np.isfinite(abs_dz_um) and not np.isfinite(distance_um):\n                                continue\n                            rows_53a.append({\n                                \'gene\': str(getattr(rr, \'gene\', \'unknown\')),\n                                \'anat_label\': int(al),\n                                \'dx_um\': float(dx_um),\n                                \'dy_um\': float(dy_um),\n                                \'dz_um\': float(dz_um),\n                                \'xy_um\': float(xy_um),\n                                \'|dz_um|\': float(abs_dz_um),\n                                \'distance_um\': float(distance_um),\n                            })\n                        if rows_53a:\n                            df_final_comp = pd.DataFrame(rows_53a)\n                    except Exception:\n                        df_final_comp = pd.DataFrame()\n\n    if df_final_comp is not None and not getattr(df_final_comp, \'empty\', True):\n        if \'xy_um\' not in df_final_comp.columns and {\'dx_um\', \'dy_um\'}.issubset(df_final_comp.columns):\n            dxv = pd.to_numeric(df_final_comp[\'dx_um\'], errors=\'coerce\').to_numpy(dtype=float)\n            dyv = pd.to_numeric(df_final_comp[\'dy_um\'], errors=\'coerce\').to_numpy(dtype=float)\n            df_final_comp[\'xy_um\'] = np.sqrt(dxv * dxv + dyv * dyv)\n        if \'|dz_um|\' not in df_final_comp.columns and \'dz_um\' in df_final_comp.columns:\n            df_final_comp[\'|dz_um|\'] = np.abs(pd.to_numeric(df_final_comp[\'dz_um\'], errors=\'coerce\').to_numpy(dtype=float))\n\n        _xy_vec = pd.to_numeric(df_final_comp.get(\'xy_um\', np.nan), errors=\'coerce\').to_numpy(dtype=float)\n        _z_vec = pd.to_numeric(df_final_comp.get(\'|dz_um|\', np.nan), errors=\'coerce\').to_numpy(dtype=float)\n        if np.isfinite(anat_med_rad_xy_um):\n            df_final_comp[\'is_low_conf_match_xy_globalR50\'] = _xy_vec > float(anat_med_rad_xy_um)\n        else:\n            df_final_comp[\'is_low_conf_match_xy_globalR50\'] = False\n        if np.isfinite(anat_med_rad_z_um):\n            df_final_comp[\'is_low_conf_match_z_globalR50\'] = _z_vec > float(anat_med_rad_z_um)\n        else:\n            df_final_comp[\'is_low_conf_match_z_globalR50\'] = False\n        df_final_comp = df_final_comp\n\n    if df_final_comp is None or getattr(df_final_comp, \'empty\', True):\n        ax_hcr.text(0.5, 0.5, \'No in-plane HCR\u2192anatomy label offsets available\', ha=\'center\', va=\'center\', transform=ax_hcr.transAxes)\n        ax_hcr.set_title(\'In-plane HCR labels stay close to anatomy matches\')\n    else:\n        if \'gene\' not in df_final_comp.columns:\n            df_final_comp[\'gene\'] = \'unknown\'\n        df_final_comp[\'gene\'] = df_final_comp[\'gene\'].astype(str)\n\n        _genes_present = sorted(set(df_final_comp[\'gene\']))\n        gene_order_plot = [g for g in GENE_ORDER if g in _genes_present] + [g for g in _genes_present if g not in GENE_ORDER]\n        axis_specs = [(\'XY\', \'xy_um\', float(anat_med_rad_xy_um))]\n\n        plot_values = []\n        plot_positions = []\n        plot_colors = []\n        plot_genes = []\n        axis_vals = {\'XY\': [], \'Z\': []}\n        genes_used = []\n\n        n_genes = len(gene_order_plot)\n        if n_genes <= 1:\n            offsets = np.array([0.0])\n        else:\n            offsets = np.linspace(-0.32, 0.32, n_genes)\n\n        for ai, (axis_label, col, _ref_rad) in enumerate(axis_specs, start=1):\n            for gi, g in enumerate(gene_order_plot):\n                sub = df_final_comp[df_final_comp[\'gene\'] == g]\n                if sub.empty or col not in sub.columns:\n                    continue\n                vals = pd.to_numeric(sub[col], errors=\'coerce\').dropna().to_numpy(dtype=float)\n                vals = np.abs(vals)\n                if vals.size == 0:\n                    continue\n                pos = ai + offsets[min(gi, len(offsets) - 1)]\n                plot_values.append(vals)\n                plot_positions.append(float(pos))\n                plot_colors.append(GENE_COLORS.get(g, \'#666666\'))\n                plot_genes.append(g)\n                axis_vals[axis_label].append(vals)\n                if g not in genes_used:\n                    genes_used.append(g)\n\n        if not plot_values:\n            ax_hcr.text(0.5, 0.5, \'No finite in-plane HCR\u2192anatomy XY offsets\', ha=\'center\', va=\'center\', transform=ax_hcr.transAxes)\n            ax_hcr.set_title(\'In-plane HCR labels stay close to anatomy matches\')\n        else:\n            bp = ax_hcr.boxplot(\n                plot_values,\n                positions=plot_positions,\n                widths=0.14,\n                patch_artist=True,\n                showfliers=False,\n                medianprops={\'color\': \'black\', \'linewidth\': 1.2},\n                whiskerprops={\'color\': \'#333333\', \'linewidth\': 1.0},\n                capprops={\'color\': \'#333333\', \'linewidth\': 1.0},\n                boxprops={\'linewidth\': 1.0},\n            )\n            for patch, c in zip(bp[\'boxes\'], plot_colors):\n                patch.set_facecolor(c)\n                patch.set_edgecolor(\'black\')\n                patch.set_alpha(0.8)\n\n            rng = np.random.default_rng(42)\n            for pos, vals, c in zip(plot_positions, plot_values, plot_colors):\n                jitter = rng.uniform(-0.028, 0.028, size=len(vals))\n                ax_hcr.scatter(np.full(len(vals), pos) + jitter, vals, s=8, color=\'black\', alpha=1.0, linewidths=0, zorder=2)\n\n            low_conf_counts_hcr = {}\n            for axis_center, (axis_label, _col, ref_rad) in zip([1.0], axis_specs):\n                vals_axis = np.concatenate(axis_vals[axis_label]) if axis_vals[axis_label] else np.array([])\n                vals_axis = vals_axis[np.isfinite(vals_axis)] if vals_axis.size else vals_axis\n                n_total = int(vals_axis.size)\n                if np.isfinite(ref_rad) and n_total > 0:\n                    n_low = int(np.sum(vals_axis > ref_rad))\n                    low_conf_counts_hcr[axis_label] = {\n                        \'n_low\': n_low,\n                        \'n_total\': n_total,\n                        \'threshold_um\': float(ref_rad),\n                    }\n                    ax_hcr.hlines(ref_rad, axis_center - 0.45, axis_center + 0.45, colors=\'#c1121f\', linestyles=\'--\', linewidth=1.3)\n                    ax_hcr.text(axis_center + 0.48, ref_rad, f\'Anat R50={ref_rad:.2f}\', fontsize=8, va=\'center\', color=\'#c1121f\')\n                else:\n                    low_conf_counts_hcr[axis_label] = {\n                        \'n_low\': np.nan,\n                        \'n_total\': n_total,\n                        \'threshold_um\': float(ref_rad) if np.isfinite(ref_rad) else np.nan,\n                    }\n\n            hcr_low_conf_match_counts_globalR50 = low_conf_counts_hcr\n\n            bottom_y_values.extend([np.asarray(v, dtype=float) for v in plot_values if np.size(v)])\n\n            ax_hcr.set_xlim(0.4, 1.6)\n            ax_hcr.set_xticks([1])\n            ax_hcr.set_xticklabels([\'XY\'])\n            ax_hcr.set_ylabel(\'Offset (\xb5m)\')\n            n_pairs = int(len(df_final_comp))\n            _hcr_lc_parts = []\n            for _axis in [\'XY\']:\n                _c = low_conf_counts_hcr.get(_axis, {})\n                _n_low = _c.get(\'n_low\', np.nan)\n                _n_tot = _c.get(\'n_total\', 0)\n                if np.isfinite(_n_low):\n                    _hcr_lc_parts.append(f"{_axis} {_n_low:.0f}/{_n_tot}")\n            _hcr_lc_txt = \'; \'.join(_hcr_lc_parts) if _hcr_lc_parts else \'n/a\'\n            ax_hcr.set_title(\'In-plane HCR labels stay close to anatomy matches\')\n            ax_hcr.grid(axis=\'y\', alpha=0.25)\n            xy_group_vals = np.concatenate(axis_vals[\'XY\']) if axis_vals[\'XY\'] else np.array([])\n            xy_group_vals = xy_group_vals[np.isfinite(xy_group_vals)] if xy_group_vals.size else xy_group_vals\n            if xy_group_vals.size:\n                print(f"[53a] Bottom-right (HCR\u2192Anatomy) group median XY offset: {float(np.median(xy_group_vals)):.3f} \xb5m (n={int(xy_group_vals.size)})")\n\n            n_cells_gene = (\n                df_final_comp.groupby(\'gene\')[\'anat_label\'].nunique().astype(int)\n                if \'anat_label\' in df_final_comp.columns\n                else df_final_comp.groupby(\'gene\').size().astype(int)\n            )\n            gene_handles = [\n                plt.Line2D(\n                    [0], [0], color=\'none\', marker=\'s\',\n                    markerfacecolor=GENE_COLORS.get(g, \'#666666\'), markeredgecolor=\'black\',\n                    markersize=8, label=f"{g} (n={int(n_cells_gene.get(g, 0))})"\n                )\n                for g in genes_used\n            ]\n            ref_handles = [\n                plt.Line2D([0], [0], color=\'#c1121f\', linestyle=\'--\', linewidth=1.3, label=\'Anat R50 (match-confidence threshold)\'),\n            ]\n            ax_hcr.legend(handles=(gene_handles + ref_handles), loc=\'upper right\', frameon=False, fontsize=8, ncol=1)\n\n    # Match Y-axis range across bottom-left and bottom-right\n    _bottom_all = [np.asarray(v, dtype=float) for v in bottom_y_values if np.size(v)]\n    if _bottom_all:\n        _finite = np.concatenate([v[np.isfinite(v)] for v in _bottom_all if np.isfinite(v).any()]) if any(np.isfinite(v).any() for v in _bottom_all) else np.array([])\n        if _finite.size:\n            _ymax = float(np.max(_finite))\n            if np.isfinite(_ymax) and _ymax > 0:\n                y_shared_max = max(1.0, _ymax * 1.08)\n                ax_off.set_ylim(0.0, y_shared_max)\n                ax_hcr.set_ylim(0.0, y_shared_max)\n\n    fig.suptitle(\'Registration and segmentation quality support cross-modal matching\', fontsize=14)\n    plt.tight_layout(rect=[0, 0, 1, 0.97])\n    FIG_53A_LAST = fig\n    FIG_53A_FISH_ID = FISH_ID\n    try:\n        fig.canvas.draw()\n        FIG_53A_RGBA = np.asarray(fig.canvas.buffer_rgba()).copy()\n    except Exception:\n        FIG_53A_RGBA = None\n    plt.show()\n',
    '56': '# [56]\ntry:\n    REQUIRE_FISH_STATE = require_fish_state\nexcept NameError:\n    REQUIRE_FISH_STATE = None\nif callable(REQUIRE_FISH_STATE) and not REQUIRE_FISH_STATE():\n    raise SystemExit\n\n\n# Deterministic current-fish inputs only: explicit upstream state or fish-scoped files on disk.\n# Average z-scored activity per stimulus and gene (Suite2p)\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nimport re\nimport math\nimport json\nfrom pathlib import Path\nfrom codeants_2pf_hcr import prepare_pairs_for_unique_cells\nfrom codeants_2pf_hcr.stimulus import build_prestim_baseline_windows, compute_zscore_stats, effective_motion_window\n\n\ntry:\n    SUITE2P_BY_REF_IDX = suite2p_by_ref_idx\n    SUITE2P_STATE_FISH_ID = SUITE2P_FISH_ID\nexcept NameError:\n    print(\'[stim] Suite2p data not loaded; run [23a] first.\')\n    raise SystemExit\nif SUITE2P_STATE_FISH_ID != FISH_ID:\n    print(\'[stim] Suite2p data from different fish; run [23a].\')\n    raise SystemExit\n\ntry:\n    df_stim\n    df_evt\n    FRAME_RATE\n    DF_STIM_STATE_FISH_ID = DF_STIM_FISH_ID\nexcept NameError:\n    print(\'[stim] Stimulus data not loaded; run [55] first.\')\n    raise SystemExit\nif DF_STIM_STATE_FISH_ID != FISH_ID:\n    print(\'[stim] Stimulus data from different fish; run [55].\')\n    raise SystemExit\n\ntry:\n    GENE_FROM_MASK_FUNC = gene_from_mask\nexcept NameError:\n    GENE_FROM_MASK_FUNC = None\n\ntry:\n    MATCH_POLICY_VERSION = str(HCR_ACTIVITY_MATCH_POLICY)\nexcept NameError:\n    MATCH_POLICY_VERSION = \'hcr_anat_first_local_geometry_response_v4_suite2p_gate\'\n\ntry:\n    HCR_MATCH_RESULTS_LOCAL = hcr_match_results\nexcept NameError:\n    HCR_MATCH_RESULTS_LOCAL = []\ntry:\n    VOX_ANAT_LOCAL = VOX_ANAT if isinstance(VOX_ANAT, dict) else {}\nexcept NameError:\n    VOX_ANAT_LOCAL = {}\n\nHCR_ALIGNED_DIR_LOCAL = Path(FISH_DIR) / \'03_analysis\' / \'confocal\' / \'aligned\'\n\n\ndef _load_diameter_cache_local():\n    candidates = []\n    try:\n        candidates.append(OUT_QA / \'diameters_df_all.pkl\')\n    except Exception:\n        pass\n    try:\n        candidates.append(OUTDIR / \'diameters_df_all.pkl\')\n    except Exception:\n        pass\n    seen = set()\n    for path in candidates:\n        path = Path(path)\n        if str(path) in seen:\n            continue\n        seen.add(str(path))\n        if not path.exists():\n            continue\n        try:\n            return pd.read_pickle(path)\n        except Exception:\n            continue\n    return None\n\n\nDF_ALL_LOCAL = _load_diameter_cache_local()\n\n# reset cached render buffers\nFIG_56_LAST = None\nFIG_56_RGBA = None\nFIG_56_FISH_ID = None\n\nWINDOW_PRE_SEC = 20\nWINDOW_POST_SEC = 50\n# Delay between logged stim start and actual stimulus onset (hardware pause not in experiment log)\nRC = RUN_CONFIG if isinstance(RUN_CONFIG, dict) else {}\nSTIM_ONSET_DELAY_SEC = float(RC.get(\'STIM_ONSET_DELAY_SEC\', 10.0))\n# Window edges: \'strict\' drops out-of-bounds trials, \'pad_nan\' keeps them and pads missing samples with NaN.\nWINDOW_EDGE_POLICY = \'pad_nan\'\nMIN_VALID_POINTS_PER_SEG = 1\nZSCORE_MIN_BASELINE_POINTS = 200\nZSCORE_MIN_BASELINE_STD = 1e-6\nTARGET_STIM_TYPES = RC.get(\'TARGET_STIM_TYPES\', None)  # list or None\nMIN_SEGMENTS = 3\n# Minimum number of unique cells per gene/stimulus to show in the plot legend/table.\nMIN_CELLS = 1\n# Fail fast if mapping integrity guarantees are not met.\nSTRICT_PAIR_INTEGRITY = bool(RC.get(\'STRICT_PAIR_INTEGRITY\', True))\nALLOW_REUSED_ROIS_WITHIN_GENE = True\nUSE_SEM = True\nMAX_GENES = 12\nHIGH_CONF_ONLY = bool(RC.get(\'HIGH_CONF_ONLY\', False))\nPAIR_SCOPE_LABEL = \'high-confidence pairs\' if HIGH_CONF_ONLY else \'all matched pairs\'\nprint(f"[stim] HIGH_CONF_ONLY={HIGH_CONF_ONLY} ({PAIR_SCOPE_LABEL})")\nDEFAULT_GENE_ORDER = [\'sst1.1\', \'sst1.2\', \'npy\', \'tac3b\', \'pth2\', \'cfos\', \'cort\']\nDEFAULT_GENE_COLORS = {\n    \'sst1.1\': \'#d62728\',\n    \'sst1.2\': \'#d61ad2\',\n    \'npy\': \'#1f9d55\',\n    \'tac3b\': \'#ffd400\',\n    \'pth2\': \'#00bcd4\',\n    \'cfos\': \'#ff7f0e\',\n    \'cort\': \'#8c564b\',\n}\nGENE_ORDER = list(RC.get(\'GENE_ORDER\', DEFAULT_GENE_ORDER))\nGENE_COLORS = dict(DEFAULT_GENE_COLORS)\ntry:\n    _gc = RC.get(\'GENE_COLORS\', {})\n    if isinstance(_gc, dict):\n        GENE_COLORS.update(_gc)\nexcept Exception:\n    pass\nSTIM_ORDER = ["LLB", "LLC",\n              "RLB", "RLC",\n              "LLB+RLC", "LLC+RLB",\n              "LLB+RLB", "LLC+RLC"]\nPLOT_NCOLS = 2\nFIG_WIDTH = 12.0\nFIG_ROW_HEIGHT = 3.5\ndefault_conf = Path(OUT_REG / \'conf_to_func_pairs.csv\')\nconf_override = RC.get(\'CONF_FUNC_CSV_ANALYSIS\', None)\nif conf_override is not None:\n    if str(FISH_ID) in str(conf_override) or Path(str(conf_override)).name.startswith(\'conf_to_func_pairs\'):\n        CONF_FUNC_CSV = Path(str(conf_override))\n    else:\n        print(\'[stim] CONF_FUNC_CSV override does not match current FISH_ID; using default\')\n        CONF_FUNC_CSV = default_conf\nelse:\n    CONF_FUNC_CSV = default_conf\n\nif WINDOW_EDGE_POLICY not in {\'strict\', \'pad_nan\'}:\n    print(f"[stim] invalid WINDOW_EDGE_POLICY=\'{WINDOW_EDGE_POLICY}\', using \'pad_nan\'")\n    WINDOW_EDGE_POLICY = \'pad_nan\'\n\n\ndef _extract_window(trace, idx0, idx1, mode=\'strict\', min_valid_points=1):\n    T = int(trace.shape[0])\n    win_len = int(idx1 - idx0)\n    if win_len <= 0:\n        return None\n    if mode == \'strict\':\n        if idx0 < 0 or idx1 > T:\n            return None\n        return trace[idx0:idx1]\n\n    seg = np.full(win_len, np.nan, dtype=np.float32)\n    src0 = max(int(idx0), 0)\n    src1 = min(int(idx1), T)\n    if src1 <= src0:\n        return None\n    dst0 = src0 - int(idx0)\n    seg[dst0:dst0 + (src1 - src0)] = trace[src0:src1]\n    if (src1 - src0) < max(1, int(min_valid_points)):\n        return None\n    return seg\n\n\n_build_prestim_baseline_windows = build_prestim_baseline_windows\n_compute_zscore_stats = compute_zscore_stats\n_effective_motion_window = effective_motion_window\n_prepare_pairs_for_unique_cells = prepare_pairs_for_unique_cells\n\n\ndef _filter_pairs_high_conf_generic(pairs_in, tag=\'[stim]\'):\n    pairs = pairs_in.copy()\n    if pairs.empty:\n        return pairs, {}\n\n    def _mask_key_candidates(mask_path):\n        p = Path(str(mask_path))\n        keys = {str(p), p.name, p.stem}\n        try:\n            keys.add(str(p.resolve()))\n        except Exception:\n            pass\n        return keys\n\n    def _add_lookup_entry(lookup, mask_path, low_conf_labels):\n        if low_conf_labels is None:\n            return\n        vals = []\n        for v in list(low_conf_labels):\n            try:\n                vals.append(int(v))\n            except Exception:\n                continue\n        if not vals:\n            return\n        vset = set(vals)\n        for key in _mask_key_candidates(mask_path):\n            lookup.setdefault(key, set()).update(vset)\n\n    def _collect_q95_lookup(mask_values):\n        lookup = {}\n\n        for res in []:\n            mask_path = res.get(\'mask_path\')\n            if mask_path is None:\n                continue\n            low_conf = res.get(\'low_conf_labels_native\', None)\n            if low_conf is None:\n                fs = res.get(\'filter_stats\', {}) if isinstance(res.get(\'filter_stats\', {}), dict) else {}\n                low_conf = fs.get(\'low_conf_labels\', [])\n            _add_lookup_entry(lookup, mask_path, low_conf)\n\n        aligned_dir = HCR_ALIGNED_DIR_LOCAL\n        if aligned_dir is not None:\n            aligned_dir = Path(aligned_dir)\n            for m in mask_values:\n                meta_path = aligned_dir / f"{Path(str(m)).stem}_in_2p_warp_meta.json"\n                if not meta_path.exists():\n                    continue\n                try:\n                    meta = json.loads(meta_path.read_text())\n                except Exception:\n                    continue\n                fs = meta.get(\'filter_stats\', {}) if isinstance(meta, dict) else {}\n                low_conf = fs.get(\'low_conf_labels\', []) if isinstance(fs, dict) else []\n                _add_lookup_entry(lookup, m, low_conf)\n        return lookup\n\n    def _low_conf_set_for_mask(mask_path, lookup):\n        for key in _mask_key_candidates(mask_path):\n            if key in lookup:\n                return lookup[key]\n        return set()\n\n    def _global_anat_r50_um():\n        try:\n            rx = float(ANAT_MED_RADIUS_XY_UM)\n        except Exception:\n            rx = np.nan\n        try:\n            rz = float(ANAT_MED_RADIUS_Z_UM)\n        except Exception:\n            rz = np.nan\n        try:\n            rx = float(rx)\n        except Exception:\n            rx = np.nan\n        try:\n            rz = float(rz)\n        except Exception:\n            rz = np.nan\n\n        if np.isfinite(rx) and np.isfinite(rz):\n            return rx, rz\n\n        df = DF_ALL_LOCAL\n        if df is None or getattr(df, \'empty\', True):\n            return rx, rz\n        req = {\'dataset\', \'x_um\', \'y_um\', \'z_um\'}\n        if not req.issubset(df.columns):\n            return rx, rz\n        sub = df[df[\'dataset\'].astype(str) == \'Anatomy\'].copy()\n        if sub.empty:\n            return rx, rz\n\n        x = pd.to_numeric(sub[\'x_um\'], errors=\'coerce\').to_numpy(dtype=float)\n        y = pd.to_numeric(sub[\'y_um\'], errors=\'coerce\').to_numpy(dtype=float)\n        z = pd.to_numeric(sub[\'z_um\'], errors=\'coerce\').to_numpy(dtype=float)\n        xy_d = ((x + y) / 2.0)\n        xy_d = xy_d[np.isfinite(xy_d)]\n        z_d = z[np.isfinite(z)]\n\n        if not np.isfinite(rx) and xy_d.size:\n            rx = float(np.median(xy_d) / 2.0)\n        if not np.isfinite(rz) and z_d.size:\n            rz = float(np.median(z_d) / 2.0)\n\n        return rx, rz\n\n    q95_lookup = _collect_q95_lookup(pairs[\'conf_mask\'].astype(str).unique().tolist())\n    if not q95_lookup:\n        print(f"{tag} WARNING: no q95 low-confidence lookup found; high-confidence filter cannot be applied.")\n\n    low_flags = []\n    for _, r in pairs[[\'conf_mask\', \'conf_label\']].iterrows():\n        low_set = _low_conf_set_for_mask(r[\'conf_mask\'], q95_lookup)\n        low_flags.append(int(r[\'conf_label\']) in low_set)\n    pairs[\'_is_low_conf_q95\'] = np.asarray(low_flags, dtype=bool)\n\n    n_before_q95 = int(len(pairs))\n    n_low_q95 = int(pairs[\'_is_low_conf_q95\'].sum())\n    n_unique_masks = int(pairs[\'conf_mask\'].nunique())\n    n_masks_with_lookup = int(len({\n        m for m in pairs[\'conf_mask\'].astype(str).unique().tolist()\n        if _low_conf_set_for_mask(m, q95_lookup)\n    }))\n    print(\n        f"{tag} q95 filter coverage: masks with lookup={n_masks_with_lookup}/{n_unique_masks}; "\n        f"rows removed={n_low_q95}/{n_before_q95}"\n    )\n    pairs = pairs[~pairs[\'_is_low_conf_q95\']].copy()\n    if pairs.empty:\n        return pairs, {\n            \'n_before_q95\': int(n_before_q95),\n            \'n_low_q95\': int(n_low_q95),\n            \'n_after_q95\': 0,\n        }\n\n    anat_r50_xy_um, anat_r50_z_um = _global_anat_r50_um()\n    _xy_txt = f"{anat_r50_xy_um:.3f}" if np.isfinite(anat_r50_xy_um) else \'nan\'\n    _z_txt = f"{anat_r50_z_um:.3f}" if np.isfinite(anat_r50_z_um) else \'nan\'\n    print(f"{tag} Global match-confidence thresholds (Anat R50): XY={_xy_txt} \xb5m, Z={_z_txt} \xb5m")\n\n    _dist_func = pd.to_numeric(pairs.get(\'dist_func_anat_um\', pd.Series(np.nan, index=pairs.index)), errors=\'coerce\').to_numpy(dtype=float)\n    if np.isfinite(anat_r50_xy_um):\n        pairs[\'_is_low_conf_func_anat_globalR50\'] = np.isfinite(_dist_func) & (_dist_func > float(anat_r50_xy_um))\n    else:\n        pairs[\'_is_low_conf_func_anat_globalR50\'] = False\n\n    _hcr_xy = np.full(len(pairs), np.nan, dtype=float)\n    _hcr_z = np.full(len(pairs), np.nan, dtype=float)\n    _dist_conf = pd.to_numeric(pairs.get(\'dist_conf_anat_um\', pd.Series(np.nan, index=pairs.index)), errors=\'coerce\').to_numpy(dtype=float)\n\n    if HCR_MATCH_RESULTS_LOCAL:\n        DX = float(VOX_ANAT_LOCAL.get(\'X\', 1.0)) if VOX_ANAT_LOCAL else 1.0\n        DY = float(VOX_ANAT_LOCAL.get(\'Y\', 1.0)) if VOX_ANAT_LOCAL else 1.0\n        DZ = float(VOX_ANAT_LOCAL.get(\'Z\', 1.0)) if VOX_ANAT_LOCAL else 1.0\n        _maps = {}\n        for _res in HCR_MATCH_RESULTS_LOCAL:\n            _mname = Path(str(_res.get(\'mask_path\', \'\'))).name\n            _dfc = _res.get(\'df_conf\')\n            _dfa = _res.get(\'df_2p\')\n            if _dfc is None or _dfa is None or _dfc.empty or _dfa.empty:\n                continue\n            try:\n                _cmap = _dfc.set_index(\'label\')[[\'x\', \'y\', \'z\']].to_dict(\'index\')\n                _amap = _dfa.set_index(\'label\')[[\'x\', \'y\', \'z\']].to_dict(\'index\')\n            except Exception:\n                continue\n            _maps[_mname] = (_cmap, _amap)\n\n        for _i, _r in enumerate(pairs[[\'conf_mask\', \'conf_label\', \'anat_label\']].itertuples(index=False)):\n            _mname = Path(str(_r.conf_mask)).name\n            _pair_maps = _maps.get(_mname)\n            if _pair_maps is None:\n                continue\n            _cmap, _amap = _pair_maps\n            _c = _cmap.get(int(_r.conf_label))\n            _a = _amap.get(int(_r.anat_label))\n            if _c is None or _a is None:\n                continue\n            _dx = (float(_c[\'x\']) - float(_a[\'x\'])) * DX\n            _dy = (float(_c[\'y\']) - float(_a[\'y\'])) * DY\n            _dz = (float(_c[\'z\']) - float(_a[\'z\'])) * DZ\n            _hcr_xy[_i] = float(np.sqrt(_dx * _dx + _dy * _dy))\n            _hcr_z[_i] = float(abs(_dz))\n\n    _hcr_xy_eff = _hcr_xy.copy()\n    _miss_xy = (~np.isfinite(_hcr_xy_eff)) & np.isfinite(_dist_conf)\n    _hcr_xy_eff[_miss_xy] = _dist_conf[_miss_xy]\n\n    pairs[\'_hcr_xy_offset_um\'] = _hcr_xy\n    pairs[\'_hcr_z_offset_um\'] = _hcr_z\n\n    _low_hcr = np.zeros(len(pairs), dtype=bool)\n    if np.isfinite(anat_r50_xy_um):\n        _low_hcr |= np.isfinite(_hcr_xy_eff) & (_hcr_xy_eff > float(anat_r50_xy_um))\n    if np.isfinite(anat_r50_z_um):\n        _low_hcr |= np.isfinite(_hcr_z) & (_hcr_z > float(anat_r50_z_um))\n    pairs[\'_is_low_conf_hcr_anat_globalR50\'] = _low_hcr\n\n    pairs[\'_is_low_conf_match_any_globalR50\'] = (\n        pairs[\'_is_low_conf_func_anat_globalR50\'].to_numpy(dtype=bool)\n        | pairs[\'_is_low_conf_hcr_anat_globalR50\'].to_numpy(dtype=bool)\n    )\n\n    _n_before_match = int(len(pairs))\n    _n_low_func = int(pairs[\'_is_low_conf_func_anat_globalR50\'].sum())\n    _n_low_hcr = int(pairs[\'_is_low_conf_hcr_anat_globalR50\'].sum())\n    _n_low_any = int(pairs[\'_is_low_conf_match_any_globalR50\'].sum())\n    print(\n        f"{tag} match-confidence filter (>Anat R50): "\n        f"func-anat={_n_low_func}/{_n_before_match}, "\n        f"hcr-anat={_n_low_hcr}/{_n_before_match}, "\n        f"union={_n_low_any}/{_n_before_match}"\n    )\n\n    pairs = pairs[~pairs[\'_is_low_conf_match_any_globalR50\']].copy()\n    stats = {\n        \'n_before_q95\': int(n_before_q95),\n        \'n_low_q95\': int(n_low_q95),\n        \'n_after_q95\': int(n_before_q95 - n_low_q95),\n        \'n_before_match_filter\': int(_n_before_match),\n        \'n_low_conf_func_anat\': int(_n_low_func),\n        \'n_low_conf_hcr_anat\': int(_n_low_hcr),\n        \'n_low_conf_any\': int(_n_low_any),\n        \'anat_r50_xy_um\': float(anat_r50_xy_um) if np.isfinite(anat_r50_xy_um) else np.nan,\n        \'anat_r50_z_um\': float(anat_r50_z_um) if np.isfinite(anat_r50_z_um) else np.nan,\n    }\n    return pairs, stats\n\n\nif df_stim is None or df_stim.empty:\n    print(\'df_stim is missing; run cell [55] first.\')\nelif df_evt is None or df_evt.empty:\n    print(\'df_evt is missing; run cell [55] first.\')\nelif not SUITE2P_BY_REF_IDX:\n    print(\'Suite2p data not loaded; run cell [23a] first.\')\nelif not Path(CONF_FUNC_CSV).exists():\n    print(f\'Missing conf_to_func_pairs.csv: {CONF_FUNC_CSV} (run [50]).\')\nelse:\n    df_stim = df_stim.copy()\n    if TARGET_STIM_TYPES is not None:\n        df_stim = df_stim[df_stim[\'type\'].isin(list(TARGET_STIM_TYPES))]\n    if df_stim.empty:\n        print(\'No stimuli available after filtering.\')\n    else:\n        pairs = pd.read_csv(CONF_FUNC_CSV)\n        if \'fish_id\' not in pairs.columns:\n            print(\'[stim] conf_to_func_pairs.csv has no fish_id column; re-run [50] for this fish.\')\n            raise SystemExit\n        if \'fish_id\' in pairs.columns:\n            pairs = pairs[pairs[\'fish_id\'].astype(str) == str(FISH_ID)].copy()\n            if pairs.empty:\n                print(\'[stim] conf_to_func_pairs.csv does not match current FISH_ID; run [50].\')\n                raise SystemExit\n        if pairs.empty:\n            print(\'conf_to_func_pairs.csv is empty.\')\n        else:\n            pair_versions = set(pairs.get(\'match_policy_version\', pd.Series(dtype=str)).dropna().astype(str).unique().tolist())\n            if (\'match_policy_version\' not in pairs.columns) or (MATCH_POLICY_VERSION not in pair_versions):\n                print(f\'[stim] conf_to_func_pairs.csv uses stale matching policy; rerun [50] to regenerate {CONF_FUNC_CSV}.\')\n                raise SystemExit\n            required_pair_response_cols = {\'response_is_active\', \'response_class\', \'response_summary_class\'}\n            missing_pair_response_cols = sorted(required_pair_response_cols - set(pairs.columns))\n            if missing_pair_response_cols:\n                print(f\'[stim] conf_to_func_pairs.csv missing response-aware columns {missing_pair_response_cols}; rerun [50] after [50ia].\')\n                raise SystemExit\n\n            if \'gene\' not in pairs.columns:\n                if \'conf_mask\' in pairs.columns:\n                    if callable(GENE_FROM_MASK_FUNC):\n                        pairs[\'gene\'] = pairs[\'conf_mask\'].apply(GENE_FROM_MASK_FUNC)\n                    else:\n                        def _gene_from_mask(path_str):\n                            name = Path(str(path_str)).name\n                            m = re.search(r\'channel\\d+_(.+?)_cp_masks\', name)\n                            gene = m.group(1) if m else name\n                            return gene.replace(\'sst1_\', \'sst1.\')\n                        pairs[\'gene\'] = pairs[\'conf_mask\'].apply(_gene_from_mask)\n                else:\n                    pairs[\'gene\'] = \'unknown\'\n\n            pairs = _prepare_pairs_for_unique_cells(pairs, strict=STRICT_PAIR_INTEGRITY)\n            if pairs.empty:\n                print(\'[stim] No valid gene+HCR+anat+functional mappings after integrity checks.\')\n                raise SystemExit\n\n            if HIGH_CONF_ONLY:\n                pairs, _hc_stats = _filter_pairs_high_conf_generic(pairs, tag=\'[stim]\')\n                stim_hc_match_filter_counts = _hc_stats\n                if pairs.empty:\n                    print(\'[stim] No pairs remain after high-confidence filtering.\')\n                    raise SystemExit\n                pairs_high_conf = pairs.copy()\n            else:\n                pairs_high_conf = None\n\n            n_in = len(pairs)\n            pairs[\'_sort_dist_func\'] = pd.to_numeric(pairs.get(\'dist_func_anat_um\', pd.Series(np.nan, index=pairs.index)), errors=\'coerce\').fillna(np.inf)\n            pairs[\'_sort_overlap\'] = pd.to_numeric(pairs.get(\'overlap_px_func_anat\', pairs.get(\'overlap_px\', pd.Series(np.nan, index=pairs.index))), errors=\'coerce\').fillna(0)\n            pairs[\'_sort_dist_conf\'] = pd.to_numeric(pairs.get(\'dist_conf_anat_um\', pd.Series(np.nan, index=pairs.index)), errors=\'coerce\').fillna(np.inf)\n            pairs = pairs.sort_values(\n                [\'gene\', \'anat_label\', \'_sort_dist_func\', \'_sort_overlap\', \'_sort_dist_conf\', \'plane\', \'func_label\', \'conf_mask\', \'conf_label\'],\n                ascending=[True, True, True, False, True, True, True, True, True],\n            )\n            pairs = pairs.drop_duplicates(subset=[\'gene\', \'anat_label\'], keep=\'first\')\n            pairs = pairs.drop(columns=[\'_sort_dist_func\', \'_sort_overlap\', \'_sort_dist_conf\'])\n            n_after_anat = len(pairs)\n\n            roi_conflict_mask = pairs.duplicated(subset=[\'plane\', \'func_label\', \'gene\'], keep=False)\n            n_roi_conflicts = int(roi_conflict_mask.sum())\n            if (not ALLOW_REUSED_ROIS_WITHIN_GENE) and STRICT_PAIR_INTEGRITY and n_roi_conflicts > 0:\n                raise RuntimeError(\n                    f"[stim] Found {n_roi_conflicts} rows where one functional ROI maps to multiple anatomy cells "\n                    "within a gene. Resolve mapping ambiguity upstream before plotting traces."\n                )\n            pairs = pairs.drop_duplicates(subset=[\'plane\', \'func_label\', \'gene\'])\n            n_after_roi = len(pairs)\n\n            dup_gene_anat_after = int(pairs.duplicated(subset=[\'gene\', \'anat_label\']).sum())\n            if STRICT_PAIR_INTEGRITY and dup_gene_anat_after > 0:\n                raise RuntimeError(\n                    f"[stim] Integrity failure: mapping not unique on (gene, anat_label), duplicates={dup_gene_anat_after}."\n                )\n\n            print(\n                f"[stim] dedup rows: {n_in} -> {n_after_anat} (unique gene+anat) -> {n_after_roi} (unique plane+func+gene); "\n                f"roi_conflicts={n_roi_conflicts}"\n            )\n            if ALLOW_REUSED_ROIS_WITHIN_GENE and n_roi_conflicts > 0:\n                print(\'[stim] Reused ROIs within gene were deduplicated before averaging traces.\')\n\n            s2p_map = SUITE2P_BY_REF_IDX\n            pairs = pairs[pairs[\'plane\'].isin(s2p_map.keys())].copy()\n            if pairs.empty:\n                print(f\'No matching Suite2p planes for {PAIR_SCOPE_LABEL}.\')\n            else:\n                fps = FRAME_RATE\n                if fps is None:\n                    fs_vals = []\n                    for p in s2p_map.values():\n                        fs = p.get(\'ops\', {}).get(\'fs\', None)\n                        if fs is not None:\n                            fs_vals.append(float(fs))\n                    if fs_vals:\n                        fps = fs_vals[0]\n                        if len(set(fs_vals)) > 1:\n                            print(\'[stim] WARNING: multiple fs values found; using the first\')\n                    else:\n                        print(\'FRAME_RATE not available; cannot align to time.\')\n                        fps = None\n\n                if fps is None or fps <= 0:\n                    print(\'Invalid FRAME_RATE; set FRAME_RATE or ensure metadata has framerate.\')\n                else:\n                    win_len = int(round((WINDOW_PRE_SEC + WINDOW_POST_SEC) * fps)) + 1\n                    tvec = (np.arange(win_len, dtype=np.float32) / float(fps)) - float(WINDOW_PRE_SEC)\n\n                    try:\n                        baseline_windows = _build_prestim_baseline_windows(\n                            df_evt,\n                            fps,\n                            STIM_ONSET_DELAY_SEC,\n                            tag=\'[stim]\',\n                        )\n                    except Exception as e:\n                        raise RuntimeError(f"[stim] unable to build z-score baseline windows: {e}")\n\n                    zstats_by_plane = {}\n                    for _plane_idx, _plane_data in s2p_map.items():\n                        _dff = _plane_data.get(\'dff\', None)\n                        if _dff is None:\n                            continue\n                        zstats_by_plane[int(_plane_idx)] = _compute_zscore_stats(\n                            _dff,\n                            baseline_windows,\n                            min_points=ZSCORE_MIN_BASELINE_POINTS,\n                            sigma_eps=ZSCORE_MIN_BASELINE_STD,\n                        )\n\n                    _roi_keys = {(int(r.plane), int(r.func_label) - 1) for r in pairs[[\'plane\', \'func_label\']].itertuples(index=False)}\n                    _n_total_rois = int(len(_roi_keys))\n                    _n_low_points = 0\n                    _n_low_sigma = 0\n                    _n_missing = 0\n                    for _plane, _roi_idx in _roi_keys:\n                        _zs = zstats_by_plane.get(_plane)\n                        if _zs is None or _roi_idx < 0 or _roi_idx >= len(_zs[\'valid\']):\n                            _n_missing += 1\n                            continue\n                        if _zs[\'low_points\'][_roi_idx]:\n                            _n_low_points += 1\n                        elif _zs[\'low_sigma\'][_roi_idx] or (not bool(_zs[\'valid\'][_roi_idx])):\n                            _n_low_sigma += 1\n                    _n_valid = _n_total_rois - _n_low_points - _n_low_sigma - _n_missing\n                    print(\n                        f"[stim] z-score ROI QC: valid={_n_valid}/{_n_total_rois}; "\n                        f"low_baseline_points={_n_low_points}; low_baseline_std={_n_low_sigma}; missing={_n_missing}"\n                    )\n\n                    # Precompute event windows per stimulus type\n                    event_windows = {}\n                    durations = {}\n                    print(f"[stim] Applying onset delay of +{STIM_ONSET_DELAY_SEC:.3f}s to experiment-log start times and trimming that offset from stimulus duration")\n                    print(f"[stim] Window edge policy: {WINDOW_EDGE_POLICY} (min_valid_points={MIN_VALID_POINTS_PER_SEG})")\n                    for _, row in df_stim.iterrows():\n                        stype = row[\'type\']\n                        t0, _, dur = _effective_motion_window(\n                            row.get(\'start\', np.nan),\n                            row.get(\'duration\', np.nan),\n                            row.get(\'end\', np.nan),\n                            STIM_ONSET_DELAY_SEC,\n                        )\n                        if not np.isfinite(t0) or not np.isfinite(dur):\n                            continue\n                        idx0 = int(round((t0 - WINDOW_PRE_SEC) * fps))\n                        idx1 = idx0 + win_len\n                        event_windows.setdefault(stype, []).append((idx0, idx1))\n                        durations.setdefault(stype, []).append(dur)\n\n                    stim_types = sorted(event_windows.keys())\n                    genes_present = sorted(pairs[\'gene\'].dropna().astype(str).unique().tolist())\n                    genes = [g for g in GENE_ORDER if g in genes_present] + [g for g in genes_present if g not in GENE_ORDER]\n                    if not genes:\n                        print(f\'No genes found in {PAIR_SCOPE_LABEL}.\')\n                    else:\n                        gene_colors = {g: GENE_COLORS.get(g, \'#666666\') for g in genes}\n\n                        results = {stype: {} for stype in stim_types}\n                        for stype in stim_types:\n                            windows = event_windows.get(stype, [])\n                            if not windows:\n                                continue\n                            for gene in genes:\n                                segs = []\n                                sub = pairs[pairs[\'gene\'] == gene]\n                                roi_seen = set()\n                                for _, row in sub.iterrows():\n                                    plane = int(row[\'plane\'])\n                                    roi_idx = int(row[\'func_label\']) - 1\n                                    dff = s2p_map[plane].get(\'dff\', None)\n                                    if dff is None:\n                                        continue\n                                    if roi_idx < 0 or roi_idx >= dff.shape[0]:\n                                        continue\n                                    zstats = zstats_by_plane.get(plane, None)\n                                    if zstats is None or roi_idx >= len(zstats[\'valid\']) or not bool(zstats[\'valid\'][roi_idx]):\n                                        continue\n                                    trace = (dff[roi_idx] - float(zstats[\'mu\'][roi_idx])) / float(zstats[\'sigma\'][roi_idx])\n                                    roi_key = (plane, roi_idx)\n                                    roi_added = False\n                                    for idx0, idx1 in windows:\n                                        seg = _extract_window(\n                                            trace,\n                                            idx0,\n                                            idx1,\n                                            mode=WINDOW_EDGE_POLICY,\n                                            min_valid_points=MIN_VALID_POINTS_PER_SEG,\n                                        )\n                                        if seg is None:\n                                            continue\n                                        segs.append(seg)\n                                        roi_added = True\n                                    if roi_added:\n                                        roi_seen.add(roi_key)\n                                if segs:\n                                    arr = np.vstack(segs).astype(np.float32, copy=False)\n                                    n_valid = np.isfinite(arr).sum(axis=0).astype(np.float32)\n                                    mean = np.divide(\n                                        np.nansum(arr, axis=0),\n                                        n_valid,\n                                        out=np.full(arr.shape[1], np.nan, dtype=np.float32),\n                                        where=n_valid > 0,\n                                    )\n                                    sem = None\n                                    if arr.shape[0] > 1:\n                                        centered = arr - mean[np.newaxis, :]\n                                        centered[~np.isfinite(arr)] = np.nan\n                                        var = np.divide(\n                                            np.nansum(centered * centered, axis=0),\n                                            n_valid - 1.0,\n                                            out=np.full(arr.shape[1], np.nan, dtype=np.float32),\n                                            where=n_valid > 1,\n                                        )\n                                        sem = np.divide(\n                                            np.sqrt(var),\n                                            np.sqrt(n_valid),\n                                            out=np.full(arr.shape[1], np.nan, dtype=np.float32),\n                                            where=n_valid > 1,\n                                        )\n                                    n_segments = int(arr.shape[0])\n                                    n_cells = int(len(roi_seen))\n                                    n_trials_per_cell = float(n_segments / n_cells) if n_cells > 0 else np.nan\n                                    results[stype][gene] = {\n                                        \'mean\': mean,\n                                        \'sem\': sem,\n                                        \'n_segments\': n_segments,\n                                        \'n_rois\': n_cells,\n                                        \'n_cells\': n_cells,\n                                        \'n_trials_per_cell\': n_trials_per_cell,\n                                    }\n\n                        # Build plot data with thresholds\n                        plot_data = {}\n                        for stype in stim_types:\n                            gene_items = [\n                                (g, v)\n                                for g, v in results.get(stype, {}).items()\n                                if v[\'n_segments\'] >= MIN_SEGMENTS and v.get(\'n_cells\', v.get(\'n_rois\', 0)) >= MIN_CELLS\n                            ]\n                            if not gene_items:\n                                continue\n                            gene_items = sorted(gene_items, key=lambda kv: kv[1][\'n_segments\'], reverse=True)\n                            gene_items = gene_items[:MAX_GENES]\n                            plot_data[stype] = gene_items\n\n                        if not plot_data:\n                            print(f\'[stim] No genes with >= {MIN_SEGMENTS} segments and >= {MIN_CELLS} cells for any stimulus\')\n                        else:\n                            plot_stims = []\n                            if STIM_ORDER:\n                                for s in STIM_ORDER:\n                                    if s in plot_data and s not in plot_stims:\n                                        plot_stims.append(s)\n                            for s in sorted(plot_data.keys()):\n                                if s not in plot_stims:\n                                    plot_stims.append(s)\n                            ncols = max(1, int(PLOT_NCOLS))\n                            nrows = int(math.ceil(len(plot_stims) / ncols))\n                            fig, axes = plt.subplots(nrows, ncols, figsize=(FIG_WIDTH, FIG_ROW_HEIGHT * nrows), sharex=True, sharey=True)\n                            axes = np.atleast_1d(axes).ravel()\n                            fig.suptitle(\n                                f"Stimulus-locked responses reveal gene-specific activity patterns ({PAIR_SCOPE_LABEL}; onset delay +{STIM_ONSET_DELAY_SEC:g}s)"\n                            )\n\n                            for ax_idx, (ax, stype) in enumerate(zip(axes, plot_stims)):\n                                row_idx = ax_idx // ncols\n                                col_idx = ax_idx % ncols\n                                gene_items = plot_data[stype]\n                                for gene, res in gene_items:\n                                    color = gene_colors.get(gene, None)\n                                    ax.plot(tvec, res[\'mean\'], label=f"{gene} (n={res.get(\'n_cells\', res.get(\'n_rois\', 0))})", color=color)\n                                    if USE_SEM and res[\'sem\'] is not None:\n                                        ax.fill_between(tvec, res[\'mean\'] - res[\'sem\'], res[\'mean\'] + res[\'sem\'], alpha=0.2, color=color)\n\n                                dur_vals = durations.get(stype, [])\n                                if dur_vals:\n                                    stim_dur = float(np.median(dur_vals))\n                                    ax.axvspan(0, stim_dur, color=\'#cccccc\', alpha=0.2)\n\n                                ax.axvline(0, color=\'k\', linestyle=\'--\', linewidth=1.0)\n                                ax.axhline(0, color=\'k\', linewidth=0.8, alpha=0.6)\n                                ax.set_title(f\'{stype}: a distinct average response profile\')\n                                if row_idx == (nrows - 1):\n                                    ax.set_xlabel(\'Time (s)\')\n                                else:\n                                    ax.set_xlabel(\'\')\n                                if col_idx == 0:\n                                    ax.set_ylabel(\'z-scored dF/F\')\n                                else:\n                                    ax.set_ylabel(\'\')\n                                ax.legend(fontsize=8, ncol=2)\n\n                            for ax in axes[len(plot_stims):]:\n                                ax.axis(\'off\')\n\n                            fig.tight_layout(rect=[0, 0, 1, 0.96])\n                            FIG_56_LAST = fig\n                            FIG_56_FISH_ID = FISH_ID\n                            try:\n                                fig.canvas.draw()\n                                FIG_56_RGBA = np.asarray(fig.canvas.buffer_rgba()).copy()\n                            except Exception:\n                                FIG_56_RGBA = None\n                            plt.show()\n\n                        # Summary table\n                        summary_rows = []\n                        for stype in stim_types:\n                            for gene in genes:\n                                res = results.get(stype, {}).get(gene, None)\n                                if res is None:\n                                    continue\n                                summary_rows.append({\n                                    \'stim_type\': stype,\n                                    \'gene\': gene,\n                                    \'n_cells\': int(res.get(\'n_cells\', res.get(\'n_rois\', 0))),\n                                    \'n_segments\': res[\'n_segments\'],\n                                    \'n_trials_per_cell\': float(res.get(\'n_trials_per_cell\', np.nan)),\n                                })\n                        summary_df = pd.DataFrame(summary_rows)\n                        if summary_df.empty:\n                            print(\'[stim] Summary is empty.\')\n                        else:\n                            summary_df = summary_df[summary_df[\'n_segments\'] > 0].reset_index(drop=True)\n                            if not summary_df.empty:\n                                try:\n                                    display(summary_df)\n                                except Exception:\n                                    print(summary_df.to_string(index=False))\n',
    '56f-qc': '# [56f-qc]\n# Hemisphere sanity-check for functional ROIs using midline-side assignment.\n# Produces two plane-grid overlays:\n#   1) all Suite2p ROIs\n#   2) [56f]-analysis ROI subset (mapping-based)\n# and a per-plane counts table.\ntry:\n    _require_fish_state_qc = require_fish_state\nexcept NameError:\n    _require_fish_state_qc = None\nif _require_fish_state_qc is not None and (not _require_fish_state_qc()):\n    raise SystemExit\n\ntry:\n    _suite2p_fish_id_qc = SUITE2P_FISH_ID\nexcept NameError:\n    _suite2p_fish_id_qc = None\nif _suite2p_fish_id_qc not in (None, FISH_ID):\n    print(\'[56f-qc] Suite2p data from different fish; run [23a].\')\n    raise SystemExit\n\nimport json\nimport math\nimport re\nfrom pathlib import Path\n\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nfrom matplotlib.lines import Line2D\nfrom matplotlib.colors import to_rgba\nfrom skimage.measure import regionprops_table\nfrom skimage.segmentation import find_boundaries\nfrom skimage.transform import resize\n\n# Match [22c] panel geometry defaults for easier side-by-side visual QA.\ntry:\n    _conf_func_csv_analysis_qc = CONF_FUNC_CSV_ANALYSIS\nexcept NameError:\n    _conf_func_csv_analysis_qc = None\ntry:\n    _conf_func_csv_qc = CONF_FUNC_CSV\nexcept NameError:\n    _conf_func_csv_qc = None\ntry:\n    _gene_from_mask_helper_qc = gene_from_mask\nexcept NameError:\n    _gene_from_mask_helper_qc = None\ntry:\n    _midline_fish_id_qc = MIDLINE_FISH_ID\nexcept NameError:\n    _midline_fish_id_qc = None\ntry:\n    _midline_json_path_qc = MIDLINE_JSON_PATH\nexcept NameError:\n    _midline_json_path_qc = None\ntry:\n    _midline_by_plane_state_qc = midline_by_plane\nexcept NameError:\n    _midline_by_plane_state_qc = None\ntry:\n    _midline_pos_side_label_qc = MIDLINE_POS_SIDE_LABEL\nexcept NameError:\n    _midline_pos_side_label_qc = None\ntry:\n    _midline_uncertain_band_qc = MIDLINE_UNCERTAIN_BAND_PX\nexcept NameError:\n    _midline_uncertain_band_qc = 0.0\ntry:\n    _midline_params_state_qc = midline_params\nexcept NameError:\n    _midline_params_state_qc = None\ntry:\n    _plane_refs_qc = plane_refs\nexcept NameError:\n    _plane_refs_qc = []\ntry:\n    _suite2p_by_ref_idx_qc = suite2p_by_ref_idx\nexcept NameError:\n    _suite2p_by_ref_idx_qc = {}\ntry:\n    _tform_for_plane_helper_qc = _tform_for_plane\nexcept NameError:\n    _tform_for_plane_helper_qc = None\ntry:\n    _midline_params_for_plane_helper_qc = _midline_params_for_plane\nexcept NameError:\n    _midline_params_for_plane_helper_qc = None\ntry:\n    _annotate_midline_side_helper_qc = _annotate_midline_side\nexcept NameError:\n    _annotate_midline_side_helper_qc = None\ntry:\n    _rescale_labels_to_ref_helper_qc = _rescale_labels_to_ref\nexcept NameError:\n    _rescale_labels_to_ref_helper_qc = None\n\n_56f_qc_label_ref_match_logged = set()\n_56f_qc_plot_cache = {}\nHEMI_QC_NCOLS = 2\nHEMI_QC_FIG_W_PER_COL = 5.5\nHEMI_QC_FIG_H_PER_ROW = HEMI_QC_FIG_W_PER_COL\nHEMI_QC_POINT_SIZE = 15.0\nHEMI_QC_BOUNDARY_ALPHA = 1.0\nHEMI_QC_SHOW_LABEL_IDS = False\nHEMI_QC_MAX_LABEL_TEXT_PER_PLANE = 120\nHEMI_QC_LABEL_FONTSIZE = 5.5\nHEMI_QC_REF_ALPHA = 1.0\nHEMI_QC_USE_MAPPING_SUBSET = True\nHEMI_QC_ZOOM_TO_ENSEMBLE = True\nHEMI_QC_ZOOM_PAD_FRAC = 0.5\nHEMI_QC_ZOOM_PAD_PX = 8.0\n\nSIDE_COLORS = {\n    \'left\': \'#1f77b4\',\n    \'right\': \'#d62728\',\n    \'midline\': \'#f2c14e\',\n    \'unknown\': \'#7f7f7f\',\n}\n\n\ndef _norm01_qc(a):\n    arr = np.asarray(a, dtype=np.float32)\n    finite = np.isfinite(arr)\n    if not finite.any():\n        return np.zeros_like(arr, dtype=np.float32)\n    lo = float(np.nanpercentile(arr[finite], 1.0))\n    hi = float(np.nanpercentile(arr[finite], 99.0))\n    if hi <= lo:\n        lo = float(np.nanmin(arr[finite]))\n        hi = float(np.nanmax(arr[finite]))\n        if hi <= lo:\n            return np.zeros_like(arr, dtype=np.float32)\n    out = (arr - lo) / (hi - lo)\n    out[~finite] = 0.0\n    return np.clip(out, 0.0, 1.0).astype(np.float32)\n\n\ndef _default_conf_csv_qc():\n    default_conf = Path(OUT_REG / \'conf_to_func_pairs.csv\')\n    conf_override = RUN_CONFIG.get(\'CONF_FUNC_CSV_ANALYSIS\', _conf_func_csv_qc)\n    if conf_override is not None:\n        if str(FISH_ID) in str(conf_override) or Path(str(conf_override)).name.startswith(\'conf_to_func_pairs\'):\n            return Path(str(conf_override))\n    return default_conf\n\n\ndef _as_bool_series_qc(s):\n    if s is None:\n        return pd.Series([], dtype=bool)\n    if pd.api.types.is_bool_dtype(s):\n        return s.fillna(False).astype(bool)\n    if pd.api.types.is_numeric_dtype(s):\n        return s.fillna(0).astype(float) != 0\n    return s.astype(str).str.strip().str.lower().isin({\'1\', \'true\', \'t\', \'yes\', \'y\'})\n\n\ndef _infer_gene_from_mask_qc(path_str):\n    if _gene_from_mask_helper_qc is not None:\n        try:\n            return str(_gene_from_mask_helper_qc(path_str))\n        except Exception:\n            pass\n    name = Path(str(path_str)).name\n    m = re.search(r\'channel\\d+_(.+?)_cp_masks\', name)\n    gene = m.group(1) if m else name\n    return gene.replace(\'sst1_\', \'sst1.\')\n\n\ndef _prepare_pairs_for_unique_cells_qc(pairs_df):\n    req_cols = [\'gene\', \'conf_mask\', \'conf_label\', \'anat_label\', \'func_label\', \'plane\']\n    missing = [c for c in req_cols if c not in pairs_df.columns]\n    if missing:\n        print(f"[56f-qc] mapping missing required columns: {missing}")\n        return pairs_df.iloc[0:0].copy()\n\n    out = pairs_df.copy()\n    if \'is_selected_for_analysis\' in out.columns:\n        selected = _as_bool_series_qc(out[\'is_selected_for_analysis\'])\n        out = out[selected].copy()\n\n    if \'gene\' not in out.columns:\n        if \'conf_mask\' in out.columns:\n            out[\'gene\'] = out[\'conf_mask\'].apply(_infer_gene_from_mask_qc)\n        else:\n            out[\'gene\'] = \'unknown\'\n\n    out[\'gene\'] = out[\'gene\'].astype(str)\n    for c in (\'conf_label\', \'anat_label\', \'func_label\', \'plane\'):\n        out[c] = pd.to_numeric(out[c], errors=\'coerce\')\n\n    required_nonnull = out[[\'gene\', \'conf_label\', \'anat_label\', \'func_label\', \'plane\']].notna().all(axis=1)\n    out = out[required_nonnull].copy()\n    if out.empty:\n        return out\n\n    out[\'conf_label\'] = out[\'conf_label\'].astype(int)\n    out[\'anat_label\'] = out[\'anat_label\'].astype(int)\n    out[\'func_label\'] = out[\'func_label\'].astype(int)\n    out[\'plane\'] = out[\'plane\'].astype(int)\n\n    out[\'_sort_dist_func\'] = pd.to_numeric(out.get(\'dist_func_anat_um\', pd.Series(np.nan, index=out.index)), errors=\'coerce\').fillna(np.inf)\n    out[\'_sort_overlap\'] = pd.to_numeric(out.get(\'overlap_px_func_anat\', out.get(\'overlap_px\', pd.Series(np.nan, index=out.index))), errors=\'coerce\').fillna(0)\n    out[\'_sort_dist_conf\'] = pd.to_numeric(out.get(\'dist_conf_anat_um\', pd.Series(np.nan, index=out.index)), errors=\'coerce\').fillna(np.inf)\n    out = out.sort_values(\n        [\'gene\', \'anat_label\', \'_sort_dist_func\', \'_sort_overlap\', \'_sort_dist_conf\', \'plane\', \'func_label\'],\n        ascending=[True, True, True, False, True, True, True],\n    )\n    out = out.drop_duplicates(subset=[\'gene\', \'anat_label\'], keep=\'first\')\n    out = out.drop_duplicates(subset=[\'plane\', \'func_label\', \'gene\'])\n    out = out.drop(columns=[\'_sort_dist_func\', \'_sort_overlap\', \'_sort_dist_conf\'])\n    return out\n\n\ndef _midline_globals_match_current_fish_qc():\n    current = str(FISH_ID).strip()\n    state_fish = _midline_fish_id_qc\n    if state_fish is None:\n        return False\n    return str(state_fish).strip() == current\n\n\ndef _path_matches_current_fish_qc(path_obj):\n    current = str(FISH_ID).strip()\n    if not current:\n        return True\n    toks = sorted(set(re.findall(r"[A-Za-z]\\d+_f\\d+", str(path_obj))))\n    if not toks:\n        return True\n    return all(t == current for t in toks)\n\n\ndef _load_midline_bundle_qc():\n    candidates = []\n    if _midline_json_path_qc is not None:\n        candidates.append(Path(_midline_json_path_qc))\n    if OUT_REG is not None:\n        candidates.append(Path(OUT_REG) / \'midline_params_func_ref.json\')\n\n    seen = set()\n    for p in candidates:\n        ps = str(p)\n        if ps in seen:\n            continue\n        seen.add(ps)\n        if not _path_matches_current_fish_qc(p):\n            continue\n        if not p.exists():\n            continue\n        try:\n            bundle = json.loads(p.read_text())\n            current = str(FISH_ID).strip()\n            bundle_fish = str((bundle or {}).get(\'fish_id\', \'\')).strip() if isinstance(bundle, dict) else \'\'\n            if bundle_fish and current and bundle_fish != current:\n                continue\n            return bundle, p\n        except Exception:\n            continue\n    return None, None\n\n\ndef _resolve_midline_context_qc():\n    per_plane = None\n    pos_label = \'right\'\n    neg_label = \'left\'\n    band = 0.0\n    source = \'none\'\n\n    if _midline_globals_match_current_fish_qc():\n        per_plane = _midline_by_plane_state_qc\n        pos_label = str(_midline_pos_side_label_qc if _midline_pos_side_label_qc is not None else pos_label).strip().lower()\n        if pos_label not in {\'left\', \'right\'}:\n            pos_label = \'right\'\n        neg_label = \'left\' if pos_label == \'right\' else \'right\'\n        band = float(_midline_uncertain_band_qc)\n        source = \'globals\'\n    elif _midline_fish_id_qc not in (None, \'\'):\n        print(f"[56f-qc] ignoring stale in-memory midline from fish {_midline_fish_id_qc}; current fish is {FISH_ID}")\n\n    if not isinstance(per_plane, dict) or not per_plane:\n        bundle, bundle_path = _load_midline_bundle_qc()\n        if isinstance(bundle, dict):\n            pp = bundle.get(\'per_plane\', {})\n            if isinstance(pp, dict) and pp:\n                per_plane = pp\n                source = str(bundle_path)\n                _sides = bundle.get(\'side_labels\', {}) if isinstance(bundle, dict) else {}\n                pos_label = str(_sides.get(\'positive\', pos_label)).strip().lower()\n                if pos_label not in {\'left\', \'right\'}:\n                    pos_label = \'right\'\n                neg_label = \'left\' if pos_label == \'right\' else \'right\'\n                try:\n                    band = float(bundle.get(\'manual\', {}).get(\'uncertain_band_px\', band))\n                except Exception:\n                    pass\n\n    return per_plane, pos_label, neg_label, band, source\n\n\n_midline_by_plane_qc, _midline_pos_label_qc, _midline_neg_label_qc, _midline_band_qc, _midline_source_qc = _resolve_midline_context_qc()\n\n\ndef _infer_midline_space_qc():\n    src_label = None\n    if _midline_globals_match_current_fish_qc():\n        mp = _midline_params_state_qc\n        if isinstance(mp, dict):\n            src_label = (mp.get(\'base\', {}) or {}).get(\'source_label\', None)\n    if src_label is None:\n        b, _ = _load_midline_bundle_qc()\n        if isinstance(b, dict):\n            src_label = (b.get(\'base\', {}) or {}).get(\'source_label\', None)\n\n    s = str(src_label).strip().lower() if src_label is not None else \'\'\n    if s in {\'warped\', \'tform-preview\', \'warped-raw\'}:\n        return \'anat\'\n    if s in {\'raw\', \'ref2d\', \'ref2d_raw\', \'ref_match\'}:\n        return \'func\'\n\n    try:\n        if _plane_refs_qc:\n            any_warped = any((pr.get(\'ref_warped\') is not None) or (pr.get(\'ref_warped_raw\') is not None) for pr in _plane_refs_qc)\n            if any_warped:\n                return \'anat\'\n    except Exception:\n        pass\n    return \'func\'\n\n\n_MIDLINE_SPACE_QC = _infer_midline_space_qc()\n\n\ndef _get_plane_ref_qc(plane_idx):\n    pmap = _suite2p_by_ref_idx_qc\n    pdata = pmap.get(int(plane_idx), None)\n    ref_idx = int(pdata.get(\'ref_idx\', plane_idx)) if isinstance(pdata, dict) else int(plane_idx)\n    if _plane_refs_qc and 0 <= ref_idx < len(_plane_refs_qc):\n        return _plane_refs_qc[ref_idx], ref_idx\n    return None, ref_idx\n\n\ndef _get_plane_tform_qc(plane_idx):\n    pr, _ = _get_plane_ref_qc(int(plane_idx))\n    if pr is None:\n        return None\n    tform = pr.get(\'tform\', None)\n    if tform is None and _tform_for_plane_helper_qc is not None:\n        try:\n            tform = _tform_for_plane_helper_qc(pr)\n        except Exception:\n            tform = None\n    return tform\n\n\ndef _xy_to_midline_space_qc(plane_idx, x, y):\n    x = np.asarray(x, dtype=float)\n    y = np.asarray(y, dtype=float)\n    if _MIDLINE_SPACE_QC != \'anat\':\n        return x, y\n    tform = _get_plane_tform_qc(int(plane_idx))\n    if tform is None:\n        return x, y\n    try:\n        pts = np.column_stack([x, y]).astype(float)\n        pts_t = np.asarray(tform(pts), dtype=float)\n        return pts_t[:, 0], pts_t[:, 1]\n    except Exception:\n        return x, y\n\n\ndef _xy_to_display_space_qc(plane_idx, x, y):\n    x = np.asarray(x, dtype=float)\n    y = np.asarray(y, dtype=float)\n    if _MIDLINE_SPACE_QC != \'anat\':\n        return x, y\n    tform = _get_plane_tform_qc(int(plane_idx))\n    if tform is None:\n        return x, y\n    try:\n        pts = np.column_stack([x, y]).astype(float)\n        pts_t = np.asarray(tform.inverse(pts), dtype=float)\n        return pts_t[:, 0], pts_t[:, 1]\n    except Exception:\n        return x, y\n\n\ndef _params_for_plane_qc(plane_idx):\n    if _midline_params_for_plane_helper_qc is not None:\n        try:\n            p = _midline_params_for_plane_helper_qc(int(plane_idx))\n            if isinstance(p, dict):\n                return p\n        except Exception:\n            pass\n    if isinstance(_midline_by_plane_qc, dict):\n        return _midline_by_plane_qc.get(int(plane_idx), _midline_by_plane_qc.get(str(plane_idx), None))\n    return None\n\n\ndef _annotate_midline_side_qc(df_in, x_col=\'x\', y_col=\'y\', plane_col=\'plane\'):\n    if df_in is None or getattr(df_in, \'empty\', True):\n        return df_in\n    if _annotate_midline_side_helper_qc is not None and _MIDLINE_SPACE_QC != \'anat\':\n        try:\n            return _annotate_midline_side_helper_qc(df_in, x_col=x_col, y_col=y_col, plane_col=plane_col)\n        except Exception as e:\n            print(f"[56f-qc] _annotate_midline_side failed; using fallback: {e}")\n\n    out = df_in.copy()\n    d = np.full(len(out), np.nan, dtype=float)\n    if not isinstance(_midline_by_plane_qc, dict) or not _midline_by_plane_qc:\n        out[\'midline_signed_dist_px\'] = d\n        out[\'midline_side\'] = \'unknown\'\n        out[\'midline_uncertain\'] = False\n        return out\n\n    for p_idx, idx in out.groupby(plane_col).groups.items():\n        p = _params_for_plane_qc(int(p_idx))\n        if not isinstance(p, dict):\n            continue\n        try:\n            x0 = float(p.get(\'x0\', np.nan))\n            y0 = float(p.get(\'y0\', np.nan))\n            th = np.deg2rad(float(p.get(\'theta_deg\', np.nan)))\n        except Exception:\n            continue\n        if not (np.isfinite(x0) and np.isfinite(y0) and np.isfinite(th)):\n            continue\n\n        nx = -np.sin(th)\n        ny = np.cos(th)\n        xv = pd.to_numeric(out.loc[idx, x_col], errors=\'coerce\').to_numpy(dtype=float)\n        yv = pd.to_numeric(out.loc[idx, y_col], errors=\'coerce\').to_numpy(dtype=float)\n        xv_m, yv_m = _xy_to_midline_space_qc(int(p_idx), xv, yv)\n        d[idx] = (xv_m - x0) * nx + (yv_m - y0) * ny\n\n    side = np.full(len(out), \'unknown\', dtype=object)\n    finite = np.isfinite(d)\n    side[(finite) & (d > _midline_band_qc)] = _midline_pos_label_qc\n    side[(finite) & (d < -_midline_band_qc)] = _midline_neg_label_qc\n    side[(finite) & (np.abs(d) <= _midline_band_qc)] = \'midline\'\n\n    out[\'midline_signed_dist_px\'] = d\n    out[\'midline_side\'] = side\n    out[\'midline_uncertain\'] = np.abs(d) <= _midline_band_qc\n    return out\n\n\ndef _line_segment_qc(x0, y0, theta_deg, shape):\n    h, w = int(shape[0]), int(shape[1])\n    L = float(max(h, w) * 2.0)\n    th = np.deg2rad(float(theta_deg))\n    dx = np.cos(th) * L\n    dy = np.sin(th) * L\n    return (x0 - dx, y0 - dy), (x0 + dx, y0 + dy)\n\n\n\ndef _ensemble_bbox_from_labels(labels):\n    if labels is None:\n        return None\n    arr = np.asarray(labels)\n    if arr.ndim != 2:\n        return None\n    ys, xs = np.nonzero(arr > 0)\n    if xs.size == 0:\n        return None\n    return float(xs.min()), float(xs.max()), float(ys.min()), float(ys.max())\n\n\ndef _apply_bbox_zoom(ax, bbox, shape, pad_frac=0.08, pad_px=8.0):\n    if bbox is None:\n        return\n    h, w = int(shape[0]), int(shape[1])\n    x0, x1, y0, y1 = [float(v) for v in bbox]\n    span = max(1.0, x1 - x0, y1 - y0)\n    pad = max(float(pad_px), float(pad_frac) * span)\n    xl = max(0.0, x0 - pad)\n    xr = min(float(w - 1), x1 + pad)\n    yt = max(0.0, y0 - pad)\n    yb = min(float(h - 1), y1 + pad)\n    ax.set_xlim(xl, xr)\n    ax.set_ylim(yb, yt)\ndef _ensure_uint_labels_qc(arr):\n    if arr is None:\n        return None\n    a = np.asarray(arr)\n    if a.ndim == 3 and a.shape[-1] in (3, 4):\n        a = a[..., 0]\n    if a.ndim == 3 and a.shape[0] == 1:\n        a = a[0]\n    if a.ndim != 2:\n        return None\n    if not np.issubdtype(a.dtype, np.integer):\n        a = np.rint(a).astype(np.int64, copy=False)\n    else:\n        a = a.astype(np.int64, copy=False)\n    a[a < 0] = 0\n    return a.astype(np.uint32, copy=False)\n\n\ndef _rescale_labels_to_ref_shape_qc(labels, ref_shape):\n    if labels is None or ref_shape is None:\n        return labels\n    ref_shape = tuple(int(v) for v in ref_shape)\n    if tuple(labels.shape) == ref_shape:\n        return labels\n    if _rescale_labels_to_ref_helper_qc is not None:\n        try:\n            out = _rescale_labels_to_ref_helper_qc(labels, ref_shape)\n            if out is not None:\n                return _ensure_uint_labels_qc(out)\n        except Exception:\n            pass\n    try:\n        out = resize(\n            labels.astype(np.float32),\n            ref_shape,\n            order=0,\n            mode=\'edge\',\n            preserve_range=True,\n            anti_aliasing=False,\n        )\n        return _ensure_uint_labels_qc(out)\n    except Exception:\n        return labels\n\n\ndef _get_plane_visuals_qc(plane_idx, plane_data):\n    labels = _ensure_uint_labels_qc(plane_data.get(\'labels\', None))\n    if labels is None:\n        return None\n\n    ref_idx = int(plane_data.get(\'ref_idx\', plane_idx))\n    pr = None\n    if _plane_refs_qc:\n        if 0 <= ref_idx < len(_plane_refs_qc):\n            pr = _plane_refs_qc[ref_idx]\n\n    ref_img = None\n    ref_src = \'none\'\n    if pr is not None:\n        ref_img = pr.get(\'ref_match\', None)\n        ref_src = \'ref_match\'\n        if ref_img is None:\n            ref_img = pr.get(\'ref2d_raw\', pr.get(\'ref2d\'))\n            ref_src = \'ref2d\'\n\n    if ref_img is not None:\n        ref_img = np.asarray(ref_img, dtype=np.float32)\n        if ref_img.ndim == 3 and ref_img.shape[-1] in (3, 4):\n            ref_img = ref_img[..., 0]\n        if ref_img.ndim != 2:\n            ref_img = None\n\n    if ref_img is not None and tuple(labels.shape) != tuple(ref_img.shape):\n        old_shape = tuple(labels.shape)\n        labels = _rescale_labels_to_ref_shape_qc(labels, ref_img.shape)\n        new_shape = tuple(labels.shape)\n        _k = (int(plane_idx), old_shape, tuple(ref_img.shape), ref_src)\n        if _k not in _56f_qc_label_ref_match_logged:\n            print(f"[56f-qc] plane {plane_idx}: rescaled labels {old_shape} -> {new_shape} to match {ref_src} {tuple(ref_img.shape)}")\n            _56f_qc_label_ref_match_logged.add(_k)\n\n    if ref_img is None:\n        ref_img = np.zeros(labels.shape, dtype=np.float32)\n    elif tuple(ref_img.shape) != tuple(labels.shape):\n        try:\n            ref_img = resize(\n                ref_img,\n                labels.shape,\n                order=1,\n                mode=\'reflect\',\n                preserve_range=True,\n                anti_aliasing=True,\n            ).astype(np.float32)\n        except Exception:\n            ref_img = np.zeros(labels.shape, dtype=np.float32)\n\n    return {\n        \'labels\': labels,\n        \'ref\': _norm01_qc(ref_img),\n        \'ref_idx\': int(ref_idx),\n        \'ref_src\': ref_src,\n    }\ndef _draw_side_overlay_qc(ax, ref_img, labels, side_df, plane_idx, title):\n    ax.imshow(ref_img, cmap=\'gray\', alpha=HEMI_QC_REF_ALPHA)\n\n    if labels is not None and side_df is not None and not side_df.empty:\n        for side_name in (\'left\', \'right\', \'midline\', \'unknown\'):\n            sub = side_df[side_df[\'midline_side\'] == side_name]\n            if sub.empty:\n                continue\n            ids = sub[\'func_label\'].astype(int).unique()\n            mask = np.isin(labels, ids)\n            if np.any(mask):\n                bnd = find_boundaries(mask, mode=\'outer\')\n                if np.any(bnd):\n                    overlay = np.zeros((labels.shape[0], labels.shape[1], 4), dtype=np.float32)\n                    rgba = to_rgba(SIDE_COLORS.get(side_name, \'#7f7f7f\'), alpha=HEMI_QC_BOUNDARY_ALPHA)\n                    overlay[bnd] = rgba\n                    ax.imshow(overlay)\n\n        for side_name in (\'left\', \'right\', \'midline\', \'unknown\'):\n            sub = side_df[side_df[\'midline_side\'] == side_name]\n            if sub.empty:\n                continue\n            ax.scatter(\n                sub[\'x\'].to_numpy(dtype=float),\n                sub[\'y\'].to_numpy(dtype=float),\n                s=HEMI_QC_POINT_SIZE,\n                c=SIDE_COLORS.get(side_name, \'#7f7f7f\'),\n                alpha=0.9,\n                linewidths=0,\n            )\n\n        if HEMI_QC_SHOW_LABEL_IDS:\n            n_txt = 0\n            for _, r in side_df.sort_values(\'func_label\').iterrows():\n                if n_txt >= HEMI_QC_MAX_LABEL_TEXT_PER_PLANE:\n                    break\n                if not (np.isfinite(r[\'x\']) and np.isfinite(r[\'y\'])):\n                    continue\n                ax.text(\n                    float(r[\'x\']) + 1.0,\n                    float(r[\'y\']) + 1.0,\n                    str(int(r[\'func_label\'])),\n                    color=\'white\',\n                    fontsize=HEMI_QC_LABEL_FONTSIZE,\n                    alpha=0.9,\n                )\n                n_txt += 1\n\n    p = _params_for_plane_qc(int(plane_idx))\n    if isinstance(p, dict):\n        try:\n            (x1, y1), (x2, y2) = _line_segment_qc(float(p[\'x0\']), float(p[\'y0\']), float(p[\'theta_deg\']), ref_img.shape)\n            xd, yd = _xy_to_display_space_qc(int(plane_idx), [x1, x2, float(p[\'x0\'])], [y1, y2, float(p[\'y0\'])])\n            ax.plot([xd[0], xd[1]], [yd[0], yd[1]], color=\'yellow\', linewidth=1.3)\n            ax.scatter([xd[2]], [yd[2]], s=9, c=\'yellow\')\n        except Exception:\n            pass\n\n    if HEMI_QC_ZOOM_TO_ENSEMBLE:\n        _bbox = _ensemble_bbox_from_labels(labels)\n        _apply_bbox_zoom(ax, _bbox, ref_img.shape, pad_frac=HEMI_QC_ZOOM_PAD_FRAC, pad_px=HEMI_QC_ZOOM_PAD_PX)\n\n    ax.set_title(title)\n    ax.axis(\'off\')\n\n\ndef _plot_plane_grid_qc(rois_df, title, plot_key):\n    if rois_df is None or rois_df.empty:\n        print(f"[56f-qc] No rows to plot: {title}")\n        _56f_qc_plot_cache[plot_key] = {\'fig\': None, \'rgba\': None}\n        return\n\n    planes = sorted(int(p) for p in rois_df[\'plane\'].dropna().astype(int).unique().tolist())\n    if not planes:\n        print(f"[56f-qc] No planes to plot: {title}")\n        _56f_qc_plot_cache[plot_key] = {\'fig\': None, \'rgba\': None}\n        return\n\n    ncols = max(1, min(int(HEMI_QC_NCOLS), len(planes)))\n    nrows = int(math.ceil(len(planes) / float(ncols)))\n    fig, axes = plt.subplots(\n        nrows,\n        ncols,\n        figsize=(HEMI_QC_FIG_W_PER_COL * ncols, HEMI_QC_FIG_H_PER_ROW * nrows),\n        squeeze=False,\n    )\n    axes = axes.ravel()\n\n    s2p_map = _suite2p_by_ref_idx_qc\n    for ax_idx, plane_idx in enumerate(planes):\n        ax = axes[ax_idx]\n        plane_data = s2p_map.get(int(plane_idx), None)\n        if not isinstance(plane_data, dict):\n            ax.text(0.5, 0.5, f\'plane {plane_idx}: missing data\', ha=\'center\', va=\'center\', transform=ax.transAxes)\n            ax.axis(\'off\')\n            continue\n\n        plane_visuals = _plane_visuals_56f_qc\n        vis = plane_visuals.get(int(plane_idx), None)\n        if vis is None:\n            vis = _get_plane_visuals_qc(int(plane_idx), plane_data)\n        if vis is None:\n            ax.text(0.5, 0.5, f\'plane {plane_idx}: labels/ref unavailable\', ha=\'center\', va=\'center\', transform=ax.transAxes)\n            ax.axis(\'off\')\n            continue\n\n        labels = vis[\'labels\']\n        ref_img = vis[\'ref\']\n        sub = rois_df[rois_df[\'plane\'].astype(int) == int(plane_idx)].copy()\n\n        counts = sub[\'midline_side\'].value_counts(dropna=False).to_dict() if not sub.empty else {}\n        ttl = (\n            f"p{plane_idx} | n={len(sub)} | "\n            f"L={int(counts.get(\'left\', 0))} R={int(counts.get(\'right\', 0))} "\n            f"M={int(counts.get(\'midline\', 0))} U={int(counts.get(\'unknown\', 0))}"\n        )\n        _draw_side_overlay_qc(ax, ref_img, labels, sub, int(plane_idx), ttl)\n\n    for ax in axes[len(planes):]:\n        ax.axis(\'off\')\n\n    handles = [\n        Line2D([0], [0], marker=\'o\', linestyle=\'None\', markersize=6, color=SIDE_COLORS[\'left\'], label=\'left\'),\n        Line2D([0], [0], marker=\'o\', linestyle=\'None\', markersize=6, color=SIDE_COLORS[\'right\'], label=\'right\'),\n        Line2D([0], [0], marker=\'o\', linestyle=\'None\', markersize=6, color=SIDE_COLORS[\'midline\'], label=\'midline\'),\n        Line2D([0], [0], marker=\'o\', linestyle=\'None\', markersize=6, color=SIDE_COLORS[\'unknown\'], label=\'unknown\'),\n        Line2D([0], [0], color=\'yellow\', linewidth=1.2, label=\'midline\'),\n    ]\n    fig.legend(handles=handles, loc=\'upper right\', ncol=5, fontsize=8, frameon=False)\n    fig.suptitle(title)\n    fig.tight_layout(rect=[0, 0, 1, 0.96])\n    rgba = None\n    try:\n        fig.canvas.draw()\n        rgba = np.asarray(fig.canvas.buffer_rgba()).copy()\n    except Exception:\n        rgba = None\n    _56f_qc_plot_cache[plot_key] = {\'fig\': fig, \'rgba\': rgba}\n    plt.show()\n\n\ndef _make_counts_qc(df, subset_name):\n    if df is None or df.empty:\n        return pd.DataFrame(columns=[\'subset\', \'plane\', \'n_total\', \'left\', \'right\', \'midline\', \'unknown\', \'left_frac\', \'right_frac\'])\n    c = (\n        df.assign(plane=df[\'plane\'].astype(int), midline_side=df[\'midline_side\'].astype(str))\n          .groupby([\'plane\', \'midline_side\'])\n          .size()\n          .unstack(fill_value=0)\n          .reset_index()\n    )\n    for col in (\'left\', \'right\', \'midline\', \'unknown\'):\n        if col not in c.columns:\n            c[col] = 0\n    c[\'n_total\'] = c[[\'left\', \'right\', \'midline\', \'unknown\']].sum(axis=1)\n    c[\'left_frac\'] = np.where(c[\'n_total\'] > 0, c[\'left\'] / c[\'n_total\'], np.nan)\n    c[\'right_frac\'] = np.where(c[\'n_total\'] > 0, c[\'right\'] / c[\'n_total\'], np.nan)\n    c[\'subset\'] = subset_name\n    return c[[\'subset\', \'plane\', \'n_total\', \'left\', \'right\', \'midline\', \'unknown\', \'left_frac\', \'right_frac\']]\n\n\nif not _suite2p_by_ref_idx_qc:\n    print(\'[56f-qc] Suite2p data not loaded; run [23a] first.\')\nelse:\n    s2p_map = suite2p_by_ref_idx\n\n    roi_rows = []\n    plane_visuals = {}\n    for plane_idx, plane_data in s2p_map.items():\n        vis = _get_plane_visuals_qc(int(plane_idx), plane_data)\n        if vis is None:\n            continue\n        plane_visuals[int(plane_idx)] = vis\n        arr = vis[\'labels\']\n        if arr.ndim != 2:\n            continue\n        try:\n            props = regionprops_table(arr.astype(np.int32, copy=False), properties=(\'label\', \'centroid\'))\n            rdf = pd.DataFrame(props)\n            if rdf.empty:\n                continue\n            rdf = rdf[rdf[\'label\'] != 0].copy()\n            if rdf.empty:\n                continue\n            rdf = rdf.rename(columns={\'label\': \'func_label\', \'centroid-0\': \'y\', \'centroid-1\': \'x\'})\n            rdf[\'plane\'] = int(plane_idx)\n            roi_rows.append(rdf[[\'plane\', \'func_label\', \'x\', \'y\']])\n        except Exception:\n            continue\n\n    _plane_visuals_56f_qc = plane_visuals\n    rois_all = pd.concat(roi_rows, ignore_index=True) if roi_rows else pd.DataFrame(columns=[\'plane\', \'func_label\', \'x\', \'y\'])\n    if rois_all.empty:\n        print(\'[56f-qc] No ROI centroids found in Suite2p labels.\')\n    else:\n        rois_all = _annotate_midline_side_qc(rois_all, x_col=\'x\', y_col=\'y\', plane_col=\'plane\')\n        rois_all[\'midline_side\'] = rois_all[\'midline_side\'].astype(str).str.strip().str.lower()\n\n        subset_source = \'none\'\n        rois_analysis = rois_all.iloc[0:0].copy()\n        if HEMI_QC_USE_MAPPING_SUBSET:\n            try:\n                pairs_from_56f = pairs_with_midline_side_56f\n            except NameError:\n                pairs_from_56f = None\n            if isinstance(pairs_from_56f, pd.DataFrame) and (not pairs_from_56f.empty):\n                keys = pairs_from_56f[[\'plane\', \'func_label\']].copy()\n                keys[\'plane\'] = pd.to_numeric(keys[\'plane\'], errors=\'coerce\').astype(\'Int64\')\n                keys[\'func_label\'] = pd.to_numeric(keys[\'func_label\'], errors=\'coerce\').astype(\'Int64\')\n                keys = keys.dropna().astype({\'plane\': int, \'func_label\': int}).drop_duplicates()\n                rois_analysis = rois_all.merge(keys, on=[\'plane\', \'func_label\'], how=\'inner\')\n                subset_source = \'pairs_with_midline_side_56f\'\n            else:\n                _conf_csv_local = _default_conf_csv_qc()\n                if _conf_csv_local.exists():\n                    pairs = pd.read_csv(_conf_csv_local)\n                    if \'fish_id\' in pairs.columns:\n                        pairs = pairs[pairs[\'fish_id\'].astype(str) == str(FISH_ID)].copy()\n                    if \'gene\' not in pairs.columns:\n                        if \'conf_mask\' in pairs.columns:\n                            pairs[\'gene\'] = pairs[\'conf_mask\'].apply(_infer_gene_from_mask_qc)\n                        else:\n                            pairs[\'gene\'] = \'unknown\'\n                    pairs = _prepare_pairs_for_unique_cells_qc(pairs)\n                    if not pairs.empty:\n                        keys = pairs[[\'plane\', \'func_label\']].drop_duplicates().copy()\n                        keys[\'plane\'] = keys[\'plane\'].astype(int)\n                        keys[\'func_label\'] = keys[\'func_label\'].astype(int)\n                        rois_analysis = rois_all.merge(keys, on=[\'plane\', \'func_label\'], how=\'inner\')\n                        subset_source = f\'conf_to_func_pairs ({_conf_csv_local.name})\'\n                    else:\n                        subset_source = f\'conf_to_func_pairs empty ({_conf_csv_local.name})\'\n                else:\n                    subset_source = f\'missing conf_to_func_pairs ({_conf_csv_local})\'\n\n        print(\n            f"[56f-qc] midline source={_midline_source_qc}; space={_MIDLINE_SPACE_QC}; "\n            f"positive={_midline_pos_label_qc}, negative={_midline_neg_label_qc}, band={_midline_band_qc:.2f}px"\n        )\n        print(f"[56f-qc] analysis subset source: {subset_source}")\n\n        _plot_plane_grid_qc(\n            rois_all,\n            title=\'Midline assignment separates the full ROI population into coherent left and right groups\',\n            plot_key=\'all\',\n        )\n        FIG_56F_QC_ALL_LAST = _56f_qc_plot_cache.get(\'all\', {}).get(\'fig\')\n        FIG_56F_QC_ALL_RGBA = _56f_qc_plot_cache.get(\'all\', {}).get(\'rgba\')\n\n        if rois_analysis.empty:\n            print(\'[56f-qc] analysis subset is empty; skipping subset figure.\')\n            FIG_56F_QC_SUBSET_LAST = None\n            FIG_56F_QC_SUBSET_RGBA = None\n        else:\n            _plot_plane_grid_qc(\n                rois_analysis,\n                title=\'The laterality-analysis subset preserves the same left/right organization\',\n                plot_key=\'subset\',\n            )\n            FIG_56F_QC_SUBSET_LAST = _56f_qc_plot_cache.get(\'subset\', {}).get(\'fig\')\n            FIG_56F_QC_SUBSET_RGBA = _56f_qc_plot_cache.get(\'subset\', {}).get(\'rgba\')\n\n        counts_all = _make_counts_qc(rois_all, \'all_suite2p_rois\')\n        counts_subset = _make_counts_qc(rois_analysis, \'analysis_subset\')\n        counts = pd.concat([counts_all, counts_subset], ignore_index=True)\n        counts = counts.sort_values([\'subset\', \'plane\']).reset_index(drop=True)\n\n        roi_midline_qc_all_56f = rois_all.copy()\n        roi_midline_qc_subset_56f = rois_analysis.copy()\n        roi_midline_qc_counts_56f = counts.copy()\n\n        if counts.empty:\n            print(\'[56f-qc] Counts table is empty.\')\n        else:\n            try:\n                display(counts)\n            except Exception:\n                print(counts.to_string(index=False))\n\n',
    '56f-qc-activity': '# [56f-qc-activity]\n# Spatial activity QC map per plane: where activity is strongest in functional space.\n# Defaults to z-scored activity magnitude during stimulus intervals, with midline overlay.\ntry:\n    _require_fish_state_act = require_fish_state\nexcept NameError:\n    _require_fish_state_act = None\nif _require_fish_state_act is not None and (not _require_fish_state_act()):\n    raise SystemExit\n\ntry:\n    _suite2p_fish_id_act = SUITE2P_FISH_ID\nexcept NameError:\n    _suite2p_fish_id_act = None\nif _suite2p_fish_id_act not in (None, FISH_ID):\n    print(\'[56f-qc-activity] Suite2p data from different fish; run [23a].\')\n    raise SystemExit\n\nimport json\nimport math\nimport re\nfrom pathlib import Path\n\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\n\ntry:\n    _midline_fish_id_act = MIDLINE_FISH_ID\nexcept NameError:\n    _midline_fish_id_act = None\ntry:\n    _midline_json_path_act = MIDLINE_JSON_PATH\nexcept NameError:\n    _midline_json_path_act = None\ntry:\n    _midline_by_plane_state_act = midline_by_plane\nexcept NameError:\n    _midline_by_plane_state_act = None\ntry:\n    _midline_pos_side_label_act = MIDLINE_POS_SIDE_LABEL\nexcept NameError:\n    _midline_pos_side_label_act = None\ntry:\n    _midline_uncertain_band_act = MIDLINE_UNCERTAIN_BAND_PX\nexcept NameError:\n    _midline_uncertain_band_act = 0.0\ntry:\n    _midline_params_state_act = midline_params\nexcept NameError:\n    _midline_params_state_act = None\ntry:\n    _plane_refs_act = plane_refs\nexcept NameError:\n    _plane_refs_act = []\ntry:\n    _suite2p_by_ref_idx_act = suite2p_by_ref_idx\nexcept NameError:\n    _suite2p_by_ref_idx_act = {}\ntry:\n    _tform_for_plane_helper_act = _tform_for_plane\nexcept NameError:\n    _tform_for_plane_helper_act = None\ntry:\n    _midline_params_for_plane_helper_act = _midline_params_for_plane\nexcept NameError:\n    _midline_params_for_plane_helper_act = None\ntry:\n    _annotate_midline_side_helper_act = _annotate_midline_side\nexcept NameError:\n    _annotate_midline_side_helper_act = None\ntry:\n    _rescale_labels_to_ref_helper_act = _rescale_labels_to_ref\nexcept NameError:\n    _rescale_labels_to_ref_helper_act = None\ntry:\n    _pairs_with_midline_side_56f_act = pairs_with_midline_side_56f\nexcept NameError:\n    _pairs_with_midline_side_56f_act = None\ntry:\n    _conf_func_csv_analysis_act = CONF_FUNC_CSV_ANALYSIS\nexcept NameError:\n    _conf_func_csv_analysis_act = None\ntry:\n    _conf_func_csv_act = CONF_FUNC_CSV\nexcept NameError:\n    _conf_func_csv_act = None\ntry:\n    _frame_rate_act = FRAME_RATE\nexcept NameError:\n    _frame_rate_act = None\ntry:\n    _df_stim_act = df_stim\nexcept NameError:\n    _df_stim_act = None\ntry:\n    _df_evt_act = df_evt\nexcept NameError:\n    _df_evt_act = None\n\n_56f_qc_activity_label_ref_match_logged = set()\nfrom skimage.filters import gaussian\nfrom skimage.measure import regionprops_table\nfrom skimage.transform import resize\n\nACT_QC_ACTIVITY_SOURCE = \'stim\'  # stim|full\nACT_QC_STAT = \'mean_abs\'  # mean_abs|mean|std|peak_abs\nACT_QC_USE_ZSCORE = True\nACT_QC_ONSET_DELAY_SEC = float(RUN_CONFIG.get(\'STIM_ONSET_DELAY_SEC\', 10.0))\nACT_QC_RESPONSE_EXTRA_SEC = 0.0\nACT_QC_STIM_ONLY_UNILATERAL = False\nACT_QC_MIN_BASELINE_POINTS = 200\nACT_QC_MIN_BASELINE_STD = 1e-6\nACT_QC_SMOOTH_SIGMA_PX = 1.3\nACT_QC_CMAP = \'inferno\'\nACT_QC_HEAT_ALPHA = 0.72\nACT_QC_MAP_MODE = \'centroid\'  # centroid|labels\nACT_QC_REF_INTERPOLATION = \'nearest\'\nACT_QC_HEAT_INTERPOLATION = \'nearest\'\nACT_QC_NCOLS = 2\nACT_QC_FIG_W_PER_COL = 6.5\nACT_QC_FIG_H_PER_ROW = 5.5\nACT_QC_COLORBAR_EXTRA_W_IN = 1.2\nACT_QC_COLORBAR_FRACTION = 0.03\nACT_QC_COLORBAR_PAD = 0.04\nACT_QC_LAYOUT_RIGHT = 0.85\nACT_QC_VMIN_PERCENTILE = 5.0\nACT_QC_VMAX_PERCENTILE = 99.0\nACT_QC_SHOW_CENTROIDS = False\nACT_QC_CENTROID_SIZE = 4.0\nACT_QC_ZOOM_TO_ENSEMBLE = True\nACT_QC_ZOOM_PAD_FRAC = 0.08\nACT_QC_ZOOM_PAD_PX = 8.0\nACT_QC_PLOT_ANALYSIS_SUBSET = True\nTARGET_STIM_TYPES_QC = RUN_CONFIG.get(\'TARGET_STIM_TYPES\', None)\n\nif ACT_QC_ACTIVITY_SOURCE not in {\'stim\', \'full\'}:\n    print(f"[56f-qc-activity] invalid ACT_QC_ACTIVITY_SOURCE=\'{ACT_QC_ACTIVITY_SOURCE}\', using \'stim\'")\n    ACT_QC_ACTIVITY_SOURCE = \'stim\'\nif ACT_QC_STAT not in {\'mean_abs\', \'mean\', \'std\', \'peak_abs\'}:\n    print(f"[56f-qc-activity] invalid ACT_QC_STAT=\'{ACT_QC_STAT}\', using \'mean_abs\'")\n    ACT_QC_STAT = \'mean_abs\'\nif ACT_QC_MAP_MODE not in {\'centroid\', \'labels\'}:\n    print(f"[56f-qc-activity] invalid ACT_QC_MAP_MODE=\'{ACT_QC_MAP_MODE}\', using \'centroid\'")\n    ACT_QC_MAP_MODE = \'centroid\'\n\n\ndef _norm01_local(a):\n    arr = np.asarray(a, dtype=np.float32)\n    finite = np.isfinite(arr)\n    if not finite.any():\n        return np.zeros_like(arr, dtype=np.float32)\n    lo = float(np.nanpercentile(arr[finite], 1.0))\n    hi = float(np.nanpercentile(arr[finite], 99.0))\n    if hi <= lo:\n        lo = float(np.nanmin(arr[finite]))\n        hi = float(np.nanmax(arr[finite]))\n        if hi <= lo:\n            return np.zeros_like(arr, dtype=np.float32)\n    out = (arr - lo) / (hi - lo)\n    out[~finite] = 0.0\n    return np.clip(out, 0.0, 1.0).astype(np.float32)\n\n\ndef _parse_stim_components(stype):\n    parts = [p.strip().upper() for p in str(stype).split(\'+\') if str(p).strip()]\n    if not parts:\n        return None\n    comps = []\n    seen_sides = set()\n    for part in parts:\n        m = re.match(r\'^([LR])(LB|LC)$\', part)\n        if not m:\n            return None\n        side, mode = m.group(1), m.group(2)\n        if side in seen_sides:\n            return None\n        seen_sides.add(side)\n        comps.append((side, mode))\n    return comps\n\n\ndef _build_prestim_baseline_windows_local(df_evt, fps, onset_delay_sec, tag=\'[56f-qc-activity]\'):\n    if df_evt is None or getattr(df_evt, \'empty\', True):\n        raise RuntimeError(f"{tag} df_evt is empty")\n\n    evt = df_evt[[\'event\', \'time\']].copy()\n    evt[\'event\'] = evt[\'event\'].astype(str).str.strip()\n    evt[\'time\'] = pd.to_numeric(evt[\'time\'], errors=\'coerce\')\n    evt = evt.dropna(subset=[\'event\', \'time\']).sort_values(\'time\').reset_index(drop=True)\n\n    stim_starts = {}\n    for ev, t in evt[[\'event\', \'time\']].itertuples(index=False):\n        m = re.match(r\'^(B\\d+)_stim(\\d+)_.+$\', ev)\n        if not m:\n            continue\n        key = (m.group(1), int(m.group(2)))\n        if key not in stim_starts:\n            stim_starts[key] = float(t)\n\n    windows = []\n    for ev, t_pre in evt[[\'event\', \'time\']].itertuples(index=False):\n        m = re.match(r\'^(B\\d+)_prestim(\\d+)_pause$\', ev)\n        if not m:\n            continue\n        key = (m.group(1), int(m.group(2)))\n        t_stim = stim_starts.get(key)\n        if t_stim is None:\n            continue\n        t0 = float(t_pre)\n        t1 = float(t_stim) + float(onset_delay_sec)\n        if not np.isfinite(t0) or not np.isfinite(t1) or t1 <= t0:\n            continue\n        idx0 = int(round(t0 * float(fps)))\n        idx1 = int(round(t1 * float(fps)))\n        if idx1 > idx0:\n            windows.append((idx0, idx1))\n\n    if not windows:\n        raise RuntimeError(f"{tag} no prestim baseline windows found")\n\n    windows = sorted(windows, key=lambda w: (w[0], w[1]))\n    merged = []\n    for s, e in windows:\n        if not merged or s > merged[-1][1]:\n            merged.append([s, e])\n        else:\n            merged[-1][1] = max(merged[-1][1], e)\n    return [(int(s), int(e)) for s, e in merged if e > s]\n\n\ndef _compute_zscore_stats_local(dff, baseline_windows, min_points=200, sigma_eps=1e-6):\n    n_roi = int(dff.shape[0])\n    T = int(dff.shape[1])\n    mask = np.zeros(T, dtype=bool)\n    for idx0, idx1 in baseline_windows:\n        s = max(0, int(idx0))\n        e = min(T, int(idx1))\n        if e > s:\n            mask[s:e] = True\n\n    mu = np.full(n_roi, np.nan, dtype=np.float32)\n    sigma = np.full(n_roi, np.nan, dtype=np.float32)\n    n_valid = np.zeros(n_roi, dtype=np.int32)\n\n    if mask.any():\n        base = dff[:, mask]\n        n_valid = np.isfinite(base).sum(axis=1).astype(np.int32)\n        with np.errstate(invalid=\'ignore\', divide=\'ignore\'):\n            mu = np.nanmean(base, axis=1).astype(np.float32, copy=False)\n            sigma = np.nanstd(base, axis=1).astype(np.float32, copy=False)\n\n    has_points = n_valid >= int(min_points)\n    has_sigma = np.isfinite(sigma) & (sigma > float(sigma_eps))\n    has_mu = np.isfinite(mu)\n    valid = has_points & has_sigma & has_mu\n    return {\'mu\': mu, \'sigma\': sigma, \'valid\': valid, \'n_valid\': n_valid}\n\n\ndef _midline_globals_match_current_fish():\n    current = str(FISH_ID).strip()\n    state_fish = _midline_fish_id_act\n    if state_fish is None:\n        return False\n    return str(state_fish).strip() == current\n\n\ndef _path_matches_current_fish(path_obj):\n    current = str(FISH_ID).strip()\n    if not current:\n        return True\n    toks = sorted(set(re.findall(r"[A-Za-z]\\d+_f\\d+", str(path_obj))))\n    if not toks:\n        return True\n    return all(t == current for t in toks)\n\n\ndef _load_midline_bundle():\n    candidates = []\n    if _midline_json_path_act is not None:\n        candidates.append(Path(_midline_json_path_act))\n    if OUT_REG is not None:\n        candidates.append(Path(OUT_REG) / \'midline_params_func_ref.json\')\n\n    seen = set()\n    for p in candidates:\n        ps = str(p)\n        if ps in seen:\n            continue\n        seen.add(ps)\n        if not _path_matches_current_fish(p):\n            continue\n        if not p.exists():\n            continue\n        try:\n            bundle = json.loads(p.read_text())\n            current = str(FISH_ID).strip()\n            bundle_fish = str((bundle or {}).get(\'fish_id\', \'\')).strip() if isinstance(bundle, dict) else \'\'\n            if bundle_fish and current and bundle_fish != current:\n                continue\n            return bundle, p\n        except Exception:\n            continue\n    return None, None\n\n\ndef _resolve_midline_context():\n    per_plane = None\n    pos_label = \'left\'\n    neg_label = \'right\'\n    band = 0.0\n    source = \'none\'\n\n    if _midline_globals_match_current_fish():\n        per_plane = _midline_by_plane_state_act\n        pos_label = str(_midline_pos_side_label_act if _midline_pos_side_label_act is not None else pos_label).strip().lower()\n        if pos_label not in {\'left\', \'right\'}:\n            pos_label = \'left\'\n        neg_label = \'left\' if pos_label == \'right\' else \'right\'\n        band = float(_midline_uncertain_band_act)\n        source = \'globals\'\n    elif _midline_fish_id_act not in (None, \'\'):\n        print(f"[56f-qc-activity] ignoring stale in-memory midline from fish {_midline_fish_id_act}; current fish is {FISH_ID}")\n\n    if not isinstance(per_plane, dict) or not per_plane:\n        bundle, bundle_path = _load_midline_bundle()\n        if isinstance(bundle, dict):\n            pp = bundle.get(\'per_plane\', {})\n            if isinstance(pp, dict) and pp:\n                per_plane = pp\n                source = str(bundle_path)\n                _sides = bundle.get(\'side_labels\', {}) if isinstance(bundle, dict) else {}\n                pos_label = str(_sides.get(\'positive\', pos_label)).strip().lower()\n                if pos_label not in {\'left\', \'right\'}:\n                    pos_label = \'left\'\n                neg_label = \'left\' if pos_label == \'right\' else \'right\'\n                try:\n                    band = float(bundle.get(\'manual\', {}).get(\'uncertain_band_px\', band))\n                except Exception:\n                    pass\n    return per_plane, pos_label, neg_label, band, source\n\n\n_midline_by_plane, _pos_label, _neg_label, _mid_band, _mid_source = _resolve_midline_context()\n\n\ndef _infer_midline_space_act():\n    src_label = None\n    if _midline_globals_match_current_fish():\n        mp = _midline_params_state_act\n        if isinstance(mp, dict):\n            src_label = (mp.get(\'base\', {}) or {}).get(\'source_label\', None)\n    if src_label is None:\n        b, _ = _load_midline_bundle()\n        if isinstance(b, dict):\n            src_label = (b.get(\'base\', {}) or {}).get(\'source_label\', None)\n\n    s = str(src_label).strip().lower() if src_label is not None else \'\'\n    if s in {\'warped\', \'tform-preview\', \'warped-raw\'}:\n        return \'anat\'\n    if s in {\'raw\', \'ref2d\', \'ref2d_raw\', \'ref_match\'}:\n        return \'func\'\n\n    try:\n        if _plane_refs_act:\n            any_warped = any((pr.get(\'ref_warped\') is not None) or (pr.get(\'ref_warped_raw\') is not None) for pr in _plane_refs_act)\n            if any_warped:\n                return \'anat\'\n    except Exception:\n        pass\n    return \'func\'\n\n\n_MIDLINE_SPACE_ACT = _infer_midline_space_act()\n\n\ndef _get_plane_ref_act(plane_idx):\n    pmap = _suite2p_by_ref_idx_act\n    pdata = pmap.get(int(plane_idx), None)\n    ref_idx = int(pdata.get(\'ref_idx\', plane_idx)) if isinstance(pdata, dict) else int(plane_idx)\n    if _plane_refs_act and 0 <= ref_idx < len(_plane_refs_act):\n        return _plane_refs_act[ref_idx], ref_idx\n    return None, ref_idx\n\n\ndef _get_plane_tform_act(plane_idx):\n    pr, _ = _get_plane_ref_act(int(plane_idx))\n    if pr is None:\n        return None\n    tform = pr.get(\'tform\', None)\n    if tform is None and _tform_for_plane_helper_act is not None:\n        try:\n            tform = _tform_for_plane_helper_act(pr)\n        except Exception:\n            tform = None\n    return tform\n\n\ndef _xy_to_midline_space_act(plane_idx, x, y):\n    x = np.asarray(x, dtype=float)\n    y = np.asarray(y, dtype=float)\n    if _MIDLINE_SPACE_ACT != \'anat\':\n        return x, y\n    tform = _get_plane_tform_act(int(plane_idx))\n    if tform is None:\n        return x, y\n    try:\n        pts = np.column_stack([x, y]).astype(float)\n        pts_t = np.asarray(tform(pts), dtype=float)\n        return pts_t[:, 0], pts_t[:, 1]\n    except Exception:\n        return x, y\n\n\ndef _xy_to_display_space_act(plane_idx, x, y):\n    x = np.asarray(x, dtype=float)\n    y = np.asarray(y, dtype=float)\n    if _MIDLINE_SPACE_ACT != \'anat\':\n        return x, y\n    tform = _get_plane_tform_act(int(plane_idx))\n    if tform is None:\n        return x, y\n    try:\n        pts = np.column_stack([x, y]).astype(float)\n        pts_t = np.asarray(tform.inverse(pts), dtype=float)\n        return pts_t[:, 0], pts_t[:, 1]\n    except Exception:\n        return x, y\n\n\ndef _params_for_plane(plane_idx):\n    if _midline_params_for_plane_helper_act is not None:\n        try:\n            p = _midline_params_for_plane_helper_act(int(plane_idx))\n            if isinstance(p, dict):\n                return p\n        except Exception:\n            pass\n    if isinstance(_midline_by_plane, dict):\n        return _midline_by_plane.get(int(plane_idx), _midline_by_plane.get(str(plane_idx), None))\n    return None\n\n\ndef _annotate_midline_side_resilient(df_in, x_col=\'x\', y_col=\'y\', plane_col=\'plane\'):\n    if df_in is None or getattr(df_in, \'empty\', True):\n        return df_in\n    if _annotate_midline_side_helper_act is not None and _MIDLINE_SPACE_ACT != \'anat\':\n        try:\n            return _annotate_midline_side_helper_act(df_in, x_col=x_col, y_col=y_col, plane_col=plane_col)\n        except Exception:\n            pass\n\n    out = df_in.copy()\n    d = np.full(len(out), np.nan, dtype=float)\n    if not isinstance(_midline_by_plane, dict) or not _midline_by_plane:\n        out[\'midline_signed_dist_px\'] = d\n        out[\'midline_side\'] = \'unknown\'\n        out[\'midline_uncertain\'] = False\n        return out\n\n    for p_idx, idx in out.groupby(plane_col).groups.items():\n        p = _params_for_plane(int(p_idx))\n        if not isinstance(p, dict):\n            continue\n        try:\n            x0 = float(p.get(\'x0\', np.nan))\n            y0 = float(p.get(\'y0\', np.nan))\n            th = np.deg2rad(float(p.get(\'theta_deg\', np.nan)))\n        except Exception:\n            continue\n        if not (np.isfinite(x0) and np.isfinite(y0) and np.isfinite(th)):\n            continue\n\n        nx = -np.sin(th)\n        ny = np.cos(th)\n        xv = pd.to_numeric(out.loc[idx, x_col], errors=\'coerce\').to_numpy(dtype=float)\n        yv = pd.to_numeric(out.loc[idx, y_col], errors=\'coerce\').to_numpy(dtype=float)\n        xv_m, yv_m = _xy_to_midline_space_act(int(p_idx), xv, yv)\n        d[idx] = (xv_m - x0) * nx + (yv_m - y0) * ny\n\n    side = np.full(len(out), \'unknown\', dtype=object)\n    finite = np.isfinite(d)\n    side[(finite) & (d > _mid_band)] = _pos_label\n    side[(finite) & (d < -_mid_band)] = _neg_label\n    side[(finite) & (np.abs(d) <= _mid_band)] = \'midline\'\n    out[\'midline_signed_dist_px\'] = d\n    out[\'midline_side\'] = side\n    out[\'midline_uncertain\'] = np.abs(d) <= _mid_band\n    return out\n\n\ndef _line_segment(x0, y0, theta_deg, shape):\n    h, w = int(shape[0]), int(shape[1])\n    L = float(max(h, w) * 2.0)\n    th = np.deg2rad(float(theta_deg))\n    dx = np.cos(th) * L\n    dy = np.sin(th) * L\n    return (x0 - dx, y0 - dy), (x0 + dx, y0 + dy)\n\n\ndef _ensure_uint_labels_act(arr):\n    if arr is None:\n        return None\n    a = np.asarray(arr)\n    if a.ndim == 3 and a.shape[-1] in (3, 4):\n        a = a[..., 0]\n    if a.ndim == 3 and a.shape[0] == 1:\n        a = a[0]\n    if a.ndim != 2:\n        return None\n    if not np.issubdtype(a.dtype, np.integer):\n        a = np.rint(a).astype(np.int64, copy=False)\n    else:\n        a = a.astype(np.int64, copy=False)\n    a[a < 0] = 0\n    return a.astype(np.uint32, copy=False)\n\n\ndef _rescale_labels_to_ref_shape_act(labels, ref_shape):\n    if labels is None or ref_shape is None:\n        return labels\n    ref_shape = tuple(int(v) for v in ref_shape)\n    if tuple(labels.shape) == ref_shape:\n        return labels\n    if _rescale_labels_to_ref_helper_act is not None:\n        try:\n            out = _rescale_labels_to_ref_helper_act(labels, ref_shape)\n            if out is not None:\n                return _ensure_uint_labels_act(out)\n        except Exception:\n            pass\n    try:\n        out = resize(\n            labels.astype(np.float32),\n            ref_shape,\n            order=0,\n            mode=\'edge\',\n            preserve_range=True,\n            anti_aliasing=False,\n        )\n        return _ensure_uint_labels_act(out)\n    except Exception:\n        return labels\n\n\ndef _get_plane_visuals_activity(plane_idx, plane_data):\n    labels = _ensure_uint_labels_act(plane_data.get(\'labels\', None))\n    if labels is None:\n        return None\n\n    ref_idx = int(plane_data.get(\'ref_idx\', plane_idx))\n    pr = None\n    if _plane_refs_act:\n        if 0 <= ref_idx < len(_plane_refs_act):\n            pr = _plane_refs_act[ref_idx]\n\n    ref_img = None\n    ref_src = \'none\'\n    if pr is not None:\n        ref_img = pr.get(\'ref_match\', None)\n        ref_src = \'ref_match\'\n        if ref_img is None:\n            ref_img = pr.get(\'ref2d_raw\', pr.get(\'ref2d\'))\n            ref_src = \'ref2d\'\n\n    if ref_img is not None:\n        ref_img = np.asarray(ref_img, dtype=np.float32)\n        if ref_img.ndim == 3 and ref_img.shape[-1] in (3, 4):\n            ref_img = ref_img[..., 0]\n        if ref_img.ndim != 2:\n            ref_img = None\n\n    if ref_img is not None and tuple(labels.shape) != tuple(ref_img.shape):\n        old_shape = tuple(labels.shape)\n        labels = _rescale_labels_to_ref_shape_act(labels, ref_img.shape)\n        new_shape = tuple(labels.shape)\n        _k = (int(plane_idx), old_shape, tuple(ref_img.shape), ref_src)\n        if _k not in _56f_qc_activity_label_ref_match_logged:\n            print(f"[56f-qc-activity] plane {plane_idx}: rescaled labels {old_shape} -> {new_shape} to match {ref_src} {tuple(ref_img.shape)}")\n            _56f_qc_activity_label_ref_match_logged.add(_k)\n\n    if ref_img is None:\n        ref_img = np.zeros(labels.shape, dtype=np.float32)\n    elif tuple(ref_img.shape) != tuple(labels.shape):\n        try:\n            ref_img = resize(\n                ref_img,\n                labels.shape,\n                order=1,\n                mode=\'reflect\',\n                preserve_range=True,\n                anti_aliasing=True,\n            ).astype(np.float32)\n        except Exception:\n            ref_img = np.zeros(labels.shape, dtype=np.float32)\n\n    return {\n        \'labels\': labels,\n        \'ref\': _norm01_local(ref_img),\n        \'ref_idx\': int(ref_idx),\n        \'ref_src\': ref_src,\n    }\ndef _ensemble_bbox_from_labels(labels):\n    if labels is None:\n        return None\n    arr = np.asarray(labels)\n    if arr.ndim != 2:\n        return None\n    ys, xs = np.nonzero(arr > 0)\n    if xs.size == 0:\n        return None\n    return float(xs.min()), float(xs.max()), float(ys.min()), float(ys.max())\n\n\ndef _apply_bbox_zoom(ax, bbox, shape, pad_frac=0.08, pad_px=8.0):\n    if bbox is None:\n        return\n    h, w = int(shape[0]), int(shape[1])\n    x0, x1, y0, y1 = [float(v) for v in bbox]\n    span = max(1.0, x1 - x0, y1 - y0)\n    pad = max(float(pad_px), float(pad_frac) * span)\n    xl = max(0.0, x0 - pad)\n    xr = min(float(w - 1), x1 + pad)\n    yt = max(0.0, y0 - pad)\n    yb = min(float(h - 1), y1 + pad)\n    ax.set_xlim(xl, xr)\n    ax.set_ylim(yb, yt)\ndef _build_stim_mask(T, fps, df_stim):\n    mask = np.zeros(int(T), dtype=bool)\n    used = 0\n    if df_stim is None or getattr(df_stim, \'empty\', True):\n        return mask, used\n\n    d = df_stim.copy()\n    if TARGET_STIM_TYPES_QC is not None:\n        d = d[d[\'type\'].isin(list(TARGET_STIM_TYPES_QC))]\n    if d.empty:\n        return mask, used\n\n    for _, row in d.iterrows():\n        stype = str(row.get(\'type\', \'\'))\n        if ACT_QC_STIM_ONLY_UNILATERAL:\n            comps = _parse_stim_components(stype)\n            if comps is None or len(comps) != 1:\n                continue\n        try:\n            t0 = float(row[\'start\']) + float(ACT_QC_ONSET_DELAY_SEC)\n            dur = float(row.get(\'duration\', 0.0)) + float(ACT_QC_RESPONSE_EXTRA_SEC)\n        except Exception:\n            continue\n        if not np.isfinite(t0) or not np.isfinite(dur) or dur <= 0:\n            continue\n        idx0 = int(round(t0 * float(fps)))\n        idx1 = int(round((t0 + dur) * float(fps)))\n        s = max(0, min(int(T), idx0))\n        e = max(0, min(int(T), idx1))\n        if e > s:\n            mask[s:e] = True\n            used += 1\n    return mask, used\n\n\ndef _reduce_metric(arr2d, stat):\n    if arr2d.size == 0:\n        return np.full(arr2d.shape[0], np.nan, dtype=np.float32)\n    with np.errstate(invalid=\'ignore\', divide=\'ignore\'):\n        if stat == \'mean_abs\':\n            out = np.nanmean(np.abs(arr2d), axis=1)\n        elif stat == \'mean\':\n            out = np.nanmean(arr2d, axis=1)\n        elif stat == \'std\':\n            out = np.nanstd(arr2d, axis=1)\n        elif stat == \'peak_abs\':\n            out = np.nanmax(np.abs(arr2d), axis=1)\n        else:\n            out = np.nanmean(np.abs(arr2d), axis=1)\n    return np.asarray(out, dtype=np.float32)\n\n\nif not _suite2p_by_ref_idx_act:\n    print(\'[56f-qc-activity] Suite2p data not loaded; run [23a] first.\')\nelse:\n    s2p_map = suite2p_by_ref_idx\n\n    fps = _frame_rate_act\n    if fps is None:\n        fs_vals = []\n        for p in s2p_map.values():\n            fs = p.get(\'ops\', {}).get(\'fs\', None)\n            if fs is not None:\n                fs_vals.append(float(fs))\n        if fs_vals:\n            fps = fs_vals[0]\n    if fps is None or float(fps) <= 0:\n        print(\'[56f-qc-activity] Invalid frame rate; cannot compute activity map.\')\n    else:\n        fps = float(fps)\n\n        df_stim_local = _df_stim_act\n        if ACT_QC_ACTIVITY_SOURCE == \'stim\' and (df_stim_local is None or getattr(df_stim_local, \'empty\', True)):\n            print(\'[56f-qc-activity] df_stim missing; falling back to full-session activity.\')\n            ACT_QC_ACTIVITY_SOURCE = \'full\'\n\n        baseline_windows = None\n        if ACT_QC_USE_ZSCORE:\n            df_evt_local = _df_evt_act\n            if df_evt_local is None or getattr(df_evt_local, \'empty\', True):\n                print(\'[56f-qc-activity] df_evt missing; using non-zscored activity.\')\n                ACT_QC_USE_ZSCORE = False\n            else:\n                try:\n                    baseline_windows = _build_prestim_baseline_windows_local(\n                        df_evt_local,\n                        fps,\n                        ACT_QC_ONSET_DELAY_SEC,\n                    )\n                except Exception as e:\n                    print(f"[56f-qc-activity] could not build baseline windows: {e}; using non-zscored activity.")\n                    ACT_QC_USE_ZSCORE = False\n\n        print(f"[56f-qc-activity] render mode={ACT_QC_MAP_MODE}, smooth_sigma={ACT_QC_SMOOTH_SIGMA_PX}")\n        plane_payload = []\n        all_heat_vals = []\n        summary_rows = []\n\n        for plane_idx in sorted(s2p_map.keys()):\n            plane_data = s2p_map[int(plane_idx)]\n            dff = plane_data.get(\'dff\', None)\n            if dff is None:\n                continue\n\n            vis = _get_plane_visuals_activity(int(plane_idx), plane_data)\n            if vis is None:\n                continue\n            labels = vis[\'labels\']\n            ref_vis = vis[\'ref\']\n\n            if labels.ndim != 2 or dff.ndim != 2:\n                continue\n            n_roi, T = int(dff.shape[0]), int(dff.shape[1])\n            if n_roi <= 0 or T <= 0:\n                continue\n\n            traces = np.asarray(dff, dtype=np.float32)\n            valid_z = np.ones(n_roi, dtype=bool)\n            if ACT_QC_USE_ZSCORE and baseline_windows is not None:\n                zs = _compute_zscore_stats_local(\n                    traces,\n                    baseline_windows,\n                    min_points=ACT_QC_MIN_BASELINE_POINTS,\n                    sigma_eps=ACT_QC_MIN_BASELINE_STD,\n                )\n                valid_z = np.asarray(zs[\'valid\'], dtype=bool)\n                traces_z = np.full_like(traces, np.nan, dtype=np.float32)\n                good = valid_z\n                if np.any(good):\n                    traces_z[good] = (traces[good] - zs[\'mu\'][good, None]) / zs[\'sigma\'][good, None]\n                traces = traces_z\n\n            stim_mask = np.ones(T, dtype=bool)\n            n_events_used = -1\n            if ACT_QC_ACTIVITY_SOURCE == \'stim\':\n                stim_mask, n_events_used = _build_stim_mask(T, fps, df_stim_local)\n                if not stim_mask.any():\n                    print(f"[56f-qc-activity] plane {plane_idx}: no stim frames selected; skipping")\n                    continue\n\n            traces_win = traces[:, stim_mask] if ACT_QC_ACTIVITY_SOURCE == \'stim\' else traces\n            metric = _reduce_metric(traces_win, ACT_QC_STAT)\n            metric[~np.isfinite(metric)] = np.nan\n            if ACT_QC_USE_ZSCORE:\n                metric[~valid_z] = np.nan\n\n            props = regionprops_table(labels.astype(np.int32, copy=False), properties=(\'label\', \'centroid\'))\n            roi_df = pd.DataFrame(props)\n            if roi_df.empty:\n                continue\n            roi_df = roi_df[roi_df[\'label\'] != 0].copy()\n            if roi_df.empty:\n                continue\n            roi_df = roi_df.rename(columns={\'label\': \'func_label\', \'centroid-0\': \'y\', \'centroid-1\': \'x\'})\n            roi_df[\'plane\'] = int(plane_idx)\n\n            roi_df[\'roi_idx\'] = roi_df[\'func_label\'].astype(int) - 1\n            roi_df[\'activity_metric\'] = np.nan\n            ok_idx = (roi_df[\'roi_idx\'] >= 0) & (roi_df[\'roi_idx\'] < len(metric))\n            roi_df.loc[ok_idx, \'activity_metric\'] = metric[roi_df.loc[ok_idx, \'roi_idx\'].to_numpy(dtype=int)]\n\n            roi_df = _annotate_midline_side_resilient(roi_df, x_col=\'x\', y_col=\'y\', plane_col=\'plane\')\n            roi_df[\'midline_side\'] = roi_df[\'midline_side\'].astype(str).str.strip().str.lower()\n\n            use_rows = roi_df[[\'func_label\', \'x\', \'y\', \'activity_metric\']].dropna(subset=[\'activity_metric\']).copy()\n\n            if ACT_QC_MAP_MODE == \'labels\':\n                max_label = int(np.max(labels)) if labels.size else 0\n                if max_label <= 0:\n                    continue\n                lut = np.full(max_label + 1, np.nan, dtype=np.float32)\n                if not use_rows.empty:\n                    lid = use_rows[\'func_label\'].astype(int).to_numpy()\n                    lval = use_rows[\'activity_metric\'].to_numpy(dtype=np.float32)\n                    valid_l = (lid >= 0) & (lid <= max_label) & np.isfinite(lval)\n                    lut[lid[valid_l]] = lval[valid_l]\n                metric_img = lut[labels.astype(np.int32, copy=False)]\n                metric_img[labels == 0] = np.nan\n            else:\n                # Centroid mode avoids apparent oversized ROIs from full-label footprint rendering.\n                accum = np.zeros(labels.shape, dtype=np.float32)\n                weight = np.zeros(labels.shape, dtype=np.float32)\n                if not use_rows.empty:\n                    xv = np.rint(use_rows[\'x\'].to_numpy(dtype=float)).astype(int)\n                    yv = np.rint(use_rows[\'y\'].to_numpy(dtype=float)).astype(int)\n                    vv = use_rows[\'activity_metric\'].to_numpy(dtype=np.float32)\n                    inb = (xv >= 0) & (xv < labels.shape[1]) & (yv >= 0) & (yv < labels.shape[0]) & np.isfinite(vv)\n                    if np.any(inb):\n                        np.add.at(accum, (yv[inb], xv[inb]), vv[inb])\n                        np.add.at(weight, (yv[inb], xv[inb]), 1.0)\n                metric_img = np.divide(\n                    accum,\n                    weight,\n                    out=np.full(labels.shape, np.nan, dtype=np.float32),\n                    where=weight > 0,\n                )\n\n            if ACT_QC_SMOOTH_SIGMA_PX > 0:\n                num = gaussian(np.nan_to_num(metric_img, nan=0.0), sigma=ACT_QC_SMOOTH_SIGMA_PX, preserve_range=True)\n                den = gaussian(np.isfinite(metric_img).astype(np.float32), sigma=ACT_QC_SMOOTH_SIGMA_PX, preserve_range=True)\n                heat = np.divide(num, den, out=np.full_like(num, np.nan, dtype=np.float32), where=den > 1e-6)\n            else:\n                heat = metric_img.astype(np.float32)\n\n            finite_heat = heat[np.isfinite(heat)]\n            if finite_heat.size:\n                all_heat_vals.append(finite_heat)\n\n            side_stats = {}\n            for side in (\'left\', \'right\', \'midline\', \'unknown\'):\n                vals = roi_df.loc[roi_df[\'midline_side\'] == side, \'activity_metric\'].to_numpy(dtype=float)\n                vals = vals[np.isfinite(vals)]\n                side_stats[side] = {\n                    \'n\': int(vals.size),\n                    \'median\': float(np.nanmedian(vals)) if vals.size else np.nan,\n                }\n\n            summary_rows.append({\n                \'plane\': int(plane_idx),\n                \'n_rois\': int(len(roi_df)),\n                \'n_valid_metric\': int(np.isfinite(roi_df[\'activity_metric\']).sum()),\n                \'activity_source\': ACT_QC_ACTIVITY_SOURCE,\n                \'map_mode\': ACT_QC_MAP_MODE,\n                \'n_events_used\': int(n_events_used) if n_events_used >= 0 else np.nan,\n                \'stim_frame_fraction\': float(np.mean(stim_mask)) if ACT_QC_ACTIVITY_SOURCE == \'stim\' else np.nan,\n                \'median_metric_all\': float(np.nanmedian(roi_df[\'activity_metric\'])) if np.isfinite(roi_df[\'activity_metric\']).any() else np.nan,\n                \'left_n\': int(side_stats[\'left\'][\'n\']),\n                \'right_n\': int(side_stats[\'right\'][\'n\']),\n                \'left_median\': float(side_stats[\'left\'][\'median\']),\n                \'right_median\': float(side_stats[\'right\'][\'median\']),\n                \'midline_n\': int(side_stats[\'midline\'][\'n\']),\n                \'unknown_n\': int(side_stats[\'unknown\'][\'n\']),\n            })\n\n            plane_payload.append({\n                \'plane\': int(plane_idx),\n                \'labels\': labels,\n                \'roi_df\': roi_df,\n                \'heat\': heat,\n                \'ref\': ref_vis,\n                \'side_stats\': side_stats,\n                \'n_events_used\': int(n_events_used),\n                \'stim_frac\': float(np.mean(stim_mask)) if ACT_QC_ACTIVITY_SOURCE == \'stim\' else np.nan,\n            })\n\n        if not plane_payload:\n            print(\'[56f-qc-activity] No plane payload generated.\')\n            FIG_56F_QC_ACTIVITY_LAST = None\n            FIG_56F_QC_ACTIVITY_RGBA = None\n            activity_qc_summary_56f = pd.DataFrame()\n            activity_qc_roi_56f = pd.DataFrame()\n        else:\n            finite_all = np.concatenate(all_heat_vals) if all_heat_vals else np.array([], dtype=np.float32)\n            if finite_all.size:\n                vmin = float(np.nanpercentile(finite_all, ACT_QC_VMIN_PERCENTILE))\n                vmax = float(np.nanpercentile(finite_all, ACT_QC_VMAX_PERCENTILE))\n                if not np.isfinite(vmin):\n                    vmin = float(np.nanmin(finite_all))\n                if not np.isfinite(vmax):\n                    vmax = float(np.nanmax(finite_all))\n                if vmax <= vmin:\n                    vmax = vmin + 1e-6\n            else:\n                vmin, vmax = 0.0, 1.0\n\n            n = len(plane_payload)\n            ncols = max(1, min(int(ACT_QC_NCOLS), n))\n            nrows = int(math.ceil(n / float(ncols)))\n            fig, axes = plt.subplots(\n                nrows,\n                ncols,\n                figsize=(ACT_QC_FIG_W_PER_COL * ncols + ACT_QC_COLORBAR_EXTRA_W_IN, ACT_QC_FIG_H_PER_ROW * nrows),\n                squeeze=False,\n            )\n            axes = axes.ravel()\n\n            last_im = None\n            for ax, item in zip(axes, plane_payload):\n                ax.imshow(item[\'ref\'], cmap=\'gray\', alpha=1.0, interpolation=ACT_QC_REF_INTERPOLATION)\n                last_im = ax.imshow(item[\'heat\'], cmap=ACT_QC_CMAP, alpha=ACT_QC_HEAT_ALPHA, vmin=vmin, vmax=vmax, interpolation=ACT_QC_HEAT_INTERPOLATION)\n\n                p = _params_for_plane(int(item[\'plane\']))\n                if isinstance(p, dict):\n                    try:\n                        (x1, y1), (x2, y2) = _line_segment(float(p[\'x0\']), float(p[\'y0\']), float(p[\'theta_deg\']), item[\'heat\'].shape)\n                        xd, yd = _xy_to_display_space_act(int(item[\'plane\']), [x1, x2, float(p[\'x0\'])], [y1, y2, float(p[\'y0\'])])\n                        ax.plot([xd[0], xd[1]], [yd[0], yd[1]], color=\'yellow\', linewidth=1.2)\n                        ax.scatter([xd[2]], [yd[2]], s=9, c=\'yellow\')\n                    except Exception:\n                        pass\n\n                if ACT_QC_SHOW_CENTROIDS:\n                    r = item[\'roi_df\']\n                    good = r[np.isfinite(r[\'activity_metric\'])]\n                    if not good.empty:\n                        ax.scatter(\n                            good[\'x\'].to_numpy(dtype=float),\n                            good[\'y\'].to_numpy(dtype=float),\n                            c=\'white\',\n                            s=ACT_QC_CENTROID_SIZE,\n                            alpha=0.6,\n                            linewidths=0,\n                        )\n\n                st = item[\'side_stats\']\n                title = (\n                    f"p{item[\'plane\']} | Lmed={st[\'left\'][\'median\']:.2f} (n={st[\'left\'][\'n\']}) "\n                    f"Rmed={st[\'right\'][\'median\']:.2f} (n={st[\'right\'][\'n\']})"\n                )\n                if ACT_QC_ACTIVITY_SOURCE == \'stim\':\n                    title += f"\\nev={item[\'n_events_used\']} frames={100.0*item[\'stim_frac\']:.1f}%"\n                if ACT_QC_ZOOM_TO_ENSEMBLE:\n                    _bbox = _ensemble_bbox_from_labels(item[\'labels\'])\n                    _apply_bbox_zoom(ax, _bbox, item[\'heat\'].shape, pad_frac=ACT_QC_ZOOM_PAD_FRAC, pad_px=ACT_QC_ZOOM_PAD_PX)\n\n                ax.set_title(title)\n                ax.axis(\'off\')\n\n            for ax in axes[n:]:\n                ax.axis(\'off\')\n\n            if last_im is not None:\n                cbar = fig.colorbar(last_im, ax=axes[:n], fraction=ACT_QC_COLORBAR_FRACTION, pad=ACT_QC_COLORBAR_PAD)\n                cbar.set_label(f"activity ({ACT_QC_STAT}{\' z\' if ACT_QC_USE_ZSCORE else \'\'})")\n\n            fig.suptitle(\n                f"Strong stimulus-related activity occupies coherent regions in functional space | source={ACT_QC_ACTIVITY_SOURCE} | "\n                f"stat={ACT_QC_STAT} | map={ACT_QC_MAP_MODE} | zscore={ACT_QC_USE_ZSCORE} | midline={_mid_source} | space={_MIDLINE_SPACE_ACT}"\n            )\n            fig.tight_layout(rect=[0, 0, ACT_QC_LAYOUT_RIGHT, 0.95])\n            FIG_56F_QC_ACTIVITY_LAST = fig\n            try:\n                fig.canvas.draw()\n                FIG_56F_QC_ACTIVITY_RGBA = np.asarray(fig.canvas.buffer_rgba()).copy()\n            except Exception:\n                FIG_56F_QC_ACTIVITY_RGBA = None\n            plt.show()\n\n            roi_all = pd.concat([p[\'roi_df\'] for p in plane_payload], ignore_index=True)\n            summary_df = pd.DataFrame(summary_rows).sort_values(\'plane\').reset_index(drop=True)\n            activity_qc_roi_56f = roi_all.copy()\n            activity_qc_summary_56f = summary_df.copy()\n\n            if summary_df.empty:\n                print(\'[56f-qc-activity] Summary is empty.\')\n            else:\n                try:\n                    display(summary_df)\n                except Exception:\n                    print(summary_df.to_string(index=False))\n\n            # Optional second figure: activity map restricted to [56f] analysis subset (functional-HCR linked ROIs).\n            def _load_analysis_keys_for_activity():\n                if isinstance(_pairs_with_midline_side_56f_act, pd.DataFrame) and not _pairs_with_midline_side_56f_act.empty:\n                    _k = _pairs_with_midline_side_56f_act[[\'plane\', \'func_label\']].copy()\n                    _k[\'plane\'] = pd.to_numeric(_k[\'plane\'], errors=\'coerce\')\n                    _k[\'func_label\'] = pd.to_numeric(_k[\'func_label\'], errors=\'coerce\')\n                    _k = _k.dropna().astype({\'plane\': int, \'func_label\': int}).drop_duplicates()\n                    return _k, \'pairs_with_midline_side_56f\'\n\n                _conf_csv_override = _conf_func_csv_analysis_act\n                _conf_csv_base = _conf_csv_override if _conf_csv_override else (_conf_func_csv_act if _conf_func_csv_act else str(OUT_REG / \'conf_to_func_pairs.csv\'))\n                conf_csv = Path(str(_conf_csv_base))\n                if conf_csv.exists():\n                    try:\n                        _p = pd.read_csv(conf_csv)\n                        if \'fish_id\' in _p.columns:\n                            _p = _p[_p[\'fish_id\'].astype(str) == str(FISH_ID)].copy()\n                        if \'is_selected_for_analysis\' in _p.columns:\n                            sel = _p[\'is_selected_for_analysis\']\n                            if pd.api.types.is_bool_dtype(sel):\n                                _p = _p[sel.fillna(False)].copy()\n                            elif pd.api.types.is_numeric_dtype(sel):\n                                _p = _p[sel.fillna(0).astype(float) != 0].copy()\n                            else:\n                                _p = _p[sel.astype(str).str.strip().str.lower().isin({\'1\',\'true\',\'t\',\'yes\',\'y\'})].copy()\n                        if {\'plane\', \'func_label\'}.issubset(_p.columns):\n                            _k = _p[[\'plane\', \'func_label\']].copy()\n                            _k[\'plane\'] = pd.to_numeric(_k[\'plane\'], errors=\'coerce\')\n                            _k[\'func_label\'] = pd.to_numeric(_k[\'func_label\'], errors=\'coerce\')\n                            _k = _k.dropna().astype({\'plane\': int, \'func_label\': int}).drop_duplicates()\n                            return _k, f\'conf_to_func_pairs ({conf_csv.name})\'\n                    except Exception:\n                        pass\n                return pd.DataFrame(columns=[\'plane\',\'func_label\']), \'none\'\n\n            def _activity_heat_from_roi_df(_labels, _roi_df):\n                use_rows = _roi_df[[\'func_label\', \'x\', \'y\', \'activity_metric\']].dropna(subset=[\'activity_metric\']).copy()\n\n                if ACT_QC_MAP_MODE == \'labels\':\n                    max_label = int(np.max(_labels)) if _labels.size else 0\n                    if max_label <= 0:\n                        return np.full(_labels.shape, np.nan, dtype=np.float32)\n                    lut = np.full(max_label + 1, np.nan, dtype=np.float32)\n                    if not use_rows.empty:\n                        lid = use_rows[\'func_label\'].astype(int).to_numpy()\n                        lval = use_rows[\'activity_metric\'].to_numpy(dtype=np.float32)\n                        valid_l = (lid >= 0) & (lid <= max_label) & np.isfinite(lval)\n                        lut[lid[valid_l]] = lval[valid_l]\n                    metric_img = lut[_labels.astype(np.int32, copy=False)]\n                    metric_img[_labels == 0] = np.nan\n                else:\n                    accum = np.zeros(_labels.shape, dtype=np.float32)\n                    weight = np.zeros(_labels.shape, dtype=np.float32)\n                    if not use_rows.empty:\n                        xv = np.rint(use_rows[\'x\'].to_numpy(dtype=float)).astype(int)\n                        yv = np.rint(use_rows[\'y\'].to_numpy(dtype=float)).astype(int)\n                        vv = use_rows[\'activity_metric\'].to_numpy(dtype=np.float32)\n                        inb = (xv >= 0) & (xv < _labels.shape[1]) & (yv >= 0) & (yv < _labels.shape[0]) & np.isfinite(vv)\n                        if np.any(inb):\n                            np.add.at(accum, (yv[inb], xv[inb]), vv[inb])\n                            np.add.at(weight, (yv[inb], xv[inb]), 1.0)\n                    metric_img = np.divide(\n                        accum,\n                        weight,\n                        out=np.full(_labels.shape, np.nan, dtype=np.float32),\n                        where=weight > 0,\n                    )\n\n                if ACT_QC_SMOOTH_SIGMA_PX > 0:\n                    num = gaussian(np.nan_to_num(metric_img, nan=0.0), sigma=ACT_QC_SMOOTH_SIGMA_PX, preserve_range=True)\n                    den = gaussian(np.isfinite(metric_img).astype(np.float32), sigma=ACT_QC_SMOOTH_SIGMA_PX, preserve_range=True)\n                    return np.divide(num, den, out=np.full_like(num, np.nan, dtype=np.float32), where=den > 1e-6)\n                return metric_img.astype(np.float32)\n\n            if ACT_QC_PLOT_ANALYSIS_SUBSET:\n                subset_keys_df, subset_src = _load_analysis_keys_for_activity()\n                print(f"[56f-qc-activity] analysis subset source: {subset_src}; n_keys={len(subset_keys_df)}")\n\n                if subset_keys_df.empty:\n                    FIG_56F_QC_ACTIVITY_SUBSET_LAST = None\n                    FIG_56F_QC_ACTIVITY_SUBSET_RGBA = None\n                    activity_qc_roi_subset_56f = pd.DataFrame()\n                    activity_qc_summary_subset_56f = pd.DataFrame()\n                    print(\'[56f-qc-activity] No analysis subset keys found; skipping subset figure.\')\n                else:\n                    roi_subset_all = roi_all.merge(subset_keys_df, on=[\'plane\', \'func_label\'], how=\'inner\')\n                    if roi_subset_all.empty:\n                        FIG_56F_QC_ACTIVITY_SUBSET_LAST = None\n                        FIG_56F_QC_ACTIVITY_SUBSET_RGBA = None\n                        activity_qc_roi_subset_56f = pd.DataFrame()\n                        activity_qc_summary_subset_56f = pd.DataFrame()\n                        print(\'[56f-qc-activity] Subset merge is empty; skipping subset figure.\')\n                    else:\n                        subset_payload = []\n                        subset_heat_vals = []\n                        subset_summary_rows = []\n\n                        for item in plane_payload:\n                            pidx = int(item[\'plane\'])\n                            labels_p = item[\'labels\']\n                            ref_p = item[\'ref\']\n                            roi_sub = roi_subset_all[roi_subset_all[\'plane\'].astype(int) == pidx].copy()\n                            heat_sub = _activity_heat_from_roi_df(labels_p, roi_sub) if not roi_sub.empty else np.full(labels_p.shape, np.nan, dtype=np.float32)\n\n                            finite_sub = heat_sub[np.isfinite(heat_sub)]\n                            if finite_sub.size:\n                                subset_heat_vals.append(finite_sub)\n\n                            side_sub = {}\n                            for side in (\'left\', \'right\', \'midline\', \'unknown\'):\n                                vals = roi_sub.loc[roi_sub[\'midline_side\'] == side, \'activity_metric\'].to_numpy(dtype=float) if not roi_sub.empty else np.array([], dtype=float)\n                                vals = vals[np.isfinite(vals)] if vals.size else vals\n                                side_sub[side] = {\n                                    \'n\': int(vals.size),\n                                    \'median\': float(np.nanmedian(vals)) if vals.size else np.nan,\n                                }\n\n                            subset_summary_rows.append({\n                                \'plane\': pidx,\n                                \'n_rois_subset\': int(len(roi_sub)),\n                                \'n_valid_metric_subset\': int(np.isfinite(roi_sub[\'activity_metric\']).sum()) if not roi_sub.empty else 0,\n                                \'activity_source\': ACT_QC_ACTIVITY_SOURCE,\n                                \'map_mode\': ACT_QC_MAP_MODE,\n                                \'subset_source\': subset_src,\n                                \'n_events_used\': int(item[\'n_events_used\']) if item.get(\'n_events_used\', -1) >= 0 else np.nan,\n                                \'stim_frame_fraction\': float(item.get(\'stim_frac\', np.nan)) if ACT_QC_ACTIVITY_SOURCE == \'stim\' else np.nan,\n                                \'median_metric_subset\': float(np.nanmedian(roi_sub[\'activity_metric\'])) if (not roi_sub.empty and np.isfinite(roi_sub[\'activity_metric\']).any()) else np.nan,\n                                \'left_n\': int(side_sub[\'left\'][\'n\']),\n                                \'right_n\': int(side_sub[\'right\'][\'n\']),\n                                \'left_median\': float(side_sub[\'left\'][\'median\']),\n                                \'right_median\': float(side_sub[\'right\'][\'median\']),\n                                \'midline_n\': int(side_sub[\'midline\'][\'n\']),\n                                \'unknown_n\': int(side_sub[\'unknown\'][\'n\']),\n                            })\n\n                            subset_payload.append({\n                                \'plane\': pidx,\n                                \'labels\': labels_p,\n                                \'ref\': ref_p,\n                                \'roi_df\': roi_sub,\n                                \'heat\': heat_sub,\n                                \'side_stats\': side_sub,\n                                \'n_events_used\': int(item.get(\'n_events_used\', -1)),\n                                \'stim_frac\': float(item.get(\'stim_frac\', np.nan)),\n                            })\n\n                        finite_subset_all = np.concatenate(subset_heat_vals) if subset_heat_vals else np.array([], dtype=np.float32)\n                        if finite_subset_all.size:\n                            vmin_s = float(np.nanpercentile(finite_subset_all, ACT_QC_VMIN_PERCENTILE))\n                            vmax_s = float(np.nanpercentile(finite_subset_all, ACT_QC_VMAX_PERCENTILE))\n                            if not np.isfinite(vmin_s):\n                                vmin_s = float(np.nanmin(finite_subset_all))\n                            if not np.isfinite(vmax_s):\n                                vmax_s = float(np.nanmax(finite_subset_all))\n                            if vmax_s <= vmin_s:\n                                vmax_s = vmin_s + 1e-6\n                        else:\n                            vmin_s, vmax_s = 0.0, 1.0\n\n                        n2 = len(subset_payload)\n                        ncols2 = max(1, min(int(ACT_QC_NCOLS), n2))\n                        nrows2 = int(math.ceil(n2 / float(ncols2)))\n                        fig2, axes2 = plt.subplots(\n                            nrows2,\n                            ncols2,\n                            figsize=(ACT_QC_FIG_W_PER_COL * ncols2 + ACT_QC_COLORBAR_EXTRA_W_IN, ACT_QC_FIG_H_PER_ROW * nrows2),\n                            squeeze=False,\n                        )\n                        axes2 = axes2.ravel()\n\n                        last_im2 = None\n                        for ax, item in zip(axes2, subset_payload):\n                            ax.imshow(item[\'ref\'], cmap=\'gray\', alpha=1.0, interpolation=ACT_QC_REF_INTERPOLATION)\n                            last_im2 = ax.imshow(item[\'heat\'], cmap=ACT_QC_CMAP, alpha=ACT_QC_HEAT_ALPHA, vmin=vmin_s, vmax=vmax_s, interpolation=ACT_QC_HEAT_INTERPOLATION)\n\n                            p = _params_for_plane(int(item[\'plane\']))\n                            if isinstance(p, dict):\n                                try:\n                                    (x1, y1), (x2, y2) = _line_segment(float(p[\'x0\']), float(p[\'y0\']), float(p[\'theta_deg\']), item[\'heat\'].shape)\n                                    xd, yd = _xy_to_display_space_act(int(item[\'plane\']), [x1, x2, float(p[\'x0\'])], [y1, y2, float(p[\'y0\'])])\n                                    ax.plot([xd[0], xd[1]], [yd[0], yd[1]], color=\'yellow\', linewidth=1.2)\n                                    ax.scatter([xd[2]], [yd[2]], s=9, c=\'yellow\')\n                                except Exception:\n                                    pass\n\n                            if ACT_QC_SHOW_CENTROIDS:\n                                r = item[\'roi_df\']\n                                good = r[np.isfinite(r[\'activity_metric\'])] if not r.empty else r\n                                if not good.empty:\n                                    ax.scatter(\n                                        good[\'x\'].to_numpy(dtype=float),\n                                        good[\'y\'].to_numpy(dtype=float),\n                                        c=\'white\',\n                                        s=ACT_QC_CENTROID_SIZE,\n                                        alpha=0.6,\n                                        linewidths=0,\n                                    )\n\n                            st = item[\'side_stats\']\n                            title2 = (\n                                f"p{item[\'plane\']} | Lmed={st[\'left\'][\'median\']:.2f} (n={st[\'left\'][\'n\']}) "\n                                f"Rmed={st[\'right\'][\'median\']:.2f} (n={st[\'right\'][\'n\']})"\n                            )\n                            if ACT_QC_ACTIVITY_SOURCE == \'stim\':\n                                title2 += f"\\nev={item[\'n_events_used\']} frames={100.0*item[\'stim_frac\']:.1f}%"\n                            if ACT_QC_ZOOM_TO_ENSEMBLE:\n                                _bbox = _ensemble_bbox_from_labels(item[\'labels\'])\n                                _apply_bbox_zoom(ax, _bbox, item[\'heat\'].shape, pad_frac=ACT_QC_ZOOM_PAD_FRAC, pad_px=ACT_QC_ZOOM_PAD_PX)\n                            ax.set_title(title2)\n                            ax.axis(\'off\')\n\n                        for ax in axes2[n2:]:\n                            ax.axis(\'off\')\n\n                        if last_im2 is not None:\n                            cbar2 = fig2.colorbar(last_im2, ax=axes2[:n2], fraction=ACT_QC_COLORBAR_FRACTION, pad=ACT_QC_COLORBAR_PAD)\n                            cbar2.set_label(f"activity ({ACT_QC_STAT}{\' z\' if ACT_QC_USE_ZSCORE else \'\'})")\n\n                        fig2.suptitle(\n                            f"The analysis subset preserves the main spatial activity hotspots | source={ACT_QC_ACTIVITY_SOURCE} | "\n                            f"stat={ACT_QC_STAT} | map={ACT_QC_MAP_MODE} | zscore={ACT_QC_USE_ZSCORE} | midline={_mid_source} | space={_MIDLINE_SPACE_ACT}"\n                        )\n                        fig2.tight_layout(rect=[0, 0, ACT_QC_LAYOUT_RIGHT, 0.95])\n                        FIG_56F_QC_ACTIVITY_SUBSET_LAST = fig2\n                        try:\n                            fig2.canvas.draw()\n                            FIG_56F_QC_ACTIVITY_SUBSET_RGBA = np.asarray(fig2.canvas.buffer_rgba()).copy()\n                        except Exception:\n                            FIG_56F_QC_ACTIVITY_SUBSET_RGBA = None\n                        plt.show()\n\n                        summary_subset_df = pd.DataFrame(subset_summary_rows).sort_values(\'plane\').reset_index(drop=True)\n                        activity_qc_roi_subset_56f = roi_subset_all.copy()\n                        activity_qc_summary_subset_56f = summary_subset_df.copy()\n\n                        if summary_subset_df.empty:\n                            print(\'[56f-qc-activity] Subset summary is empty.\')\n                        else:\n                            try:\n                                display(summary_subset_df)\n                            except Exception:\n                                print(summary_subset_df.to_string(index=False))\n\n\n\n\n',
    '56g': '# [56g]\n# BPI/activity diagnostics (2x2):\n# 1) Bout vs Continuous response plane\n# 2) BPI vs activity magnitude\n# 3) Activity-binned |BPI|\n# 4) Activity distributions by BPI/response class\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nfrom pathlib import Path\nfrom codeants_2pf_hcr import SingleFishBpiDiagnosticsConfig, prepare_single_fish_bpi_diagnostics_stage\nfrom codeants_2pf_hcr import SingleFishBpiDiagnosticsConfig, prepare_single_fish_bpi_diagnostics_stage\n\ntry:\n    REQUIRE_FISH_STATE = require_fish_state\nexcept NameError:\n    REQUIRE_FISH_STATE = None\nif callable(REQUIRE_FISH_STATE) and not REQUIRE_FISH_STATE():\n    raise SystemExit\n\ntry:\n    BPI_FISH_ID_LOCAL = BPI_FISH_ID\nexcept NameError:\n    BPI_FISH_ID_LOCAL = None\ntry:\n    bpi_cells_df_local = bpi_cells_df\nexcept NameError:\n    bpi_cells_df_local = None\n\nif BPI_FISH_ID_LOCAL not in (None, FISH_ID):\n    print(\'[56g] bpi_cells_df from different fish; run [56h].\')\n    raise SystemExit\n\ntry:\n    _run_config_56g = RUN_CONFIG if isinstance(RUN_CONFIG, dict) else {}\nexcept NameError:\n    _run_config_56g = {}\n\nBPI_ZERO_BAND = FUNC_ACTIVITY_BPI_ZERO_BAND\nBPI_POINT_ALPHA = 0.7\nBPI_POINT_SIZE = 18.0\nBPI_N_ACTIVITY_BINS = 6\nBPI_INDEX_COL = \'bpi\'\nRESPONSE_LOW = \'low activity\'\nRESPONSE_UNAVAILABLE = \'response unavailable\'\nRESPONSE_SUMMARY_RESPONSIVE = \'Responsive neurons\'\nRESPONSE_SUMMARY_LOW = \'Low activity\'\nRESPONSE_SUMMARY_UNAVAILABLE = \'Response unavailable\'\nMASTER_DETAIL_CSV = OUT_REG / \'functional_roi_activity_identity.csv\'\n\n\n\nDEFAULT_GENE_ORDER = [\'sst1.1\', \'sst1.2\', \'npy\', \'tac3b\', \'pth2\', \'cfos\', \'cort\']\nDEFAULT_GENE_COLORS = {\n    \'sst1.1\': \'#d62728\',\n    \'sst1.2\': \'#d61ad2\',\n    \'npy\': \'#1f9d55\',\n    \'tac3b\': \'#ffd400\',\n    \'pth2\': \'#00bcd4\',\n    \'cfos\': \'#ff7f0e\',\n    \'cort\': \'#8c564b\',\n}\nGENE_ORDER = list(_run_config_56g.get(\'GENE_ORDER\', DEFAULT_GENE_ORDER))\nGENE_COLORS = dict(DEFAULT_GENE_COLORS)\ntry:\n    _gc = _run_config_56g.get(\'GENE_COLORS\', {})\n    if isinstance(_gc, dict):\n        GENE_COLORS.update(_gc)\nexcept Exception:\n    pass\n\nif BPI_N_ACTIVITY_BINS < 2:\n    BPI_N_ACTIVITY_BINS = 2\n\nif bpi_cells_df_local is None or bpi_cells_df_local.empty:\n    print(\'[56g] bpi_cells_df missing/empty; run [56h] first.\')\nelse:\n    try:\n        _bpi_diag_56g = prepare_single_fish_bpi_diagnostics_stage(\n            bpi_cells_df_local,\n            fish_id=str(FISH_ID),\n            master_detail_csv=MASTER_DETAIL_CSV,\n            config=SingleFishBpiDiagnosticsConfig(\n                bpi_index_col=BPI_INDEX_COL,\n                zero_band=float(BPI_ZERO_BAND),\n                n_activity_bins=int(BPI_N_ACTIVITY_BINS),\n                response_low=RESPONSE_LOW,\n                response_unavailable=RESPONSE_UNAVAILABLE,\n                response_summary_responsive=RESPONSE_SUMMARY_RESPONSIVE,\n                response_summary_low=RESPONSE_SUMMARY_LOW,\n                response_summary_unavailable=RESPONSE_SUMMARY_UNAVAILABLE,\n            ),\n        )\n    except RuntimeError as exc:\n        print(str(exc))\n        raise SystemExit\n\n    globals().update(_bpi_diag_56g[\'bindings\'])\n    for _line in _bpi_diag_56g[\'log_lines\']:\n        print(_line)\n\n    df = _bpi_diag_56g[\'df\'].copy()\n    binned_df = _bpi_diag_56g[\'binned_df\'].copy()\n    bpi_activity_breakdown_df = _bpi_diag_56g[\'breakdown_df\'].copy()\n    activity_label = _bpi_diag_56g[\'activity_label\']\n    bout_col = _bpi_diag_56g[\'bout_col\']\n    cont_col = _bpi_diag_56g[\'cont_col\']\n    BPI_INDEX_COL = _bpi_diag_56g[\'bpi_index_col\']\n    BPI_ZERO_BAND = float(_bpi_diag_56g[\'bpi_zero_band\'])\n    _summary_56g = _bpi_diag_56g[\'summary_counts\']\n    n_total = int(_summary_56g[\'n_total\'])\n    n_nz = int(_summary_56g[\'n_near_zero\'])\n    n_nz_low = int(_summary_56g[\'n_near_zero_low\'])\n    n_nz_resp = int(_summary_56g[\'n_near_zero_responsive\'])\n    n_nz_unavailable = int(_summary_56g[\'n_near_zero_response_unavailable\'])\n\n    genes_present = sorted(df[\'gene\'].dropna().astype(str).unique().tolist())\n    gene_order = [g for g in GENE_ORDER if g in genes_present] + [g for g in genes_present if g not in GENE_ORDER]\n\n    fig, axes = plt.subplots(2, 2, figsize=(14, 10.5))\n    ax_plane = axes[0, 0]\n    ax_quad = axes[0, 1]\n    ax_bins = axes[1, 0]\n    ax_group = axes[1, 1]\n\n    for gene in gene_order:\n        sub = df[df[\'gene\'].astype(str) == str(gene)]\n        if sub.empty:\n            continue\n        ax_plane.scatter(\n            sub[cont_col].to_numpy(dtype=float),\n            sub[bout_col].to_numpy(dtype=float),\n            s=BPI_POINT_SIZE,\n            alpha=BPI_POINT_ALPHA,\n            color=GENE_COLORS.get(gene, \'#777777\'),\n            edgecolors=\'none\',\n            label=str(gene),\n        )\n\n    sub_nz = df[df[\'is_bpi_near_zero\']]\n    if not sub_nz.empty:\n        ax_plane.scatter(\n            sub_nz[cont_col].to_numpy(dtype=float),\n            sub_nz[bout_col].to_numpy(dtype=float),\n            s=max(14.0, BPI_POINT_SIZE + 8.0),\n            facecolors=\'none\',\n            edgecolors=\'black\',\n            linewidths=0.8,\n            alpha=0.8,\n            label=\'|BPI| near zero\',\n        )\n\n    combo = np.concatenate([np.abs(df[cont_col].to_numpy(dtype=float)), np.abs(df[bout_col].to_numpy(dtype=float))])\n    vmax = float(np.nanpercentile(combo, 99)) if combo.size else 1.0\n    if not np.isfinite(vmax) or vmax <= 0:\n        vmax = 1.0\n    ax_plane.plot([-vmax, vmax], [-vmax, vmax], linestyle=\'--\', color=\'black\', linewidth=1.0, alpha=0.8)\n    ax_plane.axhline(0.0, color=\'#444444\', linewidth=0.8)\n    ax_plane.axvline(0.0, color=\'#444444\', linewidth=0.8)\n    ax_plane.set_xlim(-vmax, vmax)\n    ax_plane.set_ylim(-vmax, vmax)\n    ax_plane.set_xlabel(f\'Mean continuous response ({activity_label})\')\n    ax_plane.set_ylabel(f\'Mean bout response ({activity_label})\')\n    ax_plane.set_title(\'Bout and continuous responses reveal distinct tuning\')\n    handles, labels = ax_plane.get_legend_handles_labels()\n    if handles:\n        ax_plane.legend(handles, labels, fontsize=7, ncol=2, loc=\'lower right\')\n\n    for gene in gene_order:\n        sub = df[df[\'gene\'].astype(str) == str(gene)]\n        if sub.empty:\n            continue\n        ax_quad.scatter(\n            sub[\'activity_mag\'].to_numpy(dtype=float),\n            sub[\'bpi_metric\'].to_numpy(dtype=float),\n            s=BPI_POINT_SIZE,\n            alpha=BPI_POINT_ALPHA,\n            color=GENE_COLORS.get(gene, \'#777777\'),\n            edgecolors=\'none\',\n        )\n    if not sub_nz.empty:\n        ax_quad.scatter(\n            sub_nz[\'activity_mag\'].to_numpy(dtype=float),\n            sub_nz[\'bpi_metric\'].to_numpy(dtype=float),\n            s=max(14.0, BPI_POINT_SIZE + 8.0),\n            facecolors=\'none\',\n            edgecolors=\'black\',\n            linewidths=0.8,\n            alpha=0.8,\n        )\n    ax_quad.axhline(0.0, color=\'black\', linestyle=\'--\', linewidth=1.0)\n    ax_quad.axhline(float(BPI_ZERO_BAND), color=\'#444444\', linestyle=\':\', linewidth=1.0)\n    ax_quad.axhline(-float(BPI_ZERO_BAND), color=\'#444444\', linestyle=\':\', linewidth=1.0)\n    ax_quad.set_xlabel(f\'Activity magnitude = mean(|Bout|, |Continuous|) {activity_label}\')\n    ax_quad.set_ylabel(f\'BPI ({BPI_INDEX_COL})\')\n    ax_quad.set_ylim(-1.02, 1.02)\n    ax_quad.set_xlim(left=0)\n    ax_quad.set_title(\'Strong tuning is not explained by weak activity alone\')\n    ax_quad.text(\n        0.02,\n        0.98,\n        f"Near-zero BPI: {n_nz}/{n_total}\\nlow activity: {n_nz_low}\\nresponsive: {n_nz_resp}\\nresponse unavailable: {n_nz_unavailable}",\n        transform=ax_quad.transAxes,\n        ha=\'left\',\n        va=\'top\',\n        fontsize=8.5,\n        bbox=dict(facecolor=\'white\', edgecolor=\'#cccccc\', alpha=0.85),\n    )\n\n    if not binned_df.empty:\n        ax_bins.plot(binned_df[\'activity_mid\'], binned_df[\'median_abs_bpi\'], marker=\'o\', linewidth=1.5, color=\'#1f77b4\')\n        for row in binned_df.itertuples(index=False):\n            ax_bins.text(float(row.activity_mid), float(row.median_abs_bpi), f"n={int(row.n)}", fontsize=8, ha=\'left\', va=\'bottom\')\n    ax_bins.set_xlabel(f\'Activity magnitude ({activity_label})\')\n    ax_bins.set_ylabel(\'Median |BPI|\')\n    ax_bins.set_title(\'Tuning strength remains visible across activity levels\')\n    ax_bins.grid(alpha=0.25)\n\n    order = [\n        \'near-zero BPI + low activity\',\n        \'near-zero BPI + responsive\',\n        \'near-zero BPI + response unavailable\',\n        \'non-zero BPI\',\n    ]\n    grouped = [df.loc[df[\'interpretation\'] == label, \'activity_mag\'].to_numpy(dtype=float) for label in order]\n    valid = [i for i, arr in enumerate(grouped) if arr.size]\n    if valid:\n        shown = [grouped[i] for i in valid]\n        labels_shown = [order[i] for i in valid]\n        parts = ax_group.violinplot(shown, showmeans=False, showmedians=True, showextrema=False)\n        for body in parts[\'bodies\']:\n            body.set_facecolor(\'#9ecae1\')\n            body.set_edgecolor(\'black\')\n            body.set_alpha(0.8)\n        ax_group.set_xticks(range(1, len(labels_shown) + 1))\n        ax_group.set_xticklabels(labels_shown, rotation=15, ha=\'right\')\n    ax_group.set_ylabel(f\'Activity magnitude ({activity_label})\')\n    ax_group.set_title(\'Low activity and weak tuning occupy different regimes\')\n    ax_group.grid(axis=\'y\', alpha=0.25)\n\n    fig.suptitle(\'Stimulus bias cannot be reduced to activity magnitude alone\', fontsize=14)\n    fig.tight_layout(rect=[0, 0, 1, 0.97])\n\n    bpi_activity_df = df.copy()\n    bpi_activity_bins_df = binned_df.copy()\n    FIG_BPI_ACTIVITY_LAST = fig\n    BPI_ACTIVITY_FISH_ID = FISH_ID\n    try:\n        fig.canvas.draw()\n        FIG_BPI_ACTIVITY_RGBA = np.asarray(fig.canvas.buffer_rgba()).copy()\n    except Exception:\n        FIG_BPI_ACTIVITY_RGBA = None\n\n    out_dir = OUT_QA if \'OUT_QA\' in locals() else OUT_REG if \'OUT_REG\' in locals() else OUTDIR\n    out_dir = Path(out_dir)\n    out_dir.mkdir(parents=True, exist_ok=True)\n    out_path = out_dir / \'bpi_vs_activity_2x2.png\'\n    fig.savefig(out_path, dpi=220, bbox_inches=\'tight\')\n    FIG_BPI_ACTIVITY_PATH = str(out_path)\n    print(f\'[56g] Saved diagnostic figure to {out_path}\')\n\n    plt.show()\n',
    '56h': '# [56h]\n# Unified summary: compute [56f]-style and [56e]-style data, then render one combined figure.\n# Set SHOW_56H_COMPONENT_PLOTS=True to also display intermediate component figures.\nSHOW_56H_COMPONENT_PLOTS = False\n\n# --- Embedded [56f] logic (formerly standalone [56f]) ---\n# Condensed stimulus-locked traces as a 2x2 matrix:\n#   rows: contra vs ipsi\n#   cols: bout-like vs continuous\n# Bilateral conditions are intentionally excluded.\n# Ipsi/contra is assigned per cell from midline_side (left/right), not a global side flag.\ntry:\n    _require_fish_state_56h = require_fish_state\nexcept NameError:\n    _require_fish_state_56h = None\nif _require_fish_state_56h is not None and (not _require_fish_state_56h()):\n    raise SystemExit\n\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nimport matplotlib.colors as mcolors\nimport matplotlib as mpl\nimport re\nimport json\nfrom pathlib import Path\nfrom skimage.measure import regionprops_table\nfrom matplotlib.patches import Patch\nfrom codeants_2pf_hcr import prepare_pairs_for_unique_cells\nfrom codeants_2pf_hcr.stimulus import build_prestim_baseline_windows, combine_segments, compute_zscore_stats, effective_motion_window\n\nA4_WIDTH_IN = 11.69\nFIG_WIDTH_DEFAULT_IN = A4_WIDTH_IN * 0.8\nFIG_SAVE_DPI = 300\nmpl.rcParams[\'font.family\'] = \'sans-serif\'\nmpl.rcParams[\'font.sans-serif\'] = [\'Aptos\', \'Aptos (Body)\', \'Calibri\', \'Arial\', \'DejaVu Sans\']\n\n# Deterministic current-fish inputs only: explicit upstream state or fish-scoped files on disk.\ntry:\n    _suite2p_by_ref_idx_56h = suite2p_by_ref_idx\n    _s2p_fish_id = SUITE2P_FISH_ID\nexcept NameError:\n    print(\'[stim-56f] Suite2p data not loaded; run [23a] first.\')\n    raise SystemExit\ntry:\n    _df_stim_56h = df_stim\n    _df_evt_56h = df_evt\n    _frame_rate_56h = FRAME_RATE\n    _df_stim_fish_id = DF_STIM_FISH_ID\nexcept NameError:\n    print(\'[stim-56f] Stimulus data not loaded; run [55] first.\')\n    raise SystemExit\nif _s2p_fish_id != FISH_ID:\n    print(\'[stim-56f] Suite2p data from different fish; run [23a].\')\n    raise SystemExit\nif _df_stim_fish_id != FISH_ID:\n    print(\'[stim-56f] Stimulus data from different fish; run [55].\')\n    raise SystemExit\n\ntry:\n    _plane_refs_56h = plane_refs\nexcept NameError:\n    _plane_refs_56h = []\ntry:\n    _tform_for_plane_helper_56h = _tform_for_plane\nexcept NameError:\n    _tform_for_plane_helper_56h = None\ntry:\n    _gene_from_mask_helper_56h = gene_from_mask\nexcept NameError:\n    _gene_from_mask_helper_56h = None\ntry:\n    _hcr_match_results_56h = hcr_match_results\nexcept NameError:\n    _hcr_match_results_56h = []\ntry:\n    _vox_anat_56h = VOX_ANAT if isinstance(VOX_ANAT, dict) else {}\nexcept NameError:\n    _vox_anat_56h = {}\n\n_conf_func_csv_56h = None\n_warp_results_56h = []\n_hcr_aligned_dir_56h = Path(FISH_DIR) / \'03_analysis\' / \'confocal\' / \'aligned\'\n\n\ndef _load_diameter_cache_56h():\n    candidates = []\n    try:\n        candidates.append(OUT_QA / \'diameters_df_all.pkl\')\n    except Exception:\n        pass\n    try:\n        candidates.append(OUTDIR / \'diameters_df_all.pkl\')\n    except Exception:\n        pass\n    seen = set()\n    for path in candidates:\n        path = Path(path)\n        if str(path) in seen:\n            continue\n        seen.add(str(path))\n        if not path.exists():\n            continue\n        try:\n            return pd.read_pickle(path)\n        except Exception:\n            continue\n    return None\n\n\ntry:\n    _anat_med_radius_xy_um_56h = float(ANAT_MED_RADIUS_XY_UM)\nexcept Exception:\n    _anat_med_radius_xy_um_56h = np.nan\ntry:\n    _anat_med_radius_z_um_56h = float(ANAT_MED_RADIUS_Z_UM)\nexcept Exception:\n    _anat_med_radius_z_um_56h = np.nan\n\n_df_all_filt_56h = None\n_df_all_56h = _load_diameter_cache_56h()\n\n# Force disk-backed midline parameters for the current fish; do not reuse stale in-memory tuning state.\n_midline_fish_id_56h = None\n_midline_json_path_56h = Path(OUT_REG) / \'midline_params_func_ref.json\'\n_midline_by_plane_56h = None\n_midline_pos_side_label_56h = None\n_midline_uncertain_band_56h = 0.0\n_midline_params_56h = None\n_annotate_midline_side_helper_56h = None\n\n# reset cached render buffers for downstream composites\nFIG_56F_LAST = None\nFIG_56F_RGBA = None\n# clear [56h] outputs to avoid stale reuse if this run exits early\nresults_stim_ipsi_contra = None\nresults_stim_ipsi_contra_per_cell = None\nsummary_df_stim_ipsi_contra = None\nsummary_df_stim_ipsi_contra_per_cell = None\nSTIM_IPSI_CONTRA_FISH_ID = None\nstim_events_unilateral_56f = None\npairs_with_midline_side_56f = None\npairs_56h_selected = None\npairs_high_conf = None\npanel_mode_durations_56f = {}\nstim_hc_match_filter_counts_56h = None\nbpi_trials_df = None\nbpi_cells_df = None\nbpi_gene_summary_df = None\nBPI_FISH_ID = None\nFIG_56H_FISH_ID = None\nFIG_56H_PER_GENE_TRACE = None\nFIG_56H_PER_GENE_TRACE_PATH = None\nFIG_56H_PER_GENE_TRACE_RGBA = None\ntry:\n    FISH_PLOTS_DIR_56H = Path(FISH_DIR) / \'04_plots\'\nexcept Exception:\n    FISH_PLOTS_DIR_56H = OUT_QA\npair_usage_summary_56h = pd.DataFrame(columns=[\'gene\', \'n_responsive_labels\', \'n_unique_rois\', \'n_reused_labels\'])\npair_label_count_map_56h = {}\n\nWINDOW_PRE_SEC = 20\nWINDOW_POST_SEC = 50\ntry:\n    RC = RUN_CONFIG if isinstance(RUN_CONFIG, dict) else {}\nexcept NameError:\n    RC = {}\nSTIM_ONSET_DELAY_SEC = float(RC.get(\'STIM_ONSET_DELAY_SEC\', 10.0))\nWINDOW_EDGE_POLICY = \'pad_nan\'\nMIN_VALID_POINTS_PER_SEG = 1\nZSCORE_MIN_BASELINE_POINTS = 200\nZSCORE_MIN_BASELINE_STD = 1e-6\nTARGET_STIM_TYPES = RC.get(\'TARGET_STIM_TYPES\', None)\nMIN_SEGMENTS = 3\nMIN_CELLS = 1\nSTRICT_PAIR_INTEGRITY = bool(RC.get(\'STRICT_PAIR_INTEGRITY\', True))\nALLOW_REUSED_ROIS_WITHIN_GENE = True\nUSE_SEM = True\nMAX_GENES = 12\nHIGH_CONF_ONLY = bool(RC.get(\'HIGH_CONF_ONLY\', False))\nPAIR_SCOPE_LABEL = \'high-confidence pairs\' if HIGH_CONF_ONLY else \'all matched pairs\'\nprint(f"[56h] HIGH_CONF_ONLY={HIGH_CONF_ONLY} ({PAIR_SCOPE_LABEL})")\n\ndef _format_gene_roi_usage_label_56h(gene, roi_n):\n    gene = str(gene)\n    roi_n = int(roi_n)\n    label_n = int(pair_label_count_map_56h.get(gene, roi_n))\n    if label_n != roi_n:\n        return f"{gene} (ROI n={roi_n}; labels={label_n})"\n    return f"{gene} (n={roi_n})"\ntry:\n    MATCH_POLICY_VERSION = str(HCR_ACTIVITY_MATCH_POLICY)\nexcept NameError:\n    MATCH_POLICY_VERSION = \'hcr_anat_first_local_geometry_response_v4_suite2p_gate\'\n\n# Legacy global-side fallback (used only if midline side cannot be resolved for a cell).\nIPSI_SIDE = \'L\'\nif IPSI_SIDE not in {\'L\', \'R\'}:\n    print(f"[stim-56f] invalid IPSI_SIDE={IPSI_SIDE}; using \'L\'")\n    IPSI_SIDE = \'L\'\nCONTRA_SIDE = \'R\' if IPSI_SIDE == \'L\' else \'L\'\nUSE_GLOBAL_IPSI_FALLBACK = False\n\nDEFAULT_GENE_ORDER = [\'sst1.1\', \'sst1.2\', \'npy\', \'tac3b\', \'pth2\', \'cfos\', \'cort\']\nDEFAULT_GENE_COLORS = {\n    \'sst1.1\': \'#d62728\',\n    \'sst1.2\': \'#d61ad2\',\n    \'npy\': \'#1f9d55\',\n    \'tac3b\': \'#ffd400\',\n    \'pth2\': \'#00bcd4\',\n    \'cfos\': \'#ff7f0e\',\n    \'cort\': \'#8c564b\',\n}\nGENE_ORDER = list(RC.get(\'GENE_ORDER\', DEFAULT_GENE_ORDER))\nGENE_COLORS = dict(DEFAULT_GENE_COLORS)\ntry:\n    _gc = RC.get(\'GENE_COLORS\', {})\n    if isinstance(_gc, dict):\n        GENE_COLORS.update(_gc)\nexcept Exception:\n    pass\n\nPANEL_GRID = [\n    [\'contra_LB\', \'contra_LC\'],\n    [\'ipsi_LB\', \'ipsi_LC\'],\n]\nPLOT_ORDER = [p for row in PANEL_GRID for p in row]\nPLOT_TITLES = {\n    \'ipsi_LB\': \'Ipsi bout-like\',\n    \'ipsi_LC\': \'Ipsi continuous\',\n    \'contra_LB\': \'Contra bout-like\',\n    \'contra_LC\': \'Contra continuous\',\n}\nFIG_WIDTH = 12.0\nFIG_ROW_HEIGHT = 3.6\n\nif WINDOW_EDGE_POLICY not in {\'strict\', \'pad_nan\'}:\n    print(f"[stim-56f] invalid WINDOW_EDGE_POLICY=\'{WINDOW_EDGE_POLICY}\', using \'pad_nan\'")\n    WINDOW_EDGE_POLICY = \'pad_nan\'\n\n\ndef _extract_window_local(trace, idx0, idx1, mode=\'strict\', min_valid_points=1):\n    T = int(trace.shape[0])\n    win_len = int(idx1 - idx0)\n    if win_len <= 0:\n        return None\n    if mode == \'strict\':\n        if idx0 < 0 or idx1 > T:\n            return None\n        return trace[idx0:idx1]\n\n    seg = np.full(win_len, np.nan, dtype=np.float32)\n    src0 = max(int(idx0), 0)\n    src1 = min(int(idx1), T)\n    if src1 <= src0:\n        return None\n    dst0 = src0 - int(idx0)\n    seg[dst0:dst0 + (src1 - src0)] = trace[src0:src1]\n    if (src1 - src0) < max(1, int(min_valid_points)):\n        return None\n    return seg\n\n\n_build_prestim_baseline_windows_local = build_prestim_baseline_windows\n_combine_segments = combine_segments\n_compute_zscore_stats_local = compute_zscore_stats\n_effective_motion_window_local = effective_motion_window\n_prepare_pairs_for_unique_cells_local = prepare_pairs_for_unique_cells\n\n\ndef _filter_pairs_high_conf_generic(pairs_in, tag=\'[stim-56f]\'):\n    pairs = pairs_in.copy()\n    if pairs.empty:\n        return pairs, {}\n\n    def _mask_key_candidates(mask_path):\n        p = Path(str(mask_path))\n        keys = {str(p), p.name, p.stem}\n        try:\n            keys.add(str(p.resolve()))\n        except Exception:\n            pass\n        return keys\n\n    def _add_lookup_entry(lookup, mask_path, low_conf_labels):\n        if low_conf_labels is None:\n            return\n        vals = []\n        for v in list(low_conf_labels):\n            try:\n                vals.append(int(v))\n            except Exception:\n                continue\n        if not vals:\n            return\n        vset = set(vals)\n        for key in _mask_key_candidates(mask_path):\n            lookup.setdefault(key, set()).update(vset)\n\n    def _collect_q95_lookup(mask_values):\n        lookup = {}\n\n        for res in (_warp_results_56h or []):\n            mask_path = res.get(\'mask_path\')\n            if mask_path is None:\n                continue\n            low_conf = res.get(\'low_conf_labels_native\', None)\n            if low_conf is None:\n                fs = res.get(\'filter_stats\', {}) if isinstance(res.get(\'filter_stats\', {}), dict) else {}\n                low_conf = fs.get(\'low_conf_labels\', [])\n            _add_lookup_entry(lookup, mask_path, low_conf)\n\n        aligned_dir = _hcr_aligned_dir_56h\n        if aligned_dir is not None:\n            aligned_dir = Path(aligned_dir)\n            for m in mask_values:\n                meta_path = aligned_dir / f"{Path(str(m)).stem}_in_2p_warp_meta.json"\n                if not meta_path.exists():\n                    continue\n                try:\n                    meta = json.loads(meta_path.read_text())\n                except Exception:\n                    continue\n                fs = meta.get(\'filter_stats\', {}) if isinstance(meta, dict) else {}\n                low_conf = fs.get(\'low_conf_labels\', []) if isinstance(fs, dict) else []\n                _add_lookup_entry(lookup, m, low_conf)\n        return lookup\n\n    def _low_conf_set_for_mask(mask_path, lookup):\n        for key in _mask_key_candidates(mask_path):\n            if key in lookup:\n                return lookup[key]\n        return set()\n\n    def _global_anat_r50_um():\n        rx = _anat_med_radius_xy_um_56h\n        rz = _anat_med_radius_z_um_56h\n        try:\n            rx = float(rx)\n        except Exception:\n            rx = np.nan\n        try:\n            rz = float(rz)\n        except Exception:\n            rz = np.nan\n\n        if np.isfinite(rx) and np.isfinite(rz):\n            return rx, rz\n\n        df = _df_all_filt_56h if _df_all_filt_56h is not None else _df_all_56h\n        if df is None or getattr(df, \'empty\', True):\n            return rx, rz\n        req = {\'dataset\', \'x_um\', \'y_um\', \'z_um\'}\n        if not req.issubset(df.columns):\n            return rx, rz\n        sub = df[df[\'dataset\'].astype(str) == \'Anatomy\'].copy()\n        if sub.empty:\n            return rx, rz\n\n        x = pd.to_numeric(sub[\'x_um\'], errors=\'coerce\').to_numpy(dtype=float)\n        y = pd.to_numeric(sub[\'y_um\'], errors=\'coerce\').to_numpy(dtype=float)\n        z = pd.to_numeric(sub[\'z_um\'], errors=\'coerce\').to_numpy(dtype=float)\n        xy_d = ((x + y) / 2.0)\n        xy_d = xy_d[np.isfinite(xy_d)]\n        z_d = z[np.isfinite(z)]\n\n        if not np.isfinite(rx) and xy_d.size:\n            rx = float(np.median(xy_d) / 2.0)\n        if not np.isfinite(rz) and z_d.size:\n            rz = float(np.median(z_d) / 2.0)\n\n        return rx, rz\n\n    q95_lookup = _collect_q95_lookup(pairs[\'conf_mask\'].astype(str).unique().tolist())\n    if not q95_lookup:\n        print(f"{tag} WARNING: no q95 low-confidence lookup found; high-confidence filter cannot be applied.")\n\n    low_flags = []\n    for _, r in pairs[[\'conf_mask\', \'conf_label\']].iterrows():\n        low_set = _low_conf_set_for_mask(r[\'conf_mask\'], q95_lookup)\n        low_flags.append(int(r[\'conf_label\']) in low_set)\n    pairs[\'_is_low_conf_q95\'] = np.asarray(low_flags, dtype=bool)\n\n    n_before_q95 = int(len(pairs))\n    n_low_q95 = int(pairs[\'_is_low_conf_q95\'].sum())\n    n_unique_masks = int(pairs[\'conf_mask\'].nunique())\n    n_masks_with_lookup = int(len({\n        m for m in pairs[\'conf_mask\'].astype(str).unique().tolist()\n        if _low_conf_set_for_mask(m, q95_lookup)\n    }))\n    print(\n        f"{tag} q95 filter coverage: masks with lookup={n_masks_with_lookup}/{n_unique_masks}; "\n        f"rows removed={n_low_q95}/{n_before_q95}"\n    )\n    pairs = pairs[~pairs[\'_is_low_conf_q95\']].copy()\n    if pairs.empty:\n        return pairs, {\n            \'n_before_q95\': int(n_before_q95),\n            \'n_low_q95\': int(n_low_q95),\n            \'n_after_q95\': 0,\n        }\n\n    anat_r50_xy_um, anat_r50_z_um = _global_anat_r50_um()\n    _xy_txt = f"{anat_r50_xy_um:.3f}" if np.isfinite(anat_r50_xy_um) else \'nan\'\n    _z_txt = f"{anat_r50_z_um:.3f}" if np.isfinite(anat_r50_z_um) else \'nan\'\n    print(f"{tag} Global match-confidence thresholds (Anat R50): XY={_xy_txt} \xb5m, Z={_z_txt} \xb5m")\n\n    _dist_func = pd.to_numeric(pairs.get(\'dist_func_anat_um\', pd.Series(np.nan, index=pairs.index)), errors=\'coerce\').to_numpy(dtype=float)\n    if np.isfinite(anat_r50_xy_um):\n        pairs[\'_is_low_conf_func_anat_globalR50\'] = np.isfinite(_dist_func) & (_dist_func > float(anat_r50_xy_um))\n    else:\n        pairs[\'_is_low_conf_func_anat_globalR50\'] = False\n\n    _hcr_xy = np.full(len(pairs), np.nan, dtype=float)\n    _hcr_z = np.full(len(pairs), np.nan, dtype=float)\n    _dist_conf = pd.to_numeric(pairs.get(\'dist_conf_anat_um\', pd.Series(np.nan, index=pairs.index)), errors=\'coerce\').to_numpy(dtype=float)\n\n    if _hcr_match_results_56h:\n        DX = float(_vox_anat_56h.get(\'X\', 1.0)) if _vox_anat_56h else 1.0\n        DY = float(_vox_anat_56h.get(\'Y\', 1.0)) if _vox_anat_56h else 1.0\n        DZ = float(_vox_anat_56h.get(\'Z\', 1.0)) if _vox_anat_56h else 1.0\n        _maps = {}\n        for _res in _hcr_match_results_56h:\n            _mname = Path(str(_res.get(\'mask_path\', \'\'))).name\n            _dfc = _res.get(\'df_conf\')\n            _dfa = _res.get(\'df_2p\')\n            if _dfc is None or _dfa is None or _dfc.empty or _dfa.empty:\n                continue\n            try:\n                _cmap = _dfc.set_index(\'label\')[[\'x\', \'y\', \'z\']].to_dict(\'index\')\n                _amap = _dfa.set_index(\'label\')[[\'x\', \'y\', \'z\']].to_dict(\'index\')\n            except Exception:\n                continue\n            _maps[_mname] = (_cmap, _amap)\n\n        for _i, _r in enumerate(pairs[[\'conf_mask\', \'conf_label\', \'anat_label\']].itertuples(index=False)):\n            _mname = Path(str(_r.conf_mask)).name\n            _pair_maps = _maps.get(_mname)\n            if _pair_maps is None:\n                continue\n            _cmap, _amap = _pair_maps\n            _c = _cmap.get(int(_r.conf_label))\n            _a = _amap.get(int(_r.anat_label))\n            if _c is None or _a is None:\n                continue\n            _dx = (float(_c[\'x\']) - float(_a[\'x\'])) * DX\n            _dy = (float(_c[\'y\']) - float(_a[\'y\'])) * DY\n            _dz = (float(_c[\'z\']) - float(_a[\'z\'])) * DZ\n            _hcr_xy[_i] = float(np.sqrt(_dx * _dx + _dy * _dy))\n            _hcr_z[_i] = float(abs(_dz))\n\n    _hcr_xy_eff = _hcr_xy.copy()\n    _miss_xy = (~np.isfinite(_hcr_xy_eff)) & np.isfinite(_dist_conf)\n    _hcr_xy_eff[_miss_xy] = _dist_conf[_miss_xy]\n\n    pairs[\'_hcr_xy_offset_um\'] = _hcr_xy\n    pairs[\'_hcr_z_offset_um\'] = _hcr_z\n\n    _low_hcr = np.zeros(len(pairs), dtype=bool)\n    if np.isfinite(anat_r50_xy_um):\n        _low_hcr |= np.isfinite(_hcr_xy_eff) & (_hcr_xy_eff > float(anat_r50_xy_um))\n    if np.isfinite(anat_r50_z_um):\n        _low_hcr |= np.isfinite(_hcr_z) & (_hcr_z > float(anat_r50_z_um))\n    pairs[\'_is_low_conf_hcr_anat_globalR50\'] = _low_hcr\n\n    pairs[\'_is_low_conf_match_any_globalR50\'] = (\n        pairs[\'_is_low_conf_func_anat_globalR50\'].to_numpy(dtype=bool)\n        | pairs[\'_is_low_conf_hcr_anat_globalR50\'].to_numpy(dtype=bool)\n    )\n\n    _n_before_match = int(len(pairs))\n    _n_low_func = int(pairs[\'_is_low_conf_func_anat_globalR50\'].sum())\n    _n_low_hcr = int(pairs[\'_is_low_conf_hcr_anat_globalR50\'].sum())\n    _n_low_any = int(pairs[\'_is_low_conf_match_any_globalR50\'].sum())\n    print(\n        f"{tag} match-confidence filter (>Anat R50): "\n        f"func-anat={_n_low_func}/{_n_before_match}, "\n        f"hcr-anat={_n_low_hcr}/{_n_before_match}, "\n        f"union={_n_low_any}/{_n_before_match}"\n    )\n\n    pairs = pairs[~pairs[\'_is_low_conf_match_any_globalR50\']].copy()\n    stats = {\n        \'n_before_q95\': int(n_before_q95),\n        \'n_low_q95\': int(n_low_q95),\n        \'n_after_q95\': int(n_before_q95 - n_low_q95),\n        \'n_before_match_filter\': int(_n_before_match),\n        \'n_low_conf_func_anat\': int(_n_low_func),\n        \'n_low_conf_hcr_anat\': int(_n_low_hcr),\n        \'n_low_conf_any\': int(_n_low_any),\n        \'anat_r50_xy_um\': float(anat_r50_xy_um) if np.isfinite(anat_r50_xy_um) else np.nan,\n        \'anat_r50_z_um\': float(anat_r50_z_um) if np.isfinite(anat_r50_z_um) else np.nan,\n    }\n    return pairs, stats\n\n\ndef _midline_globals_match_current_fish():\n    current = str(FISH_ID).strip()\n    state_fish = _midline_fish_id_56h\n    if state_fish is None:\n        return False\n    return str(state_fish).strip() == current\n\n\ndef _path_matches_current_fish(path_obj):\n    current = str(FISH_ID).strip()\n    if not current:\n        return True\n    toks = sorted(set(re.findall(r"[A-Za-z]\\d+_f\\d+", str(path_obj))))\n    if not toks:\n        return True\n    return all(t == current for t in toks)\n\n\ndef _load_midline_bundle():\n    candidates = []\n    if _midline_json_path_56h is not None:\n        candidates.append(Path(_midline_json_path_56h))\n    if OUT_REG is not None:\n        candidates.append(Path(OUT_REG) / \'midline_params_func_ref.json\')\n    seen = set()\n    for p in candidates:\n        ps = str(p)\n        if ps in seen:\n            continue\n        seen.add(ps)\n        if not _path_matches_current_fish(p):\n            continue\n        if not p.exists():\n            continue\n        try:\n            bundle = json.loads(p.read_text())\n            current = str(FISH_ID).strip()\n            bundle_fish = str((bundle or {}).get(\'fish_id\', \'\')).strip() if isinstance(bundle, dict) else \'\'\n            if bundle_fish and current and bundle_fish != current:\n                continue\n            return bundle, p\n        except Exception:\n            continue\n    return None, None\n\n\ndef _annotate_midline_side_resilient(df_in, x_col=\'x\', y_col=\'y\', plane_col=\'plane\'):\n    out = df_in.copy()\n    d = np.full(len(out), np.nan, dtype=float)\n\n    midline_globals_ok = _midline_globals_match_current_fish()\n    if midline_globals_ok:\n        per_plane_map = _midline_by_plane_56h\n        pos_label = str(_midline_pos_side_label_56h if _midline_pos_side_label_56h is not None else \'right\').strip().lower()\n        if pos_label not in {\'left\', \'right\'}:\n            pos_label = \'right\'\n        neg_label = \'left\' if pos_label == \'right\' else \'right\'\n        band = float(_midline_uncertain_band_56h)\n        _mp = _midline_params_56h\n    else:\n        per_plane_map = None\n        pos_label = \'right\'\n        neg_label = \'left\'\n        band = 0.0\n        _mp = None\n        if _midline_fish_id_56h not in (None, \'\'):\n            print(f"[stim-56f] ignoring stale in-memory midline from fish {_midline_fish_id_56h}; current fish is {FISH_ID}")\n\n    src_label = None\n    if isinstance(_mp, dict):\n        try:\n            src_label = (_mp.get(\'base\', {}) or {}).get(\'source_label\', None)\n        except Exception:\n            src_label = None\n\n    bundle = None\n    if not isinstance(per_plane_map, dict) or not per_plane_map:\n        bundle, bundle_path = _load_midline_bundle()\n        if isinstance(bundle, dict):\n            per_plane = bundle.get(\'per_plane\', {})\n            if isinstance(per_plane, dict) and per_plane:\n                per_plane_map = per_plane\n                _sides = bundle.get(\'side_labels\', {}) if isinstance(bundle, dict) else {}\n                pos_label = str(_sides.get(\'positive\', pos_label)).strip().lower()\n                if pos_label not in {\'left\', \'right\'}:\n                    pos_label = \'right\'\n                neg_label = \'left\' if pos_label == \'right\' else \'right\'\n                try:\n                    band = float(bundle.get(\'manual\', {}).get(\'uncertain_band_px\', band))\n                except Exception:\n                    pass\n                try:\n                    src_label = (bundle.get(\'base\', {}) or {}).get(\'source_label\', src_label)\n                except Exception:\n                    pass\n                print(f"[stim-56f] loaded midline params from {bundle_path}")\n\n    src = str(src_label).strip().lower() if src_label is not None else \'\'\n    if src in {\'warped\', \'tform-preview\', \'warped-raw\'}:\n        midline_space = \'anat\'\n    elif src in {\'raw\', \'ref2d\', \'ref2d_raw\', \'ref_match\'}:\n        midline_space = \'func\'\n    else:\n        try:\n            if _plane_refs_56h:\n                any_warped = any((pr.get(\'ref_warped\') is not None) or (pr.get(\'ref_warped_raw\') is not None) for pr in _plane_refs_56h)\n                midline_space = \'anat\' if any_warped else \'func\'\n            else:\n                midline_space = \'func\'\n        except Exception:\n            midline_space = \'func\'\n\n    if midline_globals_ok and _annotate_midline_side_helper_56h is not None and midline_space != \'anat\':\n        try:\n            out = _annotate_midline_side(out, x_col=x_col, y_col=y_col, plane_col=plane_col)\n            out[\'midline_space\'] = midline_space\n            return out\n        except Exception as _e:\n            print(f"[stim-56f] _annotate_midline_side failed, using local fallback: {_e}")\n\n    if not isinstance(per_plane_map, dict) or not per_plane_map:\n        out[\'midline_signed_dist_px\'] = d\n        out[\'midline_side\'] = \'unknown\'\n        out[\'midline_uncertain\'] = False\n        out[\'midline_space\'] = midline_space\n        return out\n\n    def _params_for_plane(p_idx):\n        return per_plane_map.get(int(p_idx), per_plane_map.get(str(p_idx), None))\n\n    def _plane_tform(p_idx):\n        pmap = _suite2p_by_ref_idx_56h\n        pdata = pmap.get(int(p_idx), None)\n        ref_idx = int(pdata.get(\'ref_idx\', p_idx)) if isinstance(pdata, dict) else int(p_idx)\n        if not _plane_refs_56h:\n            return None\n        if not (0 <= ref_idx < len(_plane_refs_56h)):\n            return None\n        pr = _plane_refs_56h[ref_idx]\n        tf = pr.get(\'tform\', None)\n        if tf is None and _tform_for_plane_helper_56h is not None:\n            try:\n                tf = _tform_for_plane_helper_56h(pr)\n            except Exception:\n                tf = None\n        return tf\n\n    for p_idx, idx in out.groupby(plane_col).groups.items():\n        p = _params_for_plane(int(p_idx))\n        if not isinstance(p, dict):\n            continue\n        try:\n            x0 = float(p.get(\'x0\', np.nan))\n            y0 = float(p.get(\'y0\', np.nan))\n            th = np.deg2rad(float(p.get(\'theta_deg\', np.nan)))\n        except Exception:\n            continue\n        if not (np.isfinite(x0) and np.isfinite(y0) and np.isfinite(th)):\n            continue\n\n        nx = -np.sin(th)\n        ny = np.cos(th)\n        xv = pd.to_numeric(out.loc[idx, x_col], errors=\'coerce\').to_numpy(dtype=float)\n        yv = pd.to_numeric(out.loc[idx, y_col], errors=\'coerce\').to_numpy(dtype=float)\n\n        if midline_space == \'anat\':\n            tf = _plane_tform(int(p_idx))\n            if tf is not None:\n                try:\n                    pts = np.column_stack([xv, yv]).astype(float)\n                    pts_t = np.asarray(tf(pts), dtype=float)\n                    xv_use = pts_t[:, 0]\n                    yv_use = pts_t[:, 1]\n                except Exception:\n                    xv_use, yv_use = xv, yv\n            else:\n                xv_use, yv_use = xv, yv\n        else:\n            xv_use, yv_use = xv, yv\n\n        d[idx] = (xv_use - x0) * nx + (yv_use - y0) * ny\n\n    side = np.full(len(out), \'unknown\', dtype=object)\n    finite = np.isfinite(d)\n    side[(finite) & (d > band)] = pos_label\n    side[(finite) & (d < -band)] = neg_label\n    side[(finite) & (np.abs(d) <= band)] = \'midline\'\n\n    out[\'midline_signed_dist_px\'] = d\n    out[\'midline_side\'] = side\n    out[\'midline_uncertain\'] = np.abs(d) <= band\n    out[\'midline_space\'] = midline_space\n    return out\n\nCONF_FUNC_CSV = _default_conf_csv_qc()\n\nif _df_stim_56h is None or _df_stim_56h.empty:\n    print(\'df_stim is missing; run cell [55] first.\')\nelif _df_evt_56h is None or _df_evt_56h.empty:\n    print(\'df_evt is missing; run cell [55] first.\')\nelif not _suite2p_by_ref_idx_56h:\n    print(\'Suite2p data not loaded; run cell [23a] first.\')\nelif not CONF_FUNC_CSV.exists():\n    print(f\'Missing conf_to_func_pairs.csv: {CONF_FUNC_CSV} (run [50]).\')\nelse:\n    df_stim = _df_stim_56h.copy()\n    if TARGET_STIM_TYPES is not None:\n        df_stim = df_stim[df_stim[\'type\'].isin(list(TARGET_STIM_TYPES))]\n    if df_stim.empty:\n        print(\'No stimuli available after filtering.\')\n    else:\n        pairs = pd.read_csv(CONF_FUNC_CSV)\n        if \'fish_id\' not in pairs.columns:\n            print(\'[stim-56f] conf_to_func_pairs.csv has no fish_id column; re-run [50] for this fish.\')\n            raise SystemExit\n        pairs = pairs[pairs[\'fish_id\'].astype(str) == str(FISH_ID)].copy()\n        pair_versions = set(pairs.get(\'match_policy_version\', pd.Series(dtype=str)).dropna().astype(str).unique().tolist())\n        if pairs.empty:\n            print(\'[stim-56f] conf_to_func_pairs.csv does not match current FISH_ID; run [50].\')\n            raise SystemExit\n        if (\'match_policy_version\' not in pairs.columns) or (MATCH_POLICY_VERSION not in pair_versions):\n            print(f\'[stim-56f] conf_to_func_pairs.csv uses stale matching policy; rerun [50] to regenerate {CONF_FUNC_CSV}.\')\n            raise SystemExit\n        required_pair_response_cols = {\'response_is_active\', \'response_class\', \'response_summary_class\'}\n        missing_pair_response_cols = sorted(required_pair_response_cols - set(pairs.columns))\n        if missing_pair_response_cols:\n            print(f\'[stim-56f] conf_to_func_pairs.csv missing response-aware columns {missing_pair_response_cols}; rerun [50] after [50ia].\')\n            raise SystemExit\n\n        if \'gene\' not in pairs.columns:\n            if \'conf_mask\' in pairs.columns:\n                pairs[\'gene\'] = pairs[\'conf_mask\'].apply(_infer_gene_from_mask)\n            else:\n                pairs[\'gene\'] = \'unknown\'\n\n        pairs = _prepare_pairs_for_unique_cells_local(pairs, strict=STRICT_PAIR_INTEGRITY)\n        if pairs.empty:\n            print(\'[stim-56f] No valid gene+HCR+anat+functional mappings after integrity checks.\')\n            raise SystemExit\n\n        if HIGH_CONF_ONLY:\n            pairs, _hc_stats = _filter_pairs_high_conf_generic(pairs, tag=\'[stim-56f]\')\n            stim_hc_match_filter_counts_56h = _hc_stats\n            if pairs.empty:\n                print(\'[stim-56f] No pairs remain after high-confidence filtering.\')\n                raise SystemExit\n            pairs_high_conf = pairs.copy()\n        else:\n            pairs_high_conf = None\n\n        n_in = len(pairs)\n        pairs[\'_sort_dist_func\'] = pd.to_numeric(pairs.get(\'dist_func_anat_um\', pd.Series(np.nan, index=pairs.index)), errors=\'coerce\').fillna(np.inf)\n        pairs[\'_sort_overlap\'] = pd.to_numeric(pairs.get(\'overlap_px_func_anat\', pairs.get(\'overlap_px\', pd.Series(np.nan, index=pairs.index))), errors=\'coerce\').fillna(0)\n        pairs[\'_sort_dist_conf\'] = pd.to_numeric(pairs.get(\'dist_conf_anat_um\', pd.Series(np.nan, index=pairs.index)), errors=\'coerce\').fillna(np.inf)\n        pairs = pairs.sort_values(\n            [\'gene\', \'anat_label\', \'_sort_dist_func\', \'_sort_overlap\', \'_sort_dist_conf\', \'plane\', \'func_label\', \'conf_mask\', \'conf_label\'],\n            ascending=[True, True, True, False, True, True, True, True, True],\n        )\n        pairs = pairs.drop_duplicates(subset=[\'gene\', \'anat_label\'], keep=\'first\')\n        pairs = pairs.drop(columns=[\'_sort_dist_func\', \'_sort_overlap\', \'_sort_dist_conf\'])\n        n_after_anat = len(pairs)\n        pairs_pre_roi_dedup = pairs.copy()\n\n        roi_conflict_mask = pairs.duplicated(subset=[\'plane\', \'func_label\', \'gene\'], keep=False)\n        n_roi_conflicts = int(roi_conflict_mask.sum())\n        if (not ALLOW_REUSED_ROIS_WITHIN_GENE) and STRICT_PAIR_INTEGRITY and n_roi_conflicts > 0:\n            raise RuntimeError(\n                f"[stim-56f] Found {n_roi_conflicts} rows where one functional ROI maps to multiple anatomy cells "\n                "within a gene. Resolve mapping ambiguity upstream before plotting traces."\n            )\n        pairs = pairs.drop_duplicates(subset=[\'plane\', \'func_label\', \'gene\'])\n        n_after_roi = len(pairs)\n\n        dup_gene_anat_after = int(pairs.duplicated(subset=[\'gene\', \'anat_label\']).sum())\n        if STRICT_PAIR_INTEGRITY and dup_gene_anat_after > 0:\n            raise RuntimeError(\n                f"[stim-56f] Integrity failure: mapping not unique on (gene, anat_label), duplicates={dup_gene_anat_after}."\n            )\n\n        print(\n            f"[stim-56f] dedup rows: {n_in} -> {n_after_anat} (unique gene+anat) -> {n_after_roi} (unique plane+func+gene); "\n            f"roi_conflicts={n_roi_conflicts}"\n        )\n        if ALLOW_REUSED_ROIS_WITHIN_GENE and n_roi_conflicts > 0:\n            print(\'[stim-56f] Reused ROIs within gene were deduplicated before averaging traces.\')\n\n        s2p_map = _suite2p_by_ref_idx_56h\n        pairs_pre_roi_dedup = pairs_pre_roi_dedup[pairs_pre_roi_dedup[\'plane\'].isin(s2p_map.keys())].copy()\n        pairs = pairs[pairs[\'plane\'].isin(s2p_map.keys())].copy()\n        if pairs.empty:\n            print(f\'No matching Suite2p planes for {PAIR_SCOPE_LABEL}.\')\n        else:\n            pairs_56h_selected = pairs.copy()\n            pair_usage_summary_56h = (\n                pairs_pre_roi_dedup.groupby(\'gene\', as_index=False)\n                .agg(n_responsive_labels=(\'anat_label\', \'size\'))\n                .merge(\n                    pairs_56h_selected.groupby(\'gene\', as_index=False).agg(n_unique_rois=(\'func_label\', \'size\')),\n                    on=\'gene\',\n                    how=\'outer\',\n                )\n            )\n            if not pair_usage_summary_56h.empty:\n                pair_usage_summary_56h[\'n_responsive_labels\'] = pd.to_numeric(pair_usage_summary_56h[\'n_responsive_labels\'], errors=\'coerce\').fillna(0).astype(int)\n                pair_usage_summary_56h[\'n_unique_rois\'] = pd.to_numeric(pair_usage_summary_56h[\'n_unique_rois\'], errors=\'coerce\').fillna(0).astype(int)\n                pair_usage_summary_56h[\'n_reused_labels\'] = pair_usage_summary_56h[\'n_responsive_labels\'] - pair_usage_summary_56h[\'n_unique_rois\']\n                pair_usage_summary_56h = pair_usage_summary_56h.sort_values(\'gene\').reset_index(drop=True)\n                pair_label_count_map_56h = pair_usage_summary_56h.set_index(\'gene\')[\'n_responsive_labels\'].to_dict()\n                if int(pair_usage_summary_56h[\'n_reused_labels\'].clip(lower=0).sum()) > 0:\n                    print(\'[56h] Reused responsive HCR labels were deduplicated to unique (gene, plane, func_label) ROIs for BPI/traces.\')\n                    try:\n                        display(pair_usage_summary_56h)\n                    except Exception:\n                        print(pair_usage_summary_56h.to_string(index=False))\n            fps = _frame_rate_56h\n            if fps is None:\n                fs_vals = []\n                for p in s2p_map.values():\n                    fs = p.get(\'ops\', {}).get(\'fs\', None)\n                    if fs is not None:\n                        fs_vals.append(float(fs))\n                if fs_vals:\n                    fps = fs_vals[0]\n                    if len(set(fs_vals)) > 1:\n                        print(\'[stim-56f] WARNING: multiple fs values found; using the first\')\n                else:\n                    print(\'FRAME_RATE not available; cannot align to time.\')\n                    fps = None\n\n            if fps is None or fps <= 0:\n                print(\'Invalid FRAME_RATE; set FRAME_RATE or ensure metadata has framerate.\')\n            else:\n                win_len = int(round((WINDOW_PRE_SEC + WINDOW_POST_SEC) * fps)) + 1\n                tvec = (np.arange(win_len, dtype=np.float32) / float(fps)) - float(WINDOW_PRE_SEC)\n\n                try:\n                    baseline_windows = _build_prestim_baseline_windows_local(\n                        _df_evt_56h,\n                        fps,\n                        STIM_ONSET_DELAY_SEC,\n                        tag=\'[stim-56f]\',\n                    )\n                except Exception as e:\n                    raise RuntimeError(f"[stim-56f] unable to build z-score baseline windows: {e}")\n\n                zstats_by_plane = {}\n                for _plane_idx, _plane_data in s2p_map.items():\n                    _dff = _plane_data.get(\'dff\', None)\n                    if _dff is None:\n                        continue\n                    zstats_by_plane[int(_plane_idx)] = _compute_zscore_stats_local(\n                        _dff,\n                        baseline_windows,\n                        min_points=ZSCORE_MIN_BASELINE_POINTS,\n                        sigma_eps=ZSCORE_MIN_BASELINE_STD,\n                    )\n\n                # Build ROI centroid table (functional space) from Suite2p label masks.\n                roi_rows = []\n                for _plane_idx, _plane_data in s2p_map.items():\n                    labels = _plane_data.get(\'labels\', None)\n                    if labels is None:\n                        continue\n                    arr = np.asarray(labels)\n                    if arr.ndim != 2:\n                        continue\n                    try:\n                        props = regionprops_table(arr.astype(np.int32, copy=False), properties=(\'label\', \'centroid\'))\n                        rdf = pd.DataFrame(props)\n                        if rdf.empty:\n                            continue\n                        rdf = rdf[rdf[\'label\'] != 0].copy()\n                        if rdf.empty:\n                            continue\n                        rdf[\'plane\'] = int(_plane_idx)\n                        rdf = rdf.rename(columns={\'label\': \'func_label\', \'centroid-0\': \'y\', \'centroid-1\': \'x\'})\n                        roi_rows.append(rdf[[\'plane\', \'func_label\', \'x\', \'y\']])\n                    except Exception:\n                        continue\n\n                roi_df = pd.concat(roi_rows, ignore_index=True) if roi_rows else pd.DataFrame(columns=[\'plane\', \'func_label\', \'x\', \'y\'])\n                pairs = pairs.merge(roi_df, on=[\'plane\', \'func_label\'], how=\'left\')\n                n_missing_xy = int(pairs[[\'x\', \'y\']].isna().any(axis=1).sum())\n                if n_missing_xy > 0:\n                    print(f"[stim-56f] WARNING: missing ROI centroids for {n_missing_xy}/{len(pairs)} rows")\n\n                pairs = _annotate_midline_side_resilient(pairs, x_col=\'x\', y_col=\'y\', plane_col=\'plane\')\n                pairs[\'midline_side\'] = pairs[\'midline_side\'].astype(str).str.strip().str.lower()\n\n                valid_midline = pairs[\'midline_side\'].isin({\'left\', \'right\'})\n                n_valid_midline = int(valid_midline.sum())\n                n_invalid_midline = int(len(pairs) - n_valid_midline)\n                if n_invalid_midline > 0:\n                    print(f"[stim-56f] dropped {n_invalid_midline} rows with ambiguous/missing midline_side")\n                pairs = pairs[valid_midline].copy()\n\n                if pairs.empty and USE_GLOBAL_IPSI_FALLBACK:\n                    print(\'[stim-56f] midline-side assignment unavailable; using global IPSI_SIDE fallback per fish.\')\n                elif pairs.empty:\n                    print(\'[stim-56f] No rows with valid midline_side. Run [22c] midline tuning first (or set USE_GLOBAL_IPSI_FALLBACK=True).\')\n                    raise SystemExit\n\n                TRACE_DEDUP_KEYS_56H = [\'gene\', \'plane\', \'func_label\', \'midline_side\']\n                FAIL_ON_SEGMENT_DUPLICATION_56H = True\n\n                # Load low-activity ROI pairs from hcr_activity_status.csv\n                low_activity_pairs_56h = None\n                hcr_status_path_56h = OUT_REG / \'hcr_activity_status.csv\'\n                if hcr_status_path_56h.exists():\n                    try:\n                        status_df_low = pd.read_csv(hcr_status_path_56h)\n                        if \'fish_id\' in status_df_low.columns:\n                            status_df_low = status_df_low[status_df_low[\'fish_id\'].astype(str) == str(FISH_ID)].copy()\n                        \n                        # Filter for in-plane low-activity ROIs only\n                        low_df = status_df_low[\n                            status_df_low[\'functional_status\'].astype(str) == \'in-plane low-activity ROI\'\n                        ].copy()\n                        \n                        if not low_df.empty:\n                            low_df[\'plane\'] = pd.to_numeric(low_df.get(\'selected_plane\', pd.Series(np.nan)), errors=\'coerce\').astype(\'Int64\')\n                            low_df[\'func_label\'] = pd.to_numeric(low_df.get(\'selected_func_label\', pd.Series(np.nan)), errors=\'coerce\').astype(\'Int64\')\n                            low_df[\'gene\'] = low_df[\'gene\'].astype(str).str.strip()\n                            \n                            # Keep only rows with valid plane, func_label, and gene\n                            low_df = low_df[low_df[[\'plane\', \'func_label\', \'gene\']].notna().all(axis=1)].copy()\n                            \n                            if not low_df.empty:\n                                low_df = low_df[[\'gene\', \'plane\', \'func_label\']].copy()\n                                low_df[\'plane\'] = low_df[\'plane\'].astype(int)\n                                low_df[\'func_label\'] = low_df[\'func_label\'].astype(int)\n                                _n_low_before_dedup = int(len(low_df))\n                                low_df = low_df.drop_duplicates(subset=[\'gene\', \'plane\', \'func_label\'], keep=\'first\').reset_index(drop=True)\n                                _n_low_dups = int(_n_low_before_dedup - len(low_df))\n                                if _n_low_dups > 0:\n                                    print(f"[56h] dropped {_n_low_dups} duplicate low-activity ROI rows")\n                                low_df[\'is_low_activity\'] = True\n                                low_activity_pairs_56h = low_df.copy()\n                                print(f"[56h] Loaded {len(low_activity_pairs_56h)} low-activity ROI pairs from hcr_activity_status.csv")\n                    except Exception as e:\n                        print(f"[56h] WARNING: Could not load low-activity pairs: {e}")\n                        low_activity_pairs_56h = None\n\n                # Parse unilateral stimulus events.\n                stim_events = []\n                invalid_stims = set()\n                bilateral_dropped = 0\n                mode_durations = {\'LB\': [], \'LC\': []}\n                phys_side_mode_counts = {\'left_LB\': 0, \'left_LC\': 0, \'right_LB\': 0, \'right_LC\': 0}\n\n                print(f"[stim-56f] Applying onset delay of +{STIM_ONSET_DELAY_SEC:.3f}s to experiment-log start times and trimming that offset from stimulus duration")\n                print(f"[stim-56f] Window edge policy: {WINDOW_EDGE_POLICY} (min_valid_points={MIN_VALID_POINTS_PER_SEG})")\n\n                for _, row in df_stim.iterrows():\n                    stype = str(row.get(\'type\', \'\'))\n                    comps = _parse_stim_components(stype)\n                    if comps is None:\n                        invalid_stims.add(stype)\n                        continue\n                    if len(comps) != 1:\n                        bilateral_dropped += 1\n                        continue\n\n                    side_letter, mode = comps[0]\n                    side_word = \'left\' if side_letter == \'L\' else \'right\'\n\n                    t0, _, dur = _effective_motion_window_local(\n                        row.get(\'start\', np.nan),\n                        row.get(\'duration\', np.nan),\n                        row.get(\'end\', np.nan),\n                        STIM_ONSET_DELAY_SEC,\n                    )\n                    if not np.isfinite(t0) or not np.isfinite(dur):\n                        continue\n                    idx0 = int(round((t0 - WINDOW_PRE_SEC) * fps))\n                    idx1 = idx0 + win_len\n\n                    stim_events.append({\n                        \'side\': side_word,\n                        \'mode\': mode,\n                        \'idx0\': idx0,\n                        \'idx1\': idx1,\n                        \'dur\': dur,\n                        \'stim_type\': stype,\n                    })\n                    mode_durations[mode].append(dur)\n                    phys_side_mode_counts[f"{side_word}_{mode}"] += 1\n\n                if invalid_stims:\n                    print(f"[stim-56f] ignored unparseable/invalid stim types: {sorted(invalid_stims)}")\n                if bilateral_dropped:\n                    print(f"[stim-56f] dropped bilateral events by design: n={bilateral_dropped}")\n\n                if not stim_events:\n                    print(\'[stim-56f] No unilateral stimulus events available after parsing/filtering.\')\n                    raise SystemExit\n\n                print(f"[stim-56f] unilateral event counts: {phys_side_mode_counts}")\n                print(f"[stim-56f] cells with side labels: left={(pairs[\'midline_side\']==\'left\').sum()}, right={(pairs[\'midline_side\']==\'right\').sum()}")\n\n                def _mode_events_per_cell_label_56h(mode):\n                    left_n = int(phys_side_mode_counts.get(f\'left_{mode}\', 0))\n                    right_n = int(phys_side_mode_counts.get(f\'right_{mode}\', 0))\n                    if left_n == right_n:\n                        return str(left_n)\n                    return f"{min(left_n, right_n)}-{max(left_n, right_n)}"\n\n                # Combine responsive and low-activity pairs for trace extraction\n                pairs_for_traces = pairs.copy()\n                pairs_for_traces[\'is_low_activity\'] = False\n                _n_pairs_resp_before_dedup_56h = int(len(pairs_for_traces))\n                pairs_for_traces[\'gene\'] = pairs_for_traces[\'gene\'].astype(str).str.strip()\n                pairs_for_traces = pairs_for_traces.drop_duplicates(subset=[\'gene\', \'plane\', \'func_label\'], keep=\'first\').reset_index(drop=True)\n                _n_pairs_resp_dedup_56h = int(_n_pairs_resp_before_dedup_56h - len(pairs_for_traces))\n                if _n_pairs_resp_dedup_56h > 0:\n                    print(f"[56h] dropped {_n_pairs_resp_dedup_56h} duplicate responsive trace rows by (gene, plane, func_label)")\n                \n                if low_activity_pairs_56h is not None and not low_activity_pairs_56h.empty:\n                    # Add low-activity pairs not already in responsive pairs\n                    responsive_keys = set(pairs[[\'plane\', \'func_label\']].apply(tuple, axis=1))\n                    low_activity_new = low_activity_pairs_56h[\n                        ~low_activity_pairs_56h[[\'plane\', \'func_label\']].apply(tuple, axis=1).isin(responsive_keys)\n                    ].copy()\n                    \n                    if not low_activity_new.empty:\n                        low_activity_new = low_activity_new.merge(\n                            roi_df[[\'plane\', \'func_label\', \'x\', \'y\']],\n                            on=[\'plane\', \'func_label\'],\n                            how=\'left\',\n                        )\n                        low_activity_new = _annotate_midline_side_resilient(\n                            low_activity_new,\n                            x_col=\'x\',\n                            y_col=\'y\',\n                            plane_col=\'plane\',\n                        )\n                        low_activity_new[\'midline_side\'] = low_activity_new[\'midline_side\'].astype(str).str.strip().str.lower()\n                        if USE_GLOBAL_IPSI_FALLBACK:\n                            _fallback_side = \'left\' if IPSI_SIDE == \'L\' else \'right\'\n                            _valid_low_side = low_activity_new[\'midline_side\'].isin({\'left\', \'right\'})\n                            low_activity_new.loc[~_valid_low_side, \'midline_side\'] = _fallback_side\n                        _valid_low_side = low_activity_new[\'midline_side\'].isin({\'left\', \'right\'})\n                        _n_low_drop = int((~_valid_low_side).sum())\n                        if _n_low_drop > 0:\n                            print(f"[56h] dropped {_n_low_drop} low-activity rows with ambiguous/missing midline_side")\n                        low_activity_new = low_activity_new[_valid_low_side].copy()\n                        low_activity_new = low_activity_new[[\'gene\', \'plane\', \'func_label\', \'is_low_activity\', \'midline_side\']].copy()\n                        low_activity_new = low_activity_new.drop_duplicates(subset=[\'gene\', \'plane\', \'func_label\'], keep=\'first\').reset_index(drop=True)\n\n                    if not low_activity_new.empty:\n                        pairs_for_traces = pd.concat([pairs_for_traces, low_activity_new], ignore_index=True)\n                        print(f"[56h] Added {len(low_activity_new)} low-activity ROI pairs to trace extraction")\n\n                pairs_for_traces[\'gene\'] = pairs_for_traces[\'gene\'].astype(str).str.strip()\n                _n_pairs_before_final_dedup_56h = int(len(pairs_for_traces))\n                pairs_for_traces = pairs_for_traces.sort_values([\'is_low_activity\'], ascending=[True]).reset_index(drop=True)\n                pairs_for_traces = pairs_for_traces.drop_duplicates(subset=[\'gene\', \'plane\', \'func_label\'], keep=\'first\').reset_index(drop=True)\n                _n_pairs_dedup_dropped_56h = int(_n_pairs_before_final_dedup_56h - len(pairs_for_traces))\n                if _n_pairs_dedup_dropped_56h > 0:\n                    print("[56h] dropped "\n                          f"{_n_pairs_dedup_dropped_56h} duplicate trace-candidate rows by (gene, plane, func_label) "\n                          "after preferring responsive over low-activity rows")\n\n                genes_present = sorted(pairs_for_traces[\'gene\'].dropna().astype(str).unique().tolist())\n                genes = [g for g in GENE_ORDER if g in genes_present] + [g for g in genes_present if g not in GENE_ORDER]\n                if not genes:\n                    print(f\'No genes found in {PAIR_SCOPE_LABEL}.\')\n                else:\n                    gene_colors = {g: GENE_COLORS.get(g, \'#666666\') for g in genes}\n\n                    # Accumulate per panel/gene segments and ROI sets.\n                    panel_acc = {p: {} for p in PLOT_ORDER}\n                    panel_cell_acc = {p: {} for p in PLOT_ORDER}\n\n                    for gene in genes:\n                        sub = pairs_for_traces[pairs_for_traces[\'gene\'] == gene].copy()\n                        sub = sub.drop_duplicates(subset=[\'plane\', \'func_label\'], keep=\'first\').reset_index(drop=True)\n                        for _, row in sub.iterrows():\n                            plane = int(row[\'plane\'])\n                            roi_idx = int(row[\'func_label\']) - 1\n                            dff = s2p_map[plane].get(\'dff\', None)\n                            if dff is None:\n                                continue\n                            if roi_idx < 0 or roi_idx >= dff.shape[0]:\n                                continue\n\n                            zstats = zstats_by_plane.get(plane, None)\n                            if zstats is None or roi_idx >= len(zstats[\'valid\']) or not bool(zstats[\'valid\'][roi_idx]):\n                                continue\n\n                            trace = (dff[roi_idx] - float(zstats[\'mu\'][roi_idx])) / float(zstats[\'sigma\'][roi_idx])\n                            roi_key = (plane, roi_idx)\n                            cell_side = str(row.get(\'midline_side\', \'unknown\')).strip().lower()\n\n                            if cell_side not in {\'left\', \'right\'}:\n                                if USE_GLOBAL_IPSI_FALLBACK:\n                                    cell_side = \'left\' if IPSI_SIDE == \'L\' else \'right\'\n                                else:\n                                    continue\n\n                            for ev in stim_events:\n                                panel = (\'ipsi\' if ev[\'side\'] == cell_side else \'contra\') + f"_{ev[\'mode\']}"\n                                if panel not in panel_acc:\n                                    continue\n                                seg = _extract_window_local(\n                                    trace,\n                                    ev[\'idx0\'],\n                                    ev[\'idx1\'],\n                                    mode=WINDOW_EDGE_POLICY,\n                                    min_valid_points=MIN_VALID_POINTS_PER_SEG,\n                                )\n                                if seg is None:\n                                    continue\n                                gacc = panel_acc[panel].setdefault(gene, {\'segs\': [], \'rois\': set(), \'source_types\': []})\n                                gacc[\'segs\'].append(seg)\n                                gacc[\'rois\'].add(roi_key)\n                                gacc[\'source_types\'].append(ev[\'stim_type\'])\n                                gene_cell_acc = panel_cell_acc[panel].setdefault(gene, {})\n                                cacc = gene_cell_acc.setdefault(\n                                    roi_key,\n                                    {\n                                        \'segs\': [],\n                                        \'plane\': plane,\n                                        \'roi_idx\': roi_idx,\n                                        \'func_label\': int(row[\'func_label\']),\n                                        \'anat_label\': pd.to_numeric(pd.Series([row.get(\'anat_label\', np.nan)]), errors=\'coerce\').iloc[0],\n                                        \'midline_side\': cell_side,\n                                        \'source_types\': [],\n                                        \'is_low_activity\': bool(row.get(\'is_low_activity\', False)),\n                                    },\n                                )\n                                cacc[\'segs\'].append(seg)\n                                cacc[\'source_types\'].append(ev[\'stim_type\'])\n\n                    _expected_events_per_roi_56h = {\n                        side: {\n                            panel: int(sum(1 for ev in stim_events if ((\'ipsi\' if ev[\'side\'] == side else \'contra\') + f"_{ev[\'mode\']}") == panel))\n                            for panel in PLOT_ORDER\n                        }\n                        for side in [\'left\', \'right\']\n                    }\n                    for _panel, _gene_roi_map in panel_cell_acc.items():\n                        _panel_mode = \'LB\' if str(_panel).endswith(\'LB\') else \'LC\'\n                        for _gene, _roi_map in _gene_roi_map.items():\n                            for _roi_key, _cacc in _roi_map.items():\n                                _cell_side = str(_cacc.get(\'midline_side\', \'unknown\')).strip().lower()\n                                _expected_nseg = int((_expected_events_per_roi_56h.get(_cell_side, {}) or {}).get(_panel, 0))\n                                if _expected_nseg <= 0:\n                                    _expected_nseg = int(sum(1 for ev in stim_events if str(ev.get(\'mode\', \'\')) == _panel_mode))\n                                if _expected_nseg <= 0:\n                                    continue\n                                _observed_nseg = int(len(_cacc.get(\'segs\', [])))\n                                if _observed_nseg != _expected_nseg:\n                                    _msg = (\n                                        f"[56h] unexpected segment count panel={_panel} gene={_gene} roi={_roi_key} "\n                                        f"expected={_expected_nseg} observed={_observed_nseg}"\n                                    )\n                                    if FAIL_ON_SEGMENT_DUPLICATION_56H:\n                                        raise RuntimeError(_msg)\n                                    print(_msg)\n\n                    results = {panel: {} for panel in PLOT_ORDER}\n                    results_per_cell = {panel: {} for panel in PLOT_ORDER}\n                    per_cell_rows = []\n                    for panel in PLOT_ORDER:\n                        for gene, gacc in panel_acc.get(panel, {}).items():\n                            segs = gacc.get(\'segs\', [])\n                            if not segs:\n                                continue\n                            arr = np.vstack(segs).astype(np.float32, copy=False)\n                            mean, sem = _combine_segments(arr)\n                            n_segments = int(arr.shape[0])\n                            n_cells = int(len(gacc.get(\'rois\', set())))\n                            n_trials_per_cell = float(n_segments / n_cells) if n_cells > 0 else np.nan\n                            results[panel][gene] = {\n                                \'mean\': mean,\n                                \'sem\': sem,\n                                \'n_segments\': n_segments,\n                                \'n_cells\': n_cells,\n                                \'n_trials_per_cell\': n_trials_per_cell,\n                                \'source_stim_types\': sorted(set(gacc.get(\'source_types\', []))),\n                            }\n                        for gene, roi_map in panel_cell_acc.get(panel, {}).items():\n                            panel_gene_cells = {}\n                            for roi_key, cacc in roi_map.items():\n                                segs = cacc.get(\'segs\', [])\n                                if not segs:\n                                    continue\n                                arr = np.vstack(segs).astype(np.float32, copy=False)\n                                mean, sem = _combine_segments(arr)\n                                anat_label_val = cacc.get(\'anat_label\', np.nan)\n                                anat_label = int(anat_label_val) if pd.notna(anat_label_val) else np.nan\n                                entry = {\n                                    \'mean\': mean,\n                                    \'sem\': sem,\n                                    \'n_segments\': int(arr.shape[0]),\n                                    \'plane\': int(cacc.get(\'plane\', roi_key[0])),\n                                    \'roi_idx\': int(cacc.get(\'roi_idx\', roi_key[1])),\n                                    \'func_label\': int(cacc.get(\'func_label\', roi_key[1] + 1)),\n                                    \'anat_label\': anat_label,\n                                    \'midline_side\': str(cacc.get(\'midline_side\', \'unknown\')),\n                                    \'source_stim_types\': sorted(set(cacc.get(\'source_types\', []))),\n                                    \'is_low_activity\': bool(cacc.get(\'is_low_activity\', False)),\n                                }\n                                panel_gene_cells[roi_key] = entry\n                                per_cell_rows.append(\n                                    {\n                                        \'panel\': panel,\n                                        \'gene\': str(gene),\n                                        \'plane\': entry[\'plane\'],\n                                        \'roi_idx\': entry[\'roi_idx\'],\n                                        \'func_label\': entry[\'func_label\'],\n                                        \'anat_label\': entry[\'anat_label\'],\n                                        \'n_segments\': entry[\'n_segments\'],\n                                        \'midline_side\': entry[\'midline_side\'],\n                                        \'source_stim_types\': \';\'.join(entry[\'source_stim_types\']),\n                                    }\n                                )\n                            if panel_gene_cells:\n                                results_per_cell[panel][gene] = panel_gene_cells\n\n                    results_stim_ipsi_contra_per_cell = results_per_cell\n                    summary_df_stim_ipsi_contra_per_cell = pd.DataFrame(per_cell_rows)\n\n                    fig, axes = plt.subplots(2, 2, figsize=(FIG_WIDTH, FIG_ROW_HEIGHT * 2), sharex=True, sharey=True)\n                    fig.suptitle(\n                        f"Ipsi and contra stimulus responses separate into interpretable gene-level patterns ({PAIR_SCOPE_LABEL}; unilateral only; n = unique ROIs after reused-label dedup)"\n                    )\n\n                    for r_idx, row_panels in enumerate(PANEL_GRID):\n                        for c_idx, panel in enumerate(row_panels):\n                            ax = axes[r_idx, c_idx]\n                            gene_items = [\n                                (g, v)\n                                for g, v in results.get(panel, {}).items()\n                                if v[\'n_segments\'] >= MIN_SEGMENTS and v.get(\'n_cells\', 0) >= MIN_CELLS\n                            ]\n                            if gene_items:\n                                gene_items = sorted(gene_items, key=lambda kv: kv[1][\'n_segments\'], reverse=True)[:MAX_GENES]\n                                for gene, res in gene_items:\n                                    color = gene_colors.get(gene, None)\n                                    ax.plot(\n                                        tvec,\n                                        res[\'mean\'],\n                                        label=_format_gene_roi_usage_label_56h(gene, res.get(\'n_cells\', 0)),\n                                        color=color,\n                                    )\n                                    if USE_SEM and res[\'sem\'] is not None:\n                                        ax.fill_between(\n                                            tvec,\n                                            res[\'mean\'] - res[\'sem\'],\n                                            res[\'mean\'] + res[\'sem\'],\n                                            alpha=0.2,\n                                            color=color,\n                                        )\n                                ax.legend(fontsize=8, ncol=2)\n                            else:\n                                ax.text(0.5, 0.5, \'No data after thresholds\', transform=ax.transAxes,\n                                        ha=\'center\', va=\'center\', fontsize=9, alpha=0.8)\n\n                            mode = \'LB\' if panel.endswith(\'LB\') else \'LC\'\n                            dur_vals = mode_durations.get(mode, [])\n                            if dur_vals:\n                                stim_dur = float(np.median(dur_vals))\n                                ax.axvspan(0, stim_dur, color=\'#cccccc\', alpha=0.2)\n\n                            ax.axvline(0, color=\'k\', linestyle=\'--\', linewidth=1.0)\n                            ax.axhline(0, color=\'k\', linewidth=0.8, alpha=0.6)\n                            _n_ev_mode_label = _mode_events_per_cell_label_56h(mode)\n                            ax.set_title(f"{PLOT_TITLES.get(panel, panel)} reveals a distinct time-locked response (events/cell={_n_ev_mode_label})", fontsize=11)\n\n                            if r_idx == 1:\n                                ax.set_xlabel(\'Time (s)\')\n                            else:\n                                ax.set_xlabel(\'\')\n\n                            if c_idx == 0:\n                                ax.set_ylabel(\'z-scored dF/F\')\n                            else:\n                                ax.set_ylabel(\'\')\n\n                    fig.tight_layout(rect=[0, 0, 1, 0.96])\n                    FIG_56F_LAST = fig\n                    try:\n                        fig.canvas.draw()\n                        FIG_56F_RGBA = np.asarray(fig.canvas.buffer_rgba()).copy()\n                    except Exception:\n                        FIG_56F_RGBA = None\n                    if SHOW_56H_COMPONENT_PLOTS:\n                        plt.show()\n                    else:\n                        plt.close(fig)\n\n                    summary_rows = []\n                    for panel in PLOT_ORDER:\n                        mode = \'LB\' if panel.endswith(\'LB\') else \'LC\'\n                        n_events_mode = len([ev for ev in stim_events if ev[\'mode\'] == mode])\n                        for gene in genes:\n                            res = results.get(panel, {}).get(gene, None)\n                            if res is None:\n                                continue\n                            summary_rows.append({\n                                \'panel\': panel,\n                                \'panel_title\': PLOT_TITLES.get(panel, panel),\n                                \'gene\': gene,\n                                \'n_cells\': int(res.get(\'n_cells\', 0)),\n                                \'n_segments\': int(res[\'n_segments\']),\n                                \'n_trials_per_cell\': float(res.get(\'n_trials_per_cell\', np.nan)),\n                                \'n_unilateral_events_mode\': int(n_events_mode),\n                                \'source_stim_types\': \', \'.join(res.get(\'source_stim_types\', [])),\n                            })\n                    summary_df = pd.DataFrame(summary_rows)\n\n                    summary_df_stim_ipsi_contra = summary_df.copy()\n                    results_stim_ipsi_contra = results\n                    STIM_IPSI_CONTRA_FISH_ID = FISH_ID\n                    stim_events_unilateral_56f = pd.DataFrame(stim_events)\n                    pairs_with_midline_side_56f = pairs.copy()\n                    pairs_from_56f = pairs.copy()\n                    panel_mode_durations_56f = mode_durations\n\n                    if summary_df.empty:\n                        print(\'[stim-56f] Summary is empty.\')\n                    else:\n                        summary_df = summary_df[summary_df[\'n_segments\'] > 0].reset_index(drop=True)\n                        if not summary_df.empty:\n                            try:\n                                display(summary_df)\n                            except Exception:\n                                print(summary_df.to_string(index=False))\n\n# --- Embedded [56e] logic (formerly standalone [56e]) ---\n# Bout Preference Index (BPI) using selected pair scope (HIGH_CONF_ONLY toggle)\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nimport re\nfrom pathlib import Path\n\nBPI_MIN_TRIALS_PER_CLASS = 3\nBPI_DENOM_EPS = 1e-6\nBPI_EDGE_POLICY = \'pad_nan\'\nBPI_MIN_VALID_FRAC = 0.5\nBPI_STIM_ONSET_DELAY_SEC = STIM_ONSET_DELAY_SEC\nZSCORE_MIN_BASELINE_POINTS = 200\nZSCORE_MIN_BASELINE_STD = 1e-6\n\nDEFAULT_GENE_ORDER = [\'sst1.1\', \'sst1.2\', \'npy\', \'tac3b\', \'pth2\', \'cfos\', \'cort\']\nDEFAULT_GENE_COLORS = {\n    \'sst1.1\': \'#d62728\',\n    \'sst1.2\': \'#d61ad2\',\n    \'npy\': \'#1f9d55\',\n    \'tac3b\': \'#ffd400\',\n    \'pth2\': \'#00bcd4\',\n    \'cfos\': \'#ff7f0e\',\n    \'cort\': \'#8c564b\',\n}\nGENE_ORDER = list(RC.get(\'GENE_ORDER\', DEFAULT_GENE_ORDER))\nGENE_COLORS = dict(DEFAULT_GENE_COLORS)\ntry:\n    _gc = RC.get(\'GENE_COLORS\', {})\n    if isinstance(_gc, dict):\n        GENE_COLORS.update(_gc)\nexcept Exception:\n    pass\n\n\ndef _classify_stim_type(stype):\n    parts = [p.strip().upper() for p in str(stype).split(\'+\') if str(p).strip()]\n    if not parts:\n        return None\n    tags = []\n    for p in parts:\n        if p.endswith(\'LB\'):\n            tags.append(\'B\')\n        elif p.endswith(\'LC\'):\n            tags.append(\'C\')\n        else:\n            return None\n    if all(t == \'B\' for t in tags):\n        return \'bout\'\n    if all(t == \'C\' for t in tags):\n        return \'continuous\'\n    return \'mixed\'\n\n\ndef _extract_bpi_window(trace, idx0, idx1, mode=\'pad_nan\', min_valid_frac=0.5):\n    T = int(trace.shape[0])\n    win_len = int(idx1 - idx0)\n    if win_len <= 0:\n        return None, 0, 0\n\n    if mode == \'strict\':\n        if idx0 < 0 or idx1 > T:\n            return None, 0, win_len\n        seg = trace[idx0:idx1]\n        n_valid = int(np.isfinite(seg).sum())\n        return seg, n_valid, win_len\n\n    seg = np.full(win_len, np.nan, dtype=np.float32)\n    src0 = max(int(idx0), 0)\n    src1 = min(int(idx1), T)\n    if src1 > src0:\n        dst0 = src0 - int(idx0)\n        seg[dst0:dst0 + (src1 - src0)] = trace[src0:src1]\n    n_valid = int(np.isfinite(seg).sum())\n    min_required = int(np.ceil(float(min_valid_frac) * float(win_len)))\n    min_required = max(1, min_required)\n    if n_valid < min_required:\n        return None, n_valid, win_len\n    return seg, n_valid, win_len\n\n\ndef _build_prestim_baseline_windows_bpi(df_evt, fps, onset_delay_sec, tag=\'[bpi]\'):\n    if df_evt is None or getattr(df_evt, \'empty\', True):\n        raise RuntimeError(f"{tag} df_evt is empty; run [55].")\n\n    evt = df_evt[[\'event\', \'time\']].copy()\n    evt[\'event\'] = evt[\'event\'].astype(str).str.strip()\n    evt[\'time\'] = pd.to_numeric(evt[\'time\'], errors=\'coerce\')\n    evt = evt.dropna(subset=[\'event\', \'time\']).sort_values(\'time\').reset_index(drop=True)\n\n    stim_starts = {}\n    for ev, t in evt[[\'event\', \'time\']].itertuples(index=False):\n        m = re.match(r\'^(B\\d+)_stim(\\d+)_.+$\', ev)\n        if not m:\n            continue\n        key = (m.group(1), int(m.group(2)))\n        if key not in stim_starts:\n            stim_starts[key] = float(t)\n\n    windows = []\n    missing = 0\n    for ev, t_pre in evt[[\'event\', \'time\']].itertuples(index=False):\n        m = re.match(r\'^(B\\d+)_prestim(\\d+)_pause$\', ev)\n        if not m:\n            continue\n        key = (m.group(1), int(m.group(2)))\n        t_stim = stim_starts.get(key)\n        if t_stim is None:\n            missing += 1\n            continue\n        t0 = float(t_pre)\n        t1 = float(t_stim) + float(onset_delay_sec)\n        if not np.isfinite(t0) or not np.isfinite(t1) or t1 <= t0:\n            continue\n        idx0 = int(round(t0 * float(fps)))\n        idx1 = int(round(t1 * float(fps)))\n        if idx1 > idx0:\n            windows.append((idx0, idx1))\n\n    if not windows:\n        raise RuntimeError(f"{tag} no prestim baseline windows found in df_evt")\n\n    windows = sorted(windows, key=lambda w: (w[0], w[1]))\n    merged = []\n    for s, e in windows:\n        if not merged or s > merged[-1][1]:\n            merged.append([s, e])\n        else:\n            merged[-1][1] = max(merged[-1][1], e)\n    merged = [(int(s), int(e)) for s, e in merged if e > s]\n\n    total_sec = float(sum(e - s for s, e in merged) / float(fps))\n    if missing:\n        print(f"{tag} baseline windows: {len(merged)} (missing {missing} prestim->stim mappings; total {total_sec:.1f}s)")\n    else:\n        print(f"{tag} baseline windows: {len(merged)} (total {total_sec:.1f}s)")\n    return merged\n\n\ndef _compute_zscore_stats_bpi(dff, baseline_windows, min_points=200, sigma_eps=1e-6):\n    n_roi = int(dff.shape[0])\n    T = int(dff.shape[1])\n    mask = np.zeros(T, dtype=bool)\n    for idx0, idx1 in baseline_windows:\n        s = max(0, int(idx0))\n        e = min(T, int(idx1))\n        if e > s:\n            mask[s:e] = True\n\n    mu = np.full(n_roi, np.nan, dtype=np.float32)\n    sigma = np.full(n_roi, np.nan, dtype=np.float32)\n    n_valid = np.zeros(n_roi, dtype=np.int32)\n\n    if mask.any():\n        base = dff[:, mask]\n        n_valid = np.isfinite(base).sum(axis=1).astype(np.int32)\n        with np.errstate(invalid=\'ignore\', divide=\'ignore\'):\n            mu = np.nanmean(base, axis=1).astype(np.float32, copy=False)\n            sigma = np.nanstd(base, axis=1).astype(np.float32, copy=False)\n\n    has_points = n_valid >= int(min_points)\n    has_sigma = np.isfinite(sigma) & (sigma > float(sigma_eps))\n    has_mu = np.isfinite(mu)\n    valid = has_points & has_sigma & has_mu\n    return {\n        \'mu\': mu,\n        \'sigma\': sigma,\n        \'n_valid\': n_valid,\n        \'valid\': valid,\n    }\n\n\nif BPI_EDGE_POLICY not in {\'strict\', \'pad_nan\'}:\n    print(f"[bpi] invalid BPI_EDGE_POLICY=\'{BPI_EDGE_POLICY}\', using \'pad_nan\'")\n    BPI_EDGE_POLICY = \'pad_nan\'\n\nif not (0.0 <= BPI_MIN_VALID_FRAC <= 1.0):\n    raise ValueError(f"[bpi] BPI_MIN_VALID_FRAC must be in [0,1], got {BPI_MIN_VALID_FRAC}")\n\nif _df_stim_56h is None or _df_stim_56h.empty:\n    print(\'[bpi] df_stim is missing; run [55] first.\')\nelif not _suite2p_by_ref_idx_56h:\n    print(\'[bpi] Suite2p data not loaded; run [23a] first.\')\nelif pairs_56h_selected is None:\n    print(\'[bpi] Pair table from [56h] trace pass is unavailable; rerun this cell.\')\nelse:\n    pairs = pairs_56h_selected.copy()\n    if pairs.empty:\n        print(\'[bpi] Selected pair table is empty; nothing to compute.\')\n    else:\n        req_cols = {\'gene\', \'anat_label\', \'plane\', \'func_label\'}\n        missing = [c for c in req_cols if c not in pairs.columns]\n        if missing:\n            raise RuntimeError(f"[bpi] selected pair table missing required columns: {missing}")\n\n        pairs = pairs.copy()\n        pairs[\'gene\'] = pairs[\'gene\'].astype(str)\n        for c in (\'anat_label\', \'plane\', \'func_label\'):\n            pairs[c] = pd.to_numeric(pairs[c], errors=\'coerce\')\n        pairs = pairs[pairs[[\'gene\', \'anat_label\', \'plane\', \'func_label\']].notna().all(axis=1)].copy()\n        pairs[\'anat_label\'] = pairs[\'anat_label\'].astype(int)\n        pairs[\'plane\'] = pairs[\'plane\'].astype(int)\n        pairs[\'func_label\'] = pairs[\'func_label\'].astype(int)\n        pairs = pairs.drop_duplicates(subset=[\'gene\', \'anat_label\'], keep=\'first\').copy()\n\n        s2p_map = _suite2p_by_ref_idx_56h\n        pairs = pairs[pairs[\'plane\'].isin(s2p_map.keys())].copy()\n        if pairs.empty:\n            print(f\'[bpi] No rows from {PAIR_SCOPE_LABEL} map to loaded Suite2p planes.\')\n        else:\n            fps = _frame_rate_56h\n            if fps is None:\n                fs_vals = []\n                for p in s2p_map.values():\n                    fs = p.get(\'ops\', {}).get(\'fs\', None)\n                    if fs is not None:\n                        fs_vals.append(float(fs))\n                if fs_vals:\n                    fps = fs_vals[0]\n                    if len(set(fs_vals)) > 1:\n                        print(\'[bpi] WARNING: multiple fs values found; using the first\')\n            if fps is None or float(fps) <= 0:\n                raise RuntimeError(\'[bpi] Invalid FRAME_RATE. Run [55]/[23a] and ensure framerate is available.\')\n\n            try:\n                baseline_windows = _build_prestim_baseline_windows_bpi(\n                    _df_evt_56h,\n                    fps,\n                    BPI_STIM_ONSET_DELAY_SEC,\n                    tag=\'[bpi]\',\n                )\n            except Exception as e:\n                raise RuntimeError(f"[bpi] unable to build baseline windows for z-scoring: {e}")\n\n            zstats_by_plane = {}\n            for _plane_idx, _plane_data in s2p_map.items():\n                _dff = _plane_data.get(\'dff\', None)\n                if _dff is None:\n                    continue\n                zstats_by_plane[int(_plane_idx)] = _compute_zscore_stats_bpi(\n                    _dff,\n                    baseline_windows,\n                    min_points=ZSCORE_MIN_BASELINE_POINTS,\n                    sigma_eps=ZSCORE_MIN_BASELINE_STD,\n                )\n\n            df_stim = _df_stim_56h.copy()\n            stim_events = []\n            for _, r in df_stim.iterrows():\n                stype = str(r.get(\'type\', \'\'))\n                sclass = _classify_stim_type(stype)\n                if sclass not in {\'bout\', \'continuous\'}:\n                    continue\n                t0, t1, dur = _effective_motion_window_local(\n                    r.get(\'start\', np.nan),\n                    r.get(\'duration\', np.nan),\n                    r.get(\'end\', np.nan),\n                    BPI_STIM_ONSET_DELAY_SEC,\n                )\n                if not np.isfinite(t0) or not np.isfinite(t1) or not np.isfinite(dur):\n                    continue\n                idx0 = int(round(t0 * float(fps)))\n                idx1 = int(round(t1 * float(fps)))\n                if idx1 <= idx0:\n                    continue\n                stim_events.append({\n                    \'block\': r.get(\'block\', np.nan),\n                    \'stim_idx\': int(r.get(\'stim_idx\', -1)) if pd.notna(r.get(\'stim_idx\', np.nan)) else -1,\n                    \'stim_type\': stype,\n                    \'stim_class\': sclass,\n                    \'start_s\': t0,\n                    \'duration_s\': dur,\n                    \'idx0\': idx0,\n                    \'idx1\': idx1,\n                })\n\n            if not stim_events:\n                raise RuntimeError(\'[bpi] No bout/continuous stimulus events found. Mixed stimuli are excluded by design.\')\n\n            df_events = pd.DataFrame(stim_events)\n            print(\n                f"[bpi] events: bout={int((df_events[\'stim_class\']==\'bout\').sum())}, "\n                f"continuous={int((df_events[\'stim_class\']==\'continuous\').sum())}; mixed excluded"\n            )\n\n            trial_rows = []\n            for _, row in pairs.iterrows():\n                gene = str(row[\'gene\'])\n                anat_label = int(row[\'anat_label\'])\n                plane = int(row[\'plane\'])\n                roi_idx = int(row[\'func_label\']) - 1\n\n                dff = s2p_map[plane].get(\'dff\', None)\n                if dff is None:\n                    continue\n                if roi_idx < 0 or roi_idx >= dff.shape[0]:\n                    continue\n                trace = dff[roi_idx].astype(np.float32, copy=False)\n                zstats = zstats_by_plane.get(int(plane), None)\n                trace_z = None\n                if zstats is not None and roi_idx < len(zstats[\'valid\']) and bool(zstats[\'valid\'][roi_idx]):\n                    trace_z = (trace - float(zstats[\'mu\'][roi_idx])) / float(zstats[\'sigma\'][roi_idx])\n\n                for ev in stim_events:\n                    seg, n_valid, n_total = _extract_bpi_window(\n                        trace,\n                        ev[\'idx0\'],\n                        ev[\'idx1\'],\n                        mode=BPI_EDGE_POLICY,\n                        min_valid_frac=BPI_MIN_VALID_FRAC,\n                    )\n                    if seg is None:\n                        continue\n                    resp = float(np.nanmean(seg))\n                    if not np.isfinite(resp):\n                        continue\n\n                    resp_z = np.nan\n                    if trace_z is not None:\n                        seg_z, _, _ = _extract_bpi_window(\n                            trace_z,\n                            ev[\'idx0\'],\n                            ev[\'idx1\'],\n                            mode=BPI_EDGE_POLICY,\n                            min_valid_frac=BPI_MIN_VALID_FRAC,\n                        )\n                        if seg_z is not None:\n                            _resp_z = float(np.nanmean(seg_z))\n                            if np.isfinite(_resp_z):\n                                resp_z = _resp_z\n\n                    trial_rows.append({\n                        \'fish_id\': str(FISH_ID),\n                        \'gene\': gene,\n                        \'anat_label\': anat_label,\n                        \'plane\': plane,\n                        \'func_label\': int(row[\'func_label\']),\n                        \'cell_key\': f"{gene}|{anat_label}",\n                        \'stim_type\': ev[\'stim_type\'],\n                        \'stim_class\': ev[\'stim_class\'],\n                        \'block\': ev[\'block\'],\n                        \'stim_idx\': ev[\'stim_idx\'],\n                        \'response_mean_dff\': resp,\n                        \'response_mean_zdff\': resp_z,\n                        \'n_valid_samples\': int(n_valid),\n                        \'n_total_samples\': int(n_total),\n                        \'duration_s\': float(ev[\'duration_s\']),\n                    })\n\n            trial_df = pd.DataFrame(trial_rows)\n            if trial_df.empty:\n                print(\'[bpi] No valid trial responses after window extraction.\')\n            else:\n                cell_rows = []\n                grp = trial_df.groupby([\'fish_id\', \'gene\', \'anat_label\', \'plane\', \'func_label\', \'cell_key\'], dropna=False)\n                for keys, sub in grp:\n                    bout_vals = sub.loc[sub[\'stim_class\'] == \'bout\', \'response_mean_dff\'].to_numpy(dtype=float)\n                    cont_vals = sub.loc[sub[\'stim_class\'] == \'continuous\', \'response_mean_dff\'].to_numpy(dtype=float)\n                    n_b = int(np.isfinite(bout_vals).sum())\n                    n_c = int(np.isfinite(cont_vals).sum())\n                    if n_b < BPI_MIN_TRIALS_PER_CLASS or n_c < BPI_MIN_TRIALS_PER_CLASS:\n                        continue\n                    b = float(np.nanmean(bout_vals))\n                    c = float(np.nanmean(cont_vals))\n                    denom = b + c\n                    if not np.isfinite(denom) or abs(denom) <= float(BPI_DENOM_EPS):\n                        continue\n                    bpi = float((b - c) / denom)\n                    if not np.isfinite(bpi):\n                        continue\n\n                    bout_vals_z = sub.loc[sub[\'stim_class\'] == \'bout\', \'response_mean_zdff\'].to_numpy(dtype=float)\n                    cont_vals_z = sub.loc[sub[\'stim_class\'] == \'continuous\', \'response_mean_zdff\'].to_numpy(dtype=float)\n                    n_b_z = int(np.isfinite(bout_vals_z).sum())\n                    n_c_z = int(np.isfinite(cont_vals_z).sum())\n                    b_z = float(np.nanmean(bout_vals_z)) if n_b_z > 0 else np.nan\n                    c_z = float(np.nanmean(cont_vals_z)) if n_c_z > 0 else np.nan\n                    denom_z = b_z + c_z if np.isfinite(b_z) and np.isfinite(c_z) else np.nan\n                    bpi_z = np.nan\n                    if n_b_z >= BPI_MIN_TRIALS_PER_CLASS and n_c_z >= BPI_MIN_TRIALS_PER_CLASS:\n                        if np.isfinite(denom_z) and abs(denom_z) > float(BPI_DENOM_EPS):\n                            _bpi_z = float((b_z - c_z) / denom_z)\n                            if np.isfinite(_bpi_z):\n                                bpi_z = _bpi_z\n                    fish_id, gene, anat_label, plane, func_label, cell_key = keys\n                    cell_rows.append({\n                        \'fish_id\': fish_id,\n                        \'gene\': gene,\n                        \'anat_label\': int(anat_label),\n                        \'plane\': int(plane),\n                        \'plane_idx\': int(plane),\n                        \'func_label\': int(func_label),\n                        \'cell_key\': cell_key,\n                        \'n_bout_trials\': n_b,\n                        \'n_cont_trials\': n_c,\n                        \'n_bout_trials_z\': n_b_z,\n                        \'n_cont_trials_z\': n_c_z,\n                        \'mean_bout_dff\': b,\n                        \'mean_cont_dff\': c,\n                        \'mean_bout_zdff\': b_z,\n                        \'mean_cont_zdff\': c_z,\n                        \'denom\': float(denom),\n                        \'denom_z\': float(denom_z) if np.isfinite(denom_z) else np.nan,\n                        \'bpi\': bpi,\n                        \'bpi_z\': bpi_z,\n                    })\n\n                cell_df = pd.DataFrame(cell_rows)\n                if not cell_df.empty:\n                    if \'plane_idx\' not in cell_df.columns and \'plane\' in cell_df.columns:\n                        cell_df[\'plane_idx\'] = pd.to_numeric(cell_df[\'plane\'], errors=\'coerce\').astype(\'Int64\')\n                    if \'plane\' not in cell_df.columns and \'plane_idx\' in cell_df.columns:\n                        cell_df[\'plane\'] = pd.to_numeric(cell_df[\'plane_idx\'], errors=\'coerce\').astype(\'Int64\')\n                if cell_df.empty:\n                    print(\n                        f"[bpi] No cells passed filters (min trials/class={BPI_MIN_TRIALS_PER_CLASS}, "\n                        f"denom eps={BPI_DENOM_EPS})."\n                    )\n                else:\n                    gene_summary = (\n                        cell_df\n                        .groupby(\'gene\', as_index=False)\n                        .agg(\n                            n_cells=(\'bpi\', \'size\'),\n                            median_bpi=(\'bpi\', \'median\'),\n                            mean_bpi=(\'bpi\', \'mean\'),\n                            sd_bpi=(\'bpi\', \'std\'),\n                            pct_bpi_positive=(\'bpi\', lambda x: float(100.0 * np.mean(np.asarray(x) > 0))),\n                        )\n                    )\n                    gene_summary[\'sd_bpi\'] = gene_summary[\'sd_bpi\'].fillna(0.0)\n\n                    genes_present = set(cell_df[\'gene\'].astype(str).unique().tolist())\n                    gene_order = [g for g in GENE_ORDER if g in genes_present] + sorted([g for g in genes_present if g not in GENE_ORDER])\n                    pos = np.arange(len(gene_order), dtype=float)\n                    bpi_values = [cell_df.loc[cell_df[\'gene\'] == g, \'bpi\'].to_numpy(dtype=float) for g in gene_order]\n\n                    fig, ax = plt.subplots(1, 1, figsize=(9, 5))\n\n                    bp = ax.boxplot(\n                        bpi_values,\n                        positions=pos,\n                        widths=0.6,\n                        patch_artist=True,\n                        showfliers=False,\n                    )\n                    for patch, g in zip(bp[\'boxes\'], gene_order):\n                        patch.set_facecolor(GENE_COLORS.get(g, \'#bbbbbb\'))\n                        patch.set_alpha(0.35)\n                        patch.set_edgecolor(\'black\')\n                    for key in (\'whiskers\', \'caps\', \'medians\'):\n                        for artist in bp[key]:\n                            artist.set_color(\'black\')\n                            artist.set_linewidth(1.0)\n\n                    rng = np.random.default_rng(0)\n                    for x0, vals in zip(pos, bpi_values):\n                        if vals.size == 0:\n                            continue\n                        jitter = rng.uniform(-0.12, 0.12, size=vals.size)\n                        ax.scatter(\n                            np.full(vals.size, x0) + jitter,\n                            vals,\n                            s=18,\n                            c=\'black\',\n                            alpha=1.0,\n                            linewidths=0.0,\n                            zorder=3,\n                        )\n\n                    ax.axhline(0.0, color=\'black\', linestyle=\'--\', linewidth=1.0)\n                    xtxt = []\n                    for g in gene_order:\n                        n = int((cell_df[\'gene\'] == g).sum())\n                        xtxt.append(_format_gene_roi_usage_label_56h(g, n))\n                    ax.set_xticks(pos)\n                    ax.set_xticklabels(xtxt)\n                    ax.set_ylabel(\'BPI = (Bout - Continuous) / (Bout + Continuous)\')\n                    ax.set_ylim(-1.0, 1.0)\n                    ax.set_title(f\'Genes differ in stimulus bias across matched ROIs ({PAIR_SCOPE_LABEL}; n = unique ROIs used)\', fontsize=11)\n\n                    fig.suptitle(\n                        f"Bout Preference Index (BPI) from mean dF/F during stimulus window; "\n                        f"z-scored responses are also saved (mean_bout_zdff/mean_cont_zdff/bpi_z); "\n                        f"mixed stimuli excluded; min trials/class={BPI_MIN_TRIALS_PER_CLASS}"\n                    )\n                    fig.tight_layout(rect=[0, 0, 1, 0.95])\n\n                    bpi_trials_df = trial_df.copy()\n                    bpi_cells_df = cell_df.copy()\n                    bpi_gene_summary_df = gene_summary.copy()\n                    BPI_FISH_ID = FISH_ID\n                    FIG_BPI_LAST = fig\n                    try:\n                        fig.canvas.draw()\n                        FIG_BPI_RGBA = np.asarray(fig.canvas.buffer_rgba()).copy()\n                    except Exception:\n                        FIG_BPI_RGBA = None\n\n                    out_dir = FISH_PLOTS_DIR_56H\n                    if out_dir is not None:\n                        out_dir.mkdir(parents=True, exist_ok=True)\n                        out_name = \'bpi_high_conf.png\' if HIGH_CONF_ONLY else \'bpi_all_pairs.png\'\n                        out_path = out_dir / out_name\n                        fig.savefig(out_path, dpi=FIG_SAVE_DPI, bbox_inches=\'tight\')\n                        fig.savefig(out_path.with_suffix(\'.pdf\'), bbox_inches=\'tight\')\n                        FIG_BPI_PATH = str(out_path)\n                        print(f"[bpi] Saved figure to {out_path}")\n\n                    print(\n                        f"[bpi] cells used={len(cell_df)} (genes={len(gene_order)}); "\n                        f"trial rows={len(trial_df)}"\n                    )\n                    try:\n                        display(gene_summary.sort_values(\'gene\').reset_index(drop=True))\n                    except Exception:\n                        print(gene_summary.sort_values(\'gene\').to_string(index=False))\n\n                    if SHOW_56H_COMPONENT_PLOTS:\n                        plt.show()\n                    else:\n                        plt.close(fig)\n\n# --- Unified [56h] render ---\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nimport matplotlib.colors as mcolors\nfrom pathlib import Path\nfrom matplotlib.patches import Patch\n\nif bpi_cells_df is None or len(bpi_cells_df) == 0:\n    print(\'[56h] Missing bpi_cells_df after embedded computation; check upstream inputs.\')\n    raise SystemExit\nif results_stim_ipsi_contra is None:\n    print(\'[56h] Missing results_stim_ipsi_contra after embedded computation; check upstream inputs.\')\n    raise SystemExit\nif results_stim_ipsi_contra_per_cell is None:\n    print(\'[56h] Missing results_stim_ipsi_contra_per_cell after embedded computation; check upstream inputs.\')\n    raise SystemExit\n\ncell_df = bpi_cells_df.copy()\nresults = results_stim_ipsi_contra\nresults_per_cell = results_stim_ipsi_contra_per_cell\n\n# Defaults aligned with [56e]/[56f]\nDEFAULT_GENE_ORDER = [\'sst1.1\', \'sst1.2\', \'npy\', \'tac3b\', \'pth2\', \'cfos\', \'cort\']\nDEFAULT_GENE_COLORS = {\n    \'sst1.1\': \'#d62728\',\n    \'sst1.2\': \'#d61ad2\',\n    \'npy\': \'#1f9d55\',\n    \'tac3b\': \'#ffd400\',\n    \'pth2\': \'#00bcd4\',\n    \'cfos\': \'#ff7f0e\',\n    \'cort\': \'#8c564b\',\n}\nGENE_ORDER = list(RC.get(\'GENE_ORDER\', DEFAULT_GENE_ORDER))\nGENE_COLORS = dict(DEFAULT_GENE_COLORS)\ntry:\n    _gc = RC.get(\'GENE_COLORS\', {})\n    if isinstance(_gc, dict):\n        GENE_COLORS.update(_gc)\nexcept Exception:\n    pass\n\nPLOT_ORDER = [\'contra_LB\', \'contra_LC\', \'ipsi_LB\', \'ipsi_LC\']\nPANEL_GRID = [[\'contra_LB\', \'contra_LC\'], [\'ipsi_LB\', \'ipsi_LC\']]\nPLOT_TITLES = {\n    \'contra_LB\': \'Contra \xd7 bout-like\',\n    \'contra_LC\': \'Contra \xd7 continuous\',\n    \'ipsi_LB\': \'Ipsi \xd7 bout-like\',\n    \'ipsi_LC\': \'Ipsi \xd7 continuous\',\n}\n\nHCR_RESPONSIVE_STATUS = \'in-plane responsive ROI\'\nHCR_LOW_STATUS = \'in-plane low-activity ROI\'\nHCR_UNAVAILABLE_STATUS = \'in-plane response unavailable\'\nHCR_NO_FUNC_STATUS = \'in-plane no functional ROI candidate\'\nHCR_OUT_OF_PLANE_STATUS = \'out-of-plane anatomy label\'\nHCR_INNER_WITHIN = \'within functional planes\'\nHCR_INNER_OUTSIDE = \'outside functional planes\'\nHCR_RESPONSE_SUMMARY_RESPONSIVE = \'Responsive neurons\'\nHCR_RESPONSE_SUMMARY_LOW = \'Low activity\'\nHCR_RESPONSE_SUMMARY_UNAVAILABLE = \'Response unavailable\'\nHCR_INNER_UNMATCHED = \'unmatched\'\nHCR_STATUS_ORDER = [\n    HCR_RESPONSIVE_STATUS,\n    HCR_LOW_STATUS,\n    HCR_UNAVAILABLE_STATUS,\n    HCR_NO_FUNC_STATUS,\n    HCR_OUT_OF_PLANE_STATUS,\n    HCR_INNER_UNMATCHED,\n]\nHCR_INNER_ORDER = [HCR_INNER_WITHIN, HCR_INNER_OUTSIDE, HCR_INNER_UNMATCHED]\nHCR_OUTER_COLORS = {\n    HCR_RESPONSIVE_STATUS: \'#1b9e77\',\n    HCR_LOW_STATUS: \'#6a6a6a\',\n    HCR_UNAVAILABLE_STATUS: \'#fdb462\',\n    HCR_NO_FUNC_STATUS: \'#d73027\',\n    HCR_OUT_OF_PLANE_STATUS: \'#80b1d3\',\n    HCR_INNER_UNMATCHED: \'#bdbdbd\',\n}\nHCR_INNER_COLORS = {\n    HCR_INNER_WITHIN: \'#4daf4a\',\n    HCR_INNER_OUTSIDE: \'#377eb8\',\n    HCR_INNER_UNMATCHED: \'#bdbdbd\',\n}\nHCR_OUTER_LABELS = {\n    HCR_RESPONSIVE_STATUS: \'responsive ROI\',\n    HCR_LOW_STATUS: \'low-response ROI\',\n    HCR_UNAVAILABLE_STATUS: \'response unavailable\',\n    HCR_NO_FUNC_STATUS: \'no functional match\',\n    HCR_OUT_OF_PLANE_STATUS: \'out of plane\',\n    HCR_INNER_UNMATCHED: \'unmatched\',\n}\nHCR_INNER_SHORT_LABELS = {\n    HCR_INNER_WITHIN: \'in\',\n    HCR_INNER_OUTSIDE: \'out\',\n    HCR_INNER_UNMATCHED: \'unmatched\',\n}\n\nMIN_SEGMENTS = 3\nMIN_CELLS = 1\nMAX_GENES = 12\nUSE_SEM = True\nWINDOW_PRE_SEC = 20.0\nWINDOW_POST_SEC = 50.0\n\n# Layout controls\nFIG_WIDTH = FIG_WIDTH_DEFAULT_IN\nTOP_HEIGHT = 4.6\nROW_HEIGHT = 3.6\nHSPACE = 0.36\nWSPACE = 0.20\nTRACE_YMIN = -5.0\nTRACE_YMAX = 10.0\n\n# Per-gene trace grid controls\nPER_GENE_TRACE_FIG_WIDTH = FIG_WIDTH_DEFAULT_IN\nPER_GENE_TRACE_ROW_HEIGHT = 3.2\nPER_GENE_TRACE_HSPACE = 0.18\nPER_GENE_TRACE_BLOCK_GAP_SEC = 3.0\nPER_GENE_TRACE_LINEWIDTH_CELL = 1.0\nPER_GENE_TRACE_ALPHA_CELL = 0.65\nPER_GENE_TRACE_LOW_ACTIVITY_BASE_COLOR = "#000000"\n\nPER_GENE_TRACE_LOW_ACTIVITY_SHADE_MIN = 0.05\nPER_GENE_TRACE_LOW_ACTIVITY_SHADE_MAX = 0.2 \n\nPER_GENE_TRACE_LINEWIDTH_MEAN = 2.2\nPER_GENE_TRACE_ALPHA_MEAN = 0.95\nPER_GENE_TRACE_SHOW_MEAN = True\n\nPER_GENE_TRACE_SHADE_MIN = 0.05 \nPER_GENE_TRACE_SHADE_MAX = 0.80 # Increase to make the shaded error region lighter, decrease to make it darker\n\nPER_GENE_TRACE_TITLE_Y = 0.982\nPER_GENE_TRACE_TOP_LABEL_Y = 1.005\nPER_GENE_TRACE_YMIN = TRACE_YMIN\nPER_GENE_TRACE_YMAX = TRACE_YMAX\nPER_GENE_TRACE_BLOCK_BG_ALPHA = 0.12\nPER_GENE_TRACE_STIM_BG_ALPHA = 0.22\nPER_GENE_TRACE_MODE_BG_COLORS = {\'LB\': \'#2c7fb8\', \'LC\': \'#d95f0e\'}\nPER_GENE_TRACE_SIDE_BG_BLEND = {\'contra\': 0.78, \'ipsi\': 0.88}\nPER_GENE_TRACE_MAIN_WIDTH_RATIO = 7.4 \nPER_GENE_TRACE_DONUT_WIDTH_RATIO = 2.2 \nPER_GENE_TRACE_DONUT_WSPACE = 0.4 # space between main trace and donut, as fraction of main width\nPER_GENE_TRACE_BOTTOM = 0.2\nPER_GENE_TRACE_TOP = 0.88\nPER_GENE_TRACE_LEGEND_Y = 0.02\nPER_GENE_TRACE_DONUT_OUTER_RADIUS = 1.05\nPER_GENE_TRACE_DONUT_WIDTH_OUTER = 0.15\nPER_GENE_TRACE_DONUT_WIDTH_INNER = 0.30\nPER_GENE_TRACE_DONUT_RING_GAP = 0.03\nPER_GENE_TRACE_DONUT_OUTER_FONT = 9.0\nPER_GENE_TRACE_DONUT_INNER_FONT = 9.0\nPER_GENE_TRACE_DONUT_CENTER_FONT = 11.0\nPER_GENE_TRACE_DONUT_TITLE_FONT = 11.0\nPER_GENE_TRACE_PANEL_TITLE_FONT = 11.0\nPER_GENE_TRACE_PANEL_TITLE_Y_OFFSET = 0.028\nPER_GENE_TRACE_DONUT_CENTER_LABEL = \'labels\'\nPER_GENE_TRACE_DONUT_VIEW_SCALE = float(RUN_CONFIG_LOCAL.get(\'PER_GENE_TRACE_DONUT_VIEW_SCALE\', 0.55)) # decrease to zoom in, increase to zoom out; 1.0 means the radius defined by PER_GENE_TRACE_DONUT_OUTER_RADIUS will just fit in the view\nPER_GENE_TRACE_DONUT_VIEW_XMIN = float(RUN_CONFIG_LOCAL.get(\'PER_GENE_TRACE_DONUT_VIEW_XMIN\', -1.35))\nPER_GENE_TRACE_DONUT_VIEW_XMAX = float(RUN_CONFIG_LOCAL.get(\'PER_GENE_TRACE_DONUT_VIEW_XMAX\', 1.35))\nPER_GENE_TRACE_DONUT_VIEW_YMIN = float(RUN_CONFIG_LOCAL.get(\'PER_GENE_TRACE_DONUT_VIEW_YMIN\', -1.25))\nPER_GENE_TRACE_DONUT_VIEW_YMAX = float(RUN_CONFIG_LOCAL.get(\'PER_GENE_TRACE_DONUT_VIEW_YMAX\', 1.25))\n# Local per-condition trace window. These apply to every merged segment simultaneously.\nPER_GENE_TRACE_X_MIN = -10\nPER_GENE_TRACE_X_MAX = 30\n\n\ndef _blend_with_white_56h(color, white_mix):\n    rgb = np.asarray(mcolors.to_rgb(color), dtype=float)\n    white_mix = float(np.clip(white_mix, 0.0, 1.0))\n    return tuple(((1.0 - white_mix) * rgb) + (white_mix * np.ones(3, dtype=float)))\n\n\ndef _build_gene_shade_map_56h(base_color, roi_keys, shade_min=0.10, shade_max=0.70):\n    roi_keys = list(roi_keys)\n    if not roi_keys:\n        return {}\n    if len(roi_keys) == 1:\n        white_fracs = [0.5 * (float(shade_min) + float(shade_max))]\n    else:\n        white_fracs = np.linspace(float(shade_min), float(shade_max), len(roi_keys))\n    return {\n        roi_key: _blend_with_white_56h(base_color, white_frac)\n        for roi_key, white_frac in zip(roi_keys, white_fracs)\n    }\n\n\ndef _condition_bg_color_56h(panel):\n    side = \'contra\' if str(panel).startswith(\'contra\') else \'ipsi\'\n    mode = \'LB\' if str(panel).endswith(\'LB\') else \'LC\'\n    base_color = PER_GENE_TRACE_MODE_BG_COLORS.get(mode, \'#cccccc\')\n    white_mix = PER_GENE_TRACE_SIDE_BG_BLEND.get(side, 0.84)\n    return _blend_with_white_56h(base_color, white_mix)\n\n\ndef _bool_from_any_56h(value):\n    if value is None:\n        return False\n    try:\n        if pd.isna(value):\n            return False\n    except Exception:\n        pass\n    if isinstance(value, (bool, np.bool_)):\n        return bool(value)\n    if isinstance(value, (int, np.integer)):\n        return bool(value)\n    if isinstance(value, (float, np.floating)):\n        if not np.isfinite(value):\n            return False\n        return bool(int(value))\n    return str(value).strip().lower() in {\'1\', \'true\', \'t\', \'yes\', \'y\'}\n\n\ndef _wedge_midpoint_56h(wedge, radius):\n    theta = np.deg2rad((float(wedge.theta1) + float(wedge.theta2)) / 2.0)\n    return theta, float(radius) * np.cos(theta), float(radius) * np.sin(theta)\n\n\ndef _tangent_rotation_56h(theta_rad):\n    theta_deg = ((float(np.rad2deg(theta_rad)) + 180.0) % 360.0) - 180.0\n    rot = theta_deg - 90.0\n    if rot < -90.0:\n        rot += 180.0\n    elif rot > 90.0:\n        rot -= 180.0\n    return rot\n\n\ndef _contrast_text_color_56h(color):\n    try:\n        r, g, b = mcolors.to_rgb(color)\n    except Exception:\n        return \'black\'\n    luminance = 0.2126 * r + 0.7152 * g + 0.0722 * b\n    return \'white\' if luminance < 0.52 else \'black\'\n\n\ndef _prepare_hcr_donut_counts_by_gene_56h(status_csv, master_csv):\n    status_csv = Path(status_csv)\n    master_csv = Path(master_csv)\n    if not status_csv.exists() or not master_csv.exists():\n        return {}\n\n    status_df = pd.read_csv(status_csv)\n    master_df = pd.read_csv(master_csv)\n\n    if \'fish_id\' in status_df.columns:\n        status_df = status_df[status_df[\'fish_id\'].astype(str) == str(FISH_ID)].copy()\n    if \'fish_id\' in master_df.columns:\n        master_df = master_df[master_df[\'fish_id\'].astype(str) == str(FISH_ID)].copy()\n\n    required_master_cols = {\'plane_idx\', \'func_label\', \'response_class\', \'response_summary_class\', \'response_is_active\'}\n    if status_df.empty or master_df.empty or bool(required_master_cols - set(master_df.columns)):\n        return {}\n\n    status_df[\'gene\'] = status_df[\'gene\'].astype(str)\n    status_df[\'functional_status_legacy\'] = status_df[\'functional_status\'].astype(str)\n    status_df[\'selected_plane_idx_key\'] = pd.to_numeric(\n        status_df.get(\'selected_plane\', pd.Series(index=status_df.index, dtype=float)),\n        errors=\'coerce\',\n    ).astype(\'Int64\')\n    status_df[\'selected_func_label_key\'] = pd.to_numeric(\n        status_df.get(\'selected_func_label\', pd.Series(index=status_df.index, dtype=float)),\n        errors=\'coerce\',\n    ).astype(\'Int64\')\n\n    prior_response_class = status_df.get(\'response_class\', pd.Series(pd.NA, index=status_df.index, dtype=\'object\')).copy()\n    prior_response_summary_class = status_df.get(\'response_summary_class\', pd.Series(pd.NA, index=status_df.index, dtype=\'object\')).copy()\n    prior_response_is_active = status_df.get(\'response_is_active\', pd.Series(pd.NA, index=status_df.index, dtype=\'object\')).copy()\n\n    response_lookup = master_df[[\'plane_idx\', \'func_label\', \'response_class\', \'response_summary_class\', \'response_is_active\']].copy()\n    response_lookup[\'plane_idx_key\'] = pd.to_numeric(response_lookup[\'plane_idx\'], errors=\'coerce\').astype(\'Int64\')\n    response_lookup[\'func_label_key\'] = pd.to_numeric(response_lookup[\'func_label\'], errors=\'coerce\').astype(\'Int64\')\n    response_lookup = (\n        response_lookup\n        .drop(columns=[\'plane_idx\', \'func_label\'])\n        .drop_duplicates(subset=[\'plane_idx_key\', \'func_label_key\'], keep=\'last\')\n        .reset_index(drop=True)\n        .rename(columns={\n            \'response_class\': \'_lookup_response_class\',\n            \'response_summary_class\': \'_lookup_response_summary_class\',\n            \'response_is_active\': \'_lookup_response_is_active\',\n        })\n    )\n\n    status_df = status_df.merge(\n        response_lookup,\n        left_on=[\'selected_plane_idx_key\', \'selected_func_label_key\'],\n        right_on=[\'plane_idx_key\', \'func_label_key\'],\n        how=\'left\',\n    )\n    lookup_response_class = status_df.get(\'_lookup_response_class\', pd.Series(pd.NA, index=status_df.index, dtype=\'object\'))\n    lookup_response_summary_class = status_df.get(\'_lookup_response_summary_class\', pd.Series(pd.NA, index=status_df.index, dtype=\'object\'))\n    lookup_response_is_active = status_df.get(\'_lookup_response_is_active\', pd.Series(pd.NA, index=status_df.index, dtype=\'object\'))\n    status_df[\'response_class\'] = lookup_response_class.where(lookup_response_class.notna(), prior_response_class)\n    status_df[\'response_summary_class\'] = lookup_response_summary_class.where(lookup_response_summary_class.notna(), prior_response_summary_class)\n    status_df[\'response_is_active\'] = lookup_response_is_active.where(lookup_response_is_active.notna(), prior_response_is_active)\n    status_df[\'response_class\'] = status_df[\'response_class\'].fillna(\'response unavailable\').astype(str)\n    status_df[\'response_summary_class\'] = status_df[\'response_summary_class\'].fillna(HCR_RESPONSE_SUMMARY_UNAVAILABLE).astype(str)\n    status_df[\'response_is_active\'] = status_df[\'response_is_active\'].map(_bool_from_any_56h).astype(bool)\n\n    status_df[\'outer_status\'] = np.select(\n        [\n            status_df[\'functional_status_legacy\'].eq(HCR_OUT_OF_PLANE_STATUS),\n            status_df[\'functional_status_legacy\'].eq(HCR_NO_FUNC_STATUS),\n            status_df[\'response_summary_class\'].eq(HCR_RESPONSE_SUMMARY_RESPONSIVE),\n            status_df[\'response_summary_class\'].eq(HCR_RESPONSE_SUMMARY_LOW),\n        ],\n        [\n            HCR_OUT_OF_PLANE_STATUS,\n            HCR_NO_FUNC_STATUS,\n            HCR_RESPONSIVE_STATUS,\n            HCR_LOW_STATUS,\n        ],\n        default=HCR_UNAVAILABLE_STATUS,\n    )\n\n    def _compute_hq_mask_counts_by_gene_local():\n        counts = {}\n        summary_table = None\n        if isinstance(summary_table, pd.DataFrame) and (not summary_table.empty):\n            summary = summary_table.copy()\n            if \'fish_id\' in summary.columns:\n                summary = summary[summary[\'fish_id\'].astype(str) == str(FISH_ID)].copy()\n            required = {\'gene\', \'conf_labels_after_prewarp_filter\', \'n_low_conf_q95_prewarp\'}\n            if required.issubset(summary.columns):\n                summary[\'gene\'] = summary[\'gene\'].astype(str)\n                after_vals = pd.to_numeric(summary[\'conf_labels_after_prewarp_filter\'], errors=\'coerce\').fillna(0)\n                low_vals = pd.to_numeric(summary[\'n_low_conf_q95_prewarp\'], errors=\'coerce\').fillna(0)\n                hq_vals = (after_vals - low_vals).clip(lower=0).astype(int)\n                for g, n in zip(summary[\'gene\'], hq_vals):\n                    counts[str(g)] = counts.get(str(g), 0) + int(n)\n        if counts:\n            return counts\n\n        for res in (_hcr_match_results_56h or []):\n            mask_path = str(res.get(\'mask_path\', \'\'))\n            gene = _infer_gene_from_mask(mask_path)\n            warped = np.asarray(res.get(\'warped\', np.zeros((0, 0, 0), dtype=np.uint16)))\n            if warped.size == 0:\n                continue\n            labels = np.unique(warped)\n            labels = labels[labels > 0].astype(int)\n            low_conf = set()\n            fs = res.get(\'filter_stats\', {}) if isinstance(res.get(\'filter_stats\', {}), dict) else {}\n            for v in list(fs.get(\'low_conf_labels\', [])):\n                try:\n                    low_conf.add(int(v))\n                except Exception:\n                    pass\n            n_hq = int(sum(1 for l in labels if int(l) not in low_conf))\n            counts[str(gene)] = counts.get(str(gene), 0) + n_hq\n        return counts\n\n    hq_counts_by_gene = _compute_hq_mask_counts_by_gene_local()\n    status_df[\'gene\'] = status_df[\'gene\'].astype(str)\n    within_counts = status_df[~status_df[\'functional_status_legacy\'].eq(HCR_OUT_OF_PLANE_STATUS)].groupby(\'gene\').size().to_dict()\n    outside_counts = status_df[status_df[\'functional_status_legacy\'].eq(HCR_OUT_OF_PLANE_STATUS)].groupby(\'gene\').size().to_dict()\n\n    by_gene = {}\n    all_genes = sorted(set([str(g) for g in status_df[\'gene\'].dropna().astype(str).tolist()]) | set([str(g) for g in hq_counts_by_gene.keys()]))\n    for gene in all_genes:\n        n_within = int(within_counts.get(str(gene), 0))\n        n_outside = int(outside_counts.get(str(gene), 0))\n        n_hq = int(hq_counts_by_gene.get(str(gene), n_within + n_outside))\n        n_unmatched = max(0, int(n_hq) - int(n_within) - int(n_outside))\n\n        inner_counts = {\n            HCR_INNER_WITHIN: int(n_within),\n            HCR_INNER_OUTSIDE: int(n_outside),\n            HCR_INNER_UNMATCHED: int(n_unmatched),\n        }\n        outer_counts = {label: 0 for label in HCR_STATUS_ORDER}\n        sub = status_df[status_df[\'gene\'].astype(str) == str(gene)].copy()\n        for label in HCR_STATUS_ORDER:\n            if label == HCR_INNER_UNMATCHED:\n                outer_counts[label] = int(n_unmatched)\n            else:\n                outer_counts[label] = int((sub[\'outer_status\'] == label).sum())\n\n        by_gene[str(gene)] = {\n            \'n_labels\': int(n_within + n_outside + n_unmatched),\n            \'inner_counts\': inner_counts,\n            \'outer_counts\': outer_counts,\n        }\n    return by_gene\n\n# Determine genes present in either panel\ngenes_bpi = set(cell_df[\'gene\'].dropna().astype(str).unique().tolist())\ngenes_trace = set()\nfor panel in PLOT_ORDER:\n    genes_trace.update(list((results.get(panel, {}) or {}).keys()))\ngenes_present = genes_bpi | genes_trace\nif not genes_present:\n    print(\'[56h] No genes available after filtering.\')\n    raise SystemExit\n\ngenes = [g for g in GENE_ORDER if g in genes_present] + sorted([g for g in genes_present if g not in GENE_ORDER])\ngene_colors = {g: GENE_COLORS.get(g, \'#666666\') for g in genes}\n\ngenes_trace_per_cell = set()\ngene_roi_keys_per_cell = {}\nfor panel in PLOT_ORDER:\n    for gene, roi_map in (results_per_cell.get(panel, {}) or {}).items():\n        if not roi_map:\n            continue\n        genes_trace_per_cell.add(str(gene))\n        gene_roi_keys_per_cell.setdefault(str(gene), set()).update(list(roi_map.keys()))\n\nstatus_csv_56h = OUT_REG / \'hcr_activity_status.csv\'\nmaster_csv_56h = OUT_REG / \'functional_roi_activity_identity.csv\'\nhcr_donut_counts_by_gene_56h = _prepare_hcr_donut_counts_by_gene_56h(status_csv_56h, master_csv_56h)\nif not hcr_donut_counts_by_gene_56h:\n    print(\'[56h] HCR status donuts unavailable; missing or incomplete hcr_activity_status/master ROI table.\')\n\n# Build a shared time vector from first available trace\nn_t = None\nfor panel in PLOT_ORDER:\n    pdat = results.get(panel, {}) or {}\n    for g in genes:\n        res = pdat.get(g, None)\n        if res is None:\n            continue\n        m = res.get(\'mean\', None)\n        if m is None:\n            continue\n        try:\n            n_t = int(len(m))\n        except Exception:\n            n_t = None\n        if n_t and n_t > 1:\n            break\n    if n_t and n_t > 1:\n        break\n\nif not n_t or n_t <= 1:\n    print(\'[56h] No valid trace arrays found in results_stim_ipsi_contra; check embedded [56f]-style inputs.\')\n    raise SystemExit\n\n# Use same nominal window as [56f]\ntvec = np.linspace(-WINDOW_PRE_SEC, WINDOW_POST_SEC, n_t)\n\n# Event counts/durations (if available)\nstim_events_df = stim_events_unilateral_56f\nmode_event_counts = {\'LB\': 0, \'LC\': 0}\nmode_event_counts_by_side = {\n    \'left\': {\'LB\': 0, \'LC\': 0},\n    \'right\': {\'LB\': 0, \'LC\': 0},\n}\nif isinstance(stim_events_df, pd.DataFrame) and not stim_events_df.empty and \'mode\' in stim_events_df.columns:\n    for mode in (\'LB\', \'LC\'):\n        mode_event_counts[mode] = int((stim_events_df[\'mode\'].astype(str) == mode).sum())\n    if \'side\' in stim_events_df.columns:\n        _side_norm = stim_events_df[\'side\'].astype(str).str.strip().str.lower()\n        for _side in (\'left\', \'right\'):\n            for _mode in (\'LB\', \'LC\'):\n                mode_event_counts_by_side[_side][_mode] = int(((_side_norm == _side) & (stim_events_df[\'mode\'].astype(str) == _mode)).sum())\n    else:\n        for _mode in (\'LB\', \'LC\'):\n            half = int(mode_event_counts[_mode] // 2)\n            mode_event_counts_by_side[\'left\'][_mode] = half\n            mode_event_counts_by_side[\'right\'][_mode] = int(mode_event_counts[_mode] - half)\n\ndef _mode_events_per_cell_label_unified(mode):\n    left_n = int(mode_event_counts_by_side.get(\'left\', {}).get(mode, 0))\n    right_n = int(mode_event_counts_by_side.get(\'right\', {}).get(mode, 0))\n    if left_n == right_n:\n        return str(left_n)\n    return f"{min(left_n, right_n)}-{max(left_n, right_n)}"\n\nmode_durations = panel_mode_durations_56f\nif not isinstance(mode_durations, dict):\n    mode_durations = {}\n\n# Create unified figure: top full-width + bottom 2x2\nfig = plt.figure(figsize=(FIG_WIDTH, TOP_HEIGHT + 2 * ROW_HEIGHT + 0.4))\ngs = fig.add_gridspec(3, 2, height_ratios=[TOP_HEIGHT / ROW_HEIGHT, 1.0, 1.0], hspace=HSPACE, wspace=WSPACE)\nax_bpi = fig.add_subplot(gs[0, :])\naxes = np.array([\n    [fig.add_subplot(gs[1, 0]), fig.add_subplot(gs[1, 1])],\n    [fig.add_subplot(gs[2, 0]), fig.add_subplot(gs[2, 1])],\n])\n\n# ---- Top: BPI box + points (same logic as [56e]) ----\nbpi_gene_order = [g for g in genes if g in genes_bpi]\npos = np.arange(len(bpi_gene_order), dtype=float)\nbpi_values = [cell_df.loc[cell_df[\'gene\'] == g, \'bpi\'].to_numpy(dtype=float) for g in bpi_gene_order]\n\nif len(bpi_gene_order) == 0:\n    ax_bpi.text(0.5, 0.5, \'No BPI data available\', transform=ax_bpi.transAxes, ha=\'center\', va=\'center\')\n    ax_bpi.set_axis_off()\nelse:\n    bp = ax_bpi.boxplot(\n        bpi_values,\n        positions=pos,\n        widths=0.6,\n        patch_artist=True,\n        showfliers=False,\n    )\n    for patch, g in zip(bp[\'boxes\'], bpi_gene_order):\n        patch.set_facecolor(gene_colors.get(g, \'#bbbbbb\'))\n        patch.set_alpha(0.35)\n        patch.set_edgecolor(\'black\')\n    for key in (\'whiskers\', \'caps\', \'medians\'):\n        for artist in bp[key]:\n            artist.set_color(\'black\')\n            artist.set_linewidth(1.0)\n\n    rng = np.random.default_rng(0)\n    for x0, vals in zip(pos, bpi_values):\n        if vals.size == 0:\n            continue\n        jitter = rng.uniform(-0.12, 0.12, size=vals.size)\n        ax_bpi.scatter(\n            np.full(vals.size, x0) + jitter,\n            vals,\n            s=18,\n            c=\'black\',\n            alpha=1.0,\n            linewidths=0.0,\n            zorder=3,\n        )\n\n    ax_bpi.axhline(0.0, color=\'black\', linestyle=\'--\', linewidth=1.0)\n    xtxt = []\n    for g in bpi_gene_order:\n        n = int((cell_df[\'gene\'] == g).sum())\n        xtxt.append(_format_gene_roi_usage_label_56h(g, n))\n    ax_bpi.set_xticks(pos)\n    ax_bpi.set_xticklabels(xtxt)\n    ax_bpi.set_ylabel(\'BPI = (Bout - Continuous) / (Bout + Continuous)\')\n    ax_bpi.set_ylim(-1.0, 1.0)\n    ax_bpi.set_title(f\'Genes differ in stimulus bias across matched ROIs ({PAIR_SCOPE_LABEL}; n = unique ROIs used)\', fontsize=11)\n\n# ---- Bottom: 2x2 traces (same logic as [56f]) ----\nfor r_idx, row_panels in enumerate(PANEL_GRID):\n    for c_idx, panel in enumerate(row_panels):\n        ax = axes[r_idx, c_idx]\n        pdat = results.get(panel, {}) or {}\n\n        gene_items = []\n        for g in genes:\n            res = pdat.get(g, None)\n            if res is None:\n                continue\n            n_segments = int(res.get(\'n_segments\', 0))\n            n_cells = int(res.get(\'n_cells\', 0))\n            if n_segments >= MIN_SEGMENTS and n_cells >= MIN_CELLS:\n                gene_items.append((g, res))\n\n        if gene_items:\n            gene_items = sorted(gene_items, key=lambda kv: int(kv[1].get(\'n_segments\', 0)), reverse=True)[:MAX_GENES]\n            for gene, res in gene_items:\n                mean = np.asarray(res.get(\'mean\', []), dtype=float)\n                sem = res.get(\'sem\', None)\n                color = gene_colors.get(gene, None)\n                if mean.size != tvec.size:\n                    continue\n                ax.plot(tvec, mean, label=_format_gene_roi_usage_label_56h(gene, res.get(\'n_cells\', 0)), color=color)\n                if USE_SEM and sem is not None:\n                    sem_arr = np.asarray(sem, dtype=float)\n                    if sem_arr.size == mean.size:\n                        ax.fill_between(tvec, mean - sem_arr, mean + sem_arr, alpha=0.2, color=color)\n            ax.legend(fontsize=8, ncol=2)\n        else:\n            ax.text(0.5, 0.5, \'No data after thresholds\', transform=ax.transAxes,\n                    ha=\'center\', va=\'center\', fontsize=9, alpha=0.8)\n\n        mode = \'LB\' if panel.endswith(\'LB\') else \'LC\'\n        dur_vals = mode_durations.get(mode, [])\n        if isinstance(dur_vals, (list, tuple, np.ndarray)) and len(dur_vals) > 0:\n            stim_dur = float(np.median(np.asarray(dur_vals, dtype=float)))\n            if np.isfinite(stim_dur) and stim_dur > 0:\n                ax.axvspan(0, stim_dur, color=\'#cccccc\', alpha=0.2)\n\n        ax.axvline(0, color=\'k\', linestyle=\'--\', linewidth=1.0)\n        ax.axhline(0, color=\'k\', linewidth=0.8, alpha=0.6)\n        _n_ev_mode = _mode_events_per_cell_label_unified(mode)\n        ax.set_title(f"{PLOT_TITLES.get(panel, panel)} reveals a distinct time-locked response (events/cell={_n_ev_mode})", fontsize=11)\n\n        if r_idx == 1:\n            ax.set_xlabel(\'Time (s)\')\n        else:\n            ax.set_xlabel(\'\')\n        if c_idx == 0:\n            ax.set_ylabel(\'z-scored dF/F\')\n        else:\n            ax.set_ylabel(\'\')\n\n        ax.set_ylim(TRACE_YMIN, TRACE_YMAX)\n\nfig.suptitle(f\'Stimulus bias and time-locked responses support the same gene-level differences ({PAIR_SCOPE_LABEL}; n = unique ROIs after reused-label dedup)\', y=0.995)\nfig.tight_layout(rect=[0, 0, 1, 0.98])\n\nFIG_56E_56F_COMBINED = fig\nFIG_56H_FISH_ID = FISH_ID\ntry:\n    fig.canvas.draw()\n    FIG_56E_56F_COMBINED_RGBA = np.asarray(fig.canvas.buffer_rgba()).copy()\nexcept Exception:\n    FIG_56E_56F_COMBINED_RGBA = None\n\nout_dir = FISH_PLOTS_DIR_56H\nif out_dir is not None:\n    out_dir.mkdir(parents=True, exist_ok=True)\n    out_path = out_dir / \'composite_56e_56f_unified.png\'\n    fig.savefig(out_path, dpi=FIG_SAVE_DPI, bbox_inches=\'tight\')\n    fig.savefig(out_path.with_suffix(\'.pdf\'), bbox_inches=\'tight\')\n    FIG_56E_56F_COMBINED_PATH = str(out_path)\n    print(f"[56h] Saved unified figure to {out_path}")\n\nplt.show()\n\nper_gene_trace_genes = []\nfor gene in genes:\n    roi_keys = sorted(\n        list(gene_roi_keys_per_cell.get(gene, set())),\n        key=lambda key: (int(key[0]), int(key[1])),\n    )\n    if not roi_keys:\n        continue\n    if any((results_per_cell.get(panel, {}) or {}).get(gene, {}) for panel in PLOT_ORDER):\n        per_gene_trace_genes.append(gene)\n\nif not per_gene_trace_genes:\n    print(\'[56h] No per-gene ROI traces available after filtering.\')\nelse:\n    fig_pg = plt.figure(\n        figsize=(PER_GENE_TRACE_FIG_WIDTH, PER_GENE_TRACE_ROW_HEIGHT * len(per_gene_trace_genes))\n    )\n    gs_pg = fig_pg.add_gridspec(\n        len(per_gene_trace_genes),\n        2,\n        width_ratios=[PER_GENE_TRACE_DONUT_WIDTH_RATIO, PER_GENE_TRACE_MAIN_WIDTH_RATIO],\n        hspace=PER_GENE_TRACE_HSPACE,\n        wspace=PER_GENE_TRACE_DONUT_WSPACE,\n    )\n\n    trace_axes_pg = []\n    donut_axes_pg = []\n    for row_idx in range(len(per_gene_trace_genes)):\n        sharex_ax = trace_axes_pg[0] if trace_axes_pg else None\n        sharey_ax = trace_axes_pg[0] if trace_axes_pg else None\n        donut_axes_pg.append(fig_pg.add_subplot(gs_pg[row_idx, 0]))\n        trace_axes_pg.append(fig_pg.add_subplot(gs_pg[row_idx, 1], sharex=sharex_ax, sharey=sharey_ax))\n\n    segment_xmin = float(tvec[0]) if PER_GENE_TRACE_X_MIN is None else float(PER_GENE_TRACE_X_MIN)\n    segment_xmax = float(tvec[-1]) if PER_GENE_TRACE_X_MAX is None else float(PER_GENE_TRACE_X_MAX)\n    plot_mask = (tvec >= segment_xmin) & (tvec <= segment_xmax)\n    if int(np.count_nonzero(plot_mask)) < 2:\n        raise ValueError(\n            f"[56h] Invalid PER_GENE_TRACE_X_MIN/MAX -> [{segment_xmin}, {segment_xmax}] leaves fewer than 2 timepoints."\n        )\n    plot_tvec = tvec[plot_mask]\n    block_span = float(plot_tvec[-1] - plot_tvec[0])\n    block_offsets = {\n        panel: (idx * (block_span + float(PER_GENE_TRACE_BLOCK_GAP_SEC))) - float(plot_tvec[0])\n        for idx, panel in enumerate(PLOT_ORDER)\n    }\n    block_starts = {panel: float(plot_tvec[0] + shift) for panel, shift in block_offsets.items()}\n    block_ends = {panel: float(plot_tvec[-1] + shift) for panel, shift in block_offsets.items()}\n    block_centers = {panel: 0.5 * (block_starts[panel] + block_ends[panel]) for panel in PLOT_ORDER}\n    onset_positions = {panel: float(0.0 + shift) for panel, shift in block_offsets.items()}\n    merged_xlim = (\n        min(block_starts.values()),\n        max(block_ends.values()),\n    )\n    merged_xticks = [block_centers[panel] for panel in PLOT_ORDER]\n    merged_xticklabels = [\n        f"{(\'Ipsi\' if str(panel).startswith(\'ipsi\') else \'Contra\' if str(panel).startswith(\'contra\') else str(panel))}\\n{(\'bout-like\' if str(panel).endswith(\'LB\') else \'continuous\' if str(panel).endswith(\'LC\') else str(panel))}"\n        for panel in PLOT_ORDER\n    ]\n\n    for row_idx, gene in enumerate(per_gene_trace_genes):\n        ax = trace_axes_pg[row_idx]\n        ax_donut = donut_axes_pg[row_idx]\n        roi_keys = sorted(\n            list(gene_roi_keys_per_cell.get(gene, set())),\n            key=lambda key: (int(key[0]), int(key[1])),\n        )\n        gene_label = _format_gene_roi_usage_label_56h(gene, len(roi_keys))\n        roi_alpha_map = {}\n        if roi_keys:\n            alpha_vals = np.linspace(PER_GENE_TRACE_SHADE_MIN, PER_GENE_TRACE_SHADE_MAX, num=len(roi_keys), endpoint=True)\n            roi_alpha_map = {key: float(alpha_vals[idx]) for idx, key in enumerate(roi_keys)}\n        low_activity_roi_keys = []\n        for roi_key in roi_keys:\n            found_low_activity = False\n            for panel in PLOT_ORDER:\n                roi_map = (results_per_cell.get(panel, {}) or {}).get(gene, {})\n                cell_res = roi_map.get(roi_key, None)\n                if cell_res is not None and bool(cell_res.get(\'is_low_activity\', False)):\n                    found_low_activity = True\n                    break\n            if found_low_activity:\n                low_activity_roi_keys.append(roi_key)\n        low_activity_shade_map = {}\n        if low_activity_roi_keys:\n            low_activity_shade_map = _build_gene_shade_map_56h(\n                PER_GENE_TRACE_LOW_ACTIVITY_BASE_COLOR,\n                low_activity_roi_keys,\n                shade_min=PER_GENE_TRACE_LOW_ACTIVITY_SHADE_MIN,\n                shade_max=PER_GENE_TRACE_LOW_ACTIVITY_SHADE_MAX,\n            )\n\n        for panel in PLOT_ORDER:\n            roi_map = (results_per_cell.get(panel, {}) or {}).get(gene, {})\n            x_block = plot_tvec + block_offsets[panel]\n            bg_color = _condition_bg_color_56h(panel)\n            ax.axvspan(\n                block_starts[panel],\n                block_ends[panel],\n                color=bg_color,\n                alpha=PER_GENE_TRACE_BLOCK_BG_ALPHA,\n                zorder=0,\n            )\n\n            if roi_map:\n                for roi_key in roi_keys:\n                    cell_res = roi_map.get(roi_key, None)\n                    if cell_res is None:\n                        continue\n                    mean = np.asarray(cell_res.get(\'mean\', []), dtype=float)\n                    if mean.size != tvec.size:\n                        continue\n                    plot_mean = mean[plot_mask]\n                    if bool(cell_res.get(\'is_low_activity\', False)):\n                        trace_color = low_activity_shade_map.get(roi_key, PER_GENE_TRACE_LOW_ACTIVITY_BASE_COLOR)\n                    else:\n                        trace_color = gene_colors.get(gene, \'#666666\')\n                    ax.plot(\n                        x_block,\n                        plot_mean,\n                        color=trace_color,\n                        linewidth=PER_GENE_TRACE_LINEWIDTH_CELL,\n                        alpha=roi_alpha_map.get(roi_key, PER_GENE_TRACE_ALPHA_CELL),\n                    )\n\n            if PER_GENE_TRACE_SHOW_MEAN:\n                gene_res = (results.get(panel, {}) or {}).get(gene, None)\n                if gene_res is not None:\n                    gene_mean = np.asarray(gene_res.get(\'mean\', []), dtype=float)\n                    if gene_mean.size == tvec.size:\n                        plot_gene_mean = gene_mean[plot_mask]\n                        ax.plot(\n                            x_block,\n                            plot_gene_mean,\n                            color=gene_colors.get(gene, \'#666666\'),\n                            linewidth=PER_GENE_TRACE_LINEWIDTH_MEAN,\n                            alpha=PER_GENE_TRACE_ALPHA_MEAN,\n                        )\n\n            mode = \'LB\' if panel.endswith(\'LB\') else \'LC\'\n            dur_vals = mode_durations.get(mode, [])\n            if isinstance(dur_vals, (list, tuple, np.ndarray)) and len(dur_vals) > 0:\n                stim_dur = float(np.median(np.asarray(dur_vals, dtype=float)))\n                if np.isfinite(stim_dur) and stim_dur > 0:\n                    stim_start_local = max(0.0, segment_xmin)\n                    stim_end_local = min(float(stim_dur), segment_xmax)\n                    if stim_end_local > stim_start_local:\n                        ax.axvspan(\n                            block_offsets[panel] + stim_start_local,\n                            block_offsets[panel] + stim_end_local,\n                            color=bg_color,\n                            alpha=PER_GENE_TRACE_STIM_BG_ALPHA,\n                            zorder=0,\n                        )\n\n            if segment_xmin <= 0.0 <= segment_xmax:\n                ax.axvline(onset_positions[panel], color=\'k\', linestyle=\'--\', linewidth=0.9, alpha=0.7)\n\n        ax.axhline(0, color=\'k\', linewidth=0.8, alpha=0.6)\n        ax.set_xlim(*merged_xlim)\n        ax.set_ylim(PER_GENE_TRACE_YMIN, PER_GENE_TRACE_YMAX)\n        ax.set_ylabel(f"{gene} (n = {len(roi_keys)})\\nZ-scored \u2206F/F")\n\n        if row_idx == 0:\n            for panel in PLOT_ORDER:\n                mode = \'LB\' if panel.endswith(\'LB\') else \'LC\'\n                _n_ev_mode = _mode_events_per_cell_label_unified(mode)\n                ax.text(\n                    block_centers[panel],\n                    PER_GENE_TRACE_TOP_LABEL_Y,\n                    f"n={_n_ev_mode} events/cell",\n                    transform=ax.get_xaxis_transform(),\n                    ha=\'center\',\n                    va=\'bottom\',\n                    fontsize=9,\n                )\n\n        if row_idx == len(per_gene_trace_genes) - 1:\n            ax.set_xticks(merged_xticks)\n            ax.set_xticklabels(merged_xticklabels)\n        else:\n            ax.set_xticks(merged_xticks)\n            ax.set_xticklabels([])\n            ax.tick_params(axis=\'x\', which=\'both\', bottom=False, labelbottom=False)\n\n        ax_donut.set_aspect(\'equal\')\n        donut_counts = hcr_donut_counts_by_gene_56h.get(gene, None)\n        if donut_counts is None or int(donut_counts.get(\'n_labels\', 0)) <= 0:\n            ax_donut.text(\n                0.5,\n                0.5,\n                \'HCR status\\nunavailable\',\n                transform=ax_donut.transAxes,\n                ha=\'center\',\n                va=\'center\',\n                fontsize=8,\n                alpha=0.75,\n            )\n            ax_donut.axis(\'off\')\n        else:\n            inner_labels = [label for label in HCR_INNER_ORDER if int(donut_counts[\'inner_counts\'].get(label, 0)) > 0]\n            inner_sizes = [float(donut_counts[\'inner_counts\'][label]) for label in inner_labels]\n            inner_cols = [HCR_INNER_COLORS.get(label, \'#cccccc\') for label in inner_labels]\n\n            outer_labels = [label for label in HCR_STATUS_ORDER if int(donut_counts[\'outer_counts\'].get(label, 0)) > 0]\n            outer_sizes = [float(donut_counts[\'outer_counts\'][label]) for label in outer_labels]\n            outer_cols = [HCR_OUTER_COLORS.get(label, \'#cccccc\') for label in outer_labels]\n\n            inner_outer_radius = PER_GENE_TRACE_DONUT_OUTER_RADIUS - PER_GENE_TRACE_DONUT_WIDTH_OUTER - PER_GENE_TRACE_DONUT_RING_GAP\n            inner_mid_radius = inner_outer_radius - (PER_GENE_TRACE_DONUT_WIDTH_INNER / 2.0)\n            outer_mid_radius = PER_GENE_TRACE_DONUT_OUTER_RADIUS - (PER_GENE_TRACE_DONUT_WIDTH_OUTER / 2.0)\n            centre_radius = inner_outer_radius - PER_GENE_TRACE_DONUT_WIDTH_INNER\n\n            outer_wedges, _ = ax_donut.pie(\n                outer_sizes,\n                radius=PER_GENE_TRACE_DONUT_OUTER_RADIUS,\n                labels=None,\n                colors=outer_cols,\n                startangle=90,\n                counterclock=False,\n                wedgeprops=dict(width=PER_GENE_TRACE_DONUT_WIDTH_OUTER, edgecolor=\'white\', linewidth=0.9),\n            )\n            inner_wedges, _ = ax_donut.pie(\n                inner_sizes,\n                radius=inner_outer_radius,\n                labels=None,\n                colors=inner_cols,\n                startangle=90,\n                counterclock=False,\n                wedgeprops=dict(width=PER_GENE_TRACE_DONUT_WIDTH_INNER, edgecolor=\'white\', linewidth=0.9),\n            )\n\n            for wedge, label, val, color in zip(inner_wedges, inner_labels, inner_sizes, inner_cols):\n                if float(val) <= 0:\n                    continue\n                theta, x, y = _wedge_midpoint_56h(wedge, inner_mid_radius)\n                ax_donut.text(\n                    x,\n                    y,\n                    f"{HCR_INNER_SHORT_LABELS.get(label, label)}\\n{int(round(val))}",\n                    ha=\'center\',\n                    va=\'center\',\n                    rotation=_tangent_rotation_56h(theta),\n                    rotation_mode=\'anchor\',\n                    fontsize=PER_GENE_TRACE_DONUT_INNER_FONT,\n                    color=_contrast_text_color_56h(color),\n                )\n\n            for wedge, val, color in zip(outer_wedges, outer_sizes, outer_cols):\n                if float(val) <= 0:\n                    continue\n                theta, x, y = _wedge_midpoint_56h(wedge, outer_mid_radius)\n                ax_donut.text(\n                    x,\n                    y,\n                    f"{int(round(val))}",\n                    ha=\'center\',\n                    va=\'center\',\n                    rotation=_tangent_rotation_56h(theta),\n                    rotation_mode=\'anchor\',\n                    fontsize=PER_GENE_TRACE_DONUT_OUTER_FONT,\n                    color=_contrast_text_color_56h(color),\n                )\n\n            centre_circle = plt.Circle((0, 0), centre_radius, fc=\'white\', ec=\'white\')\n            ax_donut.add_artist(centre_circle)\n            ax_donut.text(\n                0,\n                0,\n                f"{gene}\\nn = {int(donut_counts[\'n_labels\'])}",\n                ha=\'center\',\n                va=\'center\',\n                fontsize=PER_GENE_TRACE_DONUT_CENTER_FONT,\n                fontweight=\'bold\',\n            )\n            ax_donut.set_xlim(PER_GENE_TRACE_DONUT_VIEW_XMIN * PER_GENE_TRACE_DONUT_VIEW_SCALE, PER_GENE_TRACE_DONUT_VIEW_XMAX * PER_GENE_TRACE_DONUT_VIEW_SCALE)\n            ax_donut.set_ylim(PER_GENE_TRACE_DONUT_VIEW_YMIN * PER_GENE_TRACE_DONUT_VIEW_SCALE, PER_GENE_TRACE_DONUT_VIEW_YMAX * PER_GENE_TRACE_DONUT_VIEW_SCALE)\n            ax_donut.axis(\'off\')\n\n    donut_legend_handles = [\n        Patch(facecolor=HCR_INNER_COLORS[HCR_INNER_WITHIN], edgecolor=\'none\', label=\'inner ring: within plane\'),\n        Patch(facecolor=HCR_INNER_COLORS[HCR_INNER_OUTSIDE], edgecolor=\'none\', label=\'inner ring: outside plane\'),\n        Patch(facecolor=HCR_OUTER_COLORS[HCR_RESPONSIVE_STATUS], edgecolor=\'none\', label=HCR_OUTER_LABELS[HCR_RESPONSIVE_STATUS]),\n        Patch(facecolor=HCR_OUTER_COLORS[HCR_LOW_STATUS], edgecolor=\'none\', label=HCR_OUTER_LABELS[HCR_LOW_STATUS]),\n        Patch(facecolor=HCR_OUTER_COLORS[HCR_UNAVAILABLE_STATUS], edgecolor=\'none\', label=HCR_OUTER_LABELS[HCR_UNAVAILABLE_STATUS]),\n        Patch(facecolor=HCR_OUTER_COLORS[HCR_NO_FUNC_STATUS], edgecolor=\'none\', label=HCR_OUTER_LABELS[HCR_NO_FUNC_STATUS]),\n        Patch(facecolor=HCR_OUTER_COLORS[HCR_OUT_OF_PLANE_STATUS], edgecolor=\'none\', label=HCR_OUTER_LABELS[HCR_OUT_OF_PLANE_STATUS]),\n    ]\n    fig_pg.legend(\n        handles=donut_legend_handles,\n        loc=\'lower center\',\n        bbox_to_anchor=(0.56, PER_GENE_TRACE_LEGEND_Y),\n        ncol=4,\n        frameon=False,\n        fontsize=8,\n    )\n    fig_pg.subplots_adjust(bottom=PER_GENE_TRACE_BOTTOM, top=PER_GENE_TRACE_TOP)\n    if trace_axes_pg and donut_axes_pg:\n        trace_bbox = trace_axes_pg[0].get_position()\n        donut_bbox = donut_axes_pg[0].get_position()\n        panel_title_y = min(0.995, max(trace_bbox.y1, donut_bbox.y1) + PER_GENE_TRACE_PANEL_TITLE_Y_OFFSET)\n        fig_pg.text(\n            0.5 * (donut_bbox.x0 + donut_bbox.x1),\n            panel_title_y,\n            \'HCR matching distribution\',\n            ha=\'center\',\n            va=\'bottom\',\n            fontsize=PER_GENE_TRACE_PANEL_TITLE_FONT,\n        )\n        fig_pg.text(\n            0.5 * (trace_bbox.x0 + trace_bbox.x1),\n            panel_title_y,\n            \'Stimulus-averaged traces per neuronal type\',\n            ha=\'center\',\n            va=\'bottom\',\n            fontsize=PER_GENE_TRACE_PANEL_TITLE_FONT,\n        )\n    FIG_56H_PER_GENE_TRACE = fig_pg\n    try:\n        fig_pg.canvas.draw()\n        FIG_56H_PER_GENE_TRACE_RGBA = np.asarray(fig_pg.canvas.buffer_rgba()).copy()\n    except Exception:\n        FIG_56H_PER_GENE_TRACE_RGBA = None\n\n    if out_dir is not None:\n        out_path = out_dir / \'per_gene_stimulus_trace_with_hcr_status_56h.png\'\n        fig_pg.savefig(out_path, dpi=FIG_SAVE_DPI, bbox_inches=\'tight\')\n        fig_pg.savefig(out_path.with_suffix(\'.pdf\'), bbox_inches=\'tight\')\n        FIG_56H_PER_GENE_TRACE_PATH = str(out_path)\n        print(f"[56h] Saved merged per-gene trace + HCR status figure to {out_path}")\n\n    plt.show()\n',
    '57': "# [57]\n# Full-session mean dF/F per gene with stimulus spans (deduped by gene+anat_label)\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nimport re\nfrom pathlib import Path\nfrom codeants_2pf_hcr import prepare_pairs_for_unique_cells\nfrom codeants_2pf_hcr.stimulus import effective_motion_window\n\ntry:\n    _require_fish_state_57 = require_fish_state\nexcept NameError:\n    _require_fish_state_57 = None\nif _require_fish_state_57 is not None and (not _require_fish_state_57()):\n    raise SystemExit\n\n# Deterministic current-fish inputs only: explicit upstream state plus fish-scoped mapping files.\ntry:\n    _suite2p_by_ref_idx_57 = suite2p_by_ref_idx\n    _s2p_fish_id = SUITE2P_FISH_ID\nexcept NameError:\n    print('[57] Suite2p data not loaded; run [23a] first.')\n    raise SystemExit\ntry:\n    _df_stim_57 = df_stim\n    _frame_rate_57 = FRAME_RATE\n    _df_stim_fish_id = DF_STIM_FISH_ID\nexcept NameError:\n    print('[57] Stimulus data not loaded; run [55] first.')\n    raise SystemExit\nif _s2p_fish_id != FISH_ID:\n    print('[57] Suite2p data from different fish; run [23a].')\n    raise SystemExit\nif _df_stim_fish_id != FISH_ID:\n    print('[57] Stimulus data from different fish; run [55].')\n    raise SystemExit\n\nFULL_TRACE_GENES = None  # list or None\nFULL_TRACE_MAX_GENES = 12\nFULL_TRACE_DOWNSAMPLE = 1\nFULL_TRACE_SMOOTH_SEC = 0.0\nFULL_TRACE_ALPHA = 0.9\nFULL_TRACE_LINEWIDTH = 1.0\nFULL_FIG_WIDTH_IN = 13.33\nFULL_FIG_HEIGHT_IN = 7.5\nFULL_FIG_DPI = 300\nSTIM_SPAN_ALPHA = 0.15\nFULL_Y_MIN = None\nFULL_Y_MAX = None\nFULL_Y_TICK = 0.02\ntry:\n    _run_config_57 = RUN_CONFIG if isinstance(RUN_CONFIG, dict) else {}\nexcept NameError:\n    _run_config_57 = {}\ntry:\n    _gene_from_mask_helper_57 = gene_from_mask\nexcept NameError:\n    _gene_from_mask_helper_57 = None\ntry:\n    MATCH_POLICY_VERSION = str(HCR_ACTIVITY_MATCH_POLICY)\nexcept NameError:\n    MATCH_POLICY_VERSION = 'hcr_anat_first_local_geometry_response_v4_suite2p_gate'\nFULL_TRACE_STIM_ONSET_DELAY_SEC = float(_run_config_57.get('STIM_ONSET_DELAY_SEC', 10.0))\n\nDEFAULT_GENE_ORDER = ['sst1.1', 'sst1.2', 'npy', 'tac3b', 'pth2', 'cfos', 'cort']\nDEFAULT_GENE_COLORS = {\n    'sst1.1': '#d62728',\n    'sst1.2': '#d61ad2',\n    'npy': '#1f9d55',\n    'tac3b': '#ffd400',\n    'pth2': '#00bcd4',\n    'cfos': '#ff7f0e',\n    'cort': '#8c564b',\n}\nGENE_ORDER = list(_run_config_57.get('GENE_ORDER', DEFAULT_GENE_ORDER))\nGENE_COLORS = dict(DEFAULT_GENE_COLORS)\ntry:\n    _gc = _run_config_57.get('GENE_COLORS', {})\n    if isinstance(_gc, dict):\n        GENE_COLORS.update(_gc)\nexcept Exception:\n    pass\n_conf_override = _run_config_57.get('CONF_FUNC_CSV_ANALYSIS', None)\nif _conf_override is not None and (str(FISH_ID) in str(_conf_override) or Path(str(_conf_override)).name.startswith('conf_to_func_pairs')):\n    CONF_FUNC_CSV = str(_conf_override)\nelse:\n    CONF_FUNC_CSV = str(OUT_REG / 'conf_to_func_pairs.csv')\n\n\nif _df_stim_57 is None or _df_stim_57.empty:\n    print('df_stim is missing; run cell [55] first.')\nelif not _suite2p_by_ref_idx_57:\n    print('Suite2p data not loaded; run cell [23a] first.')\nelif not Path(CONF_FUNC_CSV).exists():\n    print(f'Missing conf_to_func_pairs.csv: {CONF_FUNC_CSV} (run [50]).')\nelse:\n    df_stim = _df_stim_57.copy()\n    pairs = pd.read_csv(CONF_FUNC_CSV)\n    if 'fish_id' in pairs.columns:\n        pairs = pairs[pairs['fish_id'].astype(str) == str(FISH_ID)].copy()\n    pair_versions = set(pairs.get('match_policy_version', pd.Series(dtype=str)).dropna().astype(str).unique().tolist())\n    if pairs.empty:\n        print('conf_to_func_pairs.csv is empty.')\n    elif ('match_policy_version' not in pairs.columns) or (MATCH_POLICY_VERSION not in pair_versions):\n        print(f'[57] conf_to_func_pairs.csv uses stale matching policy; rerun [50] to regenerate {CONF_FUNC_CSV}.')\n    else:\n        required_pair_response_cols = {'response_is_active', 'response_class', 'response_summary_class'}\n        missing_pair_response_cols = sorted(required_pair_response_cols - set(pairs.columns))\n        if missing_pair_response_cols:\n            print(f'[57] conf_to_func_pairs.csv missing response-aware columns {missing_pair_response_cols}; rerun [50] after [50ia].')\n        else:\n            if 'gene' not in pairs.columns:\n                if 'conf_mask' in pairs.columns:\n                    if _gene_from_mask_helper_57 is not None:\n                        pairs['gene'] = pairs['conf_mask'].apply(_gene_from_mask_helper_57)\n                    else:\n                        def _gene_from_mask(path_str):\n                            name = Path(str(path_str)).name\n                            m = re.search(r'channel\\d+_(.+?)_cp_masks', name)\n                            gene = m.group(1) if m else name\n                            return gene.replace('sst1_', 'sst1.')\n                        pairs['gene'] = pairs['conf_mask'].apply(_gene_from_mask)\n                else:\n                    pairs['gene'] = 'unknown'\n\n            pairs = prepare_pairs_for_unique_cells(pairs, strict=False, tag='[57]').copy()\n            pairs['plane'] = pairs['plane'].astype(int)\n            pairs['func_label'] = pairs['func_label'].astype(int)\n\n            n_in = len(pairs)\n            if 'anat_label' in pairs.columns and pairs['anat_label'].notna().any():\n                pairs = pairs[pairs['anat_label'].notna()].copy()\n                pairs['anat_label'] = pairs['anat_label'].astype(int)\n                pairs['_sort_dist_func'] = pd.to_numeric(pairs.get('dist_func_anat_um', pd.Series(np.nan, index=pairs.index)), errors='coerce').fillna(np.inf)\n                pairs['_sort_overlap'] = pd.to_numeric(pairs.get('overlap_px_func_anat', pairs.get('overlap_px', pd.Series(np.nan, index=pairs.index))), errors='coerce').fillna(0)\n                pairs['_sort_dist_conf'] = pd.to_numeric(pairs.get('dist_conf_anat_um', pd.Series(np.nan, index=pairs.index)), errors='coerce').fillna(np.inf)\n                pairs = pairs.sort_values(\n                    ['gene', 'anat_label', '_sort_dist_func', '_sort_overlap', '_sort_dist_conf', 'plane', 'func_label'],\n                    ascending=[True, True, True, False, True, True, True],\n                )\n                pairs = pairs.drop_duplicates(subset=['gene', 'anat_label'], keep='first')\n                pairs = pairs.drop(columns=['_sort_dist_func', '_sort_overlap', '_sort_dist_conf'])\n            n_after_anat = len(pairs)\n\n            pairs = pairs.drop_duplicates(subset=['plane', 'func_label', 'gene'])\n            n_after_roi = len(pairs)\n            n_roi_reuse = int(max(0, n_after_anat - n_after_roi))\n            print(\n                f'[full] dedup rows: {n_in} -> {n_after_anat} (gene+anat) -> '\n                f'{n_after_roi} (unique plane+func+gene); reused_roi_rows={n_roi_reuse}'\n            )\n\n            s2p_map = _suite2p_by_ref_idx_57\n            pairs = pairs[pairs['plane'].isin(s2p_map.keys())].copy()\n            if pairs.empty:\n                print('No matching Suite2p planes for confocal pairs.')\n            else:\n                fps = _frame_rate_57\n                if fps is None:\n                    fs_vals = []\n                    for p in s2p_map.values():\n                        fs = p.get('ops', {}).get('fs', None)\n                        if fs is not None:\n                            fs_vals.append(float(fs))\n                    if fs_vals:\n                        fps = fs_vals[0]\n                        if len(set(fs_vals)) > 1:\n                            print('[stim] WARNING: multiple fs values found; using the first')\n                    else:\n                        print('FRAME_RATE not available; cannot build time axis.')\n                        fps = None\n\n                if fps is None or fps <= 0:\n                    print('Invalid FRAME_RATE; set FRAME_RATE or ensure metadata has framerate.')\n                else:\n                    lengths = [p['dff'].shape[1] for p in s2p_map.values() if p.get('dff') is not None]\n                    if not lengths:\n                        print('Suite2p dF/F traces missing.')\n                    else:\n                        T_min = int(min(lengths))\n                        if len(set(lengths)) > 1:\n                            print('[stim] WARNING: planes have different lengths; truncating to shortest.')\n\n                        if FULL_TRACE_DOWNSAMPLE < 1:\n                            FULL_TRACE_DOWNSAMPLE = 1\n\n                        time_full = np.arange(T_min, dtype=np.float32) / float(fps)\n                        if FULL_TRACE_DOWNSAMPLE > 1:\n                            time_full = time_full[::FULL_TRACE_DOWNSAMPLE]\n\n                        # Build gene -> plane -> roi indices\n                        gene_groups = {}\n                        for _, row in pairs.iterrows():\n                            gene = row['gene']\n                            plane = int(row['plane'])\n                            roi_idx = int(row['func_label']) - 1\n                            if roi_idx < 0:\n                                continue\n                            gene_groups.setdefault(gene, {}).setdefault(plane, set()).add(roi_idx)\n\n                        # Choose genes\n                        if FULL_TRACE_GENES is not None:\n                            genes = [g for g in FULL_TRACE_GENES if g in gene_groups]\n                        else:\n                            gene_counts = []\n                            for g, planes in gene_groups.items():\n                                n = sum(len(v) for v in planes.values())\n                                gene_counts.append((g, n))\n                            gene_counts = sorted(gene_counts, key=lambda x: x[1], reverse=True)\n                            genes = [g for g, _ in gene_counts[:FULL_TRACE_MAX_GENES]]\n\n                        if not genes:\n                            print('No genes available for full-session plot.')\n                        else:\n                            # Compute mean trace per gene\n                            gene_traces = {}\n                            for gene in genes:\n                                planes = gene_groups.get(gene, {})\n                                if not planes:\n                                    continue\n                                acc = np.zeros(T_min, dtype=np.float32)\n                                count = 0\n                                for plane, roi_set in planes.items():\n                                    dff = s2p_map[plane].get('dff', None)\n                                    if dff is None:\n                                        continue\n                                    roi_idx = np.array(sorted(roi_set), dtype=int)\n                                    roi_idx = roi_idx[(roi_idx >= 0) & (roi_idx < dff.shape[0])]\n                                    if roi_idx.size == 0:\n                                        continue\n                                    acc += dff[roi_idx, :T_min].sum(axis=0)\n                                    count += int(roi_idx.size)\n                                if count > 0:\n                                    mean = acc / float(count)\n                                    if FULL_TRACE_SMOOTH_SEC and FULL_TRACE_SMOOTH_SEC > 0:\n                                        win = int(round(FULL_TRACE_SMOOTH_SEC * fps))\n                                        if win > 1:\n                                            kernel = np.ones(win, dtype=np.float32) / float(win)\n                                            mean = np.convolve(mean, kernel, mode='same')\n                                    if FULL_TRACE_DOWNSAMPLE > 1:\n                                        mean = mean[::FULL_TRACE_DOWNSAMPLE]\n                                    gene_traces[gene] = mean\n\n                            if not gene_traces:\n                                print('No traces computed for selected genes.')\n                            else:\n                                # Stimulus spans\n                                stim_palette = None\n                                if stim_palette is None:\n                                    stim_palette = {\n                                        'LLC': '#0a5910',\n                                        'LLB': '#34B18C',\n                                        'RLC': '#4D0C2C',\n                                        'RLB': '#cf368f',\n                                        'LLC+RLC': '#281578',\n                                        'LLC+RLB': '#26200b',\n                                        'LLB+RLC': '#989999',\n                                        'LLB+RLB': '#94cae3',\n                                    }\n\n                                gene_colors = {g: GENE_COLORS.get(g, '#666666') for g in genes}\n\n                                fig, ax = plt.subplots(figsize=(FULL_FIG_WIDTH_IN, FULL_FIG_HEIGHT_IN), dpi=FULL_FIG_DPI)\n                                span_labels = set()\n                                for _, row in df_stim.iterrows():\n                                    stype = row['type']\n                                    t0, t1, _ = effective_motion_window(\n                                        row.get('start', np.nan),\n                                        row.get('duration', np.nan),\n                                        row.get('end', np.nan),\n                                        FULL_TRACE_STIM_ONSET_DELAY_SEC,\n                                    )\n                                    if not np.isfinite(t0) or not np.isfinite(t1):\n                                        continue\n                                    color = stim_palette.get(stype, '#cccccc')\n                                    label = stype if stype not in span_labels else None\n                                    ax.axvspan(t0, t1, color=color, alpha=STIM_SPAN_ALPHA, label=label)\n                                    span_labels.add(stype)\n\n                                for gene, trace in gene_traces.items():\n                                    ax.plot(time_full, trace, label=gene, color=gene_colors.get(gene), alpha=FULL_TRACE_ALPHA, linewidth=FULL_TRACE_LINEWIDTH)\n\n                                ax.set_title('Gene-linked populations retain distinct patterns across the full session')\n                                ax.set_xlabel('Time (s)')\n                                ax.set_ylabel('dF/F')\n\n                                if FULL_Y_MIN is not None or FULL_Y_MAX is not None:\n                                    ymin = float(FULL_Y_MIN) if FULL_Y_MIN is not None else None\n                                    ymax = float(FULL_Y_MAX) if FULL_Y_MAX is not None else None\n                                    ax.set_ylim(ymin, ymax)\n                                if FULL_Y_TICK and (FULL_Y_MIN is not None) and (FULL_Y_MAX is not None):\n                                    ax.set_yticks(np.arange(float(FULL_Y_MIN), float(FULL_Y_MAX) + 1e-9, float(FULL_Y_TICK)))\n\n                                # Legends: gene traces and stimulus spans\n                                gene_handles, gene_labels = ax.get_legend_handles_labels()\n                                stim_handles = []\n                                stim_labels = []\n                                for h, lab in zip(gene_handles, gene_labels):\n                                    if lab in stim_palette:\n                                        stim_handles.append(h)\n                                        stim_labels.append(lab)\n                                trace_handles = [h for h, lab in zip(gene_handles, gene_labels) if lab not in stim_palette]\n                                trace_labels = [lab for lab in gene_labels if lab not in stim_palette]\n\n                                gene_legend = None\n                                if trace_handles:\n                                    gene_legend = ax.legend(trace_handles, trace_labels, loc='upper right', fontsize=8, ncol=2, title='Genes')\n                                    ax.add_artist(gene_legend)\n                                if stim_handles:\n                                    ax.legend(stim_handles, stim_labels, loc='upper left', fontsize=8, ncol=2, title='Stimuli')\n\n                                plt.tight_layout()\n                                plt.show()\n",
}

_CELL_SOURCE_BY_TAG.update(MIGRATED_CELL_SOURCE_BY_TAG)

_QC_MIDLINE_POINT_TRANSFORM_REPLACEMENTS = {
    '56f-qc': [
        (
            """def _xy_to_midline_space_qc(plane_idx, x, y):
    x = np.asarray(x, dtype=float)
    y = np.asarray(y, dtype=float)
    if _MIDLINE_SPACE_QC != 'anat':
        return x, y
    tform = _get_plane_tform_qc(int(plane_idx))
    if tform is None:
        return x, y
    try:
        pts = np.column_stack([x, y]).astype(float)
        pts_t = np.asarray(tform(pts), dtype=float)
        return pts_t[:, 0], pts_t[:, 1]
    except Exception:
        return x, y
""",
            """def _xy_to_midline_space_qc(plane_idx, x, y):
    x = np.asarray(x, dtype=float)
    y = np.asarray(y, dtype=float)
    if _MIDLINE_SPACE_QC != 'anat':
        return x, y
    tform = _get_plane_tform_qc(int(plane_idx))
    if tform is None:
        return x, y
    try:
        return transform_points_between_spaces(x, y, tform, direction='moving_to_fixed')
    except Exception:
        return x, y
""",
        ),
        (
            """def _xy_to_display_space_qc(plane_idx, x, y):
    x = np.asarray(x, dtype=float)
    y = np.asarray(y, dtype=float)
    if _MIDLINE_SPACE_QC != 'anat':
        return x, y
    tform = _get_plane_tform_qc(int(plane_idx))
    if tform is None:
        return x, y
    try:
        pts = np.column_stack([x, y]).astype(float)
        pts_t = np.asarray(tform.inverse(pts), dtype=float)
        return pts_t[:, 0], pts_t[:, 1]
    except Exception:
        return x, y
""",
            """def _xy_to_display_space_qc(plane_idx, x, y):
    x = np.asarray(x, dtype=float)
    y = np.asarray(y, dtype=float)
    if _MIDLINE_SPACE_QC != 'anat':
        return x, y
    tform = _get_plane_tform_qc(int(plane_idx))
    if tform is None:
        return x, y
    try:
        return transform_points_between_spaces(x, y, tform, direction='fixed_to_moving')
    except Exception:
        return x, y
""",
        ),
    ],
    '56f-qc-activity': [
        (
            """def _xy_to_midline_space_act(plane_idx, x, y):
    x = np.asarray(x, dtype=float)
    y = np.asarray(y, dtype=float)
    if _MIDLINE_SPACE_ACT != 'anat':
        return x, y
    tform = _get_plane_tform_act(int(plane_idx))
    if tform is None:
        return x, y
    try:
        pts = np.column_stack([x, y]).astype(float)
        pts_t = np.asarray(tform(pts), dtype=float)
        return pts_t[:, 0], pts_t[:, 1]
    except Exception:
        return x, y
""",
            """def _xy_to_midline_space_act(plane_idx, x, y):
    x = np.asarray(x, dtype=float)
    y = np.asarray(y, dtype=float)
    if _MIDLINE_SPACE_ACT != 'anat':
        return x, y
    tform = _get_plane_tform_act(int(plane_idx))
    if tform is None:
        return x, y
    try:
        return transform_points_between_spaces(x, y, tform, direction='moving_to_fixed')
    except Exception:
        return x, y
""",
        ),
        (
            """def _xy_to_display_space_act(plane_idx, x, y):
    x = np.asarray(x, dtype=float)
    y = np.asarray(y, dtype=float)
    if _MIDLINE_SPACE_ACT != 'anat':
        return x, y
    tform = _get_plane_tform_act(int(plane_idx))
    if tform is None:
        return x, y
    try:
        pts = np.column_stack([x, y]).astype(float)
        pts_t = np.asarray(tform.inverse(pts), dtype=float)
        return pts_t[:, 0], pts_t[:, 1]
    except Exception:
        return x, y
""",
            """def _xy_to_display_space_act(plane_idx, x, y):
    x = np.asarray(x, dtype=float)
    y = np.asarray(y, dtype=float)
    if _MIDLINE_SPACE_ACT != 'anat':
        return x, y
    tform = _get_plane_tform_act(int(plane_idx))
    if tform is None:
        return x, y
    try:
        return transform_points_between_spaces(x, y, tform, direction='fixed_to_moving')
    except Exception:
        return x, y
""",
        ),
    ],
}

for _tag, _replacements in _QC_MIDLINE_POINT_TRANSFORM_REPLACEMENTS.items():
    _source = _CELL_SOURCE_BY_TAG.get(_tag, "")
    for _old, _new in _replacements:
        if _old not in _source:
            raise RuntimeError(f"Could not patch {_tag} midline point transform source.")
        _source = _source.replace(_old, _new)
    _CELL_SOURCE_BY_TAG[_tag] = _source


_MIDLINE_AUTHORITATIVE_CENTROID_REPLACEMENTS = {
    '22c': [
        (
            """            'method': 'functional_ref_middle_plane',
            'base_mode': str(base_mode),
""",
            """            'method': 'functional_ref_middle_plane',
            'midline_space': 'anat' if str(ref_src).strip().lower() in {'warped', 'tform-preview', 'warped-raw'} else 'func',
            'base_mode': str(base_mode),
""",
        ),
    ],
    '56f-qc': [
        (
            """def _resolve_midline_context_qc():
""",
            """def _load_authoritative_roi_centroids_qc():
    if OUT_REG is None:
        return pd.DataFrame(columns=['plane', 'func_label', 'centroid_x_anat', 'centroid_y_anat'])
    csv_path = Path(OUT_REG) / 'functional_roi_activity_identity.csv'
    if not csv_path.exists():
        return pd.DataFrame(columns=['plane', 'func_label', 'centroid_x_anat', 'centroid_y_anat'])
    try:
        df = pd.read_csv(csv_path)
    except Exception as e:
        print(f"[56f-qc] could not read authoritative ROI table for anatomy centroids: {e}")
        return pd.DataFrame(columns=['plane', 'func_label', 'centroid_x_anat', 'centroid_y_anat'])
    needed = {'func_label', 'centroid_x_anat', 'centroid_y_anat'}
    if not needed.issubset(df.columns):
        return pd.DataFrame(columns=['plane', 'func_label', 'centroid_x_anat', 'centroid_y_anat'])
    if 'fish_id' in df.columns:
        df = df[df['fish_id'].astype(str) == str(FISH_ID)].copy()
    if 'plane_idx' in df.columns:
        plane_values = df['plane_idx']
    elif 'plane' in df.columns:
        plane_values = df['plane']
    else:
        return pd.DataFrame(columns=['plane', 'func_label', 'centroid_x_anat', 'centroid_y_anat'])
    out = pd.DataFrame({
        'plane': pd.to_numeric(plane_values, errors='coerce'),
        'func_label': pd.to_numeric(df['func_label'], errors='coerce'),
        'centroid_x_anat': pd.to_numeric(df['centroid_x_anat'], errors='coerce'),
        'centroid_y_anat': pd.to_numeric(df['centroid_y_anat'], errors='coerce'),
    })
    out = out.dropna(subset=['plane', 'func_label']).copy()
    if out.empty:
        return out
    out['plane'] = out['plane'].astype(int)
    out['func_label'] = out['func_label'].astype(int)
    return out.drop_duplicates(subset=['plane', 'func_label'], keep='first')


def _attach_authoritative_roi_centroids_qc(rois_df):
    if rois_df is None or getattr(rois_df, 'empty', True):
        return rois_df
    roi_centroids = _load_authoritative_roi_centroids_qc()
    if roi_centroids.empty:
        if _MIDLINE_SPACE_QC == 'anat':
            print('[56f-qc] authoritative anatomy centroids unavailable; using transform fallback for midline assignment.')
        return rois_df
    out = rois_df.merge(roi_centroids, on=['plane', 'func_label'], how='left')
    n_with_anat = int(out[['centroid_x_anat', 'centroid_y_anat']].notna().all(axis=1).sum())
    if _MIDLINE_SPACE_QC == 'anat' and n_with_anat == 0:
        print('[56f-qc] no plotted ROIs joined to authoritative anatomy centroids; using transform fallback for midline assignment.')
    return out


def _midline_xy_columns_qc(rois_df):
    if (
        _MIDLINE_SPACE_QC == 'anat'
        and {'centroid_x_anat', 'centroid_y_anat'}.issubset(rois_df.columns)
        and rois_df[['centroid_x_anat', 'centroid_y_anat']].notna().all(axis=1).any()
    ):
        return 'centroid_x_anat', 'centroid_y_anat'
    if _MIDLINE_SPACE_QC == 'anat':
        print('[56f-qc] assigning anatomy-space midline sides from transformed display centroids.')
    return 'x', 'y'


def _resolve_midline_context_qc():
""",
        ),
        (
            """        rois_all = _annotate_midline_side_qc(rois_all, x_col='x', y_col='y', plane_col='plane')
        rois_all['midline_side'] = rois_all['midline_side'].astype(str).str.strip().str.lower()
""",
            """        rois_all = _attach_authoritative_roi_centroids_qc(rois_all)
        _mid_x_col_qc, _mid_y_col_qc = _midline_xy_columns_qc(rois_all)
        rois_all = _annotate_midline_side_qc(rois_all, x_col=_mid_x_col_qc, y_col=_mid_y_col_qc, plane_col='plane')
        rois_all['midline_side'] = rois_all['midline_side'].astype(str).str.strip().str.lower()
""",
        ),
    ],
    '56f-qc-activity': [
        (
            """def _resolve_midline_context():
""",
            """def _load_authoritative_roi_centroids_act():
    if OUT_REG is None:
        return pd.DataFrame(columns=['plane', 'func_label', 'centroid_x_anat', 'centroid_y_anat'])
    csv_path = Path(OUT_REG) / 'functional_roi_activity_identity.csv'
    if not csv_path.exists():
        return pd.DataFrame(columns=['plane', 'func_label', 'centroid_x_anat', 'centroid_y_anat'])
    try:
        df = pd.read_csv(csv_path)
    except Exception as e:
        print(f"[56f-qc-activity] could not read authoritative ROI table for anatomy centroids: {e}")
        return pd.DataFrame(columns=['plane', 'func_label', 'centroid_x_anat', 'centroid_y_anat'])
    needed = {'func_label', 'centroid_x_anat', 'centroid_y_anat'}
    if not needed.issubset(df.columns):
        return pd.DataFrame(columns=['plane', 'func_label', 'centroid_x_anat', 'centroid_y_anat'])
    if 'fish_id' in df.columns:
        df = df[df['fish_id'].astype(str) == str(FISH_ID)].copy()
    if 'plane_idx' in df.columns:
        plane_values = df['plane_idx']
    elif 'plane' in df.columns:
        plane_values = df['plane']
    else:
        return pd.DataFrame(columns=['plane', 'func_label', 'centroid_x_anat', 'centroid_y_anat'])
    out = pd.DataFrame({
        'plane': pd.to_numeric(plane_values, errors='coerce'),
        'func_label': pd.to_numeric(df['func_label'], errors='coerce'),
        'centroid_x_anat': pd.to_numeric(df['centroid_x_anat'], errors='coerce'),
        'centroid_y_anat': pd.to_numeric(df['centroid_y_anat'], errors='coerce'),
    })
    out = out.dropna(subset=['plane', 'func_label']).copy()
    if out.empty:
        return out
    out['plane'] = out['plane'].astype(int)
    out['func_label'] = out['func_label'].astype(int)
    return out.drop_duplicates(subset=['plane', 'func_label'], keep='first')


def _attach_authoritative_roi_centroids_act(rois_df):
    if rois_df is None or getattr(rois_df, 'empty', True):
        return rois_df
    roi_centroids = _load_authoritative_roi_centroids_act()
    if roi_centroids.empty:
        if _MIDLINE_SPACE_ACT == 'anat':
            print('[56f-qc-activity] authoritative anatomy centroids unavailable; using transform fallback for midline assignment.')
        return rois_df
    out = rois_df.merge(roi_centroids, on=['plane', 'func_label'], how='left')
    n_with_anat = int(out[['centroid_x_anat', 'centroid_y_anat']].notna().all(axis=1).sum())
    if _MIDLINE_SPACE_ACT == 'anat' and n_with_anat == 0:
        print('[56f-qc-activity] no plotted ROIs joined to authoritative anatomy centroids; using transform fallback for midline assignment.')
    return out


def _midline_xy_columns_act(rois_df):
    if (
        _MIDLINE_SPACE_ACT == 'anat'
        and {'centroid_x_anat', 'centroid_y_anat'}.issubset(rois_df.columns)
        and rois_df[['centroid_x_anat', 'centroid_y_anat']].notna().all(axis=1).any()
    ):
        return 'centroid_x_anat', 'centroid_y_anat'
    if _MIDLINE_SPACE_ACT == 'anat':
        print('[56f-qc-activity] assigning anatomy-space midline sides from transformed display centroids.')
    return 'x', 'y'


def _resolve_midline_context():
""",
        ),
        (
            """            roi_df = _annotate_midline_side_resilient(roi_df, x_col='x', y_col='y', plane_col='plane')
            roi_df['midline_side'] = roi_df['midline_side'].astype(str).str.strip().str.lower()
""",
            """            roi_df = _attach_authoritative_roi_centroids_act(roi_df)
            _mid_x_col_act, _mid_y_col_act = _midline_xy_columns_act(roi_df)
            roi_df = _annotate_midline_side_resilient(roi_df, x_col=_mid_x_col_act, y_col=_mid_y_col_act, plane_col='plane')
            roi_df['midline_side'] = roi_df['midline_side'].astype(str).str.strip().str.lower()
""",
        ),
    ],
}

for _tag, _replacements in _MIDLINE_AUTHORITATIVE_CENTROID_REPLACEMENTS.items():
    _source = _CELL_SOURCE_BY_TAG.get(_tag, "")
    for _old, _new in _replacements:
        if _old not in _source:
            raise RuntimeError(f"Could not patch {_tag} authoritative midline source.")
        _source = _source.replace(_old, _new)
    _CELL_SOURCE_BY_TAG[_tag] = _source


_MIDLINE_SPACE_AND_COORD_REPLACEMENTS = {
    '56f-qc': [
        (
            """def _infer_midline_space_qc():
    src_label = None
    if _midline_globals_match_current_fish_qc():
        mp = _midline_params_state_qc
        if isinstance(mp, dict):
            src_label = (mp.get('base', {}) or {}).get('source_label', None)
    if src_label is None:
        b, _ = _load_midline_bundle_qc()
        if isinstance(b, dict):
            src_label = (b.get('base', {}) or {}).get('source_label', None)

    s = str(src_label).strip().lower() if src_label is not None else ''
""",
            """def _infer_midline_space_qc():
    src_label = None
    explicit_space = None
    if _midline_globals_match_current_fish_qc():
        mp = _midline_params_state_qc
        if isinstance(mp, dict):
            explicit_space = mp.get('midline_space', mp.get('space', None))
            src_label = (mp.get('base', {}) or {}).get('source_label', None)
    if explicit_space is None or str(explicit_space).strip().lower() not in {'anat', 'func'}:
        b, _ = _load_midline_bundle_qc()
        if isinstance(b, dict):
            explicit_space = b.get('midline_space', b.get('space', explicit_space))
            if src_label is None:
                src_label = (b.get('base', {}) or {}).get('source_label', None)

    s_space = str(explicit_space).strip().lower() if explicit_space is not None else ''
    if s_space in {'anat', 'func'}:
        return s_space

    s = str(src_label).strip().lower() if src_label is not None else ''
""",
        ),
        (
            """        xv_m, yv_m = _xy_to_midline_space_qc(int(p_idx), xv, yv)
        d[idx] = (xv_m - x0) * nx + (yv_m - y0) * ny
""",
            """        if str(x_col) == 'centroid_x_anat' and str(y_col) == 'centroid_y_anat':
            xv_m, yv_m = xv, yv
        else:
            xv_m, yv_m = _xy_to_midline_space_qc(int(p_idx), xv, yv)
        d[idx] = (xv_m - x0) * nx + (yv_m - y0) * ny
""",
        ),
    ],
    '56f-qc-activity': [
        (
            """def _infer_midline_space_act():
    src_label = None
    if _midline_globals_match_current_fish():
        mp = _midline_params_state_act
        if isinstance(mp, dict):
            src_label = (mp.get('base', {}) or {}).get('source_label', None)
    if src_label is None:
        b, _ = _load_midline_bundle()
        if isinstance(b, dict):
            src_label = (b.get('base', {}) or {}).get('source_label', None)

    s = str(src_label).strip().lower() if src_label is not None else ''
""",
            """def _infer_midline_space_act():
    src_label = None
    explicit_space = None
    if _midline_globals_match_current_fish():
        mp = _midline_params_state_act
        if isinstance(mp, dict):
            explicit_space = mp.get('midline_space', mp.get('space', None))
            src_label = (mp.get('base', {}) or {}).get('source_label', None)
    if explicit_space is None or str(explicit_space).strip().lower() not in {'anat', 'func'}:
        b, _ = _load_midline_bundle()
        if isinstance(b, dict):
            explicit_space = b.get('midline_space', b.get('space', explicit_space))
            if src_label is None:
                src_label = (b.get('base', {}) or {}).get('source_label', None)

    s_space = str(explicit_space).strip().lower() if explicit_space is not None else ''
    if s_space in {'anat', 'func'}:
        return s_space

    s = str(src_label).strip().lower() if src_label is not None else ''
""",
        ),
        (
            """        xv_m, yv_m = _xy_to_midline_space_act(int(p_idx), xv, yv)
        d[idx] = (xv_m - x0) * nx + (yv_m - y0) * ny
""",
            """        if str(x_col) == 'centroid_x_anat' and str(y_col) == 'centroid_y_anat':
            xv_m, yv_m = xv, yv
        else:
            xv_m, yv_m = _xy_to_midline_space_act(int(p_idx), xv, yv)
        d[idx] = (xv_m - x0) * nx + (yv_m - y0) * ny
""",
        ),
    ],
}

for _tag, _replacements in _MIDLINE_SPACE_AND_COORD_REPLACEMENTS.items():
    _source = _CELL_SOURCE_BY_TAG.get(_tag, "")
    for _old, _new in _replacements:
        if _old not in _source:
            raise RuntimeError(f"Could not patch {_tag} midline coordinate-space source.")
        _source = _source.replace(_old, _new)
    _CELL_SOURCE_BY_TAG[_tag] = _source


_MIDLINE_DISPLAY_SPACE_REPLACEMENTS = {
    '56f-qc': [
        (
            """try:
    _rescale_labels_to_ref_helper_qc = _rescale_labels_to_ref
except NameError:
    _rescale_labels_to_ref_helper_qc = None
""",
            """try:
    _rescale_labels_to_ref_helper_qc = _rescale_labels_to_ref
except NameError:
    _rescale_labels_to_ref_helper_qc = None
try:
    _resolve_plane_transform_helper_qc = resolve_plane_transform
except NameError:
    _resolve_plane_transform_helper_qc = None
try:
    _resample_labels_nn_helper_qc = resample_labels_nn
except NameError:
    _resample_labels_nn_helper_qc = None
""",
        ),
        (
            """def _get_plane_tform_qc(plane_idx):
    pr, _ = _get_plane_ref_qc(int(plane_idx))
    if pr is None:
        return None
    tform = pr.get('tform', None)
    if tform is None and _tform_for_plane_helper_qc is not None:
        try:
            tform = _tform_for_plane_helper_qc(pr)
        except Exception:
            tform = None
    return tform
""",
            """def _get_plane_tform_qc(plane_idx):
    pr, _ = _get_plane_ref_qc(int(plane_idx))
    if pr is None:
        return None
    tform = None
    if _resolve_plane_transform_helper_qc is not None:
        try:
            tform = _resolve_plane_transform_helper_qc(pr)
        except Exception:
            tform = None
    if tform is None:
        tform = pr.get('tform', None)
    if tform is None and _tform_for_plane_helper_qc is not None:
        try:
            tform = _tform_for_plane_helper_qc(pr)
        except Exception:
            tform = None
    return tform
""",
        ),
        (
            """    ref_img = None
    ref_src = 'none'
    if pr is not None:
        ref_img = pr.get('ref_match', None)
        ref_src = 'ref_match'
        if ref_img is None:
            ref_img = pr.get('ref2d_raw', pr.get('ref2d'))
            ref_src = 'ref2d'
""",
            """    ref_img = None
    ref_src = 'none'
    display_space = 'func'
    if pr is not None:
        if _MIDLINE_SPACE_QC == 'anat':
            ref_img = pr.get('ref_warped', None)
            ref_src = 'ref_warped'
            if ref_img is None:
                ref_img = pr.get('ref_warped_raw', None)
                ref_src = 'ref_warped_raw'
            if ref_img is not None:
                display_space = 'anat'
        if ref_img is None:
            ref_img = pr.get('ref_match', None)
            ref_src = 'ref_match'
        if ref_img is None:
            ref_img = pr.get('ref2d_raw', pr.get('ref2d'))
            ref_src = 'ref2d'
""",
        ),
        (
            """    if ref_img is not None and tuple(labels.shape) != tuple(ref_img.shape):
        old_shape = tuple(labels.shape)
        labels = _rescale_labels_to_ref_shape_qc(labels, ref_img.shape)
        new_shape = tuple(labels.shape)
        _k = (int(plane_idx), old_shape, tuple(ref_img.shape), ref_src)
        if _k not in _56f_qc_label_ref_match_logged:
            print(f"[56f-qc] plane {plane_idx}: rescaled labels {old_shape} -> {new_shape} to match {ref_src} {tuple(ref_img.shape)}")
            _56f_qc_label_ref_match_logged.add(_k)
""",
            """    if ref_img is not None and display_space == 'anat':
        tform = _get_plane_tform_qc(int(plane_idx))
        if tform is not None and _resample_labels_nn_helper_qc is not None:
            old_shape = tuple(labels.shape)
            try:
                labels = _ensure_uint_labels_qc(_resample_labels_nn_helper_qc(labels, tform, output_shape=ref_img.shape))
                new_shape = tuple(labels.shape)
                _k = (int(plane_idx), old_shape, tuple(ref_img.shape), ref_src, 'warp')
                if _k not in _56f_qc_label_ref_match_logged:
                    print(f"[56f-qc] plane {plane_idx}: warped labels {old_shape} -> {new_shape} to match {ref_src} {tuple(ref_img.shape)}")
                    _56f_qc_label_ref_match_logged.add(_k)
            except Exception as e:
                print(f"[56f-qc] plane {plane_idx}: could not warp labels to anatomy display; using resize fallback: {e}")

    if ref_img is not None and tuple(labels.shape) != tuple(ref_img.shape):
        old_shape = tuple(labels.shape)
        labels = _rescale_labels_to_ref_shape_qc(labels, ref_img.shape)
        new_shape = tuple(labels.shape)
        _k = (int(plane_idx), old_shape, tuple(ref_img.shape), ref_src, 'resize')
        if _k not in _56f_qc_label_ref_match_logged:
            print(f"[56f-qc] plane {plane_idx}: rescaled labels {old_shape} -> {new_shape} to match {ref_src} {tuple(ref_img.shape)}")
            _56f_qc_label_ref_match_logged.add(_k)
""",
        ),
        (
            """        'ref_src': ref_src,
""",
            """        'ref_src': ref_src,
        'display_space': display_space,
""",
        ),
        (
            """def _draw_side_overlay_qc(ax, ref_img, labels, side_df, plane_idx, title):
""",
            """def _draw_side_overlay_qc(ax, ref_img, labels, side_df, plane_idx, title, display_space='func'):
""",
        ),
        (
            """            (x1, y1), (x2, y2) = _line_segment_qc(float(p['x0']), float(p['y0']), float(p['theta_deg']), ref_img.shape)
            xd, yd = _xy_to_display_space_qc(int(plane_idx), [x1, x2, float(p['x0'])], [y1, y2, float(p['y0'])])
            ax.plot([xd[0], xd[1]], [yd[0], yd[1]], color='yellow', linewidth=1.3)
            ax.scatter([xd[2]], [yd[2]], s=9, c='yellow')
""",
            """            (x1, y1), (x2, y2) = _line_segment_qc(float(p['x0']), float(p['y0']), float(p['theta_deg']), ref_img.shape)
            if str(display_space).strip().lower() == 'anat':
                xd = np.asarray([x1, x2, float(p['x0'])], dtype=float)
                yd = np.asarray([y1, y2, float(p['y0'])], dtype=float)
            else:
                xd, yd = _xy_to_display_space_qc(int(plane_idx), [x1, x2, float(p['x0'])], [y1, y2, float(p['y0'])])
            ax.plot([xd[0], xd[1]], [yd[0], yd[1]], color='yellow', linewidth=1.3)
            ax.scatter([xd[2]], [yd[2]], s=9, c='yellow')
""",
        ),
        (
            """        _draw_side_overlay_qc(ax, ref_img, labels, sub, int(plane_idx), ttl)
""",
            """        _draw_side_overlay_qc(ax, ref_img, labels, sub, int(plane_idx), ttl, display_space=vis.get('display_space', 'func'))
""",
        ),
    ],
    '56f-qc-activity': [
        (
            """try:
    _rescale_labels_to_ref_helper_act = _rescale_labels_to_ref
except NameError:
    _rescale_labels_to_ref_helper_act = None
""",
            """try:
    _rescale_labels_to_ref_helper_act = _rescale_labels_to_ref
except NameError:
    _rescale_labels_to_ref_helper_act = None
try:
    _resolve_plane_transform_helper_act = resolve_plane_transform
except NameError:
    _resolve_plane_transform_helper_act = None
try:
    _resample_labels_nn_helper_act = resample_labels_nn
except NameError:
    _resample_labels_nn_helper_act = None
""",
        ),
        (
            """def _get_plane_tform_act(plane_idx):
    pr, _ = _get_plane_ref_act(int(plane_idx))
    if pr is None:
        return None
    tform = pr.get('tform', None)
    if tform is None and _tform_for_plane_helper_act is not None:
        try:
            tform = _tform_for_plane_helper_act(pr)
        except Exception:
            tform = None
    return tform
""",
            """def _get_plane_tform_act(plane_idx):
    pr, _ = _get_plane_ref_act(int(plane_idx))
    if pr is None:
        return None
    tform = None
    if _resolve_plane_transform_helper_act is not None:
        try:
            tform = _resolve_plane_transform_helper_act(pr)
        except Exception:
            tform = None
    if tform is None:
        tform = pr.get('tform', None)
    if tform is None and _tform_for_plane_helper_act is not None:
        try:
            tform = _tform_for_plane_helper_act(pr)
        except Exception:
            tform = None
    return tform
""",
        ),
        (
            """    ref_img = None
    ref_src = 'none'
    if pr is not None:
        ref_img = pr.get('ref_match', None)
        ref_src = 'ref_match'
        if ref_img is None:
            ref_img = pr.get('ref2d_raw', pr.get('ref2d'))
            ref_src = 'ref2d'
""",
            """    ref_img = None
    ref_src = 'none'
    display_space = 'func'
    if pr is not None:
        if _MIDLINE_SPACE_ACT == 'anat':
            ref_img = pr.get('ref_warped', None)
            ref_src = 'ref_warped'
            if ref_img is None:
                ref_img = pr.get('ref_warped_raw', None)
                ref_src = 'ref_warped_raw'
            if ref_img is not None:
                display_space = 'anat'
        if ref_img is None:
            ref_img = pr.get('ref_match', None)
            ref_src = 'ref_match'
        if ref_img is None:
            ref_img = pr.get('ref2d_raw', pr.get('ref2d'))
            ref_src = 'ref2d'
""",
        ),
        (
            """    if ref_img is not None and tuple(labels.shape) != tuple(ref_img.shape):
        old_shape = tuple(labels.shape)
        labels = _rescale_labels_to_ref_shape_act(labels, ref_img.shape)
        new_shape = tuple(labels.shape)
        _k = (int(plane_idx), old_shape, tuple(ref_img.shape), ref_src)
        if _k not in _56f_qc_activity_label_ref_match_logged:
            print(f"[56f-qc-activity] plane {plane_idx}: rescaled labels {old_shape} -> {new_shape} to match {ref_src} {tuple(ref_img.shape)}")
            _56f_qc_activity_label_ref_match_logged.add(_k)
""",
            """    if ref_img is not None and display_space == 'anat':
        tform = _get_plane_tform_act(int(plane_idx))
        if tform is not None and _resample_labels_nn_helper_act is not None:
            old_shape = tuple(labels.shape)
            try:
                labels = _ensure_uint_labels_act(_resample_labels_nn_helper_act(labels, tform, output_shape=ref_img.shape))
                new_shape = tuple(labels.shape)
                _k = (int(plane_idx), old_shape, tuple(ref_img.shape), ref_src, 'warp')
                if _k not in _56f_qc_activity_label_ref_match_logged:
                    print(f"[56f-qc-activity] plane {plane_idx}: warped labels {old_shape} -> {new_shape} to match {ref_src} {tuple(ref_img.shape)}")
                    _56f_qc_activity_label_ref_match_logged.add(_k)
            except Exception as e:
                print(f"[56f-qc-activity] plane {plane_idx}: could not warp labels to anatomy display; using resize fallback: {e}")

    if ref_img is not None and tuple(labels.shape) != tuple(ref_img.shape):
        old_shape = tuple(labels.shape)
        labels = _rescale_labels_to_ref_shape_act(labels, ref_img.shape)
        new_shape = tuple(labels.shape)
        _k = (int(plane_idx), old_shape, tuple(ref_img.shape), ref_src, 'resize')
        if _k not in _56f_qc_activity_label_ref_match_logged:
            print(f"[56f-qc-activity] plane {plane_idx}: rescaled labels {old_shape} -> {new_shape} to match {ref_src} {tuple(ref_img.shape)}")
            _56f_qc_activity_label_ref_match_logged.add(_k)
""",
        ),
        (
            """        'ref_src': ref_src,
""",
            """        'ref_src': ref_src,
        'display_space': display_space,
""",
        ),
        (
            """                'side_stats': side_stats,
                'n_events_used': int(n_events_used),
""",
            """                'side_stats': side_stats,
                'display_space': vis.get('display_space', 'func'),
                'n_events_used': int(n_events_used),
""",
        ),
        (
            """                        (x1, y1), (x2, y2) = _line_segment(float(p['x0']), float(p['y0']), float(p['theta_deg']), item['heat'].shape)
                        xd, yd = _xy_to_display_space_act(int(item['plane']), [x1, x2, float(p['x0'])], [y1, y2, float(p['y0'])])
                        ax.plot([xd[0], xd[1]], [yd[0], yd[1]], color='yellow', linewidth=1.2)
                        ax.scatter([xd[2]], [yd[2]], s=9, c='yellow')
""",
            """                        (x1, y1), (x2, y2) = _line_segment(float(p['x0']), float(p['y0']), float(p['theta_deg']), item['heat'].shape)
                        if str(item.get('display_space', 'func')).strip().lower() == 'anat':
                            xd = np.asarray([x1, x2, float(p['x0'])], dtype=float)
                            yd = np.asarray([y1, y2, float(p['y0'])], dtype=float)
                        else:
                            xd, yd = _xy_to_display_space_act(int(item['plane']), [x1, x2, float(p['x0'])], [y1, y2, float(p['y0'])])
                        ax.plot([xd[0], xd[1]], [yd[0], yd[1]], color='yellow', linewidth=1.2)
                        ax.scatter([xd[2]], [yd[2]], s=9, c='yellow')
""",
        ),
    ],
}

for _tag, _replacements in _MIDLINE_DISPLAY_SPACE_REPLACEMENTS.items():
    _source = _CELL_SOURCE_BY_TAG.get(_tag, "")
    for _old, _new in _replacements:
        if _old not in _source:
            raise RuntimeError(f"Could not patch {_tag} midline display-space source.")
        _source = _source.replace(_old, _new)
    _CELL_SOURCE_BY_TAG[_tag] = _source


_MIDLINE_ACTIVITY_SUBSET_DISPLAY_REPLACEMENTS = {
    '56f-qc-activity': [
        (
            """                                'side_stats': side_sub,
                                'n_events_used': int(item.get('n_events_used', -1)),
""",
            """                                'side_stats': side_sub,
                                'display_space': item.get('display_space', 'func'),
                                'n_events_used': int(item.get('n_events_used', -1)),
""",
        ),
        (
            """                                    (x1, y1), (x2, y2) = _line_segment(float(p['x0']), float(p['y0']), float(p['theta_deg']), item['heat'].shape)
                                    xd, yd = _xy_to_display_space_act(int(item['plane']), [x1, x2, float(p['x0'])], [y1, y2, float(p['y0'])])
                                    ax.plot([xd[0], xd[1]], [yd[0], yd[1]], color='yellow', linewidth=1.2)
                                    ax.scatter([xd[2]], [yd[2]], s=9, c='yellow')
""",
            """                                    (x1, y1), (x2, y2) = _line_segment(float(p['x0']), float(p['y0']), float(p['theta_deg']), item['heat'].shape)
                                    if str(item.get('display_space', 'func')).strip().lower() == 'anat':
                                        xd = np.asarray([x1, x2, float(p['x0'])], dtype=float)
                                        yd = np.asarray([y1, y2, float(p['y0'])], dtype=float)
                                    else:
                                        xd, yd = _xy_to_display_space_act(int(item['plane']), [x1, x2, float(p['x0'])], [y1, y2, float(p['y0'])])
                                    ax.plot([xd[0], xd[1]], [yd[0], yd[1]], color='yellow', linewidth=1.2)
                                    ax.scatter([xd[2]], [yd[2]], s=9, c='yellow')
""",
        ),
    ],
}

for _tag, _replacements in _MIDLINE_ACTIVITY_SUBSET_DISPLAY_REPLACEMENTS.items():
    _source = _CELL_SOURCE_BY_TAG.get(_tag, "")
    for _old, _new in _replacements:
        if _old not in _source:
            raise RuntimeError(f"Could not patch {_tag} activity subset display-space source.")
        _source = _source.replace(_old, _new)
    _CELL_SOURCE_BY_TAG[_tag] = _source


_SST12_EVENT_TRACE_56H_REPLACEMENTS = [
    (
        """FIG_56H_PER_GENE_TRACE_RGBA = None
try:
    FISH_PLOTS_DIR_56H = Path(FISH_DIR) / '04_plots'
""",
        """FIG_56H_PER_GENE_TRACE_RGBA = None
FIG_56H_SST12_EVENT_TRACE = None
FIG_56H_SST12_EVENT_TRACE_PATH = None
FIG_56H_SST12_EVENT_TRACE_RGBA = None
SST12_CONTRA_LC_EVENT_TRACE_TARGET_56H = None
try:
    FISH_PLOTS_DIR_56H = Path(FISH_DIR) / '04_plots'
""",
    ),
    (
        """                                        'is_low_activity': bool(row.get('is_low_activity', False)),
                                    },
                                )
                                cacc['segs'].append(seg)
""",
        """                                        'is_low_activity': bool(row.get('is_low_activity', False)),
                                        'response_class': row.get('response_class', None),
                                        'response_summary_class': row.get('response_summary_class', None),
                                        'response_is_active': row.get('response_is_active', None),
                                    },
                                )
                                cacc['segs'].append(seg)
""",
    ),
    (
        """                                    'is_low_activity': bool(cacc.get('is_low_activity', False)),
                                }
                                panel_gene_cells[roi_key] = entry
""",
        """                                    'is_low_activity': bool(cacc.get('is_low_activity', False)),
                                    'response_class': cacc.get('response_class', None),
                                    'response_summary_class': cacc.get('response_summary_class', None),
                                    'response_is_active': cacc.get('response_is_active', None),
                                    'segments': arr.copy(),
                                }
                                panel_gene_cells[roi_key] = entry
""",
    ),
    (
        """    if out_dir is not None:
        out_path = out_dir / 'per_gene_stimulus_trace_with_hcr_status_56h.png'
        fig_pg.savefig(out_path, dpi=FIG_SAVE_DPI, bbox_inches='tight')
        fig_pg.savefig(out_path.with_suffix('.pdf'), bbox_inches='tight')
        FIG_56H_PER_GENE_TRACE_PATH = str(out_path)
        print(f"[56h] Saved merged per-gene trace + HCR status figure to {out_path}")

    plt.show()
""",
        """    if out_dir is not None:
        out_path = out_dir / 'per_gene_stimulus_trace_with_hcr_status_56h.png'
        fig_pg.savefig(out_path, dpi=FIG_SAVE_DPI, bbox_inches='tight')
        fig_pg.savefig(out_path.with_suffix('.pdf'), bbox_inches='tight')
        FIG_56H_PER_GENE_TRACE_PATH = str(out_path)
        print(f"[56h] Saved merged per-gene trace + HCR status figure to {out_path}")

    _sst12_gene_56h = 'sst1.2'
    _sst12_panel_56h = 'contra_LC'
    _sst12_roi_map_56h = (results_per_cell.get(_sst12_panel_56h, {}) or {}).get(_sst12_gene_56h, {})
    if not _sst12_roi_map_56h:
        print('[56h] No sst1.2 contra-continuous per-cell traces available for all-events diagnostic.')
    else:
        _sst12_mode_56h = 'LC'
        _sst12_dur_vals_56h = mode_durations.get(_sst12_mode_56h, [])
        _sst12_stim_dur_56h = np.nan
        if isinstance(_sst12_dur_vals_56h, (list, tuple, np.ndarray)) and len(_sst12_dur_vals_56h) > 0:
            _sst12_stim_dur_56h = float(np.nanmedian(np.asarray(_sst12_dur_vals_56h, dtype=float)))
        _sst12_score_mask_56h = tvec >= 0
        if np.isfinite(_sst12_stim_dur_56h) and _sst12_stim_dur_56h > 0:
            _sst12_score_mask_56h = (tvec >= 0) & (tvec <= _sst12_stim_dur_56h)
        if int(np.count_nonzero(_sst12_score_mask_56h)) < 1:
            _sst12_score_mask_56h = tvec >= 0

        _sst12_candidates_56h = []
        for _roi_key_56h, _cell_res_56h in _sst12_roi_map_56h.items():
            _mean_56h = np.asarray(_cell_res_56h.get('mean', []), dtype=float)
            _segments_56h = np.asarray(_cell_res_56h.get('segments', []), dtype=float)
            if _mean_56h.size != tvec.size or _segments_56h.ndim != 2 or _segments_56h.shape[1] != tvec.size:
                continue
            _score_56h = float(np.nanmean(_mean_56h[_sst12_score_mask_56h]))
            if not np.isfinite(_score_56h):
                _score_56h = float(np.nanmax(_mean_56h[_sst12_score_mask_56h]))
            if np.isfinite(_score_56h):
                _sst12_candidates_56h.append((_score_56h, _roi_key_56h, _cell_res_56h))

        if not _sst12_candidates_56h:
            print('[56h] No finite sst1.2 contra-continuous event segments available for all-events diagnostic.')
        else:
            _sst12_score_56h, _sst12_roi_key_56h, _sst12_cell_res_56h = max(_sst12_candidates_56h, key=lambda item: item[0])
            _sst12_segments_56h = np.asarray(_sst12_cell_res_56h.get('segments', []), dtype=float)
            _sst12_mean_56h = np.asarray(_sst12_cell_res_56h.get('mean', []), dtype=float)
            _sst12_plot_segments_56h = _sst12_segments_56h[:, plot_mask]
            _sst12_plot_mean_56h = _sst12_mean_56h[plot_mask]

            fig_sst12, ax_sst12 = plt.subplots(figsize=(FIG_WIDTH_DEFAULT_IN * 0.78, 3.4))
            for _idx_56h, _seg_56h in enumerate(_sst12_plot_segments_56h):
                _label_56h = 'individual events' if _idx_56h == 0 else None
                ax_sst12.plot(plot_tvec, _seg_56h, color='#8c8c8c', linewidth=0.9, alpha=0.42, label=_label_56h)
            ax_sst12.plot(
                plot_tvec,
                _sst12_plot_mean_56h,
                color=GENE_COLORS.get(_sst12_gene_56h, '#d61ad2'),
                linewidth=2.4,
                alpha=0.96,
                label='event mean',
            )
            if np.isfinite(_sst12_stim_dur_56h) and _sst12_stim_dur_56h > 0:
                _stim_start_local_56h = max(0.0, segment_xmin)
                _stim_end_local_56h = min(float(_sst12_stim_dur_56h), segment_xmax)
                if _stim_end_local_56h > _stim_start_local_56h:
                    ax_sst12.axvspan(
                        _stim_start_local_56h,
                        _stim_end_local_56h,
                        color=_condition_bg_color_56h(_sst12_panel_56h),
                        alpha=PER_GENE_TRACE_STIM_BG_ALPHA,
                        zorder=0,
                    )
            if segment_xmin <= 0.0 <= segment_xmax:
                ax_sst12.axvline(0.0, color='k', linestyle='--', linewidth=0.9, alpha=0.75)
            ax_sst12.axhline(0.0, color='k', linewidth=0.8, alpha=0.55)
            ax_sst12.set_xlim(segment_xmin, segment_xmax)
            ax_sst12.set_ylim(PER_GENE_TRACE_YMIN, PER_GENE_TRACE_YMAX)
            ax_sst12.set_xlabel('Time from stimulus onset (s)')
            ax_sst12.set_ylabel('z-scored dF/F')
            _sst12_response_label_56h = str(_sst12_cell_res_56h.get('response_class') or 'response class unavailable')
            ax_sst12.set_title(
                f"{FISH_ID} sst1.2 contra continuous all events | "
                f"plane={int(_sst12_cell_res_56h.get('plane'))}, func_label={int(_sst12_cell_res_56h.get('func_label'))}, "
                f"events={int(_sst12_segments_56h.shape[0])}, {_sst12_response_label_56h}"
            )
            ax_sst12.legend(loc='upper right', frameon=False, fontsize=8)
            fig_sst12.tight_layout()

            FIG_56H_SST12_EVENT_TRACE = fig_sst12
            SST12_CONTRA_LC_EVENT_TRACE_TARGET_56H = {
                'fish_id': str(FISH_ID),
                'gene': _sst12_gene_56h,
                'panel': _sst12_panel_56h,
                'plane': int(_sst12_cell_res_56h.get('plane')),
                'roi_idx': int(_sst12_cell_res_56h.get('roi_idx')),
                'func_label': int(_sst12_cell_res_56h.get('func_label')),
                'anat_label': _sst12_cell_res_56h.get('anat_label'),
                'n_events': int(_sst12_segments_56h.shape[0]),
                'selection_score_mean_z_during_continuous': float(_sst12_score_56h),
                'response_class': _sst12_cell_res_56h.get('response_class'),
                'response_summary_class': _sst12_cell_res_56h.get('response_summary_class'),
                'is_low_activity': bool(_sst12_cell_res_56h.get('is_low_activity', False)),
            }
            try:
                fig_sst12.canvas.draw()
                FIG_56H_SST12_EVENT_TRACE_RGBA = np.asarray(fig_sst12.canvas.buffer_rgba()).copy()
            except Exception:
                FIG_56H_SST12_EVENT_TRACE_RGBA = None

            if out_dir is not None:
                out_path_sst12 = out_dir / 'sst12_contra_continuous_event_traces_56h.png'
                fig_sst12.savefig(out_path_sst12, dpi=FIG_SAVE_DPI, bbox_inches='tight')
                fig_sst12.savefig(out_path_sst12.with_suffix('.pdf'), bbox_inches='tight')
                FIG_56H_SST12_EVENT_TRACE_PATH = str(out_path_sst12)
                print(f"[56h] Saved sst1.2 contra-continuous all-events diagnostic to {out_path_sst12}")

            plt.show()

    plt.show()
""",
    ),
]

_source = _CELL_SOURCE_BY_TAG.get("56h", "")
for _old, _new in _SST12_EVENT_TRACE_56H_REPLACEMENTS:
    if _old not in _source:
        raise RuntimeError("Could not patch [56h] sst1.2 all-events diagnostic source.")
    _source = _source.replace(_old, _new)
_CELL_SOURCE_BY_TAG["56h"] = _source


_POSTER_TRACE_56H_REPLACEMENTS = [
    (
        """FIG_56H_SST12_EVENT_TRACE = None
FIG_56H_SST12_EVENT_TRACE_PATH = None
FIG_56H_SST12_EVENT_TRACE_RGBA = None
SST12_CONTRA_LC_EVENT_TRACE_TARGET_56H = None
try:
""",
        """FIG_56H_SST12_EVENT_TRACE = None
FIG_56H_SST12_EVENT_TRACE_PATH = None
FIG_56H_SST12_EVENT_TRACE_RGBA = None
SST12_CONTRA_LC_EVENT_TRACE_TARGET_56H = None
FIG_56H_POSTER_TRACE = None
FIG_56H_POSTER_TRACE_PATH = None
FIG_56H_POSTER_TRACE_RGBA = None
try:
""",
    ),
    (
        """    _sst12_gene_56h = 'sst1.2'
    _sst12_panel_56h = 'contra_LC'
""",
        """    _poster_gene_order_56h = ['sst1.1', 'sst1.2', 'cfos', 'pth2', 'npy', 'tac3b']
    _poster_gene_colors_56h = {
        'sst1.1': '#d62728',
        'sst1.2': '#2ca02c',
        'cfos': '#08306b',
        'pth2': '#00bcd4',
        'npy': '#d61ad2',
        'tac3b': '#ffd400',
    }
    _poster_genes_56h = [
        _gene_56h
        for _gene_56h in _poster_gene_order_56h
        if any((results.get(_panel_56h, {}) or {}).get(_gene_56h, None) is not None for _panel_56h in PLOT_ORDER)
    ]
    if not _poster_genes_56h:
        print('[56h] No requested poster genes available for one-row average-trace figure.')
    else:
        fig_poster, ax_poster = plt.subplots(figsize=(FIG_WIDTH_DEFAULT_IN, 3.2))
        _poster_block_seen_labels_56h = set()
        for _panel_56h in PLOT_ORDER:
            _x_block_56h = plot_tvec + block_offsets[_panel_56h]
            _bg_color_56h = _condition_bg_color_56h(_panel_56h)
            ax_poster.axvspan(
                block_starts[_panel_56h],
                block_ends[_panel_56h],
                color=_bg_color_56h,
                alpha=PER_GENE_TRACE_BLOCK_BG_ALPHA,
                zorder=0,
            )
            _mode_56h = 'LB' if _panel_56h.endswith('LB') else 'LC'
            _dur_vals_56h = mode_durations.get(_mode_56h, [])
            if isinstance(_dur_vals_56h, (list, tuple, np.ndarray)) and len(_dur_vals_56h) > 0:
                _stim_dur_56h = float(np.nanmedian(np.asarray(_dur_vals_56h, dtype=float)))
                if np.isfinite(_stim_dur_56h) and _stim_dur_56h > 0:
                    _stim_start_local_56h = max(0.0, segment_xmin)
                    _stim_end_local_56h = min(float(_stim_dur_56h), segment_xmax)
                    if _stim_end_local_56h > _stim_start_local_56h:
                        ax_poster.axvspan(
                            block_offsets[_panel_56h] + _stim_start_local_56h,
                            block_offsets[_panel_56h] + _stim_end_local_56h,
                            color=_bg_color_56h,
                            alpha=PER_GENE_TRACE_STIM_BG_ALPHA,
                            zorder=0,
                        )
            if segment_xmin <= 0.0 <= segment_xmax:
                ax_poster.axvline(onset_positions[_panel_56h], color='k', linestyle='--', linewidth=0.85, alpha=0.65)
            for _gene_56h in _poster_genes_56h:
                _gene_res_56h = (results.get(_panel_56h, {}) or {}).get(_gene_56h, None)
                if _gene_res_56h is None:
                    continue
                _gene_mean_56h = np.asarray(_gene_res_56h.get('mean', []), dtype=float)
                if _gene_mean_56h.size != tvec.size:
                    continue
                _label_56h = _gene_56h if _gene_56h not in _poster_block_seen_labels_56h else None
                ax_poster.plot(
                    _x_block_56h,
                    _gene_mean_56h[plot_mask],
                    color=_poster_gene_colors_56h.get(_gene_56h, '#666666'),
                    linewidth=2.0,
                    alpha=0.96,
                    label=_label_56h,
                )
                _poster_block_seen_labels_56h.add(_gene_56h)

        ax_poster.axhline(0.0, color='k', linewidth=0.8, alpha=0.55)
        ax_poster.set_xlim(*merged_xlim)
        ax_poster.set_ylim(PER_GENE_TRACE_YMIN, PER_GENE_TRACE_YMAX)
        ax_poster.set_xticks(merged_xticks)
        ax_poster.set_xticklabels(merged_xticklabels)
        ax_poster.set_ylabel('z-scored dF/F')
        ax_poster.set_title('Stimulus-averaged responses by neuronal type')
        ax_poster.legend(loc='upper right', ncol=3, frameon=False, fontsize=8)
        fig_poster.tight_layout()

        FIG_56H_POSTER_TRACE = fig_poster
        try:
            fig_poster.canvas.draw()
            FIG_56H_POSTER_TRACE_RGBA = np.asarray(fig_poster.canvas.buffer_rgba()).copy()
        except Exception:
            FIG_56H_POSTER_TRACE_RGBA = None

        if out_dir is not None:
            out_path_poster = out_dir / 'poster_single_row_average_traces_56h.png'
            fig_poster.savefig(out_path_poster, dpi=FIG_SAVE_DPI, bbox_inches='tight')
            fig_poster.savefig(out_path_poster.with_suffix('.pdf'), bbox_inches='tight')
            FIG_56H_POSTER_TRACE_PATH = str(out_path_poster)
            print(f"[56h] Saved poster one-row average-trace figure to {out_path_poster}")

        plt.show()

    _sst12_gene_56h = 'sst1.2'
    _sst12_panel_56h = 'contra_LC'
""",
    ),
]

_source = _CELL_SOURCE_BY_TAG.get("56h", "")
for _old, _new in _POSTER_TRACE_56H_REPLACEMENTS:
    if _old not in _source:
        raise RuntimeError("Could not patch [56h] poster average-trace source.")
    _source = _source.replace(_old, _new)
_CELL_SOURCE_BY_TAG["56h"] = _source


def _validate_22c_midline_commit_source() -> None:
    source = _CELL_SOURCE_BY_TAG.get("22c", "")
    required_snippets = (
        "def _params_for_plane(plane_idx, dy_manual, dtheta_manual):",
        "p = _params_for_plane(int(item['plane_idx']), dy_manual, dtheta_manual)",
        "bundle, per_plane = _build_bundle(dy, dth)",
        "'midline_space': 'anat' if str(ref_src).strip().lower()",
    )
    missing = [snippet for snippet in required_snippets if snippet not in source]
    if missing:
        raise RuntimeError("[22c] midline commit source contract is stale; _params_for_plane must accept slider values.")


_validate_22c_midline_commit_source()


def _exec_stage(tag: str, namespace: MutableMapping[str, Any]) -> None:
    if not isinstance(namespace, MutableMapping):
        raise TypeError("namespace must be a mutable mapping, usually globals()")
    if tag in _QC_MIDLINE_POINT_TRANSFORM_REPLACEMENTS:
        from .matching import resolve_plane_transform, resample_labels_nn, transform_points_between_spaces

        namespace.setdefault("transform_points_between_spaces", transform_points_between_spaces)
        namespace.setdefault("resolve_plane_transform", resolve_plane_transform)
        namespace.setdefault("resample_labels_nn", resample_labels_nn)
    source = _CELL_SOURCE_BY_TAG[tag]
    code = compile(source, f"<single-fish-notebook-cell-{tag}>", "exec")
    exec(code, namespace)

def run_single_fish_cell_22c_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [22c] in ``namespace``."""
    _exec_stage('22c', namespace)

def run_single_fish_cell_30_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [30] in ``namespace``."""
    _exec_stage('30', namespace)

def run_single_fish_cell_34c_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned regional ROI/anatomy review cell [34c] in ``namespace``."""
    if not isinstance(namespace, MutableMapping):
        raise TypeError("namespace must be a mutable mapping, usually globals()")
    from .plots.qa import show_regional_match_review_stage

    result = show_regional_match_review_stage(
        plane_refs=namespace.get("plane_refs"),
        anat_labels_path=namespace.get("ANAT_LABELS_PATH"),
        anat_stack=namespace.get("anat", namespace.get("anat_f")),
        func_labels=namespace.get("func_labels"),
        func_labels_path=namespace.get("FUNC_LABELS_PATH"),
        out_seg=namespace.get("OUT_SEG"),
        suite2p_by_ref_idx=namespace.get("suite2p_by_ref_idx"),
        out_reg=namespace.get("OUT_REG"),
        out_qa=namespace.get("OUT_QA"),
        plane_indices="all",
        use_suite2p_labels=True,
        apply_func_orientation_func=(
            None
            if namespace.get("SUITE2P_INPUT_XY_FRAME") == "codeants_2p_canonical_xy_v1"
            else namespace.get("apply_func_orientation")
        ),
        imread_func=namespace.get("imread_any"),
        crop_pad_px=24,
        save_outputs=True,
        render_display=True,
    )
    namespace["regional_match_review_result"] = result
    for line in result.get("log_lines", []):
        print(line)

def run_single_fish_cell_38_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [38] in ``namespace``."""
    _exec_stage('38', namespace)

def run_single_fish_cell_40_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [40] in ``namespace``."""
    _exec_stage('40', namespace)

def run_single_fish_cell_41_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [41] in ``namespace``."""
    _exec_stage('41', namespace)

def run_single_fish_cell_44_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [44] in ``namespace``."""
    _exec_stage('44', namespace)

def run_single_fish_cell_46_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [46] in ``namespace``."""
    _exec_stage('46', namespace)

def run_single_fish_cell_47_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [47] in ``namespace``."""
    _exec_stage('47', namespace)

def run_single_fish_cell_47b_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [47b] in ``namespace``."""
    _exec_stage('47b', namespace)

def run_single_fish_cell_50_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [50] in ``namespace``."""
    _exec_stage('50', namespace)

def run_single_fish_cell_50i_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [50i] in ``namespace``."""
    _exec_stage('50i', namespace)

def run_single_fish_cell_50ia_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [50ia] in ``namespace``."""
    _exec_stage('50ia', namespace)

def run_single_fish_cell_50e_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [50e] in ``namespace``."""
    _exec_stage('50e', namespace)

def run_single_fish_cell_50f_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [50f] in ``namespace``."""
    _exec_stage('50f', namespace)

def run_single_fish_cell_50g_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [50g] in ``namespace``."""
    _exec_stage('50g', namespace)

def run_single_fish_cell_51_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [51] in ``namespace``."""
    _exec_stage('51', namespace)

def run_single_fish_cell_53_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [53] in ``namespace``."""
    _exec_stage('53', namespace)

def run_single_fish_cell_53a_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [53a] in ``namespace``."""
    _exec_stage('53a', namespace)

def run_single_fish_cell_54_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [54] in ``namespace``."""
    _exec_stage('54', namespace)

def run_single_fish_cell_56_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [56] in ``namespace``."""
    _exec_stage('56', namespace)

def run_single_fish_cell_56d_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [56d] in ``namespace``."""
    _exec_stage('56d', namespace)

def run_single_fish_cell_56f_qc_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [56f-qc] in ``namespace``."""
    _exec_stage('56f-qc', namespace)

def run_single_fish_cell_56f_qc_activity_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [56f-qc-activity] in ``namespace``."""
    _exec_stage('56f-qc-activity', namespace)

def run_single_fish_cell_56g_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [56g] in ``namespace``."""
    _exec_stage('56g', namespace)

def run_single_fish_cell_56h_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [56h] in ``namespace``."""
    _exec_stage('56h', namespace)

def run_single_fish_cell_57_stage(namespace: MutableMapping[str, Any]) -> None:
    """Run package-owned legacy notebook cell [57] in ``namespace``."""
    _exec_stage('57', namespace)

__all__ = [
    'run_single_fish_cell_22c_stage',
    'run_single_fish_cell_30_stage',
    'run_single_fish_cell_34c_stage',
    'run_single_fish_cell_38_stage',
    'run_single_fish_cell_40_stage',
    'run_single_fish_cell_41_stage',
    'run_single_fish_cell_44_stage',
    'run_single_fish_cell_46_stage',
    'run_single_fish_cell_47_stage',
    'run_single_fish_cell_47b_stage',
    'run_single_fish_cell_50_stage',
    'run_single_fish_cell_50i_stage',
    'run_single_fish_cell_50ia_stage',
    'run_single_fish_cell_50e_stage',
    'run_single_fish_cell_50f_stage',
    'run_single_fish_cell_50g_stage',
    'run_single_fish_cell_51_stage',
    'run_single_fish_cell_53_stage',
    'run_single_fish_cell_53a_stage',
    'run_single_fish_cell_54_stage',
    'run_single_fish_cell_56_stage',
    'run_single_fish_cell_56d_stage',
    'run_single_fish_cell_56f_qc_stage',
    'run_single_fish_cell_56f_qc_activity_stage',
    'run_single_fish_cell_56g_stage',
    'run_single_fish_cell_56h_stage',
    'run_single_fish_cell_57_stage',
]
