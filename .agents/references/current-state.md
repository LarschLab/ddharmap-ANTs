# Current State

**Purpose:** short status of the intentional mixed migration state.

**Use this file when:** editing downstream cells that may combine ROI-centric and HCR-centric paths.

## Current mixed state (intentional)

- ROI-centric path (`[50i]`, `[50ia]`, `[50j]`, `[50k]`) is used for authoritative whole-population outputs.
- HCR-centric identified-cell path (`[50]`, `[50e]`, `[51]`, `[56]`, `[56h]`, `[57]`) remains active for identified-cell activity workflows.
- Cohort notebook path (`notebooks/multi_fish_56h_56g.ipynb`) is a downstream multi-fish consumer of single-fish outputs.

## Cohort authority boundary

- Cohort stages aggregate and visualize outputs from single-fish canonical tables.
- Cohort notebook stages are **not** authoritative for single-fish identity/response semantics.

## Practical warnings

- The current agentic staged CLI surface is still mostly post-preprocessing for full-chain parity: assume preprocessing has already completed before running `contracts`, `audit-inputs`, `status`, or `stage-status`. `compare-staged`, `freeze-legacy-baseline`, and `compare-legacy-baseline` now also accept declared upstream writer-stage names, but upstream comparisons only activate from staged `pipeline_outputs` or same-pipeline-root persisted upstream manifests, not from legacy preprocessing files alone. Same-pipeline-root upstream manifests provide the actual output paths for status/compare when a writer used explicit output overrides.
- Existing `L395_f11` staged outputs are the first baseline/control for staged parity checks. Treat them as accepted comparison evidence for this migration slice, not as cohort-wide scientific invariants.
- `--write-manifest` persists JSON manifests under `03_analysis/functional/pipeline_manifests/` only when explicitly requested.
- Downstream staged output folders still have read-only inventory/status surfaces through `stage-status`, but `assign-hcr-identity`, `score-activity-bpi`, `export-canonical-tables`, `make-qa-report`, and `make-figures` now also have writer commands. `assign-hcr-identity` now requires staged ROI/anatomy geometry, staged functional/anatomy plane refs, Suite2p, anatomy labels, and staged HCR/anatomy artifacts; it recomputes `anatomy_identity_lookup.csv`, the ROI identity master, and the HCR-centric activity/status/candidate CSV family before comparing against accepted controls. `make-qa-report` writes Markdown, HTML, PDF, and JSON review artifacts from staged outputs, including a biologist review guide with concrete questions, evidence, and actions. `make-figures` now renders all declared final figure PNGs from staged canonical CSVs plus declared `[56i]` AUC inputs.
- Upstream preprocessing, registration, matching, and broader comparison commands remain declarative contracts only where their package-owned writers, comparison specs, and real-data validation are not yet complete. Legacy-baseline commands now share the declared comparison-stage list with `compare-staged`; upstream stages without a declared control path are still presence/nonempty checks until stronger baseline sources are defined. For explicit upstream output destinations, keep the same `--pipeline-root` and persist the stage manifest if later `status`/`compare-staged` should follow those exact paths. Upstream writer manifests record `pipeline_root`; read-only status re-describes manifest input/output paths so same-root writer manifests can report `current` instead of drifting because the inventory view uses broader candidate globs.
- For upstream stages on fish without accepted legacy/control outputs, `compare-staged` reports missing controls as warning-only when the staged output exists and is readable/nonempty. This supports cross-fish writer validation without pretending parity was measured. Downstream stages and missing staged outputs still fail under `--strict`.
- `compare-staged` compares existing declared staged outputs only. It does not execute, recompute, promote, render, or freeze baseline bundles; use `freeze-legacy-baseline` for an explicit frozen bundle and `compare-legacy-baseline` to compare staged outputs against it.
- Directory-style staged outputs count as nonempty only when they contain at least one real file; `.DS_Store` and AppleDouble sidecars do not satisfy the check. Cellpose segmentation directory comparisons are stricter and require at least one real `*_cp_masks.tif` mask file.
- `[50]` is response-aware via join to the `[50ia]` master ROI table.
- The ROI-centric and HCR-centric paths intentionally have different candidate universes. The ROI-centric path starts from all Suite2p ROIs, including `iscell=0` rows with no extractable cellular response information, and then applies response/BPI thresholds. The HCR-centric path starts from accepted HCR/anatomy labels and searches local functional candidates so identified cells are not lost through whole-population ROI competition.
- Downstream HCR-centric consumers are expected to require response-aware columns and fail fast if missing.
- Multi-session single-fish functional preprocessing can map later output planes to later imaging sessions; response/BPI and `[56i]` AUC consumers should use per-plane stimulus contexts rather than assuming one latest metadata/log CSV applies to all planes. If preprocessing metadata is missing but explicit session logs exist, stimulus-context resolution can infer an equal contiguous split across sessions and records that provenance.
- `[50l]` is now package-rendered by `plots.analysis.render_single_fish_50l_composite`; its notebook cell should remain a thin wrapper around canonical `[50ia]` and `[56i]` tables.
- `[56h]` includes an auto-selected `sst1.2` contra-continuous all-events trace diagnostic for sanity-checking a suspicious mean response; it is diagnostic only and does not change response semantics.
- `[56h]` includes a poster-only one-row average-trace figure with fixed gene colors; it is not an authoritative table or response-definition output.
- `[57a-responsive-identity-donut]` is a downstream hybrid figure only; it does not redefine upstream geometry/response semantics from `[50i]`/`[50ia]`.
- Existing staged output folders and diagnostic audit artifacts are accepted baseline evidence for `L395_f11`; do not treat them as proof that recomputation logic exists for baseline-copy writer stages.
- HCR activity export finalization is package-owned (`matching.finalize_hcr_activity_export_tables`), and `assign-hcr-identity` now writes recomputed HCR activity/status/pair/candidate CSVs from the label-first replay. `[50e]` `hcr_activity_status_summary.csv` is regenerated from the recomputed status table plus HCR warp-metadata high-quality mask counts. On `L395_f11`, row/key/exact/numeric parity passes; byte differences remain warning-only for recomputed CSVs.
- `audit-hcr-activity-replay` is now the read-only surface for measuring HCR-centric replay parity. It runs the label-first `[50]` candidate builder and finalizer in memory, uses the ROI master only as response lookup, applies the notebook-equivalent functional orientation when rebuilding Suite2p labels, overlays and checks selected accepted `ants_rigid_affine` transformlists through AntsPyx when available, compares to accepted HCR outputs, and does not promote or write HCR CSVs. On `L395_f11`, selected-ANTs replay now has exact row and candidate-key parity with accepted HCR outputs.
- Writer-stage naming should be concrete, not generic. Treat broad names like `preprocess-anatomy` or `preprocess-hcr` as roadmap groupings only; executable stages should name the exact side effect, such as `prepare-ex-vivo-anatomy-stack`, `segment-ex-vivo-anatomy-cellpose`, or `segment-hcr-cellpose`.
- `prepare-in-vivo-anatomy-stack` is the validated canonical in vivo anatomy preparation writer for the staged CLI surface. It wraps `[14a]`, writes the registration-ready NRRD/JSON, excludes ex vivo-looking files during default source discovery, and passed `L765_f02` validation on `linnaeus` when writing to a temporary `--pipeline-root`/`--output-path`. Do not write to the default fish preprocessing path unless intentionally updating canonical preprocessing outputs.
- `prepare-functional-reference-stacks` is the validated functional reference preparation writer for the staged CLI surface. It wraps `[12]`, discovers `02_motionCorrected/*mcorrected*.tif` inputs, writes legacy-compatible `*_ref_raw.tif`/`*_ref_norm.tif` pairs under `--pipeline-root/prepare-functional-reference-stacks/functional/raw/`, and passed `L395_f11` validation on `linnaeus` with an explicit real motion-corrected source. Same-root writer manifests with exact raw/norm output files now report `current` in upstream status instead of being compared against only the summarized reference directory; this was validated on `L758_f03`.
- Broader cross-fish evidence now includes `L765_f03` at `/tmp/codeants-preprocess-crossfish-1783591488`: `prepare-in-vivo-anatomy-stack` passed with explicit temp NRRD/JSON output and uint8 `(750, 750)` Y/X checks, `prepare-functional-reference-stacks` passed with 10 raw and 10 normalized references, stage-specific compare-staged commands exited 0, and upstream status reports both persisted manifests current. The same temp root now also validates bounded two-plane `register-functional-to-anatomy --reference-plane-index 0 --reference-plane-index 1` and `match-roi-to-anatomy`: registration discovered 10 references, selected 2, wrote a 2-row transform table, and matching wrote 480 geometry rows with 288 nonzero anatomy-label assignments across 215 unique nonzero labels. The writer manifests passed with zero errors/warnings. Stage-specific compare commands report warning-only missing-control checks because `L765_f03` has no accepted parity outputs in the temp-root path. The aggregate status for that temp root is overall `fail` only because unrelated downstream controls are intentionally absent.
- `L758_f06` at `/tmp/codeants-crossfish-L758f06-1783596306` adds another independent mounted-fish validation through preprocessing, bounded registration, and ROI/anatomy matching. The temp root symlinks canonical raw/preprocessed/Suite2p inputs without writing fish outputs. `prepare-in-vivo-anatomy-stack`, `prepare-functional-reference-stacks`, `register-functional-to-anatomy --reference-plane-index 0 --reference-plane-index 1`, and `match-roi-to-anatomy` all passed with zero errors/warnings. Preprocessing compare-staged commands pass; registration/matching compare-staged commands are warning-only missing-control checks. Matching wrote 947 geometry rows with 443 nonzero anatomy-label assignments across 372 unique nonzero labels. Aggregate status is overall `fail` only because unrelated downstream controls are absent.
- `L758_f07` at `/tmp/codeants-crossfish-L758f07-1783597131` adds a fourth bounded non-baseline chain. The temp root uses canonical raw/preprocessed/Suite2p inputs as symlinks and writes all generated outputs under `/tmp`. `prepare-in-vivo-anatomy-stack`, `prepare-functional-reference-stacks`, `register-functional-to-anatomy --reference-plane-index 0 --reference-plane-index 1`, and `match-roi-to-anatomy` all passed with zero errors/warnings. Preprocessing compare-staged commands pass; registration/matching compare-staged commands are warning-only missing-control checks. Matching wrote 1,064 geometry rows with 498 nonzero anatomy-label assignments across 377 unique nonzero labels. Aggregate status is overall `fail` only because unrelated downstream controls are absent.
- `L765_f04` at `/tmp/codeants-crossfish-L765f04-1783597878` adds another bounded mounted-fish chain. The temp root uses canonical raw/preprocessed/Suite2p inputs as symlinks and writes all generated outputs under `/tmp`. `prepare-in-vivo-anatomy-stack`, `prepare-functional-reference-stacks`, `register-functional-to-anatomy --reference-plane-index 0 --reference-plane-index 1`, and `match-roi-to-anatomy` all passed with zero errors/warnings. Preprocessing compare-staged commands pass; registration/matching compare-staged commands are warning-only missing-control checks. Matching wrote 721 geometry rows with 479 nonzero anatomy-label assignments across 406 unique nonzero labels. Aggregate status is overall `fail` only because unrelated downstream controls are absent.
- `register-functional-to-anatomy` is the validated functional-to-anatomy registration writer for the staged CLI surface. It reconstructs `plane_refs` from staged functional reference TIFFs, consumes the prepared in vivo anatomy stack, writes NCC caches, in-plane comparison/recommendation CSVs, warped reference TIFFs, `plane_refs_summary.json`, and a staged `registration/tforms_by_plane.csv` under `--pipeline-root/register-functional-to-anatomy/`. The default validated path remains NCC-active; the CLI now accepts `--ants-fixed-mask-json` and `--active-inplane-method ants_rigid_affine` for the ANTs backend, plus opt-in `--ants-deterministic-seed` for deterministic ANTs diagnostics. It also accepts repeatable `--reference-plane-index` values for bounded representative-plane validation without manually copying functional reference files; selected transform rows preserve the original plane index for downstream Suite2p lookup. The writer now uses the notebook `[16]` scale-search defaults instead of the earlier constrained `0.9-1.0` sweep and passes anatomy XY voxel spacing into ANTs in-plane registration. `/tmp/codeants-ants-spacing-real-1783412010` validated five real `L395_f11` staged ANTs transformlists with canonical per-plane masks, accepted-like scaled reference shapes (`531/531/532/536/536`), and accepted-matching ANTs fixed parameters. `/tmp/codeants-planeidx-real-1783544725` validated the first-class `--reference-plane-index 0 --reference-plane-index 1` path on `L758_f03`: 10 references discovered, 2 selected, a 2-row transform table preserving plane indices 0/1, upstream stage status `pass`, and compare-staged exited 0 with zero failed checks. `/tmp/codeants-ants-seed-real-1783412998` showed seed `123` worsened accepted geometry parity to `4516/4530` labels and `4521/4530` unique-match flags, so deterministic seeding is not the promotion fix. The non-L395 registration evidence is writer/status coverage; full accepted geometry promotion still requires downstream controls or biological validation.
- Functional/anatomy visual QA now reads the canonical prepared anatomy NRRD through package image IO. If an older staged registration root warns `not a TIFF file b'NRRD'`, rerun `register-functional-to-anatomy --force-recompute` before final report review.
- `make-qa-report` now records image artifact readability metadata in `qa_report_summary.json` and surfaces a visual-check column in Markdown/HTML/PDF review checklists. The report distinguishes missing previews from unreadable previews; generated visual summaries only pass review guidance when declared image artifacts are both present and readable. `compare-staged --stage-name make-qa-report` now also parses `qa_report_summary.json`, checks required sections, checks review status validity, and warns/fails on missing or unreadable image metadata. On real `L395_f11`, `/tmp/codeants-review-guide-xgY9jvF7` was rerun with this current worktree: `make-qa-report --force-recompute` passed with zero errors/warnings, `compare-staged --stage-name make-qa-report --strict` passed with zero failed/warning checks, and all six preview images were readable with recorded dimensions in the summary.
- `register-hcr-to-anatomy` is the validated HCR-to-anatomy writer for the staged CLI surface. The default accepted path still stages small accepted TIFF/CSV/JSON artifacts from `03_analysis/confocal/aligned/` under `--pipeline-root/register-hcr-to-anatomy/confocal/aligned/`, validates final-pair schema and accepted-pair semantics, records large aligned intensity NRRDs as inputs without copying them, and reports direct-ANTs recompute readiness from current raw HCR masks, rbest/rn NRRDs, matching metadata, and transform files. The opt-in `--recompute-direct-ants` path now applies the notebook `label_voxel_floor_v3` prewarp HCR label filter, recomputes HCR label TIFFs, warp metadata, and HCR/anatomy match/review/final-pair CSVs from raw HCR masks plus current ANTs transforms, and keeps final-pair key parity against accepted controls as a fail gate. On `L395_f11`, `/tmp/codeants-hcr-filter-nooverlay-w1yksx` passed with recomputed filter stats `4/4`, accepted filter-stat overlay count `0`, and exact final-pair key parity for all four masks. The earlier `/tmp/codeants-hcr-filter-t86kC0` downstream chain fed `assign-hcr-identity`, `score-activity-bpi`, and `export-canonical-tables` with zero errors; only expected byte-parity warnings remained downstream.
- Cellpose writers have positive Helga GPU validation. Fresh `L765_f02` ex vivo anatomy segmentation is byte-identical to the accepted 15,530-label NAS mask. Fresh `L395_f11` HCR segmentation runs deterministically under the current Cellpose 4.0.8 environment, but does not reproduce the unmanifested November 2025 accepted masks; 4.0.7 produced the same fresh result and an anisotropy diagnostic was substantially worse. Direct downstream matching retained `8/9` accepted `sst1.2` and `46/62` accepted `tac3b` anatomy identities, so HCR segmentation parity is not promoted and is deferred to the planned replacement segmentation workflow. Cellpose stage manifests now include runtime/device versions and model SHA-256, while the CLI keeps stdout as clean JSON by capturing Cellpose console logs.
- `match-roi-to-anatomy` is now the validated ROI/anatomy geometry recompute writer for the staged CLI surface. In recompute mode it consumes staged `register-functional-to-anatomy/plane_refs_summary.json`, Suite2p, anatomy labels, anatomy XY spacing, and notebook-equivalent functional orientation before writing geometry-only ROI/anatomy match tables under `--pipeline-root/match-roi-to-anatomy/registration/`. It preserves staged `ants_rigid_affine` transformlists when the registration stage produced them and falls back to selected accepted transformlists only when staged transformlists are absent. On `L395_f11`, the accepted-transform fallback matched accepted ROI/anatomy keys, anatomy labels, selected labels, and unique-match flags exactly. The full staged ANTs transform path is plumbed through matching; after scale, mask, and spacing alignment it is down to 8 differing boundary ROIs (`anat_label=4522/4530`, unique matches `4523/4530`). Candidate reconstruction showed the residual rows are transform-induced overlap/competition changes rather than matching tie-breaks. Regenerated ANTs geometry is promoted on `L395_f11` with an explicit bounded-drift gate: exact key parity is required, label/unique drift up to `0.2%` is warning-only, and larger drift fails.
- On `L758_f03`, `L765_f03`, `L758_f06`, `L758_f07`, and `L765_f04`, bounded two-plane staged chains now reach `match-roi-to-anatomy pass` from the first-class `--reference-plane-index 0 --reference-plane-index 1` registration path. `L758_f03` wrote 687 geometry rows and 225 unique anatomy matches with zero failed compare checks. `L765_f03` wrote 480 geometry rows, 288 nonzero anatomy-label assignments, and 215 unique nonzero anatomy labels. `L758_f06` wrote 947 geometry rows, 443 nonzero anatomy-label assignments, and 372 unique nonzero anatomy labels. `L758_f07` wrote 1,064 geometry rows, 498 nonzero anatomy-label assignments, and 377 unique nonzero anatomy labels. `L765_f04` wrote 721 geometry rows, 479 nonzero anatomy-label assignments, and 406 unique nonzero anatomy labels. Compare-staged is warning-only for fish without accepted/control geometry. This is cross-fish writer/status evidence only; accepted ROI/anatomy parity cannot be measured for these non-baseline temp-root runs.
- Ex vivo structural analysis artifacts should live under `03_analysis/structural/ex_vivo/` and remain isolated from canonical in vivo anatomy segmentation outputs.
- Helga NAS jobs should use a headful SSH session when personal NAS credentials are needed. The username is `danin.dharmaperwira@unil.ch`; do not store or extract the password. Use a session-local `net use Y: ... /persistent:no` prompt, run the NAS-dependent job in that same session, then delete the temporary mapping before exit.
- For single-fish CLI commands, `--local-root` must be the directory that directly contains fish folders. On Helga for Danin/Microscopy data, use `Y:\default\D2c\07_Data\Danin\Microscopy`; using `Y:\default\D2c\07_Data` will write to the wrong `L765_f02` sibling path.
- Ex vivo Cellpose segmentation should consume the segmentation-ready manual-oriented ex vivo NRRD when present, not the raw or pre-rotation ex vivo stack. For `L765_f02`, use `02_reg\00_preprocessing\2p_anatomy\ex_vivo\L765_f02_exvivo_anatomy_2P_GCaMP_uint8_manual_oriented.nrrd`.
- The `L765_f02` folder visible on `linnaeus` at `/Volumes/dataDrive/dataProcessing/2p_processing/L765_f02` lacks that manual-oriented ex vivo NRRD, lacks a discoverable raw ex vivo source under `01_raw/2p/anatomy`, and lacks `02_reg/00_preprocessing/rbest`; use Helga/NAS or stage those inputs before expecting positive ex vivo/HCR Cellpose validation.
- `[4c]` path audits may report stage-owned path keys as `pending` before their writer stage initializes them; treat `warn` as path drift/mismatch and `fail` as stale or missing canonical state.
- Notebook-visible QA image stages should not rely on an import-time `Agg` backend override; package renderers are expected to display figures explicitly in notebook contexts while remaining save-safe in headless runs.
- Optional native dependencies are expected to fail stage-locally: base package imports and early notebook setup cells should not require `SimpleITK`, `cellpose`, or `ANTsPy` at import time.
- `notebooks/singleFish.ipynb` is static-contract clean for top-level helper definitions; remaining heavy legacy behavior for migrated cells is package-owned through public stage wrappers.
- Single-fish functional orientation no longer writes full `_flipX.tif` movie stacks by default. `[10]` audits existing legacy oriented movie caches, and `[12]` applies orientation to derived functional references from the original motion-corrected stacks.
- Early anatomy preprocessing now includes `[14a]` before voxel inference `[8]`; it writes a signed-16-bit-corrected 8-bit anatomy NRRD with 2P XY orientation mirrored by default through the same metadata-driven orientation convention used for functional references, anatomy Z flipped to match the bottom-to-top confocal registration convention, and Y/X resized to `750x750` at `02_reg/00_preprocessing/2p_anatomy/<fish_id>_anatomy_2P_GCaMP.nrrd`, stores cache/provenance metadata in `.nrrd.json`, rebinds `ANAT_STACK_PATH` downstream to that NRRD, and avoids duplicate TIFF image outputs.
- Keep path purposes separate:
  - ROI-centric for whole-population inference
  - HCR-centric for identified-cell activity reporting/export
- Avoid implicit hybrids in plotting cells.
