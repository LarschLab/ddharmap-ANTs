# Pre-cleanup snapshots

These files were untracked working files immediately before commit `e4683d7` (`pre-cleanup`). They are retained byte-for-byte for provenance but are not current package, notebook, or test authorities.

Their original repository-relative locations are preserved beneath `root/` and `src/`. Compare them with the current owning files before reusing or deleting them.
